# 단타봇 "SNIPER" 설계 지시서 v1 — 검증 우선 설계

> Claude Code 구현 스펙. 퀀트봇(본대)과 별개의 단기(0~3일) 전술 봇.
> **순서 강제: Phase 0(백테스트 검증)을 통과하지 못한 전략은 코드로 만들지 않는다.**
> 리스크 모듈은 새로 만들지 않고 RISK_ENGINE_SPEC_v2의 `risk/`를 import하여 공유한다.

---

## 0. 설계 철학

1. **승률이 아니라 기대값.** 기대값 = (승률×평균이익) − (패률×평균손실) − 왕복비용 0.3%. 모든 판정은 이 식 하나로 한다.
2. **덜 거래하는 봇.** 조건이 겹칠 때만 쏘는 저격 구조. 거래 횟수는 비용이다.
3. **예측 금지, 게이트만.** "내일 오르나"를 묻지 않는다. "오늘 종가의 모양이 통계적으로 연속형인가"만 묻는다.
4. **호가는 의도, 체결은 사실.** 호가 잔량은 판단에 사용 금지. 체결된 데이터만 쓴다.
5. **본대는 퀀트봇, 단타봇은 보너스 파이프.** 단타 수익이 전제인 계획을 세우지 않는다.

---

## 1. 아키텍처

```
sniper/
├── config.py                 # 파라미터 단일 정의 (9장)
├── flow_quality.py           # 진성 수급 판정 F1~F5
├── strategy_a_flow_turn.py   # 전략 A: 수급 전환
├── strategy_b_us_gap.py      # 전략 B: US 오버나이트 갭
├── overnight_gate.py         # O1~O5 보유/청산 게이트
├── exits.py                  # 가격/시간/목표 3중 청산
├── account_guard.py          # 계좌 격리 + profit sweep
└── backtest/
    ├── data_prep.py
    ├── event_study.py        # 조건별 단변량 검증
    ├── ablation.py           # 조건 조합 기여도 분해
    └── walk_forward.py
공유: risk/sizing.py, risk/pre_trade_gate.py, kill_switch/   ← 퀀트봇과 동일 척추
```

계좌 분리: 단타 전용 계좌(또는 별도 잔고 파티션). 퀀트봇 자금과 절대 혼합 금지.

---

## 2. Phase 0 — 백테스트 검증 설계 (이 장이 이 문서의 심장)

### 2.0 데이터 현실 점검 — 정직하게 시작한다

| 데이터 | 과거 확보 | 비고 |
|---|---|---|
| 일별 OHLCV·거래대금 | ✅ pykrx 3년+ | O1, O4 계산 가능 |
| 일별 VWAP | ✅ = 거래대금/거래량 | O2 계산 가능 (일간 VWAP은 정확값) |
| 투자자별 확정 순매수 | ✅ pykrx | 전략 A 진입, O3의 프록시 |
| 프로그램 차익/비차익 | ✅ KRX 일별 | F5 |
| **장중 잠정치 시계열** | ❌ 과거 데이터 없음 | **오늘부터 봇이 직접 로깅 시작** |
| 미국 지수/SOX 일별 | ✅ | 전략 B, O5 |

**핵심 제약**: O3(14:30 잠정치 유지)와 F1(오후 반납 패턴)은 과거 데이터가 없어 백테스트 불가.
→ 처리 방침:
- 백테스트는 **확정치 프록시**로 수행하되, 이는 종가 시점에 완전히 알 수 없는 값이므로 **경미한 미래참조(lookahead)임을 결과에 명시**한다. 프록시 버전과 프록시 제외 버전을 둘 다 돌려 갭을 정량화한다.
- **잠정치 로거를 Phase 0 첫날부터 가동** (11:00 / 13:00 / 14:30 / 15:30 스냅샷, 전 관심종목). 3개월 쌓이면 forward 검증에 사용. 이 로거는 read-only이며 매매와 무관 — kodex 사고 패턴 재발 방지를 위해 dry-run 개념 자체가 없는 순수 수집기로 작성.

### 2.1 검증 대상 정의

**전략 A — 수급 전환 (메인 후보)**

```
진입 신호 (D일 확정치 확인 후 D+1 시초 부근 진입):
  A1  외인+기관 동반 순매수 전환: 직전 5일 합산 순매도 → 당일 동반 순매수
  A2  당일 거래대금 ≥ 20일 평균 × 1.5
  A3  종가 > 20일 고가의 97% (저항 접근/돌파)
  A4  레짐 필터: KOSPI 레짐 BULL 또는 CAUTION (BEAR/CRISIS 진입 금지)
유니버스: KOSPI200 + KOSDAQ150, 거래대금 하위 30% 제외, 관리/투자주의 제외
```

**전략 B — US 오버나이트 갭 (서브 후보)**

```
진입 신호 (시초 동시호가):
  B1  US Overnight 시그널 4등급 이상 (기존 모듈)
  B2  해당 한국 종목의 시초 갭 < 미국 동종 섹터 등락 × 0.5 (덜 반영)
  B3  전일 외인 순매수 종목 한정
청산: 당일 종가 무조건 전량 (오버나이트 게이트 미적용, 가장 단순)
```

### 2.2 검증 방법론 — 3단계

**STEP 1: 단변량 이벤트 스터디 (`event_study.py`)**

각 조건(A1~A4, O1~O5, F3~F5)을 **하나씩 단독으로** 검증한다.

```
방법: 조건 충족 이벤트 발생일 기준 D+1, D+2, D+3 수익률 분포 측정
      vs 동일 종목 무작위 일자 베이스라인(부트스트랩 1,000회)
산출: 조건별 평균 초과수익, 승률, t-통계량, 표본수
기준: 표본수 ≥ 300건 / t ≥ 2.0 미달 조건은 이후 단계에서 제외 후보
```

목적: "다 좋아 보이는 조건"에서 **실제로 일하는 조건**을 골라내는 것. 여기서 절반은 탈락해야 정상이다.

**STEP 2: Ablation — 조합 기여도 분해 (`ablation.py`)**

```
베이스라인: A1 단독 + 고정 청산(1×ATR 손절, D+1 종가 청산)
순차 추가:  베이스라인 + A2 → +A3 → +A4 → 오버나이트 게이트 O1~O5 순차
각 단계마다 기록: 기대값, PF, 승률, 거래수, MDD
판정 룰:   조건 추가로 거래수가 줄면서 기대값이 오르면 채택
           기대값 변화 ±0.05% 이내면 그 조건은 삭제 (단순할수록 강건)
오버나이트 게이트는 추가로: O조건 충족 보유 vs 전량 당일청산 비교
           → "들고 가는 것"이 실제로 더 버는지를 데이터로 확정
```

**STEP 3: Walk-Forward (`walk_forward.py`)**

```
구간: 2023~2024 학습(in-sample) / 2025 검증(out-of-sample) / 2026 YTD 최종 확인
파라미터(k, ATR 배수, 거래대금 배수 등)는 in-sample에서만 결정
out-of-sample에서 in-sample 대비 기대값이 50% 이상 붕괴하면 과최적화 판정 → 기각
레짐별 분해 필수: BULL/CAUTION/BEAR 구간 각각의 성과 분리 표기
                  (상승장에서만 버는 전략인지 식별)
```

### 2.3 비용 모델 (전 백테스트 공통, 협상 불가)

```
왕복 비용 = 거래세 0.18% + 수수료 0.03% + 슬리피지 0.10% = 0.31% 일괄 차감
슬리피지는 추후 실측(검수 문서의 측정 체계)으로 교체하되, 검증 단계에선 보수적으로 0.10%
시초가 진입 전략(B)은 슬리피지 0.15% 적용 (동시호가 체결 불확실성)
```

### 2.4 Phase 0 합격 기준 — 이걸 못 넘으면 봇은 없다

| 지표 | 기준 |
|---|---|
| 기대값 (비용 차감 후) | 트레이드당 +0.3% 이상 |
| Profit Factor | ≥ 1.5 (out-of-sample 기준) |
| 표본수 | ≥ 300 트레이드 (3년) |
| MDD | -15% 이내 |
| out-of-sample 유지율 | in-sample 기대값의 50% 이상 |
| 레짐 강건성 | CAUTION 구간에서도 기대값 ≥ 0 |

전략 A와 B는 **동시 검증하되 동시 가동하지 않는다.** 합격한 것 하나만 먼저 실전 투입.

---

## 3. 진성 수급 판정 모듈 (`flow_quality.py`) — F스코어

외인 단타성 자금을 걸러내는 스코어 (0~5점, 실시간 운영용):

```
F1  잠정치 지속성: 14:30 잠정 순매수 ≥ 11:00 피크의 70%        [잠정치 로거 필요]
F2  가격 증거:    종가 > 당일 VWAP
F3  연속성:      확정치 기준 3일 연속 순매수
F4  규모:        5일 누적 순매수 ≥ 해당 종목 20일 평균 거래대금의 3%
F5  알맹이:      비차익(비프로그램) 순매수 비중 ≥ 60%
                 (차익 프로그램은 선물 베이시스 따라 움직이는 최단타 자금 — 제외)
```

- 백테스트 가능: F2~F5 (STEP 1에서 각각 검증)
- F1은 잠정치 로깅 3개월 축적 후 forward로만 검증
- 운영 룰: F스코어 ≥ 3 (F1 가용 시 ≥ 4)만 전략 A 진입 허용

---

## 4. 청산 룰 (`exits.py`) — 3중 잠금

```
E1  가격 손절:   진입가 − 1.0×ATR(14). 도달 즉시, 예외 없음
E2  시간 청산:   D+1 종가까지 +0.5R 미달 시 무조건 전량 청산
                ← 단타봇의 영혼. "조금만 더"가 단타를 물린 장투로 만든다
E3  목표/트레일: +1.5R 도달 시 50% 익절, 잔여분 손절을 본전으로 상향
                + 전일 저가 트레일링. 최대 보유 D+3 (그 이후 무조건 청산)
```

## 5. 오버나이트 게이트 (`overnight_gate.py`) — 14:50 자동 판정

```
O1  종가 위치: (종가−저가)/(고가−저가) ≥ 0.7      (레인지 상단 마감 = 연속형)
O2  종가 > VWAP
O3  잠정치 유지 (= F1)
O4  거래대금 전일 대비 증가
O5  US Overnight 시그널 중립 이상

판정:  5개 충족        → 전량 보유 이월
      3~4개           → 50% 청산, 잔여분 본전 손절로 이월
      2개 이하        → 당일 전량 청산
```

※ O1~O5 각각의 실제 기여는 Phase 0 STEP 1·2에서 검증 후 가중치/탈락 확정. 위 구성은 가설이지 결론이 아니다.

---

## 6. 리스크 & 계좌 룰 (`account_guard.py` + `risk/` 공유)

```
트레이드당 리스크      0.3% (퀀트봇 0.5%보다 타이트)
동시 보유             최대 3종목
하루 신규 진입         최대 2건
일일 손실 한도         -1.5% → 당일 거래 종료 (연패 추격 차단)
주간 손실 한도         -4.0% → 주간 정지 + 전 트레이드 로그 리뷰 후 수동 재개
계좌 격리             전체 자금의 10~15% 고정. 손실 보충 입금 절대 금지
원금 -20%             봇 정지 + Phase 0 재검증 전까지 재가동 금지

Profit Sweep (월말 자동):
  초과수익 = max(0, 계좌잔고 − 시작원금)
  초과수익 × 70% → 퀀트봇 계좌 이체, 단타 계좌는 고정 원금으로 리셋
  이체 기록은 ledger에 is_final 스키마로 기록 (kodex 패턴 재사용)
킬스위치: kill_switch/ 프로세스가 단타 계좌도 동일 감시 (K1~K4 적용)
```

---

## 7. 일일 운영 흐름

```
08:30  유니버스 갱신, 전일 확정치 반영 → 전략 A 후보 확정 (F스코어 ≥ 3)
09:00  전략 B 판정·진입 (가동 시) / 전략 A 시초~09:30 분할 진입
11:00  잠정치 스냅샷 #1 (로거)
13:00  잠정치 스냅샷 #2
14:30  잠정치 스냅샷 #3 → F1 산출
14:50  오버나이트 게이트 O1~O5 판정 → 보유/절반/청산 주문
15:30  잠정치 스냅샷 #4(확정 대기), 당일 트레이드 로그 기록
저녁    확정치로 F3~F5 갱신, 일일 기대값·비용 리포트 텔레그램 발송
```

---

## 8. 구현 페이즈

```
Phase 0  (지금) 잠정치 로거 가동 + backtest/ 3종 구현 + 전략 A·B 검증 리포트
         ✅ 완료 기준: 2.4 합격표를 채운 검증 리포트 (불합격이면 여기서 종료 — 그것도 성공)
Phase 1  합격 전략 1개의 시그널·청산·게이트 모듈 + risk/ 연동 + 페이퍼 20거래일
         ✅ 완료 기준: 페이퍼 기대값이 백테스트의 ±50% 밴드 내
Phase 2  소액 실전 (계좌 한도의 1/3) 20거래일 → 슬리피지 실측 → 비용 모델 갱신
Phase 3  정규 가동 + profit sweep 활성화 + (3개월 후) F1 forward 검증 반영
```

---

## 9. 파라미터 테이블

| 파라미터 | 초기값 | 비고 |
|---|---|---|
| risk_per_trade | 0.3% | 변경은 분기 1회 |
| atr_stop_mult | 1.0 | in-sample에서만 0.8~1.5 탐색 |
| target_r | 1.5R | 〃 |
| time_exit | D+1 종가 | 고정 |
| max_hold | D+3 | 고정 |
| volume_mult (A2) | 1.5× | in-sample 탐색 |
| f_score_min | 3 (F1 가용 시 4) | — |
| o_gate_full / half | 5 / 3 | Phase 0 결과로 확정 |
| daily_loss_limit | -1.5% | 고정 |
| weekly_loss_limit | -4.0% | 고정 |
| account_fraction | 10~15% | 고정, 증액 금지 |
| sweep_ratio | 70% | 월말 |
| cost_roundtrip | 0.31% | Phase 2에서 실측 교체 |

---

## 10. 안티패턴 — 단타봇 전용 금지 목록

1. **호가 잔량을 신호로 쓰지 말 것.** 취소 가능한 주문은 데이터가 아니라 연출이다.
2. **시간 청산(E2)을 늦추지 말 것.** 손실 포지션의 보유 연장은 어떤 사유로도 금지. 코드에 연장 경로를 만들지 않는다.
3. **일일 한도 도달 후 "확실한 자리"라며 재진입 금지.** 한도는 그 확신을 의심하라고 있는 것이다.
4. **백테스트 불합격 전략을 "느낌상 될 것 같아서" 가동 금지.** Phase 0 기각도 결과물이다 — 잃었을 돈을 번 것.
5. **스캘핑(분 단위) 확장 금지.** 속도 전쟁은 이 인프라의 게임이 아니다.
6. **단타 수익을 전제로 퀀트봇 자금 계획을 세우지 말 것.** sweep은 보너스지 파이프라인이 아니다.
