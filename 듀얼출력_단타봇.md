# ============================================
# 듀얼 출력 지시서 — 단타봇 전용
# ============================================
# 단타봇 세션에 통째로 복붙하세요.
# ============================================

## 핵심 변경사항

지금부터 분석 결과를 **2개 채널로 나눠서 출력**해.
같은 분석을 한 번만 실행하되, 출력만 2갈래로 나뉘는 거야.

### 채널 1: 텔레그램 (개인용 — 사장님 전용)
- **전체 수급 분석** 다 보내
- 분석한 종목 전부 (20~30종목도 OK)
- 등급 낮은 것도 포함 (C, D, F급도 "왜 안 되는지" 이유와 함께)
- 국가별 외인 수급 상세 + "미국계가 좀 빠지고 있어 주의" 같은 코멘트
- 세력 감지 의심 종목 (확실하지 않아도 "한번 봐봐" 수준)
- 확신도 낮은 시그널도 포함 ("반반인데 일단 리스트에 넣어둠")
- 어제 시그널 대비 변화 코멘트 ("어제 BUY였는데 오늘 수급 빠짐")
- 포지션 관리 제안 ("현대차 반익절 고려")
- **필터링 없이 날것 그대로**

### 채널 2: Supabase FLOWX (구독자용 — VIP 유료 서비스)
- **필터링된 결과만** 올려
- 이 데이터에 구독자가 월 5만원을 내는 거야. 퀄리티가 생명.
- 아래 기준으로 엄격하게 필터링:

**short_signals 필터 기준:**
- grade A 이상만 (AAA, AA, A) → BBB 이하는 FLOWX에 올리지 마
- total_score 65점 이상만
- signal_type: 'FORCE_BUY' 또는 'BUY'만 → 'WATCH', 'AVOID'는 올리지 마
- inst_support: True인 종목 우선 (기관 동반 없으면 score 75+ 이어야 올림)
- 진입가/손절가/목표가가 명확한 것만
- 개인 코멘트/감정/확신도 제외
- **구독자가 보고 바로 행동할 수 있는 수준만 올려**

---

## 코드 구조 예시

```python
# 분석 실행 (1번만)
all_results = run_supply_demand_analysis()  # 전체 종목 수급 분석

# ===== 채널 1: 텔레그램 (개인용) =====
# 등급순 정렬해서 전부 보내기
sorted_results = sorted(all_results, key=lambda x: x['total_score'], reverse=True)

personal_msg = f"📊 [개인] 단타 리포트 {today}\n"
personal_msg += f"분석 종목: {len(sorted_results)}개\n"
personal_msg += f"레짐: {current_regime}\n\n"

# A급 이상
a_plus = [r for r in sorted_results if r['grade'] in ['AAA','AA','A']]
personal_msg += f"🟢 A급 이상 ({len(a_plus)}개):\n"
for r in a_plus:
    personal_msg += (
        f"  {r['name']} ({r['code']}) — {r['grade']} {r['total_score']}점\n"
        f"  외인: 미{r['foreign_us']:+d} 유{r['foreign_eu']:+d} 아{r['foreign_asia']:+d}\n"
        f"  기관: {'✅ 동반' if r['inst_support'] else '❌ 미동반'}\n"
        f"  진입 {r['entry_price']} → 목표 {r['target_price']} / 손절 {r['stop_loss']}\n"
        f"  💭 {r['personal_comment']}\n\n"              # 개인 코멘트
    )

# B급 이하도 보내기 (개인용이니까)
b_below = [r for r in sorted_results if r['grade'] not in ['AAA','AA','A']]
personal_msg += f"\n🟡 B급 이하 ({len(b_below)}개):\n"
for r in b_below:
    personal_msg += (
        f"  {r['name']} — {r['grade']} {r['total_score']}점\n"
        f"  💭 {r['personal_comment']}\n"
    )

# 리스크/관리
personal_msg += f"\n⚠️ 포지션 관리:\n{position_management_notes}"
personal_msg += f"\n📝 어제 대비 변화:\n{yesterday_comparison}"

send_telegram_personal(text=personal_msg)


# ===== 채널 2: Supabase FLOWX (구독자용) =====
flowx_signals = []
for r in all_results:
    # 엄격한 필터
    if r['grade'] not in ['AAA', 'AA', 'A']:
        continue  # B급 이하 탈락
    if r['total_score'] < 65:
        continue  # 65점 미만 탈락
    if r['signal_type'] not in ['FORCE_BUY', 'BUY']:
        continue  # WATCH/AVOID 탈락
    if not r['inst_support'] and r['total_score'] < 75:
        continue  # 기관 미동반이면 75점 이상만

    flowx_signals.append({
        'date': str(today),
        'code': r['code'],
        'name': r['name'],
        'grade': r['grade'],
        'total_score': r['total_score'],
        'foreign_detail': {
            'us': r['foreign_us'],
            'eu': r['foreign_eu'],
            'asia': r['foreign_asia'],
            'other': r.get('foreign_other', 0),
            'total': r['foreign_total']
        },
        'inst_support': r['inst_support'],
        'entry_price': r['entry_price'],
        'stop_loss': r['stop_loss'],
        'target_price': r['target_price'],
        'holding_days': r['expected_days'],
        'signal_type': r['signal_type'],
        'volume_ratio': r['vol_ratio'],
        'momentum_regime': r['regime']
        # personal_comment, confidence, position_notes → 제외
    })

print(f"[FLOWX] 전체 {len(all_results)}종목 중 {len(flowx_signals)}종목만 업로드")
upload_short_signals(flowx_signals)
```

---

## 필터 강도 비교

| 항목 | 텔레그램 (개인) | FLOWX (구독자) |
|------|---------------|---------------|
| 종목 수 | 전체 (20~30개) | 필터링 (5~10개) |
| 최소 등급 | 없음 (F급도 포함) | A 이상만 |
| 최소 점수 | 없음 | 65점 이상 |
| signal_type | 전부 | FORCE_BUY, BUY만 |
| 기관 미동반 | 포함 (표시만) | score 75+ 이어야 포함 |
| 개인 코멘트 | 있음 | 없음 |
| 확신도 | 표시 | 제외 |
| 포지션 관리 | 있음 | 없음 |
| 어제 대비 변화 | 있음 | 없음 |

---

## 왜 이렇게 하는가

**너한테는** 전체 그림이 필요해. C급 종목이라도 "왜 C급인지" 알아야 시장을 읽을 수 있어.
"어제 A급이었는데 오늘 B급으로 떨어졌다" — 이 변화 자체가 시그널이야.

**구독자한테는** 액션 가능한 결과만 필요해. "이거 사라, 여기서 사라, 여기서 끊어라."
노이즈가 많으면 신뢰가 떨어져. 적게 보여주되 정확하게.

**이 차이가 너의 알파야.** 너는 날것을 보고 판단하는 사람,
구독자는 걸러진 결과를 받는 사람. 이 격차에 월 5만원의 가치가 있어.

---

## 주의사항

- **분석은 1번만 실행.** 개인용/FLOWX용 따로 분석하지 마.
- **FLOWX 필터 기준은 엄격하게 유지.** "오늘 종목이 너무 적은데..." 하고 기준을 낮추지 마. 0종목이어도 0종목이 맞으면 0종목으로 올려. 매일 10종목 뽑아야 하는 게 아니야.
- **개인 코멘트 필드:** `personal_comment`, `confidence`, `position_notes` 같은 키를 따로 만들어서, Supabase 업로드 시 빼기 쉽게.
