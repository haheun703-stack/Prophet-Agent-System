# 내일(6/1 월) TODO — 5/31 밤 인계 (사장님 "바로 진행")

> 사장님 5/31 밤: "위에 아직 해결 못한 거 내일 TODO에 올리고 바로 진행." 봇 OFF 유지(SAJANG.AUTO_TRADE_DISABLED=True), VPS HEAD 22235ed.
> 오늘 큰 수확: 점화 R&D 수렴(기계적 일봉=breakeven, STOP_REENTER가 정답) + 상한가티어 우위 입증 + 워치리스트 필터 입증 + 수급 11주체 인버전 발견.

## 1순위 — flow_market 수집기 stale 버그 fix (깨끗한 6/1 누적 선결)
- 발견(단타봇 직접): KIS FHPTJ04040000은 휴일/주말에 **빈 응답이 아니라 직전 거래일 데이터를 복제 반환**. 인계서 "휴일 자동 skip"은 부정확.
- 증거: flow_market/kospi_investor.csv — 5/23·24·25(휴장, daily에 5/25 없음 확인)가 5/22 지수종가 7847.71·기관계 +758,319로 **복제**. 5/30·31도 5/29 복제.
- fix: market_investor_collector.py 수집 시 `is_trading_day()` 가드 OR 직전행과 지수종가/값 동일하면 미저장(dedupe). 기존 stale 행(5/23/24/25, 5/30/31) 정리.
- ★ 매매 무관(조회전용). 단타봇 read-only 설계 → 코워크/Codex 적용 or 작은 패치. 6/1 본누적 전 필수.

## 2순위 — 6/1 관측 시작 (자동 누적 확인)
- 15:40 G6 → flow_market 11주체 누적 시작 → "세력(금융투자) 시장매수 vs 폭등종목" 관계 관측.
- 4단 shadow(thesis/kki) recommendation.json + 일일스냅샷 누적 시작 확인 (VPS 22235ed에 46ab5a3 배선 포함).
- 매일 로그 2줄(명분게이트/끼 분포) + flow_market 세력 라인 관측.

## 3순위 — 워치리스트 무기 봇 배선 (오늘 입증된 필터)
- 검증됨(db5cd76): 휴면→첫급등(≥15%) + 종가강도≥0.8 + 거래량≥5x + 당일상승≤30%(과열회피) → STOP_REENTER 리프트 +1.9~2.4%p·승률+7~8%p, 전 regime 견고(FULL=약세포함 +1.89p). OHLCV만으로 계산.
- 다음: ① 봇 read-only 설계(워치리스트 생성) → Codex → 6/1 shadow 배선 ② 임계 튜닝(종가강도0.9/거래량10x 리프트↑?) ③ 정교 수급(supply_analyzer S~F 10일등급) 결합 — 단순부호는 0이었음.
- ★ STOP_REENTER 자체가 새 매매행동 → SAJANG 단일통로 설계 + 게이트 회귀0 + 매도 무손상 후 paper (자동 금지).

## 4순위 — 종목별 11주체 세력 (사장님 결정 대기)
- 키움 opt10059 종목별 11주체 (KIS 종목별은 4주체 한계, KRX 인증막힘). **시장세부 1~2주 쓸모 본 뒤 결정**(사장님).
- 그래야 워치리스트 필터(종가강도·거래량) + "종목별 세력 잔류" 결합 검증 가능.

## 5순위 — 수급 인버전 종목별 정밀 검증
- 시장레벨로는 "합계가 세력 죽임" 입증(5/27: 기관계 -12,555인데 금융투자 +1,119,881 / 5/29: 81% 세력). 종목별은 4순위 데이터 후.

## 6순위 — 보안 (이월)
- 평문 키(탐사문서 2026-04-16) + VPS 로그 텔레그램 토큰 URL 노출 마스킹.

## 이월 (재개 전 필수, 미해결 — full_review)
- C-1/RC-6 매수·매도 단일게이트 분리(보호매도 항상 실행). H-1 룰4 상한가+25% 분할매도 DEAD 살리기. 게이트 8/8은 GREEN 유지.

## 오늘 정직 단서 (과신 금지)
- 점화 절대수익은 초강세장 베타 과대 → 미래기대=alpha(필터 리프트 +2%p 수준), 베어장 대비 사이징.
- 수급 단순부호 예측력 0(세력 가린 합계). 정교 수급(11주체 금융투자 라인)이 진짜 — 미검증, 6/1 forward shadow가 본검증.
- 생존편향(상폐부재)/일봉/수급 4개월·표본 thin 잔존.
