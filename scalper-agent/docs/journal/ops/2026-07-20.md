# 업무일지 2026-07-20 (월) — 루틴 3일차 (휴장 명절 다음 첫 거래일)

> 마스터 루틴: `docs/checklist/DAILY_ROUTINE.md` · 세션 시각 20:43(장 마감·nightly 완료 후)

## §0. 스코어보드 (보고 첫 줄 = 숫자 — 7/17 약속)

📊 **페이퍼 누적 327건 · 승률 50.5% · 순누적 -32.40%p (-97,200원/30만·건) · 70억 진도 0%**
**★ 정정: 유효 판정일은 5일이 아니라 3일** (7/13·7/20은 ticks 수집장애 — §5 참조). 7/15 폭등장 +15.29%p·7/16 폭락장 -30.92%p가 누적을 지배.

## §1. 아침 루틴 결과 (오늘 7/20 기준 — 장 마감 후 실측)

| # | 항목 | 결과 | 비고 |
|---|------|------|------|
| A1 | 오늘(7/20) 기초데이터 | ✅ 전채널 정상 | 종가·투자자수급 10/10·시장11주체·공매도·외국인소진율 10/10 전부 7/20·신용 7/14(T+지연 정상) |
| A2 | nightly 완주 | ✅ **31/31** | 18:00~18:59·⑲-4 데드라인 체커 첫 정규 편입 포함 |
| A3 | 20:10 텔레그램 발송 | ✅ 발송 완료 | .env fix 정착 확인(3거래일째 연속 성공) |
| — | **휴장(7/17) 처리** | ✅ **전체 skip 확인** | nightly "휴장일 — 전체 skip(exit 0)" — 캘린더 fix가 정확히 작동. 유령신호 재발 0 |
| A4 | OBSERVE 러너 | ⚠️ **정정** | 가동은 정상이나 intents 1건은 "조용한 장"이 아니라 **ticks 수집장애 결과**(§5) — 최초 보고 오류 |
| A5 | ⑲-2 대조 / ⑲-3 정산 | ✅ | 페이퍼 7/20 정산 실행·수집장애로 체결 0건 |
| A6 | 봇 안전 | ✅ | active·실매매 OFF·실주문 0·매도 무손상 |
| A9 | **전략 데드라인 대장** | ⚠️ **판정 임박** | ⑲-4가 판정 의무 3건 자동 감지·텔레그램 발송 |

## §2. F-11 마감 확인 (금요일 예약분)

- 금요일 15:45 원샷 격리 **정상 실행** (`logs/f11_oneshot.log`: "done Fri Jul 17 15:45:00"). `ticks/20260717` → `_trash_holiday_ticks_20260717/` 격리 완료·원위치 없음. 제헌절 사고 완전 종결.

## §3. ★ 내일(7/21) 첫 심판일 — S-3·S-4·S-5 데드라인 도래 (D-1)

데드라인 체계가 만들어진 뒤 **첫 판정일**. 6월부터 판정 없이 돌던 shadow 3건이 내일 심판대에 오른다:

| 전략 | 시작 | 방치 기간 | 판정 준비물 |
|------|------|-----------|-------------|
| S-3 ③-4 테마 릴레이 shadow | 6/22 | 4주 | rebuild된 shadow 산출물에서 relay 재현율·엣지 유무 |
| S-4 ③-2 early variant shadow | 6/16 | 5주 | 6/18 중간판정("진짜 게이트=재진입") 이후 단독 유지 가치 |
| S-5 6월 장기 shadow 일괄 | 6/1 | 7주 | 각 shadow가 판정 자료로 쓰인 적 있는가 전수(3-Type B/C·SDART·leader_momentum·sector_reversal) |

→ **내일 아침 루틴에서 데이터 판정안 3건 작성·보고. 폐기/연장 결정은 사장님.** (오늘은 데이터 확인 세션이라 준비물만 명시)

## §5. ★ 사고 발견 — ticks 수집 조용한 오염 (F-9 규명·근본 fix)

사장님 "문제 없나?" 질문에 재검증하다 발견. **최초 보고에서 오늘 신호 1건을 "조용한 장"이라고 단정한 것이 틀렸다** — 실제는 수집 장애였다.

- **증상**: 오늘 ticks 유효행 **8%**. 09:01·09:21 두 사이클만 정상, **09:40부터 장 마감까지 전 종목 price=0**. 7/13과 완전히 동일한 지문(F-9의 정체).
- **근본 원인 확정**: KIS API는 레이트리밋·오류를 **HTTP 200 + `rt_cd != 0` + `output` 키 결손**으로 반환한다. 기존 `_fetch_snapshot`은 `r1.json().get("output", {})` → `int(d1.get("stck_prpr", 0))` = **0을 그대로 기록**하고 예외가 없으니 `poll_once`는 이를 **성공으로 집계**했다. 오늘 로그 실측: `체결 폴링 #10: 2530/2531종목` — 성공이라 찍혔는데 내용은 전부 0. **조용한 데이터 오염.**
- **피해 규모 (VPS 전수 감사)**: ticks 79일 중 **15일 BROKEN(19%)** — 3/24·3/25·3/26·4/3·4/8·4/24·4/29·5/19·5/26·5/27·5/28·6/9·6/22·7/13·7/20. 즉 **"74일 백테스트"의 실제 유효일은 ~64일**이었고, 페이퍼 5일 중 2일이 무효(유효 3일).
- **Fix A (근본·`data/tick_collector.py`)**: `rt_cd != "0"` → 행 미기록·실패 집계 / `price <= 0` 가드 / 사이클 종료 시 실패율 로그(50%↑ = ERROR). 이제 거부는 거부로 인식되고 **로그로 보인다**.
- **Fix B (증거 보호·`tools/observe_v2_paper.py`)**: `_ticks_health` 신설 → 장부 일별 `ticks_health` 기록, 스코어보드가 **"⚠ 수집장애일 2일 포함 — 유효 판정일 3일"**을 명시. 수집장애를 "신호 없던 날"로 오독하는 것 차단.
- **S-1 판정(7/31)에 미치는 영향**: 실측 -32.4%p와 백테스트 +엣지의 간극 원인 후보에 **"표본 19% 결손"**이 추가됐다. 주말 갭 분석은 반드시 **유효일 기준**으로 다시 계산해야 한다.
- **★ Tier 1이 내 1차 fix의 치명 결함을 적발 (커밋 `d30e113`으로 반영)**:
  - **[High-1] 자기증폭** — early return이 `_fetch_snapshot`의 sleep 2회를, `continue`가 나머지 1회를 건너뛰어 **장애 중 요청속도가 성공경로의 ~3배**. 레이트리밋이 원인이면 내 fix가 리밋을 더 세게 밟는 구조였다. → 실패 경로 sleep + 연속 50건마다 10초 백오프. **실측 10.1 req/s (기준선 20의 절반 = 물러남)**.
  - **[High-2] 복구 경로 부재** — broker 세션 1회 생성·재발급 없음 → 토큰 만료면 하루 종일 0 수집. 연속 150건 시 broker 재생성 1회.
  - **[자체 발견] High-2가 낳은 신규 위험** — `_get_broker()`가 try 밖이라 **재발급 실패 시 예외가 폴링 스레드를 죽인다**. 모의로 잡아 격리(최악 시나리오에서 루프 생존 확인).
  - **[Med-2]** 파일 미생성/극소일도 BROKEN 판정 — price=0 미기록 fix가 파일 미생성 확률을 올려 '정상 0건 날' 위장 구멍이 생겼던 것.
  - **[Med-3]** 누적에 **유효일 기준 병행** — 보고문이 "★유효 3일 · 승률 · 순손익 ← S-1 판정 기준"을 명시. 라벨만 붙이고 오염 누적을 방치하던 것 해소.
- **잔여**: [F-14] 상류 원인 확증(다음 BROKEN 시 로그에 사유가 남는다)·[F-15] 20:10 요약에 ticks 채널(HIGH).

## §4. 개선점 각주 (DAILY_ROUTINE §4)

- 오늘 소진: **F-11**(금요일 예약 격리 실행 검증) + **~~F-9~~ 규명·근본 fix 완료**(7/13 ticks 8% = 오늘과 동일 사고·§5).
- 신규: **[F-14]** ticks 수집 상류 원인(레이트리밋 추정) 재시도·백오프 설계 — 오염 차단은 완료, 손실 자체를 줄이는 건 다음 단계 (MED).
- 신규: **[F-15]** **20:10 데이터 요약에 ticks 채널 추가** — grep 실측으로 확증: `data_verifier`·⑳·notify **어디도 ticks를 보지 않는데**(검증기 0개) 판정 소비자는 3개(러너·⑲-3 페이퍼·⑲ replay). 사장님이 매일 보시는 자리에 ticks 건강도가 있어야 이번 같은 19% 손실이 조기 발견된다. 내일 착수 (**HIGH — 오늘 사고의 재발 방지 본체**).
- 이월: F-4(notify 타임스탬프)·F-5(swap_monitor git)·F-6(_staging 정리)·F-7·F-8·F-10(섹터쿨다운 결합)·F-13(재편입 파이프)

## 자기성찰 (3일차)

1. **오늘 잘한 것**: ① 휴장 fix가 실제 거래일 전환(금 skip→월 정상)에서 재발 0으로 검증됨. ② 사장님 "문제 없나?" 한마디에 **내가 단정한 문장을 되짚어 검증**해서 19% 데이터 오염을 찾아냄 — 발견의 공은 질문에 있다. ③ 로컬 미러에서 "2~5월 대부분 BROKEN"으로 보였을 때 바로 보고하지 않고 **VPS 원본으로 재확인**해 15일로 정정(증분 sync 잘림 착각을 걸러냄).
2. **미흡·실수**: ★ **검증 없이 "조용한 장"이라고 일지에 단정했다.** 신호 1건이라는 이상 신호를 눈앞에 두고 원인을 안 물었다. 더 뼈아픈 건 — **바로 그날 내가 쓴 교훈("안 변한 것도 검증 대상")을 같은 문서 안에서 스스로 어겼다.** 교훈을 쓰는 것과 지키는 것은 다르다.
3. **새 교훈**: ① **"성공 카운트는 성공의 증거가 아니다"** — 수집기는 2530/2531 성공이라 찍었지만 내용은 전부 0. 파이프 건강은 **건수가 아니라 내용**으로 검증(ticks는 검증기 0개인 사각이었다). ② **"사고 fix가 사고를 키울 수 있다"** — 내 1차 fix는 실패 경로에서 페이싱을 없애 레이트리밋을 3배로 밟는 코드였다. **Tier 1을 건너뛰고 배포했으면 내일 더 크게 터졌다.** 4-Tier는 형식이 아니라 방어선이다. → 둘 다 영구 메모리 저장 O.
4. **내일 적용 1가지**: **S-3·S-4·S-5 판정안 3건 작성** — 데드라인 체계 첫 실전. 단, 판정 근거 데이터는 반드시 **유효일 기준**으로.
5. **데이터 성찰**: 누적 -32.4%p 중 7/16 폭락장 하루가 -30.9%p인데, 여기에 **표본 19% 결손**까지 확인됐다. 즉 지금 "-32.4%p"라는 숫자는 ① 유효 3일뿐 ② 그중 하루가 극단 폭락장 — **통계로 취급할 물건이 아니다.** S-1 판정(7/31)의 첫 과제는 "엣지가 있나"가 아니라 **"판정 가능한 표본이 모였나"**다. 표본 부족이면 연장 요청이 정직한 답이다.

---
**불변식 확인**: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉·picks 불변·SAJANG 단일진실 ✅
