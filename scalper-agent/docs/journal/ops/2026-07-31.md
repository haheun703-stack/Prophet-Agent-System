# 업무일지 — 2026-07-31 (금)

> 사장님 지시: "7/30 기준 종가·CSV·수급 기초데이터 업데이트 확인 → 일일 개선점 → **그 개선을 실천·행동할 수 있게 자동화 코드로 적용**하고 진행하자."
> 불변식: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉·SAJANG 단일진실·관측 없이 flip 금지

## 📊 스코어보드 (보고 첫 줄 = 숫자 — 7/17 약속)

**유효일 11일 · 상한 5건/일 · 51건 · 승률 33.3% · 순누적 −69.67%p (−209,010원/30만·건)**
S-1 데드라인 **D-29**(8/29) · S-6 **D-7**(8/7)

---

## §1. 아침 루틴 A1~A9 (전부 VPS 실측)

| # | 항목 | 결과 | 근거 |
|---|------|------|------|
| A1 | 전일(7/30) 기초데이터 | ✅ | 종가·투자자수급 10/10·시장11주체 7/30·공매도 7/30·외국인소진율 7/30(9/10)·신용 7/27(T+지연 정상)·장중틱 100.0% |
| A2 | nightly 완주 | ✅ | 7/30 18:00:02~19:03:22 (3800s) **30/30 성공** · ⑳ "완료위장 없음" |
| A3 | 20:10 텔레그램 발송 | ✅ | `[notify] 텔레그램 발송 완료` |
| A4 | OBSERVE v2 러너 | ✅ | 7/30 누적 101건 / **오늘(7/31)도 가동 중 누적 159건**(09:05~10:55·실주문 0) |
| A5 | ⑲-2 대조 | ✅ | 7/30 **PASS 일치 101/101 · drift 0 · miss 0 · phantom 0** (지연 med 207s max 1516s) |
| A6 | 봇·안전 불변식 | ✅ | `bodyhunter-bot` active · PAPER_ONLY=True · **자동매매 스위치 7/7 ON**(실매매 차단은 PAPER_ONLY 단일 — 7/30 보고분 그대로) |
| A7 | 로컬 미러 | ✅ | `daily/005930.csv` 마지막 행 2026-07-30 |
| A8 | 업무일지 | ✅ | 이 문서 |
| A9 | 전략 데드라인 | ⚠ | S-1 D-29 ❌미달(−69.67) · S-6 **D-7** 수동 판정 |

**결론: 7/30 기준 종가·CSV·수급 기초데이터는 전 채널 정상 적재 확인.**

부수 관측 — `data_verify_result.json` mtime이 오늘 06:33(20:10 정규 실행이 유일 writer여야 하는 시각이 아님).
내용은 `date=2026-07-31 status=FAIL daily_ohlcv 0/10`. **장전 박제 착시**(7/1 기 규명)로, 06:00 봇 재시작 직후
fill 전 스냅샷이다. A1은 이 파일이 아니라 원본 CSV를 직접 읽으므로 판정 무영향. 신규 사고 아님.

---

## §2. 오늘의 개선 — [F-29] 아침 점검 무인화 (자동화 코드 적용)

### 문제
저녁 20:10 데이터 요약은 cron 자동인데 **아침 점검만 사람(세션) 의존**이었다.
7/29 세션 미개설로 A1~A9가 하루 통째 공백이 된 것이 실증. 오늘 아침에도 단타봇이
**SSH 8회**를 수동으로 돌려 위 표를 만들었다 — 세션이 안 열리면 그 8회는 아무도 안 돈다.

### 처방 (신설 `tools/daily_ops_check.py` · 평일 **08:30 cron** · 텔레그램 1건)
read-only 관측기. A1~A6·A9를 자동 판정해 사장님께 1건 발송.
A7(노트북 미러)·A8(업무일지)은 VPS에서 관측 불가/사람 몫이라 대상 외.

설계 요점 — 과거 사고를 코드로 되갚은 것들:

1. **기준 거래일 앵커** — "로그의 마지막 블록"이 아니라 *ref(직전 거래일) 블록*을 찾는다.
   마지막 블록으로 짜면 **휴장일 다음 거래일마다 정상을 FAIL로 오탐**한다(A2·A3·A4 공통).
   점검기가 늑대소년이 되면 존재 이유가 사라진다.
2. **SKIP→WARN 승격 + 초록불에 "점검 불가 N건" 병기** — 운영기(VPS)에서 로그 부재는
   '정상 스킵'이 아니다. 이게 없으면 로그가 통째로 사라진 날 전 항목 SKIP → "이상 없음"이
   발송된다([F-22] 조용한 사망과 정확히 같은 통로).
3. **판정 수치 위임** — 스코어보드/데드라인은 `strategy_deadline_check.build_report()`를
   호출한다. 클론을 뜨면 [F-37](CAP_VIEW_N 로컬 복제) 재발이다. 이를 위해 해당 파일의
   main() 로직을 함수로 분리(동작 무변경·⑲-4는 subprocess `--notify` 호출이라 무영향).
4. **항목별 예외 격리 + exit 0** — 한 항목이 죽어도 나머지 점검과 발송은 산다(cron 무차단).
5. **[F-4] 동시 소진** — `notify_data_freshness.py`에 실행 스탬프
   `[notify] === YYYY-MM-DD HH:MM:SS 실행 (기준 <date>) ===` 추가. 이 로그엔 시각이 없어
   A3 확인이 tail 순서에 의존했다. 스탬프가 있어야 (1)이 성립한다.

### 검수 (4-Tier)
- **Tier 2** 호출 site: ⑲-4는 `[PY, "tools/strategy_deadline_check.py", "--notify"]` **subprocess** →
  리팩터 무영향 확인. `build_report` 이름 충돌 없음(다른 2곳은 시그니처·모듈 상이·top-level import 0).
  로그 로테이션 부재 확인(logrotate.d에 bodyhunter 항목 0·`.1/.gz` 0) → tail 한도 안전.
- **Tier 3 VPS 실증** — 양성/음성 대조 모두 수행. **음성 대조가 버그 2건을 잡았다**:
  - `last_trading_day(d)`가 **d를 포함하지 않는** 직전 거래일인데 `today-1일`을 넘겨 **이틀 전**이
    기준이 됐다 → A3가 매일 오탐할 뻔. (VPS 첫 실행에서 기준 7/29로 찍혀 발각)
  - 리팩터 잔재 `NameError: name 'day'` — 요약 라인 결손 경로에서만 터짐.
    **항목 격리 설계 덕에 전체 점검은 살아남았고**, 격리가 없었으면 조용히 전체가 죽었다.
- **pre-commit** RULE-001~008 통과.

---

## §3. [F-19] 규명 — 결론이 뒤집힘 + [F-46] 신설

7/30 장부에서 **cap5 −21.07%p인데 일일 CB는 −6%p**인 게 눈에 걸려 정량화했다.

**상한 5건 시퀀스 안에서 CB를 재적용해도 11 유효일 전부 무변화**
(51건 −69.67%p → 51건 −69.67%p · 차이 **0.00%p** · CB로 잘린 날 **0일**).

원인 실측:

| | 값 |
|---|---|
| 상한 5건 **진입** 소요(첫→막) | 중앙 **7분** (최소 0분·최대 25분) |
| **CB 발동** − 마지막 진입 | 중앙 **+30분** · **8/8일 전부 사후** |
| 7/30 실례 | 진입 09:26~09:31 완료 → CB 09:46:35 |

즉 **정합 문제가 아니라 CB 자체가 이 실행 구조에서 무력**하다. 청산이 실현되기 전에
그날 5건이 이미 다 들어가므로 서킷이 먹일 손실이 없다. → [F-19] 종결, **[F-46] 신설(라이브 전 HIGH)**.

**이건 안전 관련이라 사장님께 정확히 보고드려야 한다**: 7/9 지시서 백테스트에서 "CB 일일 −6%p 유효"로
통과한 것은 **무제한 체결 가정**이었고, 사장님 자금 룰(상한 5건)에서는 아무것도 막지 못한다.
대안 축(택1 이상·**결정은 사장님**): ①미실현 기준 CB ②진입 간격 강제(실현이 CB를 먹일 시간을 만듦) ③건당 손실 상한 하향.

---

## §4. 판정 숫자 정직성 점검 — "장부가 부당하게 나쁜 것 아닌가"

−69.67%p가 해상도 탓 아니냐는 반론을 **먼저 스스로** 검정했다(유리한 방향이라 더 엄격히).

- 유효일 cap5 51건 중 **SL 청산 23건(45%)** · TP 5 · EOD 23
- SL 청산 실제 ret: 중앙 **−3.52%** · 평균 −3.99% · 최악 −11.35% · **23/23건이 −3%보다 나쁨**
- 해상도 초과손실 합 **−22.66%p**(건당 −0.99%p)
- **SL이 정확히 −3.00%에 체결됐다면 −47.01%p** → **부호 불변**

결론: 해상도 편향은 실재하고 크기도 −22.66%p로 작지 않으나, **가장 관대한 가정에서도 판정은 미달**이다.
S-1 결론은 이 보정과 무관하게 유지된다.

---

## §5. 사장님 결정 대기 (누적)

1. **[F-46] CB 재설계** — 안전핀 3종 중 CB가 현재 무효(오늘 실증). 대안 3안 중 택1.
2. **kill_switch** — VPS 실제 false·자동매매 스위치 7/7 ON. 실매매 차단은 PAPER_ONLY 단일. 이중화 원하시면 true 복원.
3. **[F-40]** ETF 주도주 가산항 — 항상 0(매매 무해). 선정 로직에서 걷어낼지.
4. **[F-21]** 상한 구조 자체(5 vs 3 vs "밀도 높은 날 넓게") — 7/30 수렴 결론 관련.

---

## §6. 오후 — 전체검수 (5관점 병렬 + 전건 직접 재검증)

사장님 지시: "전체 커밋+푸시하고 전체 검수 진행해라 — 코드/로직/버그/슬러지".

### ★★★ 최대 발견 — 사장님 룰 자동검증이 66일간 존재한 적이 없다

`trading_coo.py:5003`의 `is_trading_day` 한 줄이 **모듈 바인딩 부재로 NameError**.
가드가 try 밖이라 예외가 스케줄러로 전파돼 job 전체가 죽었다.

- VPS 저널 실증: 매 평일 **15:45:00 NameError** (10일간 20회 · **오늘 7/31 15:45 포함**)
- 도입 커밋 `d18c3bd`(5/26) = **Daily Self-Audit을 신설한 그 커밋** = 태어날 때부터 고장
- fix 후 첫 실행에서 **즉시 위반 3건** 검출: 실계좌 현금 270,892 / 총평가 17,847,392
  = **1.5%** (30% 룰 대비 −28.5%p). **66일간 이 경보가 매일 나왔어야 했다.**

같은 뿌리로 09:15 첫 매수 가드(재가동 즉시 조용히 사망할 잠복)·봇시야 송출(하루 10회 실패)도 NameError.

**근본**: CLAUDE.md가 5/25부터 "RULE-002가 `is_trading_day` 가드를 자동 차단"이라 적어왔는데
**구현이 없었다**. 문서를 믿고 "검수 통과"를 보고해온 것이다.
→ pyflakes undefined-name을 **RULE-002(HIGH·차단)로 실제 구현**. 이번 사고는 "가드가 쓰여
있는데 동작 안 함"이라 존재 여부만 보는 규칙으론 못 잡는다 — **이름이 실재하는가**를 본다.

### 당일 fix (전부 4-Tier + VPS 실증)

| 구분 | 내용 |
|---|---|
| 휴장일 매도 통로 | `job_daily_reeval`·`job_eod_close` 가드 추가(14개 형제 job과 비대칭이었음) |
| SAJANG 단일진실 | budget_mode default off 3곳·룰D 리터럴 2곳·HARD_KILL 2상수 (값 전부 동일=동작 무변경) |
| 경보 오진 | A5 verdict 오진(내 오늘 코드)·스코어보드 silent-zero·nightly stdout 소실·RULE-008 주석 오탐 |
| P0 | NameError 3곳 + RULE-002 신설 + morning_recommendation 4기능 |
| 신규 도구 | `s6_sector_reversal_judge.py` (S-6 D-7 판정 방법 고정) |

### 에이전트 보고 중 기각한 오탐 3건 (전건 재검증 규약)

1. "3중 차단 정상" — **로컬 저장소 파일 기준**. VPS 실제는 kill_switch=false·스위치 7/7 ON.
2. "positions.json 잔존 2건 → flip 즉시 매도" — gitignore 대상 **노트북 5/27 박제본**. VPS는 0건.
3. "수급 CSV 100% placeholder(CRITICAL)" — 노트북 미러. **VPS 실측 61/572(10.7%)**.
   메커니즘은 실재하므로 [F-49]로 등재(HIGH), 심각도만 정정.

Codex CRITICAL 5건도 전부 **fix가 남긴 '문제 설명 주석'을 현재 상태로 오독**한 것으로 판정
(근거: import가 :4812·:5017에 실재·pyflakes 0건). 7/14·7/30과 같은 유형.

### 백로그 F-49~F-80 등재 (32건)

[사장님] 결정 5건: **F-62**(룰4·룰B가 룰D 종목 미커버) · **F-67**(positions.json 미등재=손절 전면 불능)
· **F-68**(+25 vs +29 문서-구현 괴리) · **F-71**(룰B timing_mode 모순) · **H-2 부스트 2종 활성화**

---

## 자기성찰 (7/16 사장님 지시 — 매일 개선하는 단타봇)

1. **오늘 잘한 것**
   - 사장님 지시("자동화로 실천")를 **오늘 아침 내가 실제로 겪은 비효율**에 정확히 붙였다.
     SSH 8회 수동 점검 → 1커맨드 → 08:30 무인. 어제 자기성찰에서 고른 "내일 적용 1가지"를 그대로 소진했다.
   - **음성 대조를 안 건너뛰었다.** 정상 케이스만 봤으면 버그 2건(기준일 −1일 오프셋·NameError)을
     그대로 배포했고, 매일 오탐하는 점검기는 첫 주에 신뢰를 잃었을 것이다.
   - **유리한 방향의 반론을 내가 먼저 검정했다**(§4). −69.67이 해상도 탓이라는 변명 여지를
     남기지 않고 −47.01(관대 가정)까지 계산해 부호 불변을 확인했다.
2. **미흡·실수**
   - `last_trading_day`의 시그니처를 **읽지 않고** "직전 거래일이니 today−1을 넣자"고 추측했다.
     정본 docstring에 "d 포함하지 않음"이 명시돼 있었다. VPS가 아니라 **코드를 먼저** 봤어야 했다.
   - 리팩터(main→build_report) 후 **잔여 참조를 grep하지 않고** 컴파일 통과만 믿었다.
     `NameError`는 컴파일로는 안 잡힌다. 리팩터 시 삭제한 지역변수는 전수 grep이 규약이어야 한다.
3. **새 교훈** (영구 메모리 저장 **O**)
   - **★ 문서가 "자동으로 차단한다"고 말하는 것을 믿지 말고 구현을 확인한다.** CLAUDE.md가
     5/25부터 "RULE-002가 `is_trading_day` 가드를 자동 차단"이라 적어왔는데 구현이 0건이었고,
     그래서 P0 3건이 66일을 살았다. **나는 그 문서를 근거로 "검수 통과"를 여러 번 보고했다.**
     체크리스트에 적힌 방어선은 등재 시점에 실행으로 확인해야 한다.
   - **"가드가 쓰여 있는가" ≠ "가드가 동작하는가".** 존재 여부만 보는 규칙으로는 오늘 사고를
     못 잡는다. 이름이 실재하는지(pyflakes undefined-name)를 봐야 한다.
   - **"정상 케이스 통과"는 점검 도구의 검수가 아니다.** 감시 도구는 *이상을 만들어 넣었을 때
     빨간불이 켜지는가*로 검수해야 한다. 오늘 버그 대부분이 음성 대조에서만 나왔다.
   - **에이전트·Codex 보고는 데이터 출처부터 의심한다.** 오늘 오탐 8건 중 3건이 "노트북 미러를
     VPS 실상태로 오인", 5건이 "fix가 남긴 문제 설명 주석을 현재 상태로 오독"이었다.
     그대로 올렸으면 사장님께 틀린 안전 판정을 보고할 뻔했다.
   - **안전핀은 '검증됐다'가 아니라 '어느 실행 구조에서 검증됐나'로 기억해야 한다.**
     CB −6%p는 무제한 체결 가정에서 유효했고 상한 5건에서는 무효다. 7/30 "전신호 평균=비실행 가정"
     교훈의 **안전장치 판(版)** — 성과 지표뿐 아니라 **안전장치도 자금·시간 제약 통과 후에만 인용**.
4. **내일 적용 1가지**
   - **08:30 첫 무인 실행 결과를 사장님 텔레그램에서 확인**하고, 오탐 0인지 대조한다.
     오탐이 1건이라도 나오면 즉시 잡는다(점검기의 신뢰는 첫 주에 결정된다).
5. **데이터 성찰**
   - 7/30 cap5 첫 체결(085910)은 09:26:03 진입 → 09:46:35 SL 청산 **−11.35%**. `why=SL`인데
     −3%가 아니다. 근본원인 1줄: **틱 해상도(~20분)에서 SL은 "−3% 이탈 후 다음 관측가"로 체결되므로,
     급락 구간에서는 손절선이 사실상 존재하지 않는다** — 이 1건이 그날 −21.07%p의 절반을 만들었고,
     [F-46](CB 무효)와 같은 뿌리(진입은 즉시, 방어는 다음 관측)에서 나온다.

---
**불변식**: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉·SAJANG 단일진실·관측 없이 flip 금지
