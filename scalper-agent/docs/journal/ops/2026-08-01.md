# 2026-08-01 (토) 업무일지 — 7/31(금) 데이터 적재 전수 확인

> 사장님 지시: "7/31(금) 종가·CSV·각자 맡은 수급들 모두 업데이트 되었는지 확인하자"
> 토요일(휴장) — `daily_ops_check` 08:30 cron은 평일 전용이라 미실행 → `--force --date 2026-07-31`로 수동 실행.
> 불변식: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉(nationality 계열 점검 대상 제외).

**📊 스코어보드 (⑲-3 페이퍼·유효일·상한 5건/일)**
**56건 · 승률 37.5% · 순누적 −55.75%p (−167,250원)** · S-1 D-28(8/29) · S-6 D-6(8/7)
→ 7/31 반영으로 51건 −69.67%p → **56건 −55.75%p** (당일 5건 기여 +13.92%p)

---

## §1. 아침 루틴 A1~A9 (기준 거래일 7/31)

| # | 항목 | 결과 | 근거 |
|---|------|------|------|
| A1 | 전일 기초데이터 | ✅ | 핵심 채널 전부 PASS (아래 §2 독립 실측으로 교차검증) |
| A2 | nightly 완주 | ✅ | `요약: 30/30 성공` (18:59:35) · ⑳ "완료위장 없음" |
| A3 | 20:10 텔레그램 | ✅ | 스탬프 `[notify] === 2026-07-31 20:10:01 실행 (기준 2026-07-31) ===` + 발송 완료 |
| A4 | OBSERVE v2 러너 | ✅ | 20260731 누적 159건 |
| A5 | ⑲-2 대조 | ✅ | 일치 159/159 · drift 0 · miss 0 · phantom 0 |
| A6 | 봇·안전 불변식 | ✅ | service=active · PAPER_ONLY=True · 자동매매 스위치 ON 7/7 |
| A7 | 로컬 미러 | ✅ | VPSSync 7/31 20:30 수신 — `daily/005930.csv` 7/31행·`flow_market/kospi` 20260731행 존재 |
| A8 | 업무일지 | ✅ | 본 문서 |
| A9 | 전략 데드라인 | ✅ | S-1 D-28 미달(−55.75 vs 기준 >0) · S-6 D-6 수동 판정 |

**추가 확인 — 어제 약속분(06:00 재시작)**: `ExecMainStartTimestamp=Sat 2026-08-01 06:00:31 KST` ·
`deploy_pull.log` "up-to-date(dc0f8c7) → bot restarted" · 상주 코드 pyflakes undefined-name **0건** ·
`trading_coo.py:5017`에 `from data.trading_calendar import is_trading_day` 실재.
→ **7/31 15:45은 fix 前이라 여전히 NameError(저널 실증·66일차 마지막 실패)**.
Daily Self-Audit 첫 정상 실행은 **8/3(월) 15:45**.

---

## §2. 채널별 독립 실측 (검증기 불신 · 원본 CSV 직접 계수)

> [F-49](검증기가 placeholder 행을 성공 집계) 때문에 A1 초록불을 그대로 채택하지 않고 전수 재계수.
> 도구: `scratchpad/audit_0801_channels.py`·`audit_0801_b.py`·`audit_0801_c.py` → §4 [F-81]로 승격 예정.

| 채널 | 적재 | 판정 |
|---|---|---|
| ① 종가(daily OHLCV) | **2492/2531 (98.5%)** · 종가0 **0건** | ✅ 결손 39종 = 전부 거래정지(마지막 적재일 6/25~7/30 산개) |
| ② 투자자수급(flow) | **2527/2531 (99.8%)** | ✅ 아래 placeholder 분해 참조 |
| ③ 외국인 소진율 | **2451/2531 (96.8%)** | ✅ 19:40 재수집분 반영(로그 당일적재 2451 일치) |
| ④ 시장 11주체 kospi | ✅ 20260731 · 비영 13/14 · 지수 6595.45 | ✅ |
| ④ 시장 11주체 **kosdaq** | ✅ 20260731 · 비영 13/14 · 지수 719.76 | ✅ **[F-54] 검증 사각이나 실제 적재는 정상** |
| ⑤ 신용잔고 | 최신 **7/28 (T+3)** · 저장 2491/2531 | ✅ 허용 7일 이내(`_verify_credit_kis`) · 계열 무결손(7/22·23·24·27·28 연속) |
| ⑥ 공매도 | **2526/2531 (99.8%)** | ✅ (전체 4165파일 중 1567건 2월 정지분은 죽은 종목 — 활성 기준 재계수함) |
| ⑦ 장중틱 | 2527파일 **47,978행 전수 price>0 100.00%** | ✅ M1 soft-fail fix 후 완전 유효일 |
| ⑧ 순위 스냅샷 | **5/6** (volume 725·fluctuation 725·strength 353·foreign_inst 1500·uplowprice 348 전부 7/31) | ⚠️ `night_futures.csv` 파일 자체 부재 |
| ⑨ foreign_f1 | ✅ `f1_2026-07-31.json` | ✅ |

### ★ ② placeholder(수급 6항목 전부 0) 68종 분해 — [F-49] 심각도 정정

- 전수 **68/2527 = 2.7%** (어제 등재값 10.7%는 **수집 완주 前 측정**이라 과대였음 — 분모 572)
- 분해: **35종 = 종가도 결손(거래정지)** / **31종 = 만성**(최근 20일 중 대부분 0·초소형주) /
  **2종만 7/31 신규**(049830 승일·263920 휴엠앤씨)
- **2종 판정 = 정상값**. 근거(코드): `flow_collector._fetch_investor_api`는 `rt_cd != "0"`이면 `None`
  반환하고, 호출부(:265)는 `df is not None and len(df) > 0`일 때만 행을 쓴다.
  → **행이 존재한다 = KIS가 성공 응답했다.** 그 행의 `종가`·`전일대비`는 수급과 **같은 `item`에서**
  파싱되는데(:346-348) 실측값이 정상(049830 종가 6200/−40 · 263920 3525/+20)이다.
  즉 수급 0은 **미수집이 아니라 KIS가 실제로 준 순매수 0**(초소형주 개인↔개인 체결로 전 주체 net 0).
  049830 당일 거래량 959주·263920 3459주로 규모도 정합.
- **결론**: [F-49]의 구조적 지적(검증기가 값을 안 본다)은 **유효**하나, **7/31 데이터에서 이로 인한
  누락은 0건**. 심각도 **HIGH → MED 정정**. 동시에 **판정식 수정 필요** — "수급 0 = 실패"로 잡으면
  오늘만 68건 오탐이 난다. 올바른 식은 **"종가·전일대비까지 0인 행"** 또는 `_last_update.json`의
  `data_through` 교차검증(`date=2026-07-31 / data_through=2026-07-31 / investor 2531 vs real 2526`).

---

## §3. 오늘의 지적 (신규·정정)

- **[F-49] 정정** — HIGH → **MED**, 판정식을 "수급 0"이 아니라 "종가까지 0 + `data_through` 교차"로 재정의.
  (어제 근거 수치 10.7%는 수집 중 측정이었음을 명시)
- **[F-53] 재확인·범위 축소** — `night_futures.csv` **파일 자체 부재**. 단 nightly 로그상
  7/28·7/29·7/30·7/31 **전일 `night_futures: 0`**이고 `status: ok`. 즉 **7/31 회귀 아님·상시 미생산**.
  `ranking_snapshot_collector.py:128-137`이 장외/실패 모두 `saved=0`으로 뭉개 원인 구분이 안 됨.
- **[F-81] 신규** — 오늘 실측 도구 3종이 scratchpad에만 존재. [F-78]과 같은 자리 →
  `tools/manual/verify_channels.py`로 통합 승격 필요(채널 전수 계수는 매 검수마다 재사용).
- **[F-82] 신규** — `data_verifier`의 CSV 판독기가 **BOM(`﻿`) + `YYYYMMDD` 포맷을 쓰는
  `flow_market`·`ranking_snapshots`를 별도 경로로 다룬다**. 오늘 내 1차 스크립트가 이 둘을 전부
  "최신 없음"으로 오판했다(내 버그). 채널마다 날짜 표기가 3종(`YYYY-MM-DD`/`YYYYMMDD`/BOM 유무)
  난립 = 향후 판독기 작성마다 같은 오판이 재발한다. 공용 `read_dated_csv()` 정본 필요 (MED).

---

## §4. 자기성찰

1. **오늘 잘한 것**
   - A1 초록불을 **채택하지 않고** 원본 2531종목 전수 재계수 → placeholder 68종을 찾아 **35/31/2로 분해**하고,
     2종의 정체를 **수집기 코드(`rt_cd` 가드 + 같은 item에서 종가 파싱)로 확정**했다. 로그가 아니라 코드로 닫음.
   - 어제 내가 HIGH로 올린 [F-49]를 **오늘 데이터로 스스로 낮췄다**(10.7%는 수집 중 측정 과대).
     내 어제 보고를 방어하지 않았다.
   - 어제 약속한 06:00 재시작을 **말로 넘기지 않고 `ExecMainStartTimestamp` + pyflakes 0으로 실측**했다.

2. **미흡·실수**
   - **내 1차 실측 스크립트가 ④·⑧을 "최신 없음"으로 오판했다.** BOM과 `YYYYMMDD` 포맷을 안 봤다.
     하마터면 **"시장 11주체·순위 스냅샷 미적재"라는 허위 경보를 사장님께 올릴 뻔했다.**
     검증기가 맞았고 내가 틀렸다 — [F-82]로 등재.
   - 어제 [F-49] 근거로 쓴 "572개 중 61개(10.7%)"는 **분모가 수집 중 스냅샷**이었다.
     7/30 "전신호 평균=비실행 가정" 교훈(**측정 시점의 제약을 표기하라**)을 **바로 다음 날 내가 어겼다.**

3. **새 교훈** (영구 메모리 저장: **O**)
   - **"값이 0"과 "값이 없음"은 다르다.** 판정식이 둘을 구분하지 못하면 오탐(오늘 68건)이나
     미탐([F-49] 통로) 중 하나가 반드시 난다. 구분 근거는 **같은 응답에서 온 다른 필드가 정상인가**.
   - **수집 중(장중) 측정치는 분모가 다르다.** 심각도를 매길 땐 **완주 후 재측정**이 의무.
   - **검증기를 의심하는 것과 내 판독기를 믿는 것은 다르다.** 오늘 불일치의 원인은 검증기가 아니라
     나였다. 대조에서 갈리면 **양쪽 다 검사**한다.

4. **내일(8/3 월) 적용 1가지**
   - **08:30 첫 무인 실행 + 15:45 Daily Self-Audit 첫 정상 실행**을 둘 다 텔레그램에서 확인한다.
     15:45은 **66일 만의 첫 실행**이므로 위반 3건(현금 1.5% 등)이 그대로 올라오는지 대조한다.

5. **데이터 성찰**
   - 7/31 스코어보드가 **−69.67 → −55.75%p로 +13.92%p 회복**(5건 추가·승률 33.3→37.5%).
     같은 날 삼성전자 **+26.81%**·코스피 **+17.9%**의 폭등장이다.
   - ~~근본원인: S-1의 손실은 종목 선택이 아니라 실행 구조(선착순 진입)에서 나온다~~
     → **★같은 날 오후 [F-84]가 이 문장을 반증했다.** 선택 규칙 비용은 **−4.63%p = 전체의 8.3%**뿐이고
     손실의 **92%는 '그날 후보 전체가 마이너스'**에서 나온다. 오전에 쓴 이 줄은 [F-26]의 서사를
     검증 없이 반복한 것이었다. **아침에 쓴 문장을 오후에 스스로 지웠다는 사실 자체를 남겨둔다.**

---

## §5. 오후 — 사장님 지시 "책 내용을 코드로 자동화, 순차 적용"

> 사장님이 피터 린치 《투자 이야기》(Learn to Earn) 전문을 주시고 "우리 단타봇이 취할 것"을 물으심.
> → 3분류(그대로 취할 것 / 번역해서 취할 것 / 취하면 안 되는 것) 정리 후 **코드 자동화 4단계** 착수.
> 지시 순서: 1단계 실행 → "2번부터 하고 1번" → 후보 변수 승인.

**신설 도구 3종** (전부 read-only·매매 무접촉·판정 로직 import 재사용으로 복제 0건)

| 도구 | 목적 | 커밋 |
|---|---|---|
| `lynch_concentration.py` | 손익 집중도(p.155·p.260) + 날/선택 효과 분해 | 69e435c |
| `f83_backtest_ledger_gap.py` | 백테스트↔장부 갭 + 열화 검사 | 2c59f64 |
| `f87_catalyst_signal_overlap.py` | 명분↔v2 교집합 규명 | 8c319a8 |
| `f86_june_regime_probe.py` | 6월 규명(기각조건 4개 사전 고정) | 40f78b9 |

**연쇄 발견 (각 단계가 다음 질문을 낳음)**

1. **[F-84]** 린치 가설 성립 — 최대 수익일 1일이 플러스의 56%. 최악 4일 제거 시 +4.28%p 전환.
   그러나 날/선택 분해 결과 **선택 비용 8.3%뿐**, 손실의 92%가 '날' 쪽 →
   자원을 '고르는 순서'에서 **'후보에 무엇을 넣는가'**로 이동해야 함.
2. **[F-83]** 같은 12일로 자른 백테스트 = **−0.186%p/건** → 앵커 +0.173은 **표본 효과**.
   러너 커버리지 100% 일치(=[F-60] 기각) · CB는 −125%p 방어(=[F-46] 정정: 넓게 담으면 작동).
   월별 분해 결과 **엣지는 6월 한 달**(+1.601·승률 71.2%)에만 존재. 6월은 KOSDAQ −12.75% 하락장.
3. **[F-87]** 명분↔v2 교집합은 무작위 기대의 **0.25배 = 서로 밀어냄**.
   원인 = **추격 상한 8%가 명분 종목 38.6%를 직접 배제**(첫 관측에 이미 8% 초과 41.7%·[F-31] 결합).
   명분 종목만 보면 진짜 경계는 **15%**(총 net 8%: +53.4 → 15%: **+137.5**).
   ⚠ 단 명분이 18:28 산출이라 **실전 불가** → 처방은 상한 완화가 아니라 **장중 실시간 명분 탐지**.
4. **[F-86]** 기각조건 4개를 **사장님 승인 후 실행 前 고정**하고 진입.
   조건4 통과 V2·V4 / 조건3 통과 V1 / **둘 다 통과 0개 → 6월 설명 실패**.
   V5 무효화 검사는 통과(6월 품질 100%) = **착시가 아니라 진짜인데 설명이 안 됨**.
   → 사전 선언대로 **S-1은 8/29 폐기가 정직한 결론**(폐기 결정은 사장님 권한).

## §6. 오후 자기성찰

1. **잘한 것**
   - **사전에 건 조건을 지켰다.** [F-86] V1(변동성)은 6월 최고·최상위 3분위 +0.534·out-of-sample도
     통과였다. 조건4(단조성) 하나만 완화하면 "6월을 설명했다"고 보고할 수 있었다. **완화하지 않았다.**
     사전 기각조건을 건 이유가 정확히 이 순간이었다.
   - **내 가설을 3번 기각했다.** ①7/23 단일사례로 세운 "고르기 문제"(전수 분해가 반증)
     ②F-87 시간축 가설(lag 편차 4건 = 노이즈) ③열화 가설(열화 아니라 6월 단일 구간).
   - **무작위 기대치 대조**를 넣어 "교집합 5건"을 "기대의 0.25배 = 배타"로 바꿔 읽었다.
     절대 건수만 봤으면 "적네"로 끝났을 것이다.

2. **미흡·실수**
   - **3일을 잘못된 문제에 썼다.** 7/28~30 R-1~R-5 등 7규칙을 기각하며 쌓은 서사의 전제가
     앵커 +0.173이었는데, **그걸 월별로 열어보는 데 오늘 30분 걸렸다.**
     평균을 받아들이고 그 위에 실험 7개를 쌓았다.
   - **도구 판정 문구를 두 번 거칠게 썼다.** ①F-87에서 D0>D1만 보고 "시간축"이라 단정
     (lag2도 5건인데) ②추격 구간을 <8% vs ≥8% 이분법으로 봐서 **진짜 경계 15%를 놓칠 뻔**.
     둘 다 출력을 읽다가 내가 잡았지만, **도구가 결론까지 써주면 그 결론을 검산해야 한다.**

3. **새 교훈** (영구 메모리 저장: **O**)
   - **★평균은 분포를 열기 전까지 근거가 아니다.** +0.173은 4·5·7월이 아니라 6월의 수치였다.
     지표 하나를 전제로 삼기 전에 **시간·구간으로 쪼개는 것이 1순위**다(린치 p.155가 준 것은
     새 아이디어가 아니라 **내 데이터를 보는 방법**이었다).
   - **★사전 기각조건이 없으면 발견은 변명이 된다.** 조건을 나중에 정하면 통과할 때까지 고칠 수 있다.
   - **교집합·상관 같은 '적다/많다'는 반드시 무작위 기대치와 대조**한다. 절대 수는 판정이 아니다.
   - **필터의 검증 표본을 기억해야 한다.** 추격 8%는 7/9에 '강력 통과'였지만 그건
     **명분 없는 전체 표본**이었다. 부분집합에서 같은 결론이 서는지는 별개 문제다.

4. **내일(8/3 월) 적용 1가지**
   - 오전 항목 유지(08:30 무인점검 + 15:45 자가감사 첫 정상 실행 대조)에 더해,
     **사장님 결정 3건**(S-1 처분 / F-87 상한 8→15% / 장중 실시간 명분 탐지 신설)을 첫 보고에 올린다.

---
**불변식**: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉·SAJANG 단일진실·관측 없이 flip 금지
