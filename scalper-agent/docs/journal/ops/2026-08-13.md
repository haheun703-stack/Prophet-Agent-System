# 2026-08-13 (목) 업무일지 — 데이터 점검 (사장님 지시: 8/12·8/13 종가·csv·수급 적재 확인)

📊 **스코어보드(08:30 정본)**: 96건 · 승률 40.6% · 순누적 **−52.89%p** (−158,670원/30만·건) · S-1 **D-16**
(8/12 91건 −48.00%p → 5건 늘며 −4.89%p 악화)

안전 불변식: 봇 OFF·실주문 0·매도 무접촉·picks 불변·SAJANG 무접촉·KRX 무접촉.

---

## 1. 아침 점검 (08:30 cron·자동)

A1~A6 전부 ✅ (nightly 29/29 · 20:10 발송 완료 · OBSERVE v2 19건 · ⑲-2 대조 19/19 drift 0 · service=active·PAPER_ONLY=True).
A9 = S-1 D-16 · 기준 >0 현재 −52.89 ❌미달 (판정일 8/29).

## 2. 8/12·8/13 채널 전수 계수 (정본 `tools/manual/verify_channels.py` · 앵커 8/4 3/3 재현 ✅)

| 채널 | 8/12(수) | 8/13(목) |
|---|---|---|
| ① 종가(daily) | **2,479**/2,531 (97.9%) · 종가0 0 | **2,467**/2,531 (97.5%) · 종가0 0 |
| ② 투자자수급 | 2,526 · 활성 **100%** · placeholder 0 | 2,526 · 활성 **100%** · placeholder 0 |
| ③ 외국인소진율 | 2,416 · 활성 95.85% | 2,408 · 활성 95.62% |
| ④ 공매도 | 2,526 · 활성 100% | 2,522 · 활성 99.84% |
| ⑤ 신용잔고(T+지연) | 최신 8/7 | 최신 **8/10** (오늘 밤 진행) |
| ⑥ 시장11주체 | kospi·kosdaq 양쪽 ✅ | kospi·kosdaq 양쪽 ✅ |
| ⑦ 순위 스냅샷 | 5종 ✅ (night_futures 부재=기존 [F-53]) | 5종 ✅ (동일) |
| ⑧ 장중틱 | 2,526파일 **47,830행** 100.00% 정본일치 | 2,526파일 **50,337행** 100.00% 정본일치 |

**판정: 양일 전 채널 정상 적재.** 국적별 0건은 KRX 차단=정상(절대룰·표 제외).
활성 종목 수 감소(2,479→2,467)는 정지 종목 증가에 따른 자연 추세.

★수집 창(18:00 nightly·19:40 소진율 재수집·20:10 요약) **밖에서** 측정 — 18:27 세션 시작
시점엔 nightly ⑬ 진행 중이었고, 그 자리에서 수치를 찍었으면 8/11 사고의 재현이었다.

## 3. [F-170] 외국인소진율 — 발견 → fix → **실효 0건 정정**

### 발견 (대조가 검출기였다)
8/11 일지 기록 vs 오늘 재측정: 8/10 **2,436 → 2,436 완전 재현**인데 8/11만 **2,435 → 2,429(−6)**.
꼬리창 아티팩트라면 더 오래된 8/10이 먼저 깎여야 하므로 아님. 구멍(앞뒤 거래일 모두 존재)
**226건 / 9거래일**.

### fix (d274aa6 · VPS 19:37:58)
`_fetch_foreign_rates_naver`(복수) 신설 → 파싱된 전 거래일 병합. 단수 함수는
`_fetch_foreign_rate_api` 하위호환으로 존치. Tier3 **47/47 PASS**(신규 4건) ·
**음성대조 2건 확인** · Codex 지적 0건 · 배포 전 **3,510파일 백업**.

### ★정정 — 배포 후 실측이 내 추정을 깼다
19:40 재수집이 **신코드로 완주**했는데 **구멍 복구 0건**. 원문 HTML을 열어보니 표는 20행이
다 있으나 **최신 1행만 실값이고 나머지는 `외국인 보유주수`·`보유율`이 전부 0**이다
(066910 실측: 8/12 순매매량 15,593인데 보유주수 **0.00%**). 파서가 `rate<=0 and holding<=0`을
거르므로 **과거 행은 되살릴 값 자체가 없다**. 대형주(005930·000660)는 20행 실값 유지 —
**소형주 한정 현상**.

⇒ ①**"226건 = 손실"은 과장**. 대부분은 삭제가 아니라 *그날 수집을 놓치면 네이버가 이후 0으로
덮어써 **영구 복구 불가**가 되는 자리*. 확인된 진짜 삭제는 8/11 **−6** 규모.
②fix의 실효 = **네이버가 실값을 유지하는 종목의 미래 손실 예방**뿐. 오늘 복구 이득 **0건**.
③fix 자체는 무해(있는 실값만 추가·삭제 0)라 되돌리지 않음.
④**남은 진짜 처방** = ghost 컷을 `row_date`(네이버가 준 값)가 아니라 **오늘 날짜** 기준으로
자르기 — 지금은 네이버가 스테일 응답을 주면 멀쩡한 최근 행을 지운다(−6의 경로).

### [F-170④] 당일 소진 (eea014d · VPS 20:49 — 사장님 지시 "바꿔라 그러면 지금")

★**오늘 얻은 교훈을 그 자리에서 적용했다 — 설계 전에 실측부터.**
ghost의 원천 `_fetch_foreign_rate_kis_snapshot` **호출자 0건**([F-148] 고아 목록과 일치),
나머지 foreign_exh 참조는 전부 읽기 ⇒ **막으려던 위협이 이미 사문**이고 살아있는 효과는
정상 행 삭제뿐이었다. 이 측정이 없었으면 "방어를 약화시키는 변경"으로 보였을 것이다.

변경 = `df.index <= row_date` → `df.index <= _today_kst_ts()` (미래 일자만 컷).
★거래일 캘린더로 자르지 않았다 — 캘린더가 틀린 적이 있고(7/17 제헌절), **캘린더 버그가
데이터 삭제로 번지는 경로**를 새로 만들지 않는다.
★삭제 행수를 세어 `ghost컷N` 로그 + warning 노출 — 이 사고가 오래 안 보인 유일한 이유가
*조용히 지웠다*는 것이었다.

검수: 신규 2건 포함 8/8 · 전체 **47/47 PASS** · Codex 0건 ·
**음성대조(행동)** 구코드에 스테일 시나리오 투입 → **06-05·06-09 삭제 확인**, 신코드 보존 ·
**VPS 실측** 005930 행수 **80 → 80**, `ghost컷0`.

### 기각된 가설 1건 (정직 기록)
구멍 3종이 전부 그날 외국인 순매수 음수라 "채널의 성질"로 결론낼 뻔했다. 전수 검정에서
**음수인데 행이 있는 종목 1,443종**으로 기각. 3종은 우연히 같은 하위집합(보유수량==외국인_수량
33종)이었다. 소표본 방향성 법칙화 금지(7/28)가 값을 했다.

## 4. 개선점 각주 — **당일 전건 소진** (사장님 지적: *"왜 각주를 내일 하는데"*)

미룰 이유가 없었다. 세 건 다 오늘 처리했다.

- ✅ **[각주-1] 완료** (52d7b60) — `SELFTEST_ANCHORS`에 8/12·8/13 추가.
  ★그냥 추가만 하지 않았다: **오늘 드리프트가 난 채널(소진율)에 애초에 앵커가 없었다.**
  ticks·daily만 있어서 [F-170]을 기계가 잡을 수 없었다 → `fex_min` 신설.
  판정식은 **정확일치가 아니라 하한(>=)** — 병합 fix로 증가는 정상이 됐고 **감소가 회귀**다.
  VPS 실측 **11/11 앵커 재현**.
- ✅ **[각주-2] 완료 (조사 결과 조치 불요 → 대신 [F-171] 발견)** — 33종은 **우리 버그가 아니다**.
  네이버 원문이 그 종목 최신 행의 `보유주수`를 그날 `순매매량`과 같은 값으로 게시한다
  (066910 8/13 둘 다 2,546). `보유수량` 소비처는 `spacex_watchlist.py:145` **한 곳뿐·매매 게이트 0건**.
  ★대신 **진짜로 매매에 닿는 값은 `소진율`** 이었고 거기서 [F-171]이 나왔다.
- ✅ **[각주-3] 완료** — [F-170④] 위 참조 (eea014d).

## 4-2. [F-171] 신규 — 소진율이 실매수 게이트에 들어가는데 날짜를 안 본다 (MED)

`risk_gate_helper.py:182` 가 `df["소진율"].iloc[-1]  # 가장 최신` 으로 **마지막 행을 날짜 확인
없이** 읽어 `is_blacklisted_for_exhaustion(rate)` 에 넣고, `auto_trader.py:650` **실매수 경로**가
그 판정으로 종목을 차단한다. 그런데 이 채널은 수집 실패가 상시(오늘 101종).

**마지막 행 나이 전수 실측(8/13)**: 당일 2,408 · 1~3일 57 · 4~7일 5 · 8~30일 15 · **31일+ 46종**
(최악 **78일**·마지막 행 5/27) ⇒ **66종이 1주 넘게 스테일한 값으로 게이트를 통과/차단당한다.**
주석 `# 가장 최신` 이 **코드가 스스로를 틀리게 설명**하는 자리 — [F-170]과 같은 계열.
인접: `auto_trader.py:60` import 폴백이 `return False` **fail-open**([F-145] 동형).

봇 OFF·실주문 0이라 지금 사고는 아니다. **매수 게이트 동작 변경이라 사장님 결정 사안** — 값 무변경.

## 자기성찰 (7/16 사장님 지시 — 매일 개선하는 단타봇)

1. **오늘 잘한 것**
   - 18:27 세션 시작 시점이 **수집 창 한가운데**임을 먼저 확인하고 8/13 수치를 보류했다.
     8/11에 똑같은 자리에서 62.2%를 최종치로 보고했던 사고를 반복하지 않았다.
   - 도구의 **앵커 자기검증(8/4 3/3 재현)** 을 인용 전에 통과시켰다.
   - 음성대조로 **구코드에서 테스트가 깨지는 것**을 확인하고서야 보호라고 불렀다(8/6 교훈).
   - 배포 전 3,510파일 백업 — 되돌릴 수 있게 해놓고 움직였다.

2. **미흡·실수**
   - ★**피해 규모를 3.7배 과장했다**(226건 vs 실제 삭제 −6 규모). "구멍이 있다"에서 "손실이다"로
     건너뛴 것이 오류다. **구멍의 원인을 확인하기 전에 크기부터 발표했다.**
   - ★**fix를 실효 검증 없이 배포하고 "자가복구된다"고 보고했다.** 단위 테스트는 내가 만든
     가짜 HTML로 통과한 것이고, **실제 네이버가 그 데이터를 주는지는 확인하지 않았다.**
     5/25 *"단위 PASS ≠ 실제 적용"* 이 오늘은 **"픽스처 PASS ≠ 외부 데이터가 그렇게 생김"** 으로 왔다.
   - 가설 1건 기각(음수 부호) — 전수 검정으로 잡았으나 애초에 3종으로 결론낼 뻔했다.
   - ★**각주 3건을 이유 없이 "내일"로 미뤘다** — 사장님이 *"왜 각주를 내일 하는데"* 하고
     지적하셔서 그 자리에서 전건 소진했다. DAILY_ROUTINE §4는 **매일 최소 1건 소진**이 룰이고,
     시각은 21시였고 막힌 것도 없었다. **미룰 근거가 없는데 미뤘다** = 내가 만든 백로그를
     내가 안 지킨 것. 그리고 미뤘으면 [F-171](실매수 게이트가 78일 된 값을 쓴다)을
     **오늘 못 찾았다** — 각주 소진이 곧 발견 경로였다.

3. **새 교훈**
   **외부 데이터에 의존하는 fix는 그 외부가 실제로 무엇을 주는지 먼저 실측하고 설계한다.**
   픽스처는 내 가정의 복사본이라 내 가정이 틀리면 같이 틀린다. → **영구 메모리 저장 O**

4. **내일 적용 1가지**
   ~~[각주-1] 앵커 추가~~ → **오늘 다 했다**(각주 3건 전건 소진). 내일 것은 하나 앞당겨
   가져온다: **[F-171] 스테일 게이트** — 사장님 결정 나오면 즉시 착수, 안 나오면 그 전에
   *"게이트에 들어가는 다른 값들도 날짜를 보는가"* 를 같은 방식으로 전수 확인한다
   (`iloc[-1]`·`.tail(1)` 로 최신값을 집는 매매 경로 전수 grep). **[F-171]은 한 건이 아니라
   유형일 가능성이 높다** — 오늘 [F-170]이 그랬듯이.

5. **데이터 성찰**
   ⑲-3 페이퍼: 5건 늘며 −4.89%p 악화(−48.00 → −52.89), 승률 40.7% → 40.6%.
   S-1 판정 기준 `>0` 대비 D-16 시점 −52.89 — 7/30 규명대로 **표본 부족이 아니라 구조적 미달**
   방향이 유지되고 있다. 근본원인 1줄: 진입은 즉시 체결되는데 방어(CB)는 다음 관측에서야 걸린다([F-46]).
