# 2026-09-07 (월) 업무일지 — 8/31~9/4 데이터 점검 + [S-8] 2주차 + 점검기 오탐 6일 정리

**🔭 [S-8] 관측(8/21~)**: 신호 655건 · 완성 8일(미완성 3) · **①리프트 −0.29%p** · ②cap5 40건 +37.07%p(건당 +0.927) · 표본 **40/150**
**[S-8]** D-25 (2026-10-02) · 수동 판정
**불변식**: 봇 OFF(PAPER) · 실주문 0 · 매도 무손상 · picks 불변(표시 SL만 룰 정합) · SAJANG 무접촉(소비만) · KRX 무접촉
**세션 공백**: 8/30~9/6 (8/29 이후 8일)

---

## 1. 사장님 지시 ① — 8/31~9/4 종가·CSV·수급 적재 확인

**판정: 5거래일 전 채널 정상.** VPS 원본 **전수 계수**(파일 전체 스캔·꼬리행 한계 없음) + 자동 계층 3종 대조.
분모 병기([F-178]): 활성 = `_active_codes_all(그날)` = 그날 종가행이 있는 종목.

| 채널 | 8/31 | 9/1 | 9/2 | 9/3 | 9/4 |
|---|---|---|---|---|---|
| 활성 종목(종가 daily) | 2,485 | 2,485 | 2,486 | 2,484 | 2,479 |
| 투자자수급 (전체파일 / 활성%) | 2,524 / **100.0%** | 2,524 / 100.0% | 2,524 / 100.0% | 2,524 / 100.0% | 2,524 / 100.0% |
| 공매도 short_bal | 2,524 / 100.0% | 2,524 / 100.0% | 2,524 / 100.0% | 2,524 / 100.0% | 2,522 / **99.9%** (2종) |
| 외국인소진율 (전체 / 활성%) | 2,415 / 96.0% | 2,407 / 95.7% | 2,401 / 95.5% | 2,406 / 95.6% | 2,422 / 96.4% |
| 시장11주체 kospi·kosdaq | ✓ | ✓ | ✓ | ✓ | ✓ |
| 장중틱 ticks (dir 파일수 · A1 유효율) | 2,524 · 100% | 동일 | 동일 | 동일 | 동일 |
| 신용 credit | 최신 9/1 — 구조적 T+지연 정상(체커) | | | | |

- 공매도 9/4 2종 결손 = 그날 20:10 발송 문구 *"공매도 결손 2종"* 과 일치. 소진율 95.5~96.4%는 8/29 대역(95.8~97.0)과 동일 — 신규 결손 유형 없음.
- **자동 계층 3종 5일 정상**: nightly `27/27` ×5(8/29 29→27 반영 후 첫 주) · 20:10 텔레그램 발송 완료 ×5 · 08:30 SEND_OK ×6(9/7 포함).
- VPS **9/6(일) 04:00 재부팅** — 8/23·8/30·9/6 매주 일요일 같은 시각 = 예약 패턴. 서비스 06:00 deploy_pull 재기동으로 복귀(NRestarts 0·active). 오늘 ticks dir 2,524 진행 중.
- 로컬 미러 005930 9/4 행 존재(VPSSync 정상). VPS tracked 변경 = 운영 상태 파일 2개만([F-181] 정상).

## 2. 점검 중 발견 3건 — 전부 당일 처리

### [F-186] 소급 점검 도구가 8/31을 "투자자수급 0/2485"로 오산 (MED·당일 소진)

`notify_data_freshness --dry-run --date 2026-08-31`이 0/2485를 냈다. 9/1 08:30 자동점검은 같은 날을 ✅.
**둘 중 하나가 틀렸으니 원본을 셌다** → 2,485/2,485 · `flow/005930_investor.csv`에 8/31 행 존재.
원인 = 8/31 뒤에 9/1~9/4 4행 + **06:30 A0가 붙인 9/7 placeholder**(0값·mtime 06:30 실측) = 5행 → `_csv_has_date` 꼬리 5행 밖.
★8/11 순위 스냅샷에서 **같은 한계를 이미 한 번 봤고** 그때는 전수 재계수로 확인만 하고 도구는 그대로 뒀다. 두 번째라 **정본 자체를 고쳤다**(호출처 6곳 무변경):
기준일이 과거면 (오늘−기준일) 달력일만큼 꼬리를 넓힌다. 기준일==오늘(20:10·08:30 운영 경로)이면 gap 0 → **종전과 동일**.
테스트 11/11 — 음성대조(정본 tail=5 직접 호출 시 8/31 False 재현) 포함.
★**도구 하나만 믿었으면 "8/31 수급 결손"을 보고했을 것이다.** 원본 계수를 병행한 것이 불일치를 잡았다.

### [F-187] 점검기가 폐기된 대상을 6영업일 매일 🚨로 찾았다 (MED·당일 소진 — **8/29 내 누락**)

8/31·9/1·9/2·9/3·9/4·9/7 08:30 본문 전부 `🚨 즉시 확인 필요: A4, A5`.
A4 = OBSERVE v2 러너 로그, A5 = nightly ⑲-2 대조 라인 — **둘 다 8/29 사장님 승인으로 내가 끈 것**이다.
★★8/29 일지 자기성찰 ④에 *"폐기·등재할 때 다음 날 아침 화면에 무엇이 뜨는지 그려본다"* 라고 **쓴 그날** 러너를 끄면서 그 화면을 안 그렸다.
스코어보드 첫 줄([F-183])만 고치고 점검 항목 6개 중 2개가 그 대상을 가리키는 것은 안 봤다.
매일 같은 오탐은 진짜 경보를 마모시킨다([F-153]) — 6일간 사장님 화면에 🚨가 떠 있었는데 실제 이상은 0이었다.
fix = `run_checks` 미등록·함수 보존(S-2/S-6 선례)·DAILY_ROUTINE §1 A4/A5 소멸 표기.
★**재발 차단 규약(기계적)**: 무엇을 끄거나 빼는 커밋 직전에 `daily_ops_check`·`notify_data_freshness`·`run_nightly_freshness_check`(⑳)에서 **그 이름을 grep한다.** "그려본다"는 의지고 grep은 절차다.

### [F-185] picks SL −3.5% — 조사 결과 **실주문 경로 아님** → HIGH→LOW 정정 + 소진

8/29 장부: *"auto_trader가 3곳(961·1529·3498)에서 소비 → 라이브 전 HIGH"*. 오늘 값의 **원천**을 따라갔다:

| auto_trader 위치 | 그 `sl`의 원천 | picks 0.965와의 관계 |
|---|---|---|
| :215 `daytrading_picks.json` 로드 | **`ewy_signal`만 읽음** | sl 미사용 |
| :961 `s["sl"]` | `rec.war_relay_stocks` | 무관 |
| :1529 `c["sl"]` | 후보 dict ← `morning_recommendation`(`SAJANG.get_normal_sl` :2061·2220·4374) / `tomorrow_picks`(:875) | 무관 |
| :3498 `watch["sl"]` → :3530 | `target_state.dynamic_sl` 우선, 없으면 watch | 무관 |

즉 **8/29 장부 문구는 `sl`이라는 키를 소비하는 것과 picks의 sl을 소비하는 것을 섞어 쓴 내 오기**다. grep 결과의 변수명만 보고 값의 원천을 안 따라갔다.
실제 소비처 **2곳** = 텔레그램 픽 표시 · FLOWX/Supabase 업로드(`upload_daytrading_picks:73`) → **사장님이 보는 값이 룰(−3%)과 달랐던 것**.
★**오후 정정**: 오전엔 PaperPortfolio(`telegram_bot:4243`)도 소비처로 적었다. 틀렸다 — 그 블록은 `:4318`에서 `SAJANG.get_take_profit/get_normal_sl`을 **직접** 호출하고 picks의 tp/sl을 읽지 않으며, 페이퍼 청산 로직엔 TP/SL이 없다(TIME_STOP만). **같은 날 두 번째 '변수명만 보고 원천 안 따라간' 오기**다.
fix = `price_levels(close)` 헬퍼 신설 · `SAJANG.get_normal_sl(close)` 파생 · 리터럴 0건(소스 대조 테스트). entry/tp 값은 종전 동일(표시 회귀 없음).
TP +5%/+8% 표시 문구는 트레일링-only 룰과 문구 모순 → **[F-188] 사장님 결정**으로 분리(표시 유지 / 문구 교체 / 삭제).

## 3. 사장님 지시 ② — "업그레이드할 수 있는 부분" = [S-8] 2주차 관측이 말하는 것

### 일별 분해 (VPS 실측 · read-only · 판정 아님)

| 일자 | 신호 | 전신호 T+3 | 시장 T+3 | **리프트** | cap5 net | 대조군(외인 단독) 리프트 |
|---|---|---|---|---|---|---|
| 8/21 | 54 | +3.29 | +2.37 | **+0.92** | +22.45 | +0.37 |
| 8/24 | 44 | +3.40 | +2.82 | **+0.58** | +16.22 | −0.20 |
| 8/25 | 59 | +0.39 | +1.52 | −1.13 | +15.43 | +0.23 |
| 8/26 | 78 | −0.61 | +0.71 | −1.32 | −3.39 | +0.01 |
| 8/27 | 53 | +0.03 | +0.61 | −0.58 | +7.58 | +0.35 |
| 8/28 | 42 | −1.56 | −1.25 | −0.30 | −11.63 | +0.23 |
| 8/31 | 45 | −4.16 | −2.13 | **−2.03** | −8.25 | +0.12 |
| 9/1 | 67 | +1.63 | +0.08 | **+1.55** | −1.32 | +0.30 |
| 9/2~9/4 | 88 / 81 / 44 | forward 미완성 | | | | |
| **8일** | 442 | | | **−0.29 (중앙 −0.44 · 양수일 3/8)** | +37.07 (건당 +0.93) | **+0.18 (양수일 7/8 · cap5 건당 +1.32)** |

### 읽는 법 — 정직하게

1. **리프트 부호가 바뀌었다.** 8/29 +0.12 → 9/7 −0.29. 8/21 등재 근거는 walk-forward 두 구간 **+0.41/+0.42**였다. 8일치가 그걸 뒤집는 표본은 아니지만(일별 −2.03~+1.55·표본 40/150), 등재 근거와 **방향이 반대**인 것은 적어둔다.
2. **대조군이 전략보다 낫다 — 리프트에서도.** 등재 때 `control` 필드에 *"cap5 수준은 대조군(외인 단독)이 더 좋았다(+0.499 vs −0.211) → 기관 조건이 실행 단계에서 해로울 가능성 판정할 것"* 이라 적었다. forward 8일은 **리프트 층에서도** 같은 방향: 외인 단독 +0.18(7/8 양수) vs 외인∩기관매도 −0.29(3/8) = **기관 매도 조건의 기여 ≈ −0.47%p**. 8/21 A/B 구간에선 그 조건이 리프트를 만들었는데(대조군 대비 +), 관측에선 깎고 있다.
3. **cap5 건당 +0.93은 여전히 실력이 아니다.** 8/21·8/24·8/25 세 날(시장 +2.37/+2.82/+1.52)이 +54.10을 만들고 나머지 5일 합은 −17.01. 8/29에 쓴 그대로다.
4. **판정하지 않는다.** 표본 40/150·D-25. 8일치로 규칙을 바꾸면 그게 in-sample 튜닝이다(8/21 게이트 기각 교훈).

### 그래서 "업그레이드" 후보 = 규칙 변경이 아니라 **재검 절차 1건**

- **[S-8 재검] 대조군(외인 3일 순매수 단독)과 '외인∩기관 동반매수' 변형을 8/21과 같은 walk-forward(A 3/18~5/29 · B 6/1~8/11)로 다시 잰다** — 도구 `tools/manual/lift_screen.py --wf` 방법론 그대로. 두 구간 부호 일치 + t>2일 때만 후보로 올린다. 8/21 1라운드에서 외인 단독은 후보에 안 올랐었다(그때 기준). 관측이 대조군 우위를 가리키니 **그 대조군을 정식으로 재는 것**이 다음 한 걸음이다. 실행은 사장님 승인 후(탐색 절차 [T-1] 규약).
- 지금 하지 않는 것: S-8 조건 변경·연장·조기 폐기. 10/2 판정은 150건 채운 뒤 두 관문으로.

### 라이브 전 HIGH 잔여 (사장님께 일람으로 — 오늘 착수 아님)

[F-46] CB 구조적 무효 · [F-92] 30% 현금룰 default off 두 번째 · [F-93] −3% SL이 15:10 엔진 값으로 덮임 · [F-97] 09:15 이중 매수 3곳 · [F-98] 매도 성공 판정이 체결수량 미확인. 전부 **봇 재가동 전 필수**이고 [F-30] 일람화가 미완이다. 봇 OFF·실주문 0인 지금은 위험 0.

## 4. 오늘 코드 변경 — 검수

| 파일 | 변경 | 근거 |
|---|---|---|
| `tools/daily_ops_check.py` | A4·A5 `run_checks` 미등록(함수 보존)·헤더 정정 | [F-187] |
| `data/data_verifier.py` | `_csv_has_date` 소급 시 꼬리행 = 5 + 달력일 gap · `_today_override` 주입 | [F-186] |
| `tools/daytrading_picks.py` | `price_levels()` 신설 · SL = `SAJANG.get_normal_sl` | [F-185] |
| `tests/test_soback_tail_9_7.py` · `tests/test_ops_a4a5_retired_picks_sl_9_7.py` | 신규 11 + 29건 | 음성대조·소스대조 포함 |

- Tier 1: 검수 에이전트(general-purpose — bkit code-analyzer 이 세션 미가용) → 결과 §4-1
- Tier 2: 호출 site grep — `_csv_has_date` 호출 **6곳**(data_verifier 5·notify 1) 전부 `today` 위치인자만 넘김(kwarg 추가 무영향 · 첫 판 "4곳"은 Tier1이 정정) · `check_a4/a5` 외부 참조 0건 · `price_levels` 소비 1곳
- Tier 3: `tools/run_all_tests.py` **52/52 PASS**(174.5s) · 첫 판 내 테스트 1건 실패 = **테스트의 가정 오류**(오늘=8/31·기준 8/24는 gap 7 소급이라 확장이 맞다) → 불변식을 "기준일==오늘 → 확장 0"으로 바로 세워 재작성
- pre-commit `tools/pre_commit_check.py` 통과 · Codex(hook) → 커밋 시
- VPS 실측 → §4-2

### 4-1. Tier 1 결과 (general-purpose 검수 에이전트 · 25 tool call · pyflakes+pre-commit+테스트 재실행)
- **H-1** 스테이징된 테스트가 수정 전 버전(9/1 FAIL) — 에이전트 검수 시점 이후 이미 재스테이징. `git diff --cached`로 11/11 판 확인 후 커밋.
- **M-1** DAILY_ROUTINE 장부가 미스테이징 — 코드만 커밋하면 [F-185]가 장부에선 열림·코드에선 닫힘 = 8/7 LEDGER 모순 패턴. **같은 커밋에 포함**(타당).
- **L-1** `date.today()`가 VPS에서 UTC라는 지적 — **해당 없음**: 오늘 VPS `date` 실측 `KST`(09:35:21 KST). gap 계산은 KST. 단 지적의 결론(운영 경로 gap=0·확장 없음)은 그대로 성립.
- **L-2** picks 경로 삽입으로 스크립트 직접 실행 시 `--send-telegram`(opt-in)이 실제 발송하게 됨(종전 ModuleNotFoundError로 조용히 무동작) — 주석 명기. cron 경로(패키지 import) 불변.
- **L-3** docstring 정확성 3건 — "호출처 4곳"→**6곳**(data_verifier 5·notify 1) · "8/11 호출처에서 넘겼다"→**전수 재계수로 확인만** · A5 주석 스테일 → 전부 정정.
- 무발견 확인: 운영 경로(target==today) 바이트 동일 · compact 정규화 · 6 호출처 위치인자 호환 · import 3모드·순환 없음 · `_nightly_block` A2 사용 유지 · RULE-008 통과 · price_levels docstring의 코드 참조 전부 실재.

### 4-2. VPS 실측 (커밋 `4f91f7e4` pull 후 · 봇 재시작 없음 · 런타임 상태 파일 무접촉)
- **A. 08:30 dry-run(기준 9/4)**: A4·A5 행 소멸 → `✅ 아침 점검 이상 없음 — 실주문 0·페이퍼` (직전까지 6영업일 `🚨 즉시 확인 필요: A4, A5`)
- **B. `--date 2026-08-31` 소급**: 투자자수급 **0/2485 → 2485/2485** · `✅ 핵심 채널 모두 정상` ([F-186] 실효)
- **C. `from tools.daytrading_picks import price_levels`**: close 255,500 → sl **247,835**(=×0.97 · 구 코드면 246,557) · entry/tp 종전 동일
- **D. 봇**: `bodyhunter-bot` active · NRestarts 0. `data_verifier`는 이미 로드된 모듈이라 봇 영향 0(06:00 재기동 시 반영) · nightly 18:00부터 새 코드 · 17:45 자동배포는 up-to-date로 통과 예정
- Codex(hook) 5패턴 PASS · pre-commit ✓ · VPS tracked 변경 = 운영 상태 파일 2개만([F-181] 정상)

## 6. 오후 — 사장님 *"순차적으로 작업 진행하자"* ① [F-188] → 조사 중 [F-189] 발견

### [F-188] 결정·소진 — 키는 지키고 문구를 룰에 맞췄다
선택지 ①유지 ②문구 교체 ③삭제 중 **②의 최소형**: JSON `tp1/tp2` 키 유지(FLOWX/Supabase 업로드가 필드명으로 읽음), 표시 `목표`→`참고목표`, 헤더 1줄 `EXIT_RULE_NOTE`(**SAJANG.TRAILING_PCT·FIXED_TP_DISABLED에서 파생** — 룰이 바뀌면 문구도 따라감), JSON `exit_rule` additive. 사장님이 삭제/유지를 원하시면 1줄 변경.

### [F-189] 조사 중 발견 — 트랙 C SL이 **−15%**였다
상한가 후속 트랙 두 곳에 `sl = int(pullback_target * 0.85)`. 룰(−3%)의 5배. **RULE-008 정규식이 `0.9x`만 잡아 두 달간 통과**했다([F-185]의 0.965는 잡혔는데 0.85는 사각). 표시 경로라 실주문 위험 0이지만 사장님 화면에 −15% 손절선이 나가고 있었다.
fix = `price_levels_limit_up()` · SL=`SAJANG.get_normal_sl(눌림목가)` · **정규식 `0.[89]x` 확장**(레포 전수 grep으로 오탐 0 확인 후) · 음성대조 테스트(구 정규식은 0.85를 놓친다).
−4.5%(`get_limit_up_hard_stop`) 대안은 6/17 '상한가 **D+0** 진입가' paper-only 결정이라 D+1 눌림 진입엔 근거가 없어 채택하지 않았다 — 사장님 이견 시 1줄.

### 검수 (커밋 `ec62b807`)
- Tier 1 ✅ 검수 에이전트 — **MED 1건 실적발**: `exit_rule`을 CLI `--save`에만 넣어 **cron 경로(`trading_coo:3243` 자기 out dict)엔 없었다** = 장부는 "했다"인데 운영 JSON엔 없는 [F-110] 모양. 즉시 양쪽 다 넣고 **두 경로 키 집합을 테스트로 고정**. LOW 1건(stdout 표 문구) 동시 반영. 무발견 6종(HTML 안전성·소비자 전수 `.get()` 관용·트랙C 의미 동일·함수 정의 순서·정규식 오탐 0·importlib 부작용 0).
- Tier 2 ✅ 소비자 전수 — `upload_daytrading_picks`·`morning_briefing`·`daytrading_performance`·`auto_trader`·`telegram_bot` 전부 명시 키 `.get()` (엄격 스키마 0건) · 텍스트에서 `목표 ` 토큰을 파싱하는 소비자 0건
- Tier 3 ✅ `run_all_tests` **53/53** · 신규 42건(음성대조·소스대조·두 경로 키집합)
- pre-commit ✓ · Codex 5패턴 PASS · **VPS 실측**: `EXIT_RULE_NOTE` 출력 정상 · A/B픽 sl 247,835(=−3%) · 트랙C 50,000 → 눌림 47,500 · sl **46,075**(구 코드면 40,375)

## 7. 사장님 지시 ② — [S-8 재검] walk-forward (신규 도구 · VPS 실측)

`tools/manual/s8_variant_wf.py` 신설(read-only·`lift_screen` 정본 재사용·판정기 아님). 구간 = 등재 시 A/B + 관측 C.
**VPS 수치가 로컬 미러와 바이트 동일** — 아래는 VPS 원본 기준.

| 규칙 | A 리프트(t) | B 리프트(t) | C 리프트(t) | cap5/건 A·B·C | 판정 |
|---|---|---|---|---|---|
| **[S-8] 외인3매수 ∩ 기관3매도** | **+0.41 (2.24)** | **+0.25 (1.06)** | −0.33 (−1.17) | −0.61 · −0.19 · −0.20 | ✅WF일관 |
| 대조군 외인3매수 단독 | +0.32 (2.39) | +0.13 (0.92) | +0.10 (1.10) | −0.47 · **+1.49** · −0.29 | ⚠방향만 |
| 외인3매수 ∩ 기관3매수(동반) | **−0.56 (−2.36)** | −0.33 (−1.22) | +0.03 (0.08) | −1.48 · −0.47 · −0.65 | ✅WF일관(음) |
| 외인3매수 ∩ 기관 당일매도 | +0.26 (2.25) | +0.21 (1.10) | −0.03 (−0.15) | −0.37 · −0.54 · −0.14 | ✅WF일관 |
| 외인3매수 ∩ 기관 당일매수 | −0.09 (−0.60) | −0.25 (−1.29) | −0.18 (−0.86) | −0.84 · −0.61 · −0.63 | ⚠방향만 |
| 기관3매도 단독 | +0.00 (0.01) | +0.21 (0.98) | −0.23 (−1.07) | −1.80 · −0.73 · **+0.71** | ⚠방향만 |
| 기관3매수 단독 | +0.13 (1.20) | −0.62 (−4.44) | +0.29 (1.90) | −1.28 · −1.56 · −0.27 | ❌비일관 |

### 결론 4가지 — 하나는 내 아침 판단의 정정이다

**① 양 구간 t>2 통과 = 0건.** 규약대로 0건 보고. 후보 승격 없음.

**② ★내 아침 제안의 전제가 기각됐다.** 오전에 *"대조군(외인 단독)이 전략보다 낫다 → 정식 재검하자"* 라고 드렸는데,
재검해 보니 **A·B 두 구간 모두 S-8이 대조군보다 낫다**(+0.41 vs +0.32 · +0.25 vs +0.13). 대조군은 B의 t가 0.92라
walk-forward 기준(양쪽 |t|≥1.0)도 못 넘는다. **대조군 우위는 관측 8일(C)에서만 나타난 착시**였다.
★재검을 안 했으면 8일치 노이즈를 근거로 축을 갈아탈 뻔했다 — 이래서 8/21 규약이 "관측 없이 flip 금지"다.

**③ 내가 올린 다른 변형도 기각.** '외인∩기관 3일 동반매수'는 A **−0.56**(t −2.36)·B −0.33으로 **명확히 나쁘다**.
즉 이 축에서 기관은 **같이 사면 해롭고, 파는 쪽이 낫다** — S-8의 원 설계 방향이 맞다.

**④ cap5는 21칸 중 18칸이 음수.** [S-8] 등재 시 진단(*"①리프트는 통과, ②실행 규칙은 미해결"*)이 그대로 재확인됐다.
리프트가 있어도 하루 5건을 고르는 순간 마이너스다.

### ★ 그런데 재검이 더 큰 것을 찾았다 — [F-191] 등재 근거가 재현되지 않는다

| | 장부(8/21 등재 근거) | 오늘 재측정 | 표준오차 |
|---|---|---|---|
| A 3/18~5/29 | +0.41 · t +1.78→**+2.24** | **+0.41 · t +2.24** (완전 일치) | ±0.18%p |
| B 6/1~8/11 | **+0.42 · t +1.78** | **+0.25 · t +1.06** | ±0.24%p |

A는 소수 둘째 자리·t 둘째 자리까지 그대로 재현되는데 **B만 안 맞는다**. 동반 기록된 `∩기관 당일매도`도 같은 패턴(A 일치·B 불일치).
'구간 표기 오류' 가설을 28개 조합으로 검정했더니 `6/1~7/31`이 +0.433(t 1.70)으로 가장 가깝지만
**그 구간에선 동반 규칙이 +0.43**(장부 +0.27)이라 **두 수치를 동시에 내는 구간은 없다**.

★**진짜 문제는 정밀도다.** B의 표준오차는 **±0.24%p**인데 등재 근거는 *"+0.41 / +0.42 — 소수 둘째 자리까지 일치"* 였다.
**0.01%p의 일치는 표준오차의 1/20**이다. 그건 재현성의 증거가 아니라 **한 경계에서 우연히 겹친 것**이고,
실제로 B의 끝을 7/15~8/20으로 움직이면 리프트가 **+0.18 ~ +0.64**로 흔들린다.

★**10/2 판정에 미치는 영향**: ①리프트 관문의 근거를 "두 구간 일치"로 인용하면 안 된다. **표준오차·경계 민감도와 함께** 적어야 한다.
★**오늘 바꾸는 것 없음** — S-8의 조건·기한·status 무접촉. 판정은 대장 criteria(두 관문·150건)로 10/2에.


## 5. 자기성찰

**① 잘한 것 — 도구 둘이 다른 말을 했을 때 원본을 셌다.**
`--date 8/31` 0/2485와 9/1 08:30 ✅가 모순이었다. 어느 쪽을 믿는 대신 파일을 열어 8/31 행을 봤고, 그래서 "수급 결손" 오보도 안 냈고 도구의 구조적 한계([F-186])도 잡았다. 8/11의 *"라벨이 아니라 값을 찍어라"* 가 그대로 일했다.

**② 미흡 — 8/29에 쓴 교훈을 8/29에 어겼고, 오늘 그 결과를 사장님 화면에서 봤다.**
"다음 날 아침 화면을 그려본다"고 적은 날 러너를 끄면서 점검기를 안 봤다. 6영업일 동안 사장님은 매일 🚨 두 개를 보셨고 그 여섯 번 다 실제 이상은 0이었다. **의지로 적은 교훈은 그날 저녁에 소멸한다.** 그래서 오늘은 규약을 grep 한 줄로 바꿨다.

**③ 교훈 → 메모리.**
*"변수명 grep은 값의 원천 추적이 아니다."* [F-185]를 HIGH로 올린 근거가 `sl`이라는 키의 소비처 3곳이었는데, 그 키의 값은 picks에서 오지 않았다. 등재 때 원천을 한 단계만 더 따라갔으면 8일간 HIGH가 아니었다. 반대로 오늘 그걸 따라갔기에 "위험하다"를 "표시가 틀렸다"로 정확히 낮출 수 있었다.

**④ 내일 적용 1가지.**
무엇을 끄거나 빼는 커밋 전에 **점검기 3종에서 그 이름을 grep한다** — 오늘 [F-187]에 적은 규약을 첫 번째 커밋부터 지킨다.

---

## 9. 오후 추가 — 외부 지시서 3건 도착 (11:27~11:33) · §1~§4 처리

작업 중 `docs/from-flowx/`에 운영자·퀀트봇 문서 3건이 들어왔다. 커밋 스테이징 중에 발견했다.
(★`git add -A scalper-agent/docs`로 **의도치 않게 앞 커밋에 딸려 들어갔다** — 문서라 무해하나 커밋 메시지와 내용이 어긋났다. 다음부터 `docs` 일괄 add 금지.)

| § | 요청 | 처리 |
|---|---|---|
| 1 | advisory 필드 축소 확인 | 영향 없음 확인 · **9/8 실검증 잔여** [F-195] |
| 2 | `nationality_flows` 재탕 정지 | **소유 확인 + 가드 당일 배포** [F-192] |
| 3 | `DATABASE_URL` 노출 | **없음** (4중 확인) [F-194] |
| 4 | 외인 국적별 수급 재연결 | **불가 — 사장님 룰** [F-193] |

### ★ §2 — 우리가 6/19 스냅샷을 77일간 매일 다시 내보내고 있었다

정보봇이 관측하고 퀀트봇이 소유자를 짚어 알려줬다. **우리 코드가 맞았다.**
`upload_short.py`가 행의 `date`를 `str(date.today())`로 찍는데 `countries.date_new`는 원천 CSV 날짜라,
**원천이 멈춰도 행은 매일 새로 찍힌다.**

| | 행수 | |
|---|---|---|
| 전체 | 273,224 | Supabase 최대 표 |
| **6/22 이후(재탕)** | **147,147** | **53.9%** |
| 6/22 이전(정상) | 126,077 | 46.1% |

★**잡은 성공하고 행수도 1,911로 일정해 로그로는 안 보였다.** 퀀트봇 8/11 B-61과 같은 모양이고,
우리 [F-153](경보 마모)·[F-22](완료위장)와도 같은 계열이다 — **수집이 멈춘 것만 보고 발행이 계속되는 건 아무도 안 봤다.**

fix 설계에서 신경 쓴 것:
- **AUTO-RECOVERY 우회 0** — 복구 경로가 같은 함수를 호출하므로 가드를 한 곳에만 둔다(두 벌은 갈라진다). 테스트로 고정.
- **무한 재시도 차단 vs 성공 위장** — 스킵해도 마커에 오늘 날짜를 남기되 `skipped`·`reason`·`count:0`을 함께 적는다.
- **판독 불가는 stale** — 모르면 발행하지 않는다.
- 정상 경로 회귀 확인(신선하면 끝까지 업로드).

★**사장님 절대룰과 충돌하지 않는다**: 이 가드는 **수집이 아니라 발행을 멈춘다.**
오히려 *"단타봇 KRX 영구 OFF"* 를 실제로 이행하는 쪽이다. 수집 재개는 하지 않았다.
**소급 정리 147,147행은 사장님 결정 대기** — 제 의견은 삭제보다 보존+무효표시다(`date_new`로 소비측 필터 가능·삭제는 비가역).

### ★ §4 — 거절이 정답인 요청

퀀트봇이 3개월째 국적별 수급이 끊겨 단타봇에 종목 단위 제공을 요청했다. **불가로 회신했다.**
사장님 6/22 영구 룰(KRX 일절 미접근·nationality 포함·6/24 위반 질책 이력)이고, 우리 원천도 6/19에 멈춰 있다.
정보봇 섹터 단위 데이터로 대체 설계하시라고 권고했다. **룰이 걸린 요청은 사장님께 올리기 전에 거절이 기본값이다.**

### 회신 문서
`docs/to-flowx/[단타봇→운영자] advisory확인_nationality정지_DATABASE_URL_수급소스_회신_260907.md`

---

## 8. 자기성찰 (오후분 — 사장님 "순차적으로 작업 진행하자")

**① 잘한 것 — 내 제안을 재검했고, 그 제안이 기각되는 것을 그대로 보고했다.**
오전에 *"대조군이 전략보다 낫다"* 라고 드린 뒤 오후에 정식으로 재보니 **A·B 두 구간 다 반대**였다.
8일 표본이 만든 착시를 내가 "다음 한 걸음"으로 제안했던 것이다. 재검을 안 했으면 노이즈로 축을 갈아탈 뻔했다.
동반매수 변형도 내가 올린 것인데 −0.56으로 기각됐다. **오늘 내 아이디어 두 개가 다 기각됐고, 그게 오늘의 수확이다.**

**② 미흡 — 같은 오기를 하루에 두 번 했다.**
오전에 *"변수명 grep은 값의 원천 추적이 아니다"* 라고 [F-185]를 정정해 놓고,
그 정정문에서 **PaperPortfolio를 소비처로 또 잘못 적었다**(`telegram_bot:4318`이 SAJANG을 직접 호출한다).
교훈을 쓰는 문장 안에서 그 교훈을 어겼다. 그리고 [F-188]을 처음 고칠 때도 **저장 경로가 둘인 것을 안 보고** CLI만 고쳤다 — Tier 1이 잡았다.
셋 다 같은 뿌리다: **한 곳을 찾으면 거기서 멈춘다.**

**③ 교훈 → 메모리.** *"두 숫자가 소수점까지 같으면 견고함이 아니라 우연을 의심한다."*
[S-8] 등재 근거 *"+0.41 / +0.42 — 소수 둘째 자리까지 일치"* 의 표준오차는 **±0.24%p**였다.
일치 폭이 SE의 1/20이면 그건 재현이 아니라 우연이다. **일치를 근거로 쓰기 전에 SE부터 계산한다.**

**④ 내일 적용 1가지.**
숫자 하나를 근거로 인용할 때 **표준오차를 같이 적는다.** 오늘 [F-191]이 그것 하나로 드러났다.
