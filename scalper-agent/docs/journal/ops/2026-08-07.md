# 2026-08-07 (금) 업무일지 — 8/6·8/7 전 채널 확인 + [F-82] 판독 정본화 + 장부 정합

> 불변식: 실매매 OFF·실주문 0·매도 무접촉·record-only·KRX 무접촉·SAJANG 무접촉
> 커밋: `14949e2`

---

## §1. 사장님 지시 답변 — 8/6·8/7 종가·CSV·수급 적재

전수(유니버스 2,531 / 활성 2,487) 기준. 표본 아님.

| 채널 | 8/6(목) | 8/7(금) |
|---|---|---|
| 종가 daily OHLCV | 2,486/2,487 **99.96%** | 2,475/2,487 **99.52%** |
| 투자자수급 investor | **100.00%** | **100.00%** |
| 공매도잔고 short_bal | **100.00%** | **100.00%** |
| 시장 11주체 flow_market | 적재 | 적재 |
| 외국인소진율 foreign_exh | 95.86% | **96.38%** (19:40 재수집 후) |
| 신용잔고 credit | 8/4까지 = T+3 | 8/4까지 = T+3 (구조적 지연 정상) |
| 장중틱 ticks | 2,527 · verdict **OK** | 2,527 · verdict **OK** |

**종가 결손 12종(8/7)은 수집 실패가 아니다.** 12종 전부 8/7 ticks가 `price 고정 · 거래량 0 ·
체결강도 0.0` = **거래정지 박제**(8/6 [F-123]에서 규명한 패턴). KIS가 정지 종목 일봉을
주지 않는 것이 정상이다. 12종 모두 investor에는 8/7 행이 있다 — [F-88]대로 **정지 판별이
성립하는 채널은 daily 단독**임을 다시 확인.

**틱 전수 생존성**(`deep=True`): 8/6 살아있음 2,486/2,527(박제 41) · 8/7 2,476/2,527(박제 51).
8/5 40건과 같은 규모.

**nightly(18:00)**: 30/30 성공 · ⑳ 완료위장 없음 · ⑲-2 OBSERVE v2 대조 PASS 23/23.
**20:10 하트비트**: 전 채널 ✅ + `아침점검(08:30) 실행·발송` 병기 → **텔레그램 발송 완료**.
어제 [F-122]에서 고친 마커 상수 위임이 첫 실전에서 정상 동작.

**⑫ 순위 스냅샷** — 어제 배포한 [F-53] 4분기 코드의 첫 실전(18:27). `night_futures`를
**skipped(장외·정상)**로 분류해 `status=ok`. 구코드였으면 실패와 장외가 같은 `0`으로 뭉개졌을 자리.

---

## §2. 오늘 소진 — [F-82] 날짜 판독 정본화

### 소진 동기 = 오늘 3번째 재발이고, 내가 밟았다

`_csv_has_date(p, "2026-08-07")`로 `flow_market`을 조회해 8/6·8/7을 **둘 다 결손**으로 셌다.
실제로는 둘 다 정상 적재. **사장님께 적재 상태를 보고하는 자리에서 판독기가 틀린 것이다.**
(8/1 = 같은 원인으로 ④·⑧ "최신 없음" 오판 / 8/5 = 컬럼 인덱스로 유효율 64.84% 오산)

### 실측으로 드러난 진짜 모양 — "중복"이 아니라 **반대 방향 3벌**

| 판독기 | 정규화 방향 |
|---|---|
| `_kis_last_date` | `2026-08-07` (ISO) |
| `verify_channels.norm_date` | `20260807` (하이픈 **제거**) |
| `_get_last_csv_date` | **무변환** (원본 `[:10]`) |

바꿔 끼우면 비교가 **조용히 뒤집힌다**. 중복보다 나쁘다.

### fix — `utils/dated_csv.py` (판독만 · 판정식 무접촉)

★**단일 형태로 통일하지 않았다.** `verify_channels:241`이 `ticks/20260807`처럼 **경로 자체를
compact로** 만들기 때문에 ISO로 밀면 디렉터리가 없어져 "데이터 0"으로 조용히 오판한다.
그래서 `iso()` / `compact()`로 **방향을 이름에 박았다** — 호출부가 원하는 형태가 코드에 드러난다.

### 판정 불변 2중 확인 (판정이 바뀌면 발견이 아니라 사고 — [F-101] 규약)

1. **함수 전수 대조** (VPS 실데이터): has 37,986건 · last 12,662건
   → daily·investor·short_bal·credit·ranking_snapshots **불일치 0**
   → 차이는 구코드가 틀리던 `flow_market` **6건뿐**(구=False→신=True)
2. **Tier 3 실경로**: 격리 트리에서 `verify_all()` 신·구 출력 **바이트 단위 동일**

호출 site 4곳이 전부 ISO 채널이라 **판정 불변은 원리적으로도 보장**된다(Tier 2에서 선확인).

검증: `tests/test_dated_csv_8_7.py` **53/53 PASS**(음성 대조 = 구 구현이 오늘 사고를 재현) ·
회귀 순위 20/20 · **사장님 룰 21/21** · pyflakes 신규 0건.

---

## §3. 별건 — 개선점 장부가 양방향으로 깨져 있었다 (오늘 발견)

### 하마터면 이미 고친 걸 다시 고칠 뻔했다

오늘 개선 후보 1순위로 `[F-100]`(HIGH·미소진)을 잡고 착수 직전 코드를 열었더니
**이미 8/5에 고쳐져 있었다**(`_evening(ref)`·커밋 70148cb). `[F-101]`도 마찬가지.

원인: 8/5 소진 커밋이 완료 **표만 덧붙이고** 원 불릿의 취소선을 긋지 않았다
→ 같은 ID가 §4 안에서 '열림'과 '완료'로 **동시 존재**(9건).

**장부 오류가 세 종류다:**

| 유형 | 사례 | 결과 |
|---|---|---|
| 닫혔는데 열려 보임 | F-96·100·101·108·110·111·113·114 | 다 끝난 일을 다시 함 |
| 열렸는데 닫혀 보임 | F-110(RULE-004 구현 0건·8/5 자체 적발) | 없는 방어선을 믿음 |
| **부분인데 전부로 표기** | **F-90** (8/7 신규 발견) | 잔여가 영구 실종 |

### F-90은 닫지 않았다

8/5 표가 `전부 소진`으로 그었으나 복원된 것은 ALIVE 분기(`intraday_eye.py:736`·`:760`)뿐이고
**지지/저항 2분기는 그대로다**(`:651` `nearest_res` · `:689` `nearest_sup*0.995`).
70148cb 커밋 메시지도 *"축이 달라 미변경 — 사장님 판단 사안으로 잔존"*이라 적었다.
**표가 커밋보다 낙관적이었다.** 표를 사실로 정정하고 항목은 **열어 둠 — 잔여는 사장님 결정**.

### 재발 차단 — LEDGER-1 (`tools/pre_commit_check.py`)

열림+완료 공존 시 **HIGH 차단**. staged `.py` 유무와 무관하게 실행(문서만 고친 커밋에서
조기 반환에 걸려 통째로 건너뛰던 자리). `tests/test_backlog_ledger_8_7.py` **14/14 PASS**,
★음성 대조로 **구 장부에서 9건 전건 검출** 확인.

★**사정권을 정직하게 적어 뒀다** — '조용한 소진'(F-88형: 고쳤는데 완료 표기가 아예 없음)은
**못 잡는다**. 코드와 장부를 대조해야 하는데 일반화가 안 된다. 이 규칙이 막는 건
*"닫은 걸 안 닫힌 것처럼 남겨두는"* 통로 하나뿐이다.

**열린 항목 99 → 91** (F-82 소진 · F-125/F-126 신규)

---

## §4. 오늘 내가 두 번 틀렸다

**① 규칙이 자기 문서를 위반으로 짖었다.** F-90 정정 노트 안에 `` `~~F-90~~` ``이라는 **글자
자체**를 인용했더니 그게 다시 완료 표기로 계수됐다. 8/6 [F-89] 관측기 자기참조와 **동형** —
자기 출력이 자기 입력이 되는 자리. fix=인라인 코드 스팬을 걷어내고 계수, 테스트로 고정.

**② 테스트 기대가 틀렸다.** 음성 대조 목록에 F-88을 넣었는데 실패했다. **코드가 옳고 내가
틀렸다** — F-88은 모순이 아니라 *완료 표기가 없던* 건이라 사정권 밖이다. 어제 [F-53]에서도
같은 자기 정정을 했다(픽스처가 틀리고 코드가 옳았음).

**③ 측정을 수집 창과 겹쳐 돌렸다.** 신·구 대조에서 `005300 foreign_exh 8/5`가 1건 튀어
원인을 파고들었더니 **코드 차이가 아니라 19:40 재수집이 그 파일을 쓰는 중**이었다.
수집 창이 닫힌 뒤 재대조하니 **불일치 0**. → [F-126] 등재.

---

## §5. 신규 등재

- **[F-125]** ⑥ "수급 4종"이 폐기 stub 2종을 성공처럼 남긴다 (LOW·[F-53] 계열)
  — `short_bal:0, short_vol:0`인데 "완료". 실측 확인 결과 **의도된 no-op**이고 실제 공매도는
  ⑬(KIS)가 100.0%로 담으므로 **데이터 사고 아님**. 다만 로그만으로 폐기/고장 구분 불가.
- **[F-126]** 실측 대조를 수집 창과 겹쳐 돌리면 없는 차이를 만든다 (LOW·방법론)
  — 대조 실측은 18:00 nightly · 19:40 재수집 · 20:10 **밖에서** 돌린다.

---

## §6. S-6 D-DAY (오늘 도래) — 사장님 결정 대기

⑲-4가 판정 보고 의무 1건을 띄웠다. 어제 사전 판정한 수치 그대로:
표본 188건 · **3개 지평 전부 미달** · 신호 등급 역단조(HOT −9.78) · 선정 리프트 −3.12%p ·
시장 대비 −6.39 · 승률 29.0%.

★**권고는 붙이지 않는다** — 7/28에 on_fail 문구를 그대로 추종해 폐기 권고를 냈다가
사장님께 기각당했다("다른 방법을 나중에 찾아봐야지"). **폐기/연장은 사장님 결정.**

---

## 자기성찰 (7/16 사장님 지시 — 매일 개선하는 단타봇)

**1. 잘한 것**
"예상 밖 1건"을 그냥 넘기지 않은 것. 검증기가 보는 채널이 전부 0 불일치였으니 "합격"이라
보고할 수 있었는데, 튄 1건을 파고들어 **읽기 레이스**임을 규명했고 그게 [F-126]이 됐다.
그리고 F-90을 표대로 닫지 않은 것 — 표를 믿었으면 잔여가 영구 실종될 뻔했다.

**2. 미흡**
장부가 깨진 걸 **오늘에야** 알았다. 8/5에 F-110으로 "완료로 닫혔는데 구현 0건"을 스스로
적발해놓고, **같은 장부의 반대 방향은 6일간 못 봤다.** 한 방향을 발견했으면 반대 방향도
그 자리에서 세었어야 했다. 그리고 대조 실측을 수집 창과 겹쳐 돌린 건 스케줄을 내가 다
알면서도 안 본 것이다.

**3. 교훈 → 메모리**
**"장부와 구현은 양방향으로 어긋난다."** 한쪽(닫힌 척)을 잡았으면 반대쪽(열린 척)도 같은
검사로 센다. 7/31 교훈("문서가 자동 차단한다고 말하면 구현을 세어 확인")의 **대칭 절반**이
6일간 비어 있었다. 그리고 **정본 판독기를 안 쓰고 새로 파싱하면 반드시 재발한다** — 8/1·8/5·8/7
세 번 모두 같은 뿌리였고, 세 번째에야 정본을 세웠다.

**4. 내일 적용 1가지**
`[F-125]` 소진 — ⑥ 반환에 종류별 상태(ok/retired/failed)를 붙여 **폐기와 고장이 로그에서
구분**되게 한다. ⑫를 어제 고쳤는데 ⑥이 같은 모양으로 남아 있다.
