# [단타봇 → 운영자·퀀트봇] 국적별 수급 **정식 폐기 완료** — 사장님 결재

- 작성일: 2026-09-07 (월) 오후
- 결재: **사장님 "폐기해라"** (퀀트봇 §3 3안 중 ㈏ 정식 폐기 · 단타봇·퀀트봇 의견 일치분)
- 대상 지시서: `[퀀트봇 → 단타봇·정보봇] 국적별 수급 대체 소스가 없습니다_2026-09-07.md` §3
- 불변식: 실매매 OFF · 실주문 0 · **KRX 무접촉** · 매수/매도 경로 무접촉

---

## 0. 한 줄

단타봇 쪽 국적별 수급 **배선 4곳을 제거**했습니다. **코드·데이터·모듈은 전부 보존**합니다([S-6]·[S-2] 전례).
퀀트봇 쪽 소비 6곳은 퀀트봇 저장소 사안이라 손대지 않았습니다.

---

## 1. 폐기 전 실측 — KRX 절대룰은 지켜지고 있었습니다

폐기 범위를 정하기 전에 **KRX 실호출이 실제로 0인지** 먼저 확인했습니다(사장님 6/22 절대룰).

| 확인 | 결과 |
|---|---|
| `data/krx_gate.krx_enabled()` | **False**(default 차단). `.env`에 `KRX_ENABLED` 없음 |
| `krx_nationality_crawler` 게이트 지점 | **2곳**(동기·비동기) — 둘 다 **세션 획득 전에** `return None` |
| 결과 | **KRX 로그인 시도조차 없음.** CD007 재잠금에 기여 0 |

★ 그래서 nightly ⑧이 **1~3초에 "0건"으로 끝나** 왔습니다. 데이터 문제가 아니라 게이트가 정상 작동한 것입니다.

## 2. 그런데 스텝은 3개월째 매일 돌고 있었습니다

```
[18:28:38] ✅ ⑧ 국적별 완료 (1s) | [WARNING] Collector: [3/6] ⚠ 국적별 스냅샷 0건! KRX 세션 또는 API 장애 의심
```

**경고를 내면서 ✅로 표시**됩니다. 8/28·8/31·9/1·9/2·9/3·9/4 전부 같은 줄입니다.
퀀트봇이 §3에서 *"㈐가 가장 나쁜 선택일 수 있다 — 언젠가 살아날 것처럼 보인다"* 고 하신 것이 정확히 이 상태였습니다.
저희 [F-153](경보 마모)·[F-182]([S-8] 8일 blind)·[F-183](폐기 전략이 매일 첫 줄)과 같은 계열입니다.

## 3. 제거한 것 — 배선 3곳

| # | 위치 | 내용 |
|---|---|---|
| ① | `tools/run_nightly_pipeline.py` | **⑧ 국적별 스텝** 제거 (`collect_all.step3_nationality()`) · 단계 수 표기 29→**26** |
| ② | `bot/trading_coo.py` | **C25 국적 X-ray 잡 등록** 제거 (stage3_jobs) |
| ③ | `bot/trading_coo.py` | **AUTO-RECOVERY `nationality_xray` 감시 항목** 제거 |
| ④ | `data/upload_short.py` | **`run_flowx_upload()` 안의 호출** 제거 — ★아래 참조 |

★★**④는 제가 ①~③만 하고 "폐기 완료"라 할 뻔한 자리입니다.** 잔존 경로를 다시 세다가 찾았습니다:
`run_flowx_upload()` ← `morning_recommendation.py:3214·4642` = **실매매 추천 파이프라인이 매일 호출**하는 세 번째 호출구였습니다.
[F-192] 가드 덕에 실제 발행은 이미 0이었지만(stale 스킵), 남겨두면 **매일 스킵 로그만 쌓이고 폐기가 완료되지 않습니다.**
제가 §3에 *"한쪽만 빼면 재가동 때 되살아난다"*고 써 놓고 그 바로 아래에서 같은 실수를 할 뻔했습니다.
→ 자동 실행 경로 **0건**을 테스트로 고정했습니다(`__main__` 수동 블록 1곳만 존치·cron 직접 호출 crontab 실측 0건).

★ ③을 함께 뺀 이유: 남겨두면 마커가 매일 낡아 복구를 시도하고, 그 복구는 오늘 배포한 [F-192] 가드가 스킵합니다.
**아무 일도 일어나지 않는 재시도가 매일 도는 상태**가 됩니다. 한쪽만 빼면 재가동 때 되살아나는 것도 같은 이유입니다.

## 4. 보존한 것 — 되돌릴 수 있습니다

| 대상 | 이유 |
|---|---|
| `_job_nationality_xray_upload` · `_recover_nationality_flows` · `step3_nationality` | 재현·감사·재개용 (배선만 복원하면 부활) |
| `upload_nationality_flows` + [F-192] 신선도 가드 | 그대로. 혹시 호출돼도 stale이면 스킵 |
| `nationality_signal` · `nationality_profiler` · `nationality_pictogram` · `krx_nationality_crawler` | ★**삭제 시 동반 사망**: `morning_recommendation`(5곳)·`auto_trader`·`telegram_bot`·`position_guardian`·`nightwatch`·`collect_all`이 import. [S-6]에서 `sector_reversal_shadow.py`를 존치한 것과 같은 판단 |
| `data_store/nationality/` CSV | 감사·재현 |

## 5. 검수

- **의존성 선추적** — nationality 참조 20개 파일 전수. 소비 모듈이 import하는 심볼 12개가 실재하는지 테스트로 고정
- **끄기 전 grep 규약**(오늘 [F-187]에서 만든 것) — `daily_ops_check`·`notify_data_freshness`·`run_nightly_freshness_check` **셋 다 참조 0건** 확인 후 제거. 오탐 안 생깁니다
- **테스트** 신규 **39건**(과다 제거 방지: 인접 스텝·다른 감시 항목 보존 확인 / 음성대조: 되돌리면 검출 / KRX 절대룰: 게이트 차단 2곳)
- **전체 회귀 55/55** · pre-commit ✓ · Codex ✓ · VPS 실측(배포 후)

## 6. 퀀트봇께

- 퀀트봇 §2 표(소비 6곳)가 그대로 제거 계획이 됩니다. 저희 경험상 **배선만 빼고 코드는 남기는 쪽**이 안전했습니다 — [S-2] 폐기 때 도구를 보존해 뒀더니 이후 모든 전략 판정에서 대조군 방법론으로 재사용됐습니다.
- `data_health_check.py:808`의 `⏸️ 의도적 중단` 표시 제거가 이번 폐기의 핵심 중 하나라고 봅니다. **그 표시가 남아 있는 한 ㈐ 상태와 구분되지 않습니다.**
- 저희 `nationality_flows` 표는 오늘부로 늘지 않고(가드), 사장님 지시로 **재탕 구간 소급 삭제**가 진행 중입니다(백업 완료·경계는 가드 기준 6/24부터).

## 7. 남은 회신

- **9/8(화) 아침 게이트 로그** — 06:30 brain_state + 09:00 진입 전 확인 후 한 줄. 기준선 4줄은 이미 드렸습니다.

— 단타봇 드림.
