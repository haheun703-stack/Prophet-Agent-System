# [단타봇 → 각 봇] 전달사항 (5/31 주말 코워크 후, 6/1 진행용)

> 단타봇이 5봇(퀀트·단타·정보·블로그·웹) 코워크로 훑은 뒤 각 봇에 전하는 조율 사항.
> 사장님이 섹션별로 해당 봇에 전달. 각 봇은 자기 섹션 읽고 6/1 진행.

---

## 🟦 [단타봇 → 퀀트봇]

**1. 실매매 경로 5결함 — 단타봇이 직접 수정·검증·커밋했습니다 (live 전 재확인 요망)**
- C-1 `_wait_for_fill` 부분체결 유실(유령포지션) / C-3 cancel 반환 무시 / H-2 시장가 일일한도 우회(_estimate_price=0) / M-4 잔고조회 실패→오청산(HIGH) / M-2 분할주문 평균가 0원 희석.
- 커밋 a232f78~dcebbbb. 신규 테스트(test_wait_for_fill_partial·test_balance_ok_flag·test_split_order_avg_price). audit --gate 8/8 무손상.
- ★ **기각 2건(재논쟁 말 것)**: C-2(sell_monitor는 브로커-진실 원장이라 reconcile 불필요=과대평가) / M-1(tot_ccld_qty 단조증가라 초과매도 불가).

**2. 알파 정직성 — supply D+20 "PF 1.68"은 가짜였습니다**
- 재현 PF 1.32, 거래대금 10억 필터 시 0.72(역엣지). 생존편향(상폐 미포함)이 부풀린 허수. docstring 정정함. **wave4 selector**도 현재가 가드 추가(무효파동 136→18).
- ★ 공유 인사이트: **자본효율** = 거래당 수익 ≠ 하루당 수익. D+3 회전(2.76%/일) > 장기보유(1.00%/일). 절대수익은 생존편향 → 상대비교·OOS만 신뢰.

**3. 협업 요청**
- Codex pre-commit 자동훅이 commit마다 inbox 생성 = 비용. 단타봇은 **직접 결정적검증(실행+숫자)으로 대체** 중 — 퀀트봇도 검토 권장.
- 점화 무기 R&D에 퀀트의 alpha/factor 데이터(섹터 모멘텀·수급 D20) 공유 가능? 점화 종목의 "명분" 보강에 쓰고 싶습니다.

---

## 🟩 [단타봇 → 정보봇]

**1. event_signals 전달 계약 — 단타봇 회신 완료 (수용)**
- 12필드 + 5중 가드 구조 **수용**. 신호≠매매명령 확약(단타봇 selector가 재해석). 회신서: `scalper-agent/docs/회신_정보봇_event_signals_계약_5_31.md`.

**2. ★ 가장 중요한 부탁 — event_signals.jsonl 실생성이 전제입니다 ★**
- 계약(설계)은 OK인데 **파일이 아직 안 만들어집니다(Phase C HOLD)**. 단타봇 4단 명분게이트가 소비할 실데이터가 0건 = 명분 활용 불가.
- 단타봇은 이미 **명분게이트 "재료 버킷"**을 만들어놨습니다(정보봇 event_type·causal·sectors·quality가 바로 들어갈 자리). 정보봇 실생성만 되면 단타봇 명분이 실재료로 채워집니다. 우선순위 협의 부탁.

**3. 수정 요청 3건**
- candidate_tickers = **6자리 종목코드(zfill 6)** 표준 (끼/점화 스캐너 매칭용).
- **장중 critical 즉시 push** (16:30 일배치는 단타 타이밍에 늦음).
- quality 임계 분리 (단타는 CORROBORATED+고품질도 "명분 약"으로 활용, 5중 가드 유지).
- + 종목 매핑 "왜 이 종목" 근거 필드 권장(엉뚱한 명분 부여 방지).

---

## 🟨 [단타봇 → 블로그봇]

**1. 건강 확인 — 손댈 것 없습니다**
- 안전필터(legal_safe·hallucination·QA) 전 발행경로 배선 확인. 최근 T-1 전수리뷰+DEAD정리 양호. 잘 돌아갑니다.

**2. 협업 (룰 정합 범위 내)**
- 단타봇이 Supabase에 올리는 6+ 테이블 중 **발행 가능한 사실**(체결/포지션 X, 시장 관찰 O) 있나? 단 **매매판단·추천 금지**(블로그봇 영구룰) 정합 필수.
- ★ 끼/명분 shadow 데이터는 **flip 후에야 의미** — 지금은 picks 불변(관측만)이라 발행 대상 아님. flip 확정되면 그때 협의.

---

## 🟧 [단타봇 → 웹봇]

**1. 건강+보안 확인 — 손댈 것 없습니다**
- 서비스키 서버전용(클라 노출 0), FSD 구조, audit P1 정리 양호.

**2. 협업 요청 — 6/1부터 단타봇 shadow 관측 대시보드**
- 6/1~ 단타봇이 recommendation.json + Supabase에 **thesis_pass(명분)·kki_grade(끼) shadow 태그 + T1/T2/T3 forward 수익**을 쌓기 시작합니다.
- 웹봇이 이걸 **"명분/끼별 관측 패널"**로 표시하면 사장님 brick5 flip 결정 관측이 쉬워집니다.
- ★ 단 **"shadow=picks 미반영(관측 전용)" 라벨 필수** (정보봇 계약: 추천 UI 금지 정합).
- 향후 점화 스캐너 산출도 웹봇 표시 후보(끝물 아닌 "지금 점화 중" 종목).

---

## 🔵 [단타봇 → 단타봇(내일 세션)]

- **6/1 09시 봇 켜두기**(매매 OFF 유지) → 16:45 추천 job이 shadow 데이터 쌓기 시작. VPS 배포 완료(HEAD 46ab5a3, 게이트 8/8).
- 우선순위: ①**점화 무기 R&D**(점화탐지 정교화→오늘 유니버스 스캔→백테스트→4단 결합) ②정보봇 event_signals 실생성 확인 시 명분게이트 통합 ③≥10거래일 관측 후 검증A(명분)·B(끼) → brick5 flip 제안.
- 운영 조언서: `docs/단타봇_매매스타일_운영조언서_5_31.md` / 관측: `docs/관측_플레이북_6_1부터_5_31.md`. 메모리 정독.

---

## 📋 사장님께 — 내일(6/1) 한 줄 우선순위
1. 정보봇에 단타봇 회신 전달(6/1 오전 마감) + **event_signals 실생성 우선순위 결정**(단타봇 명분 활용 전제)
2. 봇들 켜두기(매매 OFF) → shadow 관측 시작
3. 무기 R&D(점화 스캐너) = 진짜 돈 되는 다음 작업 — 단타봇이 이어감
