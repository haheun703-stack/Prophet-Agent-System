# 세션 로그: 2026-03-23 세션 5 (월요일)

## 작업 요약

### 1. 외국인 국적별 수급 픽토그램 차트 (74261f8)

**신규 파일**: `scalper-agent/data/nationality_pictogram.py` (522줄)

PIL 기반 사람 아이콘 백분율 차트 생성기:
- `get_total_shares()`: 3-tier fallback (short_bal.csv → cap_억/close → KIS API)
- `generate_pictogram_chart()`: 800px PIL 차트 (빨강=매수, 파랑=매도, 총주식수 비례)
- `generate_charts_batch()`: 여러 종목 일괄 생성
- `upload_chart_to_supabase()`: Supabase Storage + 테이블 upsert
- `_draw_person()`: 사람 아이콘 (머리 원 + 몸통 사다리꼴) 24px

**수정 파일**:
- `telegram_bot.py`: `국적차트` 명령어 + `reply_photo` (봇 최초 이미지 전송)
  - `cmd_nationality_chart()`: 단일/전체 종목 차트 이미지 전송
  - `_job_nationality_charts()`: 17:00 자동 생성 + Telegram 전송 + Supabase 업로드
  - 명령어 등록: `국적차트` (정확매칭) + `국적차트 삼성전자` (인자)
- `kis_trader.py`: `fetch_price()` 반환값에 `listed_shares` 필드 추가

**CLI 테스트 결과**:
- 삼성전자(005930): 44개국, 54KB PNG, 영국 -235.9만주 이탈 확인
- 삼성물산(028260): 20개국, 52KB PNG 정상 렌더링

**VPS 배포**:
- 코드 배포 + NanumGothic 폰트 설치 (`sudo apt-get install -y fonts-nanum`)
- 17:00 KST 국적 차트 스케줄 등록 확인

### 2. 메모리 규칙 0 추가

작업/보고 시작 루틴 (매 세션 필수):
1. 날짜/요일 검증 → python으로 확인 (감으로 적지 않음)
2. 데이터 기준일 → overnight/brain의 timestamp 체크
3. 거래일 여부 → trading_calendar.is_trading_day() 확인
4. 보고서 작성 → 위 결과 그대로 복사

### 3. FLOWX 웹봇 전략 기준 전달

퀀트봇/단타봇 시그널 생성 로직 전략 기준 정리:
- **퀀트봇**: 7-팩터 Soft Scoring (릴레이+사전감지+기술적+뉴스AI+국적수급+TV+MACD)
  - 총점 = Σ팩터 × 체제배수(NORMAL 1.0 ~ PANIC 0.3)
  - 매도: SL -3.5%(MTM)/-5%(NORMAL) + 트레일링 + 수급이탈
- **단타봇**: 6조건 중 3개 충족 시 진입
  - TP업사이드/가격안정/양봉/체결강도100+/AI Eye 50+/MACD 0선
  - 3분할 시간매수, 최대 2종목 동시 보유

### 4. FLOWX Supabase 테이블 생성 가이드

웹봇(Vercel Cron 3-Bot)이 필요로 하는 추가 테이블 2개 생성 안내:
- `macro_data`: 정보봇 매크로 데이터 저장 (indices/commodities/forex/crypto/bonds/vix JSONB)
- `intelligence_briefing`: 오늘의 한 줄 판정 (verdict/sentiment/confidence)

**주의사항**: 첫 생성 시 Supabase UI가 컬럼명을 한국어로 번역해서 생성 → DROP CASCADE 후 영문으로 재생성 필요했음. 3단계 분할 실행(DROP → CREATE → RLS)으로 해결.

## 커밋 이력
- `74261f8`: feat: 외국인 국적별 수급 픽토그램 차트 — PIL 사람아이콘 + 텔레그램 이미지 전송

## 스케줄 현황 (추가)
| 시간 | 잡 | 설명 |
|------|---|------|
| 17:00 | _job_nationality_charts | 국적 픽토그램 차트 자동 생성 + 전송 + Supabase |

## 텔레그램 명령어 (추가)
| 명령어 | 설명 |
|--------|------|
| `국적차트` | 추천+보유 전체 종목 차트 이미지 전송 |
| `국적차트 삼성전자` | 특정 종목 차트 |
