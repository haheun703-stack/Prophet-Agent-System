# 단타봇 매일 업무 루틴 (DAILY ROUTINE)

> **7/16 사장님 지시**: "매일 해야되는 일들 todo 리스트 만들어서 했는지 확인하고,
> 개선점을 추가로 작성해서 각주 달아서 매일 관리하자. 자기성찰을 통해 매일 개선해나가는 단타봇이 되자."
>
> 이 문서 = **마스터 루틴** (매일 반복 항목·불변). 매일의 실행 결과 = `docs/journal/ops/YYYY-MM-DD.md` (업무일지).
> 개선점 백로그 = 이 문서 하단 **§4 개선점 각주** (살아있는 목록 — 완료 시 ~~취소선~~+날짜).

---

## §1. 아침 루틴 (업무 시작 시)

> ★ **7/31부터 A1~A6·A9는 무인 자동** — `tools/daily_ops_check.py` 평일 **08:30 cron** →
> 사장님 텔레그램 1건. 세션이 열리지 않는 날에도 이상이 잡힌다([F-29] 7/29 공백 실증).
> 세션에서는 **텔레그램 결과를 먼저 읽고**, ⚠/🚨 항목만 파고들면 된다.
> 수동 재실행: `python3.11 -X utf8 tools/daily_ops_check.py --dry-run`
> A7(로컬 미러)·A8(업무일지)만 여전히 사람 몫.

| # | 항목 | 방법 | 판정 기준 |
|---|------|------|-----------|
| A1 | **전일 기초데이터 소급 실측** (KIS만·KRX 절대 무접촉) | 자동(08:30) / 수동: `python3.11 -X utf8 tools/notify_data_freshness.py --dry-run --date <전일>` | 종가·투자자수급·시장11주체·공매도·외국인소진율 전부 ✅ (신용=T+지연 정상) |
| A2 | **nightly 파이프라인 완주 확인** | VPS: `tail logs/nightly.log` | `요약: N/N 성공` + ⑳ freshness "완료위장 없음" |
| A3 | **어제 20:10 텔레그램 발송 성공 확인** | VPS: `tail logs/notify_freshness.log` | `[notify] 텔레그램 발송 완료` (실패 문구 발견 시 즉시 원인 추적) |
| A4 | **OBSERVE v2 러너 가동 확인** (평일 09~10시) | VPS: `tail logs/observe_v2.log` + intents date | 오늘 날짜 로그 존재·intents date=오늘 |
| A5 | **⑲-2 대조 + ⑲-3 페이퍼 성과 확인** (라이브 판정 증거 누적) | nightly.log의 `[observe_v2] 대조`·`[v2_paper]` 라인 | 대조 PASS (miss 0·phantom 0)·전일 정산 존재·승률/수익금 추이 |
| A6 | **봇 서비스·안전 불변식** | VPS: `systemctl is-active bodyhunter-bot` | active + 실매매 OFF(PAPER_ONLY)·실주문 0 |
| A7 | **로컬 미러 신선도** (VPSSync 수신 확인) | 로컬: `tail -1 data_store/daily/005930.csv` | 전일 행 존재 |
| A8 | **업무일지 작성 시작** | `docs/journal/ops/오늘.md` 생성 | A1~A7 결과 표 기록 + 전일 각주 이월 |
| A9 | **전략 데드라인 대장 확인** (7/17 신설) | `docs/checklist/STRATEGY_DEADLINES.md` | D-3부터 카운트다운 표기·도래 시 판정 보고 의무. **보고 첫 줄 = 스코어보드(누적 수익금·승률)** |

**이상 발견 시**: 즉시 fix(자율 권한 내) 또는 §4 각주에 등재. 큰 결정은 사장님 보고.

## §2. 자동 루틴 (VPS cron — 사람 개입 없음)

| 시각 | 작업 | 로그 |
|------|------|------|
| **08:30** | **아침 점검 A1~A6·A9 자동 + 텔레그램** (7/31 F-29 신설) | `logs/daily_ops_check.log` |
| 17:45 | VPS 코드 자동배포 (deploy_pull) | — |
| 18:00 | **nightly 관측 파이프라인** (①fill→⑳freshness, **29단계** — 8/10 S-6 폐기로 ③ 제거) | `logs/nightly.log` |
| 19:40 | foreign_exh 소진율 재수집 (네이버 저녁 게시분) | `logs/foreign_exh_late.log` |
| 20:10 | **데이터 수집완료 텔레그램 요약** → 사장님 발송 | `logs/notify_freshness.log` |
| 20:30 | VPSSync (노트북 미러 수신 — 노트북 켜져 있을 때) | 로컬 |
| 06:00 | VPS 코드 자동배포 2차 + 봇 재시작 (안전윈도) | — |

## §3. 마감 루틴 (세션 종료 전)

1. 업무일지에 **자기성찰 4문항** 작성 (§5 템플릿)
2. §4 각주 갱신 (신규 등재 / 완료 취소선)
3. 새 교훈 → 영구 메모리 반영 여부 판단
4. 코드 변경 있었으면: 4-Tier 검수 + commit + push + VPS 배포 확인

---

## §4. 개선점 각주 (살아있는 백로그 — 매일 최소 1건 소진 원칙)

> 등재 형식: `[F-번호] (등재일) 내용 — 근거`. 완료 시 ~~취소선~~ + 완료일.

- ~~**[F-1]** (7/16) **universe.json 갱신**~~ — ✅ 7/17 완료. `tools/manual/prune_universe_dead.py` 신설(dry-run 기본·--apply·백업+prune 로그). 2596→2531(4주+ stale 65종목 제거·1~4주 단기정지 16종목은 재개 가능성으로 유지). 오탐률 재측정: daily_ohlcv·investor 각 30/30 PASS. 월 1회 재실행 권장.
- ~~**[F-2]** (7/16) **notify 텔레그램 발송 실패 근본 fix**~~ — ✅ 7/16 완료. cron 무환경에서 `${TELEGRAM_BOT_TOKEN}` 해석 실패 → `.env` 로드 추가. `env -i` 실발송 검증.
- ~~**[F-3]** (7/16) **소급(--date) 검증 착시 제거**~~ — ✅ 7/16 완료. 마지막행==target → 꼬리 5행 내 target 존재(`_csv_has_date`)로 교체. 아침 A1 루틴의 전제 fix.
- ~~**[F-4]** (7/16) **notify_freshness.log 타임스탬프 부재**~~ — ✅ **7/31 완료**. `[notify] === YYYY-MM-DD HH:MM:SS 실행 (기준 <date>) ===` 스탬프 추가(`notify_data_freshness.py`). 이게 없으면 아침 A3 자동판정이 "마지막 블록"에 의존해 **휴장일 다음 거래일마다 오탐**한다 → `daily_ops_check` A3는 스탬프로 *기준 거래일의 실행분*을 집어 판정(스탬프 부재 기간은 mtime 폴백, 하루 지나면 자연 소멸).
- **[F-5]** (7/16) **swap_monitor.sh git 등록** — VPS 실사용 untracked (7/14 잔여·로컬 복사 필요).
- **[F-6]** (7/16) **VPS `_staging/`·`_upload/`·`from-jgis` 소규모 확인 후 정리** (7/14 잔여).
- **[F-7]** (7/16) **foreign_exh ⚠️ 8/10 소표본 관용 임계 검토** (7/14 LOW 잔여 — 현재 9/10, 10/10 관측 중이라 급하지 않음).
- **[F-9]** (7/16) **7/13(폭락장) ticks 유효행 8% — 수집기 장애 원인 규명** — 09:24 이후 전행 price=0(봇 1분 폴링이 폭락장에서 멈춤/스로틀 의심). 폭락장=가장 데이터가 필요한 날. 재발 시 ⑲/⑲-3 판정 불능 (MED).
- **[F-10]** (7/16) **섹터 쿨다운을 ⑲-3 페이퍼에 결합** — 7/10 사전합의 안전핀 중 유일 미리허설(섹터맵 필요). 라이브 전 필수 (MED).
- **[F-11]** (7/17) **`ticks/20260717` 스테일 dir 삭제** — 제헌절 휴장에 봇이 박제 시세를 기록(15:30까지 계속 씀 → 오후 삭제). ⑲ 필터로 무해화됐으나 데이터 위생 (LOW).
- ~~**[F-12]** (7/17) **봇 ticks 폴링의 휴장 가드 규명**~~ — ✅ 7/17 규명 완료: `_job_start_tick_polling`에 `is_trading_day()` 가드 **이미 존재**. 7/17 뚫린 건 코드가 아니라 캘린더 데이터(봇이 구 캘린더를 메모리 로드) — 근본 처방=캘린더 fix(완료)+매일 06:00 자동재시작이 반영. 코드 변경 불요.
- **[F-14]** (7/20) **ticks 수집 재시도·백오프** — 7/20 사고 상류 원인(KIS 레이트리밋 추정) 자체를 줄이는 단계. 오염 차단·가시화는 완료 (MED).
- ~~**[F-15]** (7/20) **★20:10 데이터 요약에 ticks 채널 추가**~~ — ✅ 7/21 배선·**7/23 실전 작동 확인**(7/22 요약 `장중틱(판정원천) 100.0%`). 검증기 0개→`_verify_ticks`+notify+⑳ 편입 완료.
- **[F-20]** (7/23·Tier1 M-1) **★판정 표본 25건 중 24건이 `entry_time < detected_at`** — 봇이 신호를 인지하기 前 시각의 가격으로 체결 중(러너 5분 폴링 지연). 상한 뷰가 "가장 이른 5건"을 취하므로 **신호~인지 지연이 큰 체결만 골라 담는 구조**(7/22는 09:01~09:02 신호 5건이 전부 09:25:02 단일 스캔에서 인지). 낙관 편향 → 진입행을 `detected_at` 이후 첫 관측행으로 잡는 대조 1회 필요. **현재 판정 결론(미달)은 이 편향과 무관하게 불변**이나, **지표가 플러스로 돌아서는 날 이 검증 없이 GO 금지** (HIGH — 라이브 판정 직결).
- **[F-21]** (7/23·Tier1 M-2) **상한값 5 vs 3 — 사장님 결정 대기** — 봇 설계 상한은 top_k 3+2=5지만 v2 신호는 `REFINED_CUTOFF=10:00` 하드필터라 14:50 트랜치를 못 채움 → v2 실행 가능 상한은 **3**. 실측 cap3 −6.16%p / cap5 −10.78%p(5가 더 불리 = 보수적). 코드는 상수 1줄 변경으로 즉시 전환 가능(H-1 fix로 면역화 완료). 판정 결론은 3·5 모두 미달로 동일.
- **[F-22]** (7/23·Tier1 L-4) **러너가 안 돈 날의 완료위장 사각** — `ticks/{day}` 부재 또는 `intents is None`이면 ⑲-3이 `return 0`으로 리포트 자체를 미발송하는데 nightly는 exit 0이라 ✅로 표시된다. ⑳은 `intents_date == today`인 날만 ⑲-3을 검사하므로 **러너 자체가 죽은 날은 어느 계층에서도 안 잡힌다**([F-18]과 같은 뿌리). 스코어보드가 조용히 사라지는 유일한 잔여 통로 (MED).
- ~~**[F-19]** (7/23) **CB 시뮬 기준이 판정 기준(상한 5건/일)과 불일치**~~ — ✅ **7/31 규명 완료(결론이 뒤집힘 — 정합 불필요, 대신 [F-46] 신설)**. 상한 5건 시퀀스 안에서 CB를 재적용해도 **11 유효일 전부 무변화**(51건 −69.67%p → 51건 −69.67%p·차이 0.00%p·CB로 잘린 날 **0일**). 원인 실측: 상한 5건 **진입은 중앙 7분**(최대 25분)에 끝나는데 **CB 발동은 마지막 진입보다 중앙 +30분 늦고 8/8일 전부 사후**(7/30 = 진입 09:26~09:31 완료, CB 09:46). 청산이 실현되기 前에 5건이 이미 다 들어가므로 **정합 문제가 아니라 CB 자체가 이 실행 구조에서 무력**. 도구 `scratchpad/f19_cb_cap_consistency.py`·`f19b_cb_timing.py`.
- **[F-46]** (7/31) **★ 일일 −6%p CB가 상한 5건 체제에서 구조적 무효 — 안전핀 재설계 (라이브 전 HIGH)** — [F-19] 규명의 산물. 7/9 지시서 백테스트에서 "CB 일일 −6%p 유효"로 통과했던 것은 **무제한 체결 가정**이었고, 사장님 자금 룰(상한 5건)에서는 발동 전에 그날 진입이 끝난다(위 실측). 즉 사장님께 보고된 안전핀 3종 중 CB는 **현재 아무것도 막지 못한다**. 7/30 −21.07%p가 −6%p 서킷을 그대로 통과한 것이 실례. 대안 축(택1 이상·사장님 결정): ①**미실현** 기준 CB(실현 대기 없이 평가손 합계로 차단) ②**진입 간격 강제**(1건 체결 후 N분 또는 직전 건 청산까지 대기 = 실현이 CB를 먹일 시간을 만듦) ③건당 손실 상한 하향. [F-30] 라이브 전제조건 목록에 편입.
- ~~**[F-24]** (7/27) **체결강도 역상관 — "저강도 초기진입" 축 탐색**~~ — ❌ **7/28 기각**. 백테스트 2,384건 5분위가 **역U자**(Q1 200~226 +0.014 / Q2 +0.272 / **Q3 261~321 +0.404** / Q4 +0.212 / Q5 461+ +0.149)로 단조 역상관 아님. 저강도 필터는 전부 현행보다 나쁨. 7/27 결론은 페이퍼 41건 순위정렬 아티팩트 → 진짜 원인은 **선착순 역선택**([F-26]).
- **[F-25]** (7/28) **교체후보 등재 시 "신호가 남는가" 선검증 의무** — S-1 on_fail의 명분(⑪-2) 게이트 결합판은 성적 이전에 **21일 D-1 교집합 0건**(catalyst 하루 30종 vs v2 58~117종)으로 실행 자체가 불가능했다. 검증 없이 후보가 on_fail에 오르면 D-DAY에 판정 불능. 대장 등재 시 최소 신호 수 확인을 규약화 (MED).
- **[F-26]** (7/28) **★선착순 아닌 실행가능 선택규칙 탐색 (S-1 본체·8/29)** — v2 전 신호 건당 **+0.210%p**인데 시간순 앞5건은 **−0.550%p**. 신호엔 엣지가 있고 "먼저 나온 것부터 산다"는 실행 규칙이 엣지를 역선택으로 파괴한다. 7/28 규칙 6종(고르는 순서) 전부 마이너스. ★**7/30 "언제 사는가" 축 2종도 기각**: **R-1 되돌림 진입**(k1~4% 전 구간 net −1.1~−1.3·3분할 전 음수·승률 53→33 붕괴 — 강한 놈은 안 내려와 못 사고 내려오는 놈만 사는 역선택 심화. 사장님 눌림 룰은 일봉 선정 문맥이라 별개 — 장중 돌파 후 되돌림 진입과는 다른 것으로 판명)·**R-3 지연 진입**(n10/20=baseline과 동일행 — 해상도 ~20분이라 무의미·n30 악화). baseline 앵커 재현(2,406건 net +0.173·cap5 −0.609 = 페이퍼 역선택 백테스트 재현 확인 — 테스트베드 신뢰). 도구=`tools/manual/r1_pullback_entry_backtest.py`(F-27 일부 소진). ★**R-4 재돌파도 7/30 오후 기각**: 전 p 구간 net −0.17~−1.20·WF 비일관. 단 분해 진단이 금맥 — **눌림 후 회복한 종목의 동표본 baseline은 +0.78~+1.16**(전체 +0.173의 5~6배). 엣지는 '털고 다시 가는 놈'에 집중돼 있으나 재돌파 시점 매수는 해상도 20분상 반등 꼭대기를 사서 엣지 소멸. **R-1·R-3·R-4 일관 결론 = 이 신호의 엣지는 즉시 진입에서만 산다(진입 지연 축 폐기)**. ★해부 진단(2,406건): 순위 1~2 −0.87/−1.41 vs **8+ 2,042건 +0.295**(수익 원천)·09:0x 신호 자체는 +0.253(이른 신호가 아니라 '그날 첫 체결'이 나쁨)·시각하한 09:30 cap5 −0.724(시간필터 백테스트도 기각)·best5 상한 +3.29. 날 효과와 순위 효과 얽힘 확인. ★**R-5 밀도 트리거도 7/30 기각** (K∈{2,3,5,8,12,20} 사전고정·58일): 최선 K=8 cap5 **−0.609→−0.041**(출혈 93% 감소·오늘 최대 개선)이나 **플러스 전환 실패**·WF 비일관·전 K 마이너스. ★★**7/28~30 누적 7규칙 기각의 수렴 결론 = 엣지는 '폭'에 있다**: 전신호 +0.173인데 실행가능한 어떤 "하루 5건 고르기"도 전부 마이너스(선착순 −0.61·강도순 −0.62·시각하한 −0.72·K스킵 −0.04). 특정 5건을 고르는 문제가 아니라 **밀도 높은 날 소액×다수로 넓게 담는 구조**(자금룰 연동 — 상한 재설계)가 이 신호의 그릇일 가능성. → **F-21을 "상한 5vs3"에서 "상한 구조 자체"로 확장 — 판정자료 정리 후 사장님 결정 사안** (HIGH).
- **[F-27]** (7/28) **S-1 판정 스캔 도구화** — 7/28 세션의 백테스트 덤프·후보 스캔·walk-forward·대조군 리프트 스크립트는 재사용 가치 높음(향후 전략 판정마다 필요). `tools/manual/`에 4-Tier 후 등재 (MED).
- ~~**[M1]** (7/27 이월) **tick_collector r2/r3 무가드**~~ — ✅ **7/30 완료** (soft-fail 설계). 무가드=거부 시 strength 0.0 '멀쩡해 보이는 오염행'(v2 필터 원천). 1차 설계(행 폐기)는 Tier1 검수가 과교정 적발 — ask1/bid1은 ticks CSV 소비자 0인데 price 증거까지 버려 스로틀 오후 집중 시 유효일이 TRUNCATED 탈락. 최종: r1 거부=hard(행 미기록)·r2/r3 거부=soft(해당 필드만 공란 기록·정상 0.0과 구분·판독기는 0 강제=오진입 없음)·rt_cd 엄격판정+HTTP status(M-2)·_safe_int/float(M-3 — "55710.28" 크래시가 '네트워크 예외' 오진되던 것)·사이클 요약 hard/soft 분리. 판정 연속성: 판독기 관점 거동 동일(0.0)이라 유효일 시계열 균질 유지. 단위 12/12+판독기 연동 PASS·VPS mock 3경로+실API 3/3. 잔여 상류 = [F-14] 재시도·백오프.
- ~~**[F-28]** (7/30) **신호 밀도 = 당일 레짐 프록시 검증**~~ — ❌ **7/30 당일 기각** (등재 당일 백테스트 검증 — 소표본 법칙화 금지 교훈 준수). 58 OK일 스피어만 **0.145**·3분위 비단조(−0.66/−0.76/−0.42) = 실행규칙으로 쓸 수준 아님. 아침의 "7/28 1건→7/30 68건" 직관은 전표본에서 지지되지 않음.
- ~~**[F-8]** (7/16) **아침 루틴 A1~A7 원커맨드 스크립트화**~~ / ~~**[F-29]** (7/30) **아침 점검 무인화**~~ — ✅ **7/31 완료**. `tools/daily_ops_check.py` 신설(read-only·A1~A6·A9)+평일 **08:30 cron**+텔레그램. 7/29 세션 미개설로 A1~A9 하루 공백이 실증된 건이라 스크립트화(F-8)에서 **무인화**로 승격해 한 번에 소진. 설계 요점: ①**기준 거래일 앵커** — "로그 마지막 블록"이 아니라 ref 블록을 찾는다(휴장일 다음 거래일 오탐 차단·A2/A3/A4 공통) ②**SKIP→WARN 승격**(운영기에서 로그 부재는 정상 아님) + 초록불에 "점검 불가 N건" 병기 = [F-22]류 완료위장 차단 ③판정 수치는 `strategy_deadline_check.build_report()` **위임**(상수 로컬 복제 금지·[F-37] 교훈) ④항목별 예외 격리·exit 0(cron 무차단). A7(로컬 미러)·A8(업무일지)은 노트북/사람 몫이라 대상 외.
- **[F-30]** (7/30) **라이브 전제조건 일람화** — S-1(8/29) 통과 시 곧장 소액 라이브 논의가 오는데 전제조건이 백로그에 흩어져 있음: F-10 섹터쿨다운 리허설·F-19 CB 정합·F-20/F-23 체결편향 검증·F-21 상한 결정·안전핀 SAJANG 등재. 하나의 체크리스트 문서로 묶어 "판정 통과=즉시 진행 가능 상태" 유지 (MED).
- **[F-31]** (7/30 검수) **★★ signal_time = 시장 시각이 아니라 "시총 순위 대리변수"** — `tick_collector`가 universe.json(시총 내림차순) 고정 순회라 첫 관측 시각이 rank와 완전 단조(실측 7/29: rank 0~506 중앙 **09:02:58** → rank 2025~2530 **09:19:04**·16분 격차). 소형주는 첫 관측이 이미 20분 경과 → 관측 등락률이 추격<8%를 넘겨 탈락 = **v2 표본 자체가 대형주로 기울어 있음**(cap5 rank 중앙 344 vs 나머지 1008·상위25% 비율 64% vs 28%). ★규명 결과 **"선착순 역선택"은 아티팩트 아님**(rank 3분위 전부에서 cap5 열위: 대형 −0.336 vs +0.106 / **중형 −2.284 vs +0.535** / 소형 −0.627 vs +0.125)·대형 회피 cap5도 −0.32~−0.63으로 개선 실패. ★**새 축 발견 = 시총 5분위 역U자**(Q1 대형 −0.239 / **Q3 중형 +0.536 최고** / Q5 초소형 −0.168) — F-24 체결강도 역U자와 같은 형태. 8/29 판정 및 차기 가설은 이 축 우선 (HIGH).
- **[F-32]** (7/30 검수) **판정 지표 4층 = 자금·시간 제약** — [[feedback-judge-metric-three-layers-7-23]] 3층(데이터품질·표본선택·집계규칙)에 추가. F-21 슬롯 시뮬로 "전신호 평균은 무한자금·시간제약0 가정"이 확증됨. 판정 근거 인용 시 4층 통과 명시 의무 (HIGH·규약).
- ~~**[F-33]** (7/30 검수) **short/credit 검증이 `max(dates)`+활성필터 없음 = 1개만 신선해도 PASS**~~ → ✅ **8/14 소진 (커밋 0ed7e50)**. `_active_codes_all()` 신설로 investor·short·credit **전수 전환**. ★소진이 15일 늦은 이유 = 8/14에 단타봇이 같은 문제를 "오늘 발견"으로 착각하고 착수 직전 장부를 봐서야 발견([[feedback-ledger-breaks-both-ways-8-7]]의 세 번째 방향 = *열려 있는데 새로 발견했다고 착각*). ★설계 근거는 전부 실측: 전환 비용 **0.59초**(2,531종)라 표본 유지 근거 소멸 · 정상 커버리지 실측(7/30~8/14 12거래일) **investor·short 정확히 100.0% / credit 96.8~97.0%** → credit만 최신적재일 기준 95% 임계. ★**재수집 트리거 불변**(investor_flow는 RETRY_MAP 등재라 PARTIAL이면 06:30 A0가 `_job_collect_daily` 전체 재실행 — 소수 결손은 PASS 유지 + `missing` 병기로 *보이게 하되 동작 불변*). 단위 23/23(★음성대조=구코드가 1종만 신선해도 PASS하던 케이스를 신코드가 FAIL로 잡는지 결정적 재현)·VPS 실측 동시점 구/신 대조 **판정 동일=회귀 0**.
- **[F-34]** (7/30 검수) **daily_ohlcv 임계 `ok>=n-2`(8/10) = 20% 결손도 PASS** — 근거였던 "죽은 종목 3.4% 잔존"이 F-1 prune(2596→2531·오탐 30/30 PASS)으로 소멸. CRITICAL 채널에 19~20% 맹점 잔존(7/20 ticks 결손률과 같은 자리수) (MED).
  - ★**8/14 보강 — 정량화 + 연쇄 발견(미소진 유지)**. ①검출력 계산(표본 10·stale 3종 이상이어야 PARTIAL): 결손 **10% → 검출 7.0%**(93% 미검출) · 20% → 32.2% · 30% → 61.7%. ②★새로 확인된 **연쇄**: [F-33] 소진으로 investor·short·credit이 **활성(=daily 오늘행 보유) 분모**를 쓰게 되면서, **daily가 대량 결손이면 활성 분모가 같이 줄어 나머지 채널은 여전히 100% PASS로 보인다** — 즉 *종가 250종이 빠져도 전 채널 초록불*이 성립한다. ③단타봇이 8/14에 이걸 "새 발견"으로 등재할 뻔했고 착수 전 장부 확인으로 [F-34] 기존 등재임을 확인(오늘 두 번째 같은 실수·[[feedback-ledger-breaks-both-ways-8-7]]). ④daily는 **활성 판별의 앵커라 활성필터 치환이 순환논리**(원 docstring)이므로 [F-33]과 같은 전수화가 안 통한다 → 처방 후보 = **활성 종목수의 전일 대비 급변 감시**(오늘 2,477 vs 어제 2,467 = +10 정상 / 급감이면 경보). 지금은 이 감시 계층이 **어디에도 없다**.
- **[F-35]** (7/30 검수) **⑩·⑮ 산출물 sync 미등재 (누락 4·5번째)** — `institution_accum_scan.json`(로컬 미러 6/19·6주 stale)·`kr_us_shock_observed.csv`(로컬 파일 없음). CORE_FILES는 .json만·DATA_DIRS는 디렉터리만이라 **data_store 루트 CSV는 sync 문법상 사각**. ⑳에도 없어 "생산 중단"과 "sync 중단" 구분 계층 0. 추가로 ⑩ 러너(`run_institution_accum_scan.py:44`)가 반환값 무시+무조건 exit 0 = 업로드 실패를 ✅로 보고 (MED).
- **[F-36]** (7/30 검수) **⑲-4는 산출물 0 → ⑳ 검사 불가 + 스키마 변형 1개로 무경보 사망** — `strategy_deadline_check.py:131`의 `reg.get("strategies")` 순회가 try 밖 → 최상위 dict 아니면 AttributeError → 최상위 catch → exit 0·텔레그램 0·nightly ✅. 30스텝 중 유일하게 파일을 안 남기는 스텝이라 ⑳ 대상 부재. 데드라인 체계 전체가 이 1스텝 의존 (MED).
- **[F-37]** (7/30 검수) **판정 도구 4종의 `CAP_PER_DAY=5` 로컬 복제** — `observe_v2_paper.CAP_VIEW_N`이 [F-21] 결정으로 3이 되면 신규 도구는 5 유지 → "S-1 판정 지표 미러" 주장이 조용히 깨진다. import로 고정 필요. 함께: 무표본 가드(`MIN_JUDGE_N`) 미적용으로 3건 변형이 57건과 동일 비중 출력 = F-24 재발 통로 / r4:166 baseline 0건 시 KeyError (MED).
- **[F-38]** (7/30 검수) **S-6 관측창 4거래일 영구 결손** — 6/19·6/23·6/26·7/20이 후보 0건으로 save 없이 early return → `_recorded_dates()`가 학습 못 해 매일 재시도(33/37일=10.8% 결손). 로그 위장은 7/30 fix(신규/결손 분리)했으나 **왜 그 4일만 0건인지 근본 규명은 미완**(7/20은 ticks BROKEN일). S-6 판정 8/7 전 확인 (MED).
- **[F-39]** (7/30 검수) **호가 스프레드·유동성 조건 0** — ticks에 ask1/bid1이 있는데 판정 도구 소비 0. 실측 192390 09:21 = bid 1,780/ask 1,879 = **스프레드 5.6%**, `strength 442`가 **129주**에서 산출. 이런 종목이 v2 표본에 섞여 net>0 임계 판정에 영향. COST_RT 0.2 고정은 낙관 (MED→라이브 전 HIGH).
- **[F-40]** (7/30 검수 P0) **★ ETF 주도주 보너스 = 죽은 가산항 (사장님 결정 대기)** — 소비자(`morning_recommendation.py:1228·1945`)는 매일 `etf_leader_sc`를 선정 점수에 가산하는데, 생산자 2파일이 **KRX 의존**이라 7/1 Fix5에서 삭제됨(6/22 KRX 절대 룰) → Supabase 최신 base_date 5월 → 35일 가드로 `{}` 반환 = **5주+ 가산 0**. VPS cron은 매주 토요일 "No such file"로 실패 중이었음(로그 4회 확인) → **7/30 cron 라인 제거·백업 `/tmp/cron_backup_730.txt`**. ★**에이전트의 "롤백 복구" 제안은 KRX 룰 위반이라 기각** — 대신 `etf_leader_bonus.py` docstring에 영구 비활성 사유 명시. **선정 로직에서 가산항 자체를 걷어낼지는 사장님 결정**(현재 항상 0이라 매매 무해). 7/4 fix5 복구(7591a42)가 9개만 살리고 이 2개를 빠뜨린 것도 이제 규명됨 — 빠뜨린 게 옳았음(KRX).
- **[F-41]** (7/30 검수) **죽은 텔레그램 심볼 3곳** — `send_telegram_message`·`send_message` 둘 다 repo 정의 0건인데 `verifiers/_common.py:26`(HTTP 폴백이 살려서 발송은 정상·매회 예외 1회)·`tools/daytrading_picks.py:681,689`(1·2차 폴백 **둘 다 죽음** → 항상 False)·`tools/foreign_accumulation_scanner.py:299,307`(동일). `verifiers._common.send_telegram`으로 통일 (MED).
- **[F-42]** (7/30 검수) **DEAD_KIWOOM.md KEEP 목록 오류 2건** — `pension_finance_scan.py`(291줄) "라이브 KEEP"은 오류(유일 참조가 docstring 안·C41은 6/19 institution_accum_scan으로 교체) / `daily_collector.py`(488줄) "CRON 러너 라이브"는 스테일(Windows 작업 미등록). 문서 정정 + 격리 후보 (LOW).
- **[F-43]** (7/30 검수) **확실히 죽은 12파일 + Windows 런처 13개 격리** — 5중 확인 완료분(backtest/etf_leader 5·full_period_backtest·daily_collector·pension_finance_scan·cluster_harvest_paper(S-2 폐기)·day_trade_bot(6/4 채택금지)·_archive_dead_code_7_27 잔여). Windows 작업 실측 238개 중 **`BodyHunter_VPSSync`만 Ready**, 나머지 Disabled = 데이터 메인 VPS 전환 잔재. `_archive_dead_code_7_30/` 격리 이동(삭제 X) (LOW).
- **[F-44]** (7/30 검수) **`reentry_ticks_precision_7_7.py:57` ticks 판독 클론이 시간 정렬 안 함** — 정본 `pb._read_ticks`는 `out.sort()` 하는데 이 클론은 자체 DictReader·정렬 없음. **7/7 재진입 FAIL 판정을 낸 도구**라 정합성 확인 필요(판정 수치 변동 시 사장님 보고) (MED).
- **[F-45]** (7/30 검수) **장중 시간 정의 불일치** — `verifiers/flow_monitor.py:27`(09:00~15:30) vs `bot/auto_trader.py:745`(09:00~15:20). 시간 파싱이 4계열 난립(정본 없음)·`auto_trader`의 동일 `async def _send` 클로저 **15곳 복제**(같은 파일에 `_alert()` 이미 존재)·CSV 로더 7중(정본 `csv_loader.CSVLoader`는 외부 importer 0=죽은 정본)·미사용 import 137건(핵심 생산 파일 12개는 깨끗). 점진 수렴 (LOW).
- **[F-47]** (7/31 Tier1 L-4) **A4가 "러너 가동·ticks 부재"를 "러너 정지"로 오진단** — `daily_ops_check` A4는 `누적 N` 라인으로 판정하는데, 러너는 정상 가동했지만 `ticks/{day}`가 끝내 안 생긴 날은 `ticks/{ymd} 없음 — skip` 라인만 남아 "러너 정지 의심"으로 표기된다. **경보 자체는 옳고**(둘 다 이상) 원인 문구만 틀린다. skip 라인도 스캔해 두 상태를 분기 (LOW).
- **[F-48]** (7/31 Tier1 인접관찰) **nightly 스텝 실패 시 stdout 증거 소실** — `run_nightly_pipeline.py:53`이 rc≠0일 때 **stderr 꼬리만** 로깅한다. 그런데 `run_nightly_freshness_check.py`(⑳)는 STALE 목록을 전부 **stdout**에 찍고 exit 1 하므로, **⑳이 완료위장을 적발한 날 nightly.log엔 `⚠ ⑳ … exit=1 |` 뒤가 빈 줄로만 남고 무엇이 stale인지 사라진다**. 아침 점검 A2는 FAIL을 내지만 원인을 못 전달한다. rc≠0일 때 stdout 꼬리도 함께 보존 (MED — 완료위장 적발의 증거 계층).
### ★ 7/31 전체검수(5관점) 신규 등재 — F-49~F-69

> 4건은 당일 fix 완료(휴장일 가드 2·SAJANG 파생 3·A5 오진·스코어보드 위장·F-48). 아래는 잔여.
> **[사장님]** 표시 = 매매 동작이 실제로 바뀌므로 단타봇 자율 권한 밖.

**A. 데이터·검증 계층 (관점3)**
- **[F-49]** (7/31·**8/1 심각도 정정 HIGH→MED + 판정식 재정의**) **수급 검증이 값을 보지 않는다 (미탐 통로는 실재)** — `data_verifier._csv_has_date`(:104-124)가 **첫 컬럼 날짜 문자열만** 비교. 정직한 지표(`flow/_last_update.json`의 `data_through`·`investor_real`)가 **이미 생산 중인데 검증기가 안 본다**. ★**8/1 완주 후 전수 재측정으로 정정**: 어제 근거 "572개 중 61개(10.7%)"는 **수집 중(장중) 스냅샷이라 분모가 달랐다**. 완주 후 실측 = **68/2527 = 2.7%**이고 분해하면 **35종 거래정지(daily도 결손)·31종 만성 초소형주·2종만 당일 신규**(049830·263920). 그 2종도 **정상값**으로 확정 — `flow_collector._fetch_investor_api`는 `rt_cd != "0"`이면 `None`을 반환하고 호출부(:265)는 `df is not None`일 때만 행을 쓰므로 **행 존재 = KIS 성공 응답**이고, 같은 `item`에서 파싱되는 `종가`·`전일대비`가 정상값(6200/−40·3525/+20)이다 = 수급 0은 KIS가 준 실제 순매수 0(초소형주 개인↔개인 net 0·거래량 959/3459주로 정합). ★**따라서 fix의 판정식은 "수급 0=실패"가 아니라 "종가·전일대비까지 0인 행" + `data_through` 교차검증**이어야 한다 — 전자로 짜면 7/31에만 오탐 68건 (MED).
- **[F-50]** (7/31) **16:00 이후 캐시 무조건 신뢰 → placeholder 하루 동결** — `flow_collector.py:206-228`이 "오늘 날짜 행 존재"만 보고 재수집 스킵. 장전 0값 행이 있으면 C3·nightly ⑥ 양쪽 스킵 → EOD 확정치 영영 미수집인데 [F-49] 때문에 PASS (HIGH).
- **[F-51]** (7/31) **⑳의 데이터 재검증이 어떤 판정에도 반영 안 됨** — `run_nightly_freshness_check.py:64-70`의 재검증 예외·`critical_failures`가 `stale`에 안 들어가 exit 0. 주석은 "exit 1 경로"라 주장. **완료위장 차단 스텝 자체의 구멍** (HIGH).
- **[F-52]** (7/31) **★ 7/30 M-1 fix가 봇 경로 미적용 → 오늘 실제 재발** — `save_result` 기본값이 True라 `trading_coo.py:2933·2975`·`telegram_bot.py:4073·4087·5897` 5곳이 여전히 상태 파일을 덮는다. 실증: 오늘 `data_verify_result.json` = 06:33:51·status FAIL·daily 0/10(장전 박제). A0(`_job_morning_backfill`)은 docstring이 "전일 기준"인데 코드는 `date.today()`. **근본 처방=`DataVerifier.save_result` 기본값을 False로 뒤집고 저장 필요한 2곳만 True** (HIGH).
- ~~**[F-53]** (7/31→8/6) **⑫ 수집기가 전멸해도 `status: ok`**~~ — ✅ **8/6 상태분기 완료**(배포는 nightly 이후 새벽 06:00 안전창 — 수집기라 18:00 직전 배포 금지). ★**결함 재현으로 확정**: 구코드에 6종 전멸을 주입하니 반환이 **`status: 'ok'`**(saved 전부 0), 신코드는 `'failed'`·6종 명시. fix=`saved[kind]=0`이 뭉개던 **세 상태를 분리** — `ok`/`failed`(예외)/`skipped`(장외·정상)/`empty`(예외 없이 0행=조용한 실패 후보). 집계는 `ok`가 하나도 없으면 **failed**, 일부 실패·빈응답이면 **partial**, 그 외 ok. 실패·부분실패는 `logger.error`로 **status 문자열이 nightly.log 본문에 남는다**(로그를 읽는 쪽이 알 수 있게). `__main__`은 전멸일 때만 exit 1(부분 실패가 파이프라인을 죽이지 않게 — 종류별 격리 유지). 회귀 **20/20 PASS**(`tests/test_ranking_status_8_6.py` 신설·전멸/부분/장외/빈응답1종/빈응답전종목/휴장가드) + 음성 대조(구코드에서 KeyError = 검출력 증명). ★**테스트 자체 정정 1건**: 최초 픽스처가 전 종목을 빈 응답으로 만들고 `partial`을 기대했는데 **전부 비면 전멸이라 `failed`가 맞다** — 코드가 옳고 내 기대가 틀렸다. ★**잔여**: nightly가 이 모듈을 `-c` 인라인으로 호출해 **종료코드를 보지 않는다**(`-m` 전환은 상주 파이프라인 변경이라 관측 1회 후 별건). 원문 보존:
- **[F-53-원안]** (7/31·**8/1 범위 확정**) **⑫ 순위 6종 중 1종만 검증 + 수집기가 전멸해도 `status: ok`** — `data_verifier:392-404`는 `volume.csv`만 확인. `ranking_snapshot_collector:139-140`은 무조건 ok 반환. 실측 `night_futures.csv` **아예 없음**(5/6). ★8/1 확인: 나머지 5종은 7/31 전부 정상(volume 725·fluctuation 725·strength 353·foreign_inst 1500·uplowprice 348행). `night_futures`는 **7/28~7/31 전일 `saved:0`이고 `status: ok`** = **7/31 회귀가 아니라 상시 미생산**. `ranking_snapshot_collector.py:128-137`이 장외(정상)와 실패(이상)를 **둘 다 `saved=0`으로 뭉개** 원인 구분 불가 — fix는 두 상태 분기부터 (MED).
- **[F-54]** (7/31) **⑦ 11주체 kosdaq 검증 사각** — kospi만 검증, `kosdaq_investor.csv`는 어느 검증기도 안 봄. 러너도 `print('ok' if r else ...)`라 kospi만 성공해도 ok. **코스닥이 v2 표본 주력** (MED).
- **[F-55]** (7/31) **누적 CSV 5채널 비원자적 전체 재작성** — flow/market_investor/ranking/kr_us_shock가 `df.to_csv()` 직접(daily fill은 tmp+replace 정본). ⑬⑭는 timeout 5400s 최장 스텝이라 kill 타이밍이 쓰기 중이면 **전 기간 히스토리 절단**. 탐지 계층 0(마지막 한 줄만 읽음) (MED).
- **[F-56]** (7/31) **★ sync 누락 6번째 — 사람 규율로는 안 끝난다** — CHECKLIST 참조 경로 중 `tv_scanner.json`·`nightwatch_report.json`·`guardian_latest.json`·`consensus.json` 미등재. **근본=CHECKLIST 참조 경로 ↔ sync 대상 차집합을 pre-commit에서 자동 검사** (MED·근본).

**B. 판정 파이프라인 (관점2)** — ★스코어보드 51건 −69.67%p 자체는 독립 재계산 **완전 일치**(torn 0·중복 0·look-ahead 없음)
- **[F-57]** (7/31) **"전 신호 합산" 라벨이 실제로는 CB 적용 후 수치** — `observe_v2_paper.py:181-190`이 CB로 자른 뒤를 `n`/`sum_net`으로 저장. 실측 유효 11일 **737 → 662건(75건·10.2% 소실)**, 7/30은 101 → 55. 잘린 건의 손익은 `skipped`에 미보존. **S-1 보조기준("실측↔백테스트 갭")의 두 수치가 서로 다른 모집단**이고 방향은 **낙관 편향** (HIGH).
- **[F-58]** (7/31) **`trades[].net` 박제 + params stamp 6/11일 결손** — COST_RT를 바꿔도 과거일 미소급. 원본 `ret`가 있으므로 합산을 `ret - COST_RT`로 바꾸면 상수 변경 면역 (MED).
- **[F-59]** (7/31) **⑲-3 보고 발송 실패가 전 계층 무증상** — 정산·저장은 성공하고 보고만 죽으면 nightly ✅·⑳ ✅·A2 ✅·A9 정상값. 사장님만 못 받는다. A3와 같은 발송 스탬프 도입 (MED).
- **[F-60]** (7/31) **유효일 정의에 러너 커버리지 미반영** — `_cum`은 ticks_health만 봄. 러너가 신호 절반만 잡은 날도 완전 유효일이고, cap5는 시간순이라 **누락 시 다른 5건이 뽑힌다**. history에 `verdict/matched`가 이미 있는데 미사용. 현재 14일 100% 일치라 오염 실적 0 (MED·구조).
- **[F-61]** (7/31) **cap5 정렬 키가 장부(signal_time) vs 백테스트 도구(entry_time) 상이** — 현재 선택 집합 동일이라 영향 0이나, 해상도가 바뀌면 무경보로 갈린다. `select_cap()` 헬퍼로 단일진실화 (LOW).

**C. 실매매 안전 (관점1)** — ★KIS 주문 sink 7곳 전수 게이트 PASS·`manual=True` 자동경로 0건
- **[F-62]** (7/31) **[사장님] 룰4(상한가분할)·룰B가 룰D 종목을 커버하지 않는데 코드는 커버한다고 주장** — 룰D 포지션은 `rule_b_watch`/`limit_up_split_watch` 마커를 다는데 두 룰의 필터는 `source.startswith("asset_pool")`(auto_trader:5446·5310)이라 **마커가 읽히지 않는다**. docstring(:2872)은 "룰 3 자동 적용"이라 명시적으로 잘못 서술. 재가동 시 룰D 종목이 +25% 쳐도 절반 락인 없음 → 룰4가 막으려던 무한보유 발생. fix=마커 기준 통일(매매 변경이라 사장님 결정) (HIGH).
- **[F-63]** (7/31) **NXT/predawn 예산이 30% 현금 룰 미경유** — `auto_trader:6220 cash*0.30`·`:6520 cash*0.40`이 `SAJANG.calc_budget_per_stock` 우회. 하위 `nxt_safe_buy`/`afterhours_buy`에도 현금유보 검사 없음(`safe_buy`만 있음) = **30% 룰 유일 구멍**. 현재 alert_only+AUTO_TRADE_DISABLED 이중차단·predawn 스케줄 미등록으로 무해 (HIGH·재가동 전).
- **[F-64]** (7/31) **`_job_jarvis_decision` 거래일·장중 가드 0** — 5분 반복인데 `config.yaml:293 dry_run: true`가 유일 방벽. LIVE 매도는 `_is_sell_protected`도 안 거침 (MED).
- **[F-65]** (7/31) **`cancel_order` 반환 미검증 → 이중 매수 가능** — `kis_trader:1239-1241` modify 실패 시 취소 결과를 안 보고 `buy_market` 추가 발주. 취소 실패(이미 체결)면 2회 매수. 부분매도 중이면 잔량까지 팔릴 수 있음 (MED).
- **[F-66]** (7/31) **유령 포지션** — `smart_buy:1192-1199`가 `filled_qty=0`인데 `success: True` 반환, 호출부는 success만 보고 등록 → 실체 없는 포지션 (MED).
- **[F-67]** (7/31) **[사장님] positions.json 미등재 = 트레일링·손절·hard_kill 전면 불능** — 3계층 모두 보호로 판정. `sync_positions` 자동등록분도 `sync_auto_unknown`이라 여전히 보호. 5/21 로킷헬스케어(-15.4%) 사고와 같은 방향이고 CLAUDE.md 룰 10("보호 종목도 트레일링 핸들링 OK")과 어긋남. **"보호=전 매도 차단" vs "보호=고정TP만 차단, 손절은 허용"** 결정 필요 (HIGH).

**D. 사장님 룰 준수 (관점5)** — ★룰 1·2·3·6·10·11 정상 확인(옛 +5% TP 엔진 소멸 확인)
- **[F-68]** (7/31) **[사장님] 룰4 "+29% 절반 매도"가 코드에 없음** — `SAJANG.LIMIT_UP_SELL_PCT=29.0` 참조 0건. 실제는 `pnl>=25` 즉시 시장가(auto_trader:5452). docstring은 "+29% 락인"이라 주장 = **문서가 구현보다 유리하게 서술**. 사장님 룰 문구 해석 확인 필요 (MED).
- **[F-69]** (7/31) **★ 자가감사가 "소비자 존재"를 검사하지 않는다 (근본 재발방지)** — `daily_self_audit.py:146-172`는 SAJANG 상수의 **값**만 assert하고 그 값이 실제 소비되는지는 안 본다. 그래서 오늘 적발한 룰7 SAJANG 우회·budget_mode default off가 15:45 감사를 그대로 통과했다. **상수별 소비자 grep ≥1 검사 추가**가 이번 같은 갭의 재발을 막는 유일한 구조적 조치 (MED·근본).
- **[F-70]** (7/31) **고아 상수 4종** — `LIMIT_UP_HOLD_RATIO`(limit_up_split_sell:53이 0.5 자체 하드코딩 + 주석은 "SAJANG 미등록"이라 오기)·`TRAILING_BREAK_EVEN_AT_ACTIVATION`(동작은 auto_trader:4356 하드코딩)·`RULE_B_CRON_TIME`/`RULE_C_CRON_TIME`(coo가 kst_time 하드코딩). 현재 값 일치라 무해 (LOW).
- **[F-71]** (7/31) **[사장님] 룰B timing_mode 문서-구현 모순** — `limit_up_split_sell:153`은 `previous_close`를 허용하는데 docstring(:134·144)은 "14:50 매수분 룰B 미적용"이라 명시. 14:50 asset_pool 매수분이 **36분 만에 15:26 절반 익절 대상**이 된다. 의도 확인 필요 (MED).
- **[F-72]** (7/31) **`equal_levels`/`premium_levels` 보존정책 부재** — 3/24부터 89일치·합계 2.7GB(data_store 4.7GB의 57%)·일 ~30MB 증가. 라이브 생산·소비 중이라 슬러지는 아니나 보존 기간 필요. 현재 디스크 63%·여유 22G (LOW).

**E. 슬러지·인프라 (관점4)** — ★P0 4건은 당일 fix 완료
- ~~**[P0-1]** Daily Self-Audit 66일 전무~~ — ✅ **7/31 fix**. `trading_coo.py:5003` `is_trading_day`가 **모듈 바인딩 없음** → 매 평일 15:45 NameError(VPS 저널 10일 20회·7/31 15:45 포함). 도입 커밋 d18c3bd(5/26)=자가감사 신설 커밋 = **태어날 때부터 고장**. 즉 사장님 영구 룰 13종 자동 검증이 존재한 적 없음. **fix 후 첫 실행에서 즉시 위반 3건 검출**(CRITICAL 2/HIGH 1 — 실계좌 현금 1.5% vs 30% 룰).
- ~~**[P0-2]** 09:15 첫 매수 job 가드 NameError~~ — ✅ 7/31 fix. `trading_coo.py:4808`. 현재는 `_auto_trade_disabled` 조기 return이 가려주나 **재가동 즉시 09:15 매수가 except로 조용히 사망**할 잠복이었다. 도입 커밋 2eef756(5/26)="가동 안전 보장 fix"가 버그를 심음.
- ~~**[P0-3]** 봇시야 송출 `KST` NameError~~ — ✅ 7/31 fix. `trading_coo.py:4900`. 3일간 30회 실패.
- ~~**[P0-4]** RULE-002/004가 문서에만 존재~~ — ✅ **7/31 신설**. CLAUDE.md·REVIEW_3TIER_RULE이 5/25부터 "RULE-002 자동 차단"이라 적어왔으나 `pre_commit_check.py`에 **구현 0건**이었다(P0-1~3이 66일 산 이유). **pyflakes undefined-name을 RULE-002(HIGH·차단)로 배선** — "가드가 쓰여 있는가"가 아니라 "가드가 동작하는가"를 본다. 양성/음성 대조 PASS·저장소 전체 잔여 0건. + RULE-008 주석 오탐 제거(`_strip_comment`).
- ~~**[H-2]** `morning_recommendation.py` 4개 기능 NameError 사망~~ — ✅ 7/31 fix(모듈 `json`/`datetime`/`BASE_DIR` 바인딩). ★단 **FX 섹터 부스트·인플레 체인 부스트는 선정 점수를 바꾸므로 켜지 않고 플래그로 현 동작 보존**(`FX_SECTOR_BOOST_ENABLED=False`·`INFLATION_CHAIN_BOOST_ENABLED=False`) — 66일간 미적용이던 로직을 단타봇 판단으로 켜면 '관측 없이 flip'. **활성화는 사장님 결정**. US overnight 캐시·[NXT] 태그는 부작용 없어 복원. `_universe` 이름조회는 항상 코드값이었음을 명시화(동작 보존).
- **[F-73]** (7/31) **텔레그램 발송 정본 부재 — 죽은 심볼 7곳 + 각자 구현 14개** — `send_message`/`send_telegram_message`는 저장소에 **정의 0건**인데 7곳이 import(`verifiers/_common.py:26`은 HTTP 폴백이 살려 매회 예외 1회·`foreign_accumulation_scanner.py:299`는 **UnboundLocalError 경로**·`daytrading_picks`는 항상 False). 별도로 `api.telegram.org` 직접 호출 모듈 14개. `verifiers._common.send_telegram` 정본 승격 후 통합 (MED·F-41 확장).
- **[F-74]** (7/31) **존재하지 않는 함수 import 3건(try/except 은폐)** — `etf_recommender.py:232 load_sector_report`(미존재→섹터 모멘텀 가중 0)·`limit_up_engine.py:326 detect_rotation`(실제명 `analyze_rotation`→**로테이션 게이트가 한 번도 막은 적 없음·favorable=True 고정**)·`flowx_universe_updater.py:133 load_stock_data`(→volume_ratio 항상 1.0) (MED).
- **[F-75]** (7/31) **데드 격리 후보 4종(5중 확인 완료) — 별도 패스로 이동** — `data/pension_finance_scan.py`(전 참조가 docstring·6/19 institution_accum_scan로 대체)·`data/csv_loader.py`(**죽은 정본** — 유일 참조가 F-74의 미존재 심볼. 실질 정본은 `utils/stock_utils.load_daily` 소비자 7건)·`backtest/full_period_backtest.py`·`backtest/etf_leader/` 5개(KRX 산물·복구 금지). ★7/4 오삭제 사고(9파일→nightly 이틀 사망) 때문에 **오늘은 이동하지 않고 등재만** — 격리는 집중 패스에서 (LOW).
- **[F-76]** (7/31) **⚠ 데드 오판 주의 — `tools/sync_from_vps.py`는 살아있다** — python import 0건이라 정적 그래프상 완전히 죽어 보이나 **Windows 작업 `BodyHunter_VPSSync`=Ready·매일 20:30 실행**. `DEAD_KIWOOM.md`에 근거와 함께 명시 필요(다음 검수자 오삭제 방지) (MED·문서).
- **[F-77]** (7/31) **문서 스테일 11건** — CLAUDE.md/REVIEW_3TIER_RULE의 RULE-002/004 자동차단 주장(P0-4로 해소)·STRATEGY_DEADLINES.md가 S-1을 **7/31**로 표기(JSON은 8/29 연장)·nightly "29단계"(실제 30)·cron 표 4개 누락(swap_monitor·foreign_accumulation_scanner·intraday_learning_v2·observe_v2_runner)·DEAD_KIWOOM 러너 "13종"(실제 21)·REVIEW_3TIER_RULE에 Codex/Tier4 언급 0회 (MED).
- **[F-78]** (7/31) **★판정 도구가 scratchpad에만 존재 = 소실 예정** — [F-19]/[F-46] 근거를 낸 `f19_cb_cap_consistency.py`·`f19b_cb_timing.py`가 세션 임시폴더에만 있다. 920a763이 막으려던 "판정 증거 소실 통로"와 같은 자리 → `tools/manual/`로 승격 필요 (MED).
- **[F-79]** (7/31) **노트북 `stock_data_daily/`가 6/23에서 정지** — `sync_from_vps.DATA_DIRS`가 data_store 하위만 받아 **구조적으로 못 받는다**. `market_breadth_today.py:28` 등을 노트북에서 돌리면 5주 낡은 데이터로 분석(7/1 장전 박제 착시와 동종) (MED).
- **[F-80]** (7/31) **미사용 import 55건/11파일(핵심), 주변부 147건/89파일** — `telegram_bot.py`에 33건(30건이 `_job_*` 머리에 복붙된 `from datetime import date/timedelta`). 도달불가 분기 0건·주석코드 0건은 **깨끗함 확인**. 점진 수렴 (LOW·F-45 병합).

### 8/1 오후 — 린치 《투자 이야기》 적용 1단계 결과 (F-83~F-85)

> 도구 `tools/manual/lynch_concentration.py` 신설(read-only·정본 `_cum` 위임·자기검증 내장).
> 실측: 유효 12일 56건 −55.75%p / 정본 대조 일치.

- ~~**[F-83]**~~ (8/1) **★★★ 규명 완료 — 원인은 '표본 효과'이고, 실체는 "6월 단일 구간 의존"** — 도구 `tools/manual/f83_backtest_ledger_gap.py` 신설(r1 판정함수 import·로직 복제 0). ①**같은 12일로 자른 백테스트 = 건당 −0.186%p(896건)**로 장부(−0.050)보다 오히려 나쁨 → 표본 효과 확정. ②**러너 커버리지 기각**: 장부건+CB컷=백테건이 **12일 전일 100.0% 일치**([F-60] 우려 해소). ③**CB는 무효가 아니었다**: 잘린 75건 평균 **−1.67%p/건**으로 CB가 **−125%p를 방어**했다 — [F-46]("CB 무효")과 모순이 아니라 **CB는 상한 5건에서만 무력하고 넓게 담는 구조에서는 작동**한다(= [F-84] '폭' 결론과 짝을 이룸·[F-21] 재설계 입력). ④**★열화가 아니라 6월 단일 구간**: 월별 4월 +0.316(230건)·5월 **−0.399**(593건)·**6월 +1.601(503건·승률 71.2%)**·7월 +0.002(1,339건·승률 49.7%), 건수 3분할도 −0.127 / **+1.032** / −0.179. 전체 2,666건 +0.241%p는 **6월 하나가 만들었다**. ⑤**시장 베타 아님**: 6월 KOSPI **−3.55%**·KOSDAQ **−12.75%** 하락장에서 낸 성과(7월은 KOSPI −20.57% 폭락). ★★함의 = [F-26]의 "전 신호엔 엣지가 있고 실행 규칙이 파괴한다"는 3일치 서사에서 **파괴할 엣지가 4·5·7월엔 애초에 없었다.** R-1·R-3·R-4·R-5 기각 결론은 유지되나 그 **전제 문장은 "6월에만 엣지"로 정정**해야 한다. → 신규 [F-86]으로 이월.
- **[F-87]** (8/1) **★★★ 명분↔v2 교집합 0의 진짜 원인 = 추격 상한 8%가 명분 종목을 구조적으로 배제 (사장님 매매 스타일 직결·HIGH)** — 도구 `tools/manual/f87_catalyst_signal_overlap.py` 신설. [F-25]가 "21일 교집합 0건"으로 닫은 것을 재규명. ★**무작위 기대 대조가 결정타**: 명분 30종 × v2 80종 / 2531 = 기대 20건인데 실측 **5건 = 0.25배** → 안 겹치는 게 아니라 **서로 밀어낸다**. lag 0/1/2/3 편차가 4건뿐이라 **시간축 문제 아님**(내 1차 가설 A 기각). ★**배제사유 분해**(482 관측): **추격 상한 8% 초과 243건(38.6%)**·ticks 없음 148(23.5%)·+5% 미도달 121(19.2%)·10시前 창 놓침 72(11.4%)·강도<200 41(6.5%)·**v2 통과 5건(0.8%)**. 명분 종목의 **첫 관측 등락률 중앙값 +4.46%, 첫 관측에서 이미 8% 초과 201/482(41.7%)** = [F-31] 시총순 순회 지연이 배제를 심화. ★★**추격 구간별 성과(명분 종목만)**: 0~8% 46건 +1.160 / **8~15% 88건 +0.956** / 15~25% 71건 −0.097 / 25%+ 84건 −0.749. 상한별 총net = 8% +53.4 / **15% +137.5(최대)** / 25% +130.6 / 무제한 +67.7. **진짜 경계는 8%가 아니라 15%**이고 현행 8%는 건당 최고지만 표본이 말라 총량에서 진다([F-84] '폭' 결론과 동일 방향). ★7/9 지시서의 "추격 상한 8% 강력통과(<8% +0.37 / ≥8% −0.64)"는 **명분 없는 전체 표본** 결론이었다 — 명분 종목 ≥8%는 +0.059로 죽지 않는다. ⚠⚠**실전 적용 불가 조건**: 이 명분 집합은 같은 날 **18:28 산출(look-ahead)**이고 D-1으로는 교집합 1건뿐. → **처방은 "상한을 올려라"가 아니라 "명분을 장중에 알아야 한다"** = 장중 실시간 명분 탐지가 선행조건(6/24 메모 '다음 정밀화=실시간 장중 점화'와 동일 결론에 데이터로 도달). 상한 완화는 매매 룰 변경이므로 **사장님 결정 + shadow 관측 후에만**.
- ~~**[F-86]**~~ (8/1) **★★★ 규명 완료 — 6월은 설명 불가. 사전 선언대로 S-1 폐기가 정직한 결론(사장님 결정 대기)** — 도구 `tools/manual/f86_june_regime_probe.py` 신설(r1 판정 import·로직 복제 0). ★**기각 조건 4개를 실행 前에 고정**하고 사장님 승인 후 진입(레짐 축 3번째 시도라 사후해석 차단이 최우선): ①변수는 D-1 등 사전 관측 가능만 ②후보 목록 실행 전 고정·이후 추가 금지 ③6월 임계를 4·5·7월에 적용해 부호 유지(out-of-sample) ④3분위 단조성. 섹터 집중도는 `sector_history.json`이 7/1~31일치뿐이라 **성적을 보기 前에** 제외(데이터 부재). **결과(59일)**: 조건4 통과 = V2 급등수(+0.933→−0.434→−0.572)·V4 방향(+0.519→+0.510→−0.680) / 조건3 통과 = V1 변동성(대조군 27일 1,514건 **+0.136** vs 대조군 전체 −0.075). ★**두 조건을 동시에 통과한 변수 0개** → **6월 설명 실패**. V1은 6월 13.78로 최고이고 최상위 3분위 +0.534·out-of-sample도 통과하나 **중간 구간 −0.318로 비단조** — 여기서 조건4를 완화하는 것이 정확히 사후해석이므로 **완화하지 않는다**(사전 선언 준수). ★**V5 무효화 검사는 통과**: 6월 usable_pct 100.0%로 타 월과 차이 없음 → 6월 성과는 **데이터 아티팩트가 아니라 진짜인데 설명이 안 되는 것**. 잔여 관찰: 6월 유효일이 **10일**(21 거래일 중)뿐이라 6월 대표성 자체도 제한적. ★★**판정**: 실행 前 선언 "조건 3·4를 모두 통과 못하면 6월은 설명 불가이며 S-1은 8/29 폐기가 정직한 결론"을 그대로 적용한다. 단 **폐기 결정은 사장님 권한**이며 데드라인(8/29)까지 시간이 남아 있다. → 대안 축은 [F-87](명분·추격 15%·장중 실시간 명분 탐지)로 이월 — 7/28 사장님 말씀 *"다른 방법을 나중에 찾아봐야지"*의 그 방법이 오늘 윤곽을 얻었다.
- **[F-86-원안]** (8/1) **6월 규명 — S-1의 유일한 남은 희망 축 (8/29 판정 직결·HIGH)** — [F-83]이 남긴 단 하나의 질문: **6월(503건 +1.601%p·승률 71.2%)에 무엇이 달랐는가.** 시장 방향은 아니다(하락장). 후보: ①변동성 수준 ②섹터 집중(6월 명분 테마) ③상한가/급등 종목 수 ④체결강도 분포 ⑤ticks 해상도(수집 품질 자체가 6월에 좋았을 가능성 — 7/20 사고 前). ★주의: 레짐 축은 이미 **두 번 기각**됐다([F-28] 신호밀도 스피어만 0.145 / 7/10 레짐 게이트는 PB-A에 역효과) → **세 번째 시도이므로 사전에 기각 조건을 정하고 들어갈 것**(사후 해석 금지·7/27 교훈). 6월이 재현 불가능한 우연으로 판명되면 S-1은 8/29에 **폐기가 정직한 결론**이다.
- ~~**[F-83-원안]**~~ (8/1) 실측 장부의 '전 신호'가 백테스트와 부호 반대 — 8/29 판정 전제조건 — [F-26]이 3일간 근거로 삼은 앵커는 **백테스트 58일 2,406건 = 건당 +0.173%p**인데, **실측 페이퍼 장부 12 유효일 821건 = 건당 −0.050%p**로 부호가 반대다(전체가 플러스인 날 5/12일). 모집단이 달라 직접 비교는 불가하나(①표본 기간 ②러너 커버리지[F-60] ③CB 필터[F-57]), **"신호엔 엣지가 있고 실행 규칙이 그것을 파괴한다"는 전제가 실측에서 재현되지 않았다.** 이 전제 위에 R-1·R-3·R-4·R-5 기각과 "엣지는 폭에 있다" 결론이 서 있으므로 **8/29 판정 전에 갭 규명 필수**. 우선 확인 순서: 백테스트 도구를 장부와 **같은 12일**로 잘라 재실행 → 여전히 +면 러너 커버리지, −면 표본효과 (HIGH).
- **[F-84]** (8/1) **선택 규칙(선착순)의 실제 비용은 −4.63%p = 전체 손실의 8.3%뿐** — 같은 날 안에서 (그날 전체 평균)과 (상한 5건 평균)을 비교해 날 효과를 상쇄시킨 분해 결과: 선택 리프트 평균 **−0.077%p/건**, 음(-)인 날 **5/12일로 거의 반반**. 반사실(선택 중립) 순누적 **−51.12%p** vs 실제 −55.75%p. **7/23은 선택 −3.177%p(전체 +1.183인데 앞5는 −1.994)로 최악이지만 7/22는 선택 +3.038%p로 살렸다** — 일관된 역선택이 아니라 분산에 가깝다. ★함의: **[F-21] 상한 구조·[F-26] 선택 규칙을 완벽히 고쳐도 개선 여지는 8%뿐이고, 손실의 92%는 '그날 후보 전체가 마이너스'에서 나온다** → 자원 배분을 '고르는 순서'에서 **'후보에 무엇을 넣는가(종목 품질·명분)'**로 옮겨야 한다 (HIGH·전략 방향).
- **[F-85]** (8/1) **부실 배제 필터 — 데이터 차단(린치 p.180 적용 불가)** — 린치가 꼽은 4대 숫자(수익·매출·부채·배당)를 우리는 **보유하지 않는다**. `dart_chunks/`는 공시 **제목만**(date/code/nm/rcept)이고 최신이 **2026-06-10로 7주 stale**. `nxt_eligible_builder.py:64`가 이미 *"관리종목 마커 — 별도 데이터 필요"*로 결손을 명시. KIS 종목정보 API(`search-stock-info`)는 저장소에 구현 0건. → 선행조건 = **관리종목·투자유의·자본잠식 플래그 수집기 신설**(read-only·매매 무접촉). 신규 상주 수집기이므로 **사장님 승인 사안**([F-84]의 92% 축에 직결되어 우선순위 높음).

### 8/1 신규 등재 — F-81~F-82

- ~~**[F-81]** (8/1) **채널 전수 계수 도구 승격**~~ — ✅ **8/5 완료** (사장님 승인 후 당일 소진). `tools/manual/verify_channels.py` 신설(read-only·매매 심볼 0·CSV 쓰기 0). ★**소진 동기가 곧 처방**: 8/5에 또 scratchpad로 새로 짰고 **새로 짰기 때문에** ticks price를 index 2로 하드코딩해 유효율을 64.84%로 오산했다([F-82] 재발). 그래서 설계에 사고를 박아 넣었다 — ①**컬럼은 이름으로만**(`col(header,"price")`·인덱스 하드코딩 금지를 시그니처로 강제) ②`norm_date()`가 날짜 표기 3종+BOM 흡수([F-82] 처방) ③**자체 계수와 정본 `ticks_health`를 나란히 출력하고 불일치 시 "정본을 신뢰할 것" 경고**(오늘 상황 재발 시 도구가 스스로 말한다) ④**전체 분모·활성 분모 양쪽** 커버리지([F-88] 처방) ⑤유니버스·틱판정은 정본 import(복제 0·[F-37] 교훈) ⑥`--selftest`로 업무일지 앵커 재현. **자기검증 9/9 PASS**(7/31·8/3·8/4 × ticks행·ticks%·daily_ok — 8/1 기록 2,492/47,978까지 재현). 부수 성과=활성 분모로 보면 수급·공매도가 99.8%가 아니라 **100.00%**임이 드러남.
- ~~**[F-89]** (8/5) **★08:30 무인점검 텔레그램 재시도 부재**~~ — ✅ **8/5 당일 완료**(사장님 승인). fix=①**재시도 3회+백오프(2s·5s)** — 8/3 원인 `RemoteDisconnected`는 재시도로 넘어가는 종류다 ②`send_report` 예외까지 흡수(통로가 던져도 죽지 않고 재시도) ③**미발송 누적 병기** — 다음 성공 발송에 "지난 N회 미수신(날짜)" 첨부. ★**상태 파일을 만들지 않고 자기 로그에서 역산**한 것이 설계 핵심 — 이 도구의 불변식이 '상태 파일 무접촉'이라 새 파일을 두면 불변식이 깨지고 desync·손상·원자성 통로가 새로 생긴다. 마커(`MARK_SENT`/`MARK_FAILED`)를 상수로 단일진실화(쓰는 문자열과 읽는 문자열이 갈리면 카운트가 조용히 0이 됨). ★함정 2개를 테스트로 고정: **자기참조 오염**(병기문이 다음 실행에 실패로 잡히면 영구 누적) · **재시도 중간 로그 오집계**(중간 로그 문구를 최종 실패 마커와 분리). 검수=단위/회귀 **22/22 PASS**(합성 7종 + **실제 VPS 로그로 8/3 시점 재현 → 미발송 1건 정확 검출** + 재시도 6종 mock) · VPS 실측 `--dry-run` 08:30 출력과 동일·미발송 누적 0 정확. 아래 원문 보존:
- **[F-89-원안]** (8/5) **★08:30 무인점검 텔레그램 재시도 부재 — 첫 평일에 사장님 미수신** — 7/31 배선한 cron의 **첫 평일 실행(8/3)이 발송 실패**했다(`Connection aborted. RemoteDisconnected` — 본문은 정상 생성·A1~A6 전부 ✅였음). 발송 전수 = 7/31 수동 ✅ / **8/3 🚨** / 8/4 ✅ / 8/5 ✅ = **3회 중 1회(33%) 실패**. `daily_ops_check.py`에 재시도 로직 **0건**(grep `retry|재시도|for _ in range` 무출력). 실패는 로그에 정직히 남았으나 **그 로그를 읽을 세션이 8/3에 없었다** — [F-29] 무인화의 전제("세션이 없어도 이상이 잡히게")가 **마지막 1마일에서 정확히 깨진 것**이고, 7/14 교훈("검증≠보고·자동화 마지막 1마일은 사람에 닿는 알림")과 **같은 자리 재발**. 처방=지수 백오프 3회 재시도 + 최종 실패 시 **다음 성공 발송에 "지난 N회 미발송" 병기**(실패 사실 자체가 사람에게 닿아야 함). 무인 알림 동작 변경이라 사장님 확인 후 착수 (MED).
- ~~**[F-82]**~~ ✅ **소진 8/7** (`utils/dated_csv.py` 정본 신설·판독기 3벌 위임·53/53 PASS) · 원 진단 보존: (8/1) **★날짜 표기 3종 난립 → 판독기마다 오판 재발** — 채널별로 `YYYY-MM-DD`(daily·flow·ranking_snapshots) / `YYYYMMDD`(flow_market) / **BOM(`﻿`) 선두**(flow_market·ranking_snapshots) 가 섞여 있다. **8/1 실증: 단타봇이 새로 쓴 실측 스크립트가 이 둘을 못 읽어 ④ 시장11주체·⑧ 순위 스냅샷을 "최신 없음"으로 오판**했다(실제는 정상 — 검증기가 맞고 단타봇이 틀림). 허위 경보를 사장님께 올릴 뻔한 건이고, 앞으로 판독기를 쓸 때마다 같은 자리에서 재발한다. 공용 `read_dated_csv()`(utf-8-sig + 날짜 정규화) 정본화 (MED·근본). ★**8/5 재발(같은 뿌리·다른 표면)**: ticks 판독에서 price를 index 2(`change`)로 읽어 유효율을 **64.84%**로 오산(정본 `ticks_health`는 100.0%). 8/1은 날짜 포맷, 8/5는 컬럼 인덱스 — 공통 원인은 **정본 판독기를 쓰지 않고 매번 새로 파싱**하는 것. 이번엔 보고 前 정본 대조로 걸렀으나(방어선 전진) 재발 자체가 [F-81] 승격이 지연될수록 반복됨을 실증. **처방은 `read_dated_csv()`만이 아니라 [F-81] 도구 1본화와 묶어야 한다.**  
  ★**8/7 3번째 재발이 소진 동기** — 단타봇이 `_csv_has_date(p,"2026-08-07")`로 `flow_market`을 조회해 8/6·8/7을 **둘 다 결손**으로 셌다(실제 정상). ★실측으로 드러난 진짜 모양은 "중복"이 아니라 **반대 방향 3벌**: `_kis_last_date`=ISO / `verify_channels.norm_date`=compact / `_get_last_csv_date`=무변환 → 바꿔 끼우면 비교가 조용히 뒤집힌다. fix=`utils/dated_csv.py`(`iso()`/`compact()`로 **방향을 이름에 박음** — `ticks/20260807` 경로 소비자가 실재해 단일형태 강제는 불가). ★판정 불변 2중 확인: 함수 전수 대조(37,986건 中 검증기가 보는 5채널 **불일치 0**·차이는 구코드가 틀리던 `flow_market` 8건뿐) + Tier3 실경로(`verify_all()` 신·구 출력 **바이트 동일**).

### 8/5 신규 등재 — F-88

- ~~**[F-88]**~~ ✅ **소진 8/6** (05c2a87) · ★8/7 장부 정합 — `data_verifier._load_active_codes` docstring 정정 확인. 원 진단 보존: (8/5) **★거래정지 판별이 daily 단일 채널에만 성립 — 검증기 docstring 전제가 사실과 다름** — `data_verifier._load_active_codes` docstring은 *"상폐·거래정지 종목은 daily·investor·short 3채널이 동반 정지"*라 적고 있으나 실측은 **8/4 daily 결손 41종 중 37종(90%)이 daily만 정지하고 investor·short는 계속 기록**한다(011000: daily **6/24** 정지 → investor·short **8/4**까지 = **6주 격차** / 299900 4주 / 012510·057050 3주 / 222160·203690만 동반 정지). ★영향(현재)=낮음 — 활성 분모로 보면 수급 커버리지는 오히려 더 좋다(2,527 중 37이 정지분 → 활성 2,490/2,491 ≈ 99.96%)·매매/판정 경로는 ticks 기반이라 무영향. ★영향(구조)=수급·공매도의 "99.8%"가 **정지 종목의 동결 행을 성공으로 집계**하므로 정지 누적 시 분모가 조용히 오염되고 **"수급은 도는데 종가만 죽은 종목"이라는 진짜 장애와 구분 불가**가 된다(8/5 실증: 5종을 "수집 실패 의심"으로 출력했다가 값을 열어보고서야 정지로 확정 — 하마터면 허위 경보). 처방=①docstring 정정(정지 판별=daily 단독) ②수급 커버리지를 **활성 분모**로 재정의 (MED).

### ★★★ 8/5 전체검수(6관점: Tier1 소급 + 실매매·룰준수·판정·데이터·슬러지) — F-90~F-108

> 사장님 지시 "전체 검수 진행해라. 코드·로직·버그·슬러지 등등". 에이전트 6개 병렬 후
> **전건 단타봇 직접 재검증**(7/31 오탐 8건 전건 기각 교훈). 오탐 2건 기각·1건 정정은 §하단.
> **[사장님]** = 매매 동작이 실제로 바뀌므로 단타봇 자율 권한 밖.

**A. 사장님 영구 룰 위반 (재가동 전 필수)**
- ~~**[F-90]** (8/5) **★★★[사장님] 트레일링이 고점 −1.5%로 조여진다 — 룰 3 정면 위반 (HIGH)**~~ — ✅ **8/10 완결** (8/5 ALIVE 1분기 + **8/10 잔여 2분기**). 8/7에 *"2분기 잔존"*으로 닫지 않고 열어둔 것이 옳았고, 그 2분기의 정체가 오늘 확정됐다: **`:651` BREAKING**(`trailing_sl = nearest_res` = 돌파한 저항선 그대로 → 돌파 직후엔 고점 바로 아래라 **고점 −1.5%**까지 조여짐)·**`:689` BOUNCING**(`nearest_sup * 0.995` → 실측 **고점 −2.49%**). 둘 다 ALIVE와 **같은 ratchet 경로**로 실매도까지 간다(`auto_trader:4136`이 조이는 값만 채택 → `:4496 effective_sl`). fix=4분기(BREAKING·BOUNCING·ALIVE·WEAKENING) **전부 `SAJANG.get_trailing_sl(today_high)`로 통일** — 룰 문구가 *"고점 −3% **일관**"*이므로 분기별 변형을 두지 않았다(★느슨한 쪽도 위반이다: "−3%에서 자른다"인데 −10%까지 버티는 것). S/R 정보는 사유 문자열에 보존. ★**부수 정정**: `bot/trading_cto.py` `DEFAULT_PARAMS`의 `alive_trailing_sl 0.985`·`bouncing_sl 0.995`·`risk.trailing_pct 2.0`·`sl_pct 3.5`가 **폐기된 값 그대로** 남아 있었다 — 4중 확인 결과 매매 판정 소비 **0건**(`get_param`/`get_params` 호출처 0·`cto_params.json` 부재)이고 `:351-355` **텔레그램 "주요 파라미터" 리포트 출력에만** 쓰인다. 매매는 무해하나 **사장님이 보는 화면이 폐기된 룰을 말하면 룰이 두 개인 것과 같다**([F-109] 동형) → SAJANG 파생으로 교체. 회귀 `tests/test_intraday_eye_trailing_8_10.py` **6/6**(4분기 도달 확인 + 전분기 일치 + 조임 금지 + 하드코딩 배수 부재 + CTO 리포트 + ★**음성 대조**: 현재 소스에 역치환을 가해 합성 구코드를 만들어 **BREAKING 9,850(−1.50%)·BOUNCING 9,751(−2.49%) 위반을 실제 검출** — 기준을 HEAD에 묶지 않았다). 원 진단 보존: `data/intraday_eye.py:729`
- **[F-90-원안]** (8/5) `data/intraday_eye.py:729` ALIVE 분기가 `trailing_sl = int(today_high * 0.985)` **하드코딩**. 같은 함수 20줄 아래 WEAKENING(:752)은 `SAJANG.get_trailing_sl()` 정상 사용·BOUNCING(:689)은 지지선 −0.5% = **3개 분기 중 1개만 전환**. ★소비 경로 전수 확인: `auto_trader:4136` ratchet `if new_sl > pos["trailing_sl"]` → **고점×0.985 > 고점×0.97이라 −1.5%가 항상 이긴다** → `auto_trader:4496 effective_sl = max(stop_loss, trailing_sl)`가 실매도 트리거. 즉 **ALIVE(가장 잘 가는 종목) 판정 순간 트레일링 폭이 룰의 절반으로 영구 축소**. 5분 주기 `_run_intraday_eye`가 source 필터 없이 전 보유종목 적용(asset_pool 포함). "5~10% 먹고 나온다"와 정반대 방향.  
  ★
- ~~**[F-91]** (8/5) **★★[사장님] hard_kill(−5% 최후 방어선)이 66일째 dry_run (HIGH)**~~ — ✅ **8/10 완료** (71일째 이행). `hard_kill_dry_run: true → **false**` + `hard_kill_pct: 0.05` **제거**(→ `SAJANG.HARD_KILL_PCT` 위임 — `position_safety.py:601`이 키 부재 시 SAJANG 파생을 쓴다. 값 동일 0.05·동작 무변경이나 **사장님이 룰을 고치면 매매가 실제로 바뀌게** 하는 것이 요지 = 5/26 사고 구조 차단). ★**켜기 전 안전 검증을 먼저 했다**: 매도 경로 `hard_kill_check → trader.sell_market → _execute_sell:1069 _order_gate("SELL"…) → evaluate_auto_order:119`가 **PAPER_ONLY를 BUY/SELL 구분보다 앞에서** 검사 → 실호출로 `{"reason": "PAPER_ONLY"}` 차단 확인. 즉 **지금 실주문은 여전히 0이고, 이 변경은 "재가동 시 방어선이 실제로 작동한다"를 켠 것**이다. 회귀 `tests/test_hard_kill_live_8_10.py` **6/6** — dry_run 해제·임계 SAJANG 위임·dry_run 결합 규칙(호출부 OR config)·★**PAPER_ONLY가 SELL·BUY 모두 차단**(이게 깨지면 이 fix가 사고가 되므로 같이 잠갔다)·매도 sink의 `_order_gate` 경유·★**보호 종목 제외 전건**(빈 dict·`sl_disabled`·`verification`·`manual_sync*`·`sync_auto*` 5종 + 비보호 3종 — `or` 체인으로 쓰면 하나만 살아도 통과하므로 마커마다 개별 고정). 원 진단 보존: `config.yaml:285 hard_kill_dry_run: true`인데 **같은 줄 주석이 "5/26 실전: false"** = 값이 자기 주석과 모순. VPS 원본 동일. `position_safety.py:602`가 이 값을 OR로 받아 매도 없이 로그만. 7/31에 `HARD_KILL_PCT`를 SAJANG에 올렸으나 **실행 스위치가 off라 레지스트리 등재가 무의미**. 5/21 로킷헬스케어(−15.4%) 방어선이 재가동 즉시 부재.
- **[F-92]** (8/5) **[사장님] 30% 현금 룰의 두 번째 default off — 7/31 HIGH-1이 절반만 고쳤다 (HIGH)** — `auto_trader:190-194` `dynamic_qty` default **False** → 1주 모드 early return. 7/31은 같은 함수 8줄 아래 `budget_mode`만 SAJANG화(:202). 09:15 asset_pool 경로에서 30% 검증이 사는 곳은 `_calc_split_cash_qty`(:302)뿐인데 **거기 도달 전에 return**되고, asset_pool은 `safe_buy`(30% 검사 보유)를 안 거치고 `chase_buy`로 직행(:2689) → 룰 8·9가 경고 없이 동시 무력화.
- **[F-93]** (8/5) **[사장님] 매수가 −3% SL이 매일 15:10에 엔진 값으로 덮어써진다 (HIGH)** — `auto_trader:4797 pos["stop_loss"] = target_state.dynamic_sl`. 6/18 fix는 **TP 누수만** 막고(다음 줄 `take_profit = SAJANG.get_take_profit`) SL은 방치. `dynamic_target.py:127-141` `sl_max_pct` 레짐별 **0.05~0.08** → NORMAL 최대 −5%·PANIC −8%. [F-91]로 hard_kill도 꺼져 있어 **실질 손절 하한이 −8%까지 확장**.
- **[F-94]** (8/5) **[사장님] JARVIS가 −5%에서 손절 대신 추매를 먼저 평가 (MED)** — `jarvis_decision.py:229-232` `vwap_pos=="LOWER" and pnl<=-5 → ACTION_RECOVERY_ADD`가 **return**이라 그 아래 `:239` SAJANG SL 검사에 도달 못 함. "돈 잃지마라"에 역행. `config.yaml:293 dry_run: true`가 유일 방벽.
- **[F-95]** (8/5) **[사장님] 룰 D 종목이 매수 20분 뒤 당일 청산될 수 있다 (MED)** — `job_daily_reeval`(15:10)에 `pending_next_day` 가드 **0건**(`job_eod_close:5167`엔 존재). 룰D는 `source='pre_close_d'`라 `source=="asset_pool"` 스킵(:4867)에도 안 걸림 → REVERSAL 분기(:4823 `cp<=entry → FULL_SELL`)에 걸리면 당일 청산 → 룰 D 전제(다음날 갭업)·룰 C(D+1 보호망) 무의미.

**B. 실매매 안전 (재가동 전·자율 fix 가능)**
- ~~**[F-96]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `kis_trader` rt_cd 검사 확인. 원 진단 보존: (8/5) **★`fetch_open_orders`/`fetch_balance`가 rt_cd를 안 본다 — 조회 실패를 "성공+빈결과"로 (HIGH)** — `kis_trader.py:878` `if resp is None: return {"success": True, "orders": []}`·`:884 for item in resp.get("output", [])` rt_cd 검사 0. 같은 파일 **다른 조회 11곳은 전부 `rt_cd != "0"` 검사**(691·762·832·1175·1311·1424·2168·2365·2434·2639·2649) = 설계가 아니라 **누락**. KIS는 레이트리밋을 HTTP200+`rt_cd:"1"`로 준다. 4갈래 파급: ①`_wait_for_fill:1529` 첫 폴링에서 `pending=[]` → "체결 완료" 오판 → **지정가 미취소 방치** ②`position_safety:417` hard_kill·룰4·룰B·룰C가 `positions=[]`로 루프 0회 = **최후 방어선 무증상 소멸** ③`auto_trader:2180` `_real_holdings_codes=set()` → **보유 종목 재매수** ④`filled_qty=0` 확정(F-98 상위 원인). 처방=2줄 추가(파일 내부 일관성 복원·룰 무변경).
- **[F-97]** (8/5) **★09:15 실전 매수 경로의 이중 매수 — F-65와 다른 site 3곳 (HIGH)** — `vwap_split_buy.py:186-201`(사장님 5/26 3분할 1차): `chase_buy` 실패 시 `buy_market` 폴백인데, `kis_trader:1381`이 `cancel_order` **반환값을 버린다** → 마지막 폴링 직후 체결되면 취소 실패인데 `success:False` 반환 → **2×qty 매수**. `split_cash`에서 종목당 수백만 원 = 30% 룰 관통. 더 나쁜 2곳(취소 시도조차 없음): `kis_trader:1183`(limit buy 예외 → 즉시 `buy_market`)·`:1432`(매도판).
- **[F-98]** (8/5) **매도 성공 판정이 체결수량을 안 봐 포지션이 장부에서 증발 (MED-HIGH)** — `auto_trader:4519-4553` SL/트레일링 실매도가 `success`만 보고 `_positions.pop()`. `smart_sell`은 `filled_qty`를 담아 주는데 소비 0. 부분체결/F-96 상황에서 **실제 보유 중인데 장부에서 사라져** 트레일링·SL 대상 밖이 되고, `position_safety:437`은 메모리 미등록을 "사장님 보호"로 판정해 스킵 → **3계층 전부 불능**. F-67이 사장님 결정을 기다리는 상태를 **봇이 스스로 만드는 경로**. 동일 패턴 `:4987`·`:5196`·`:3830`.
- **[F-99]** (8/5) **휴장일·장중 시간 가드 잔여 2건 (MED)** — ①`_job_intraday_verification_scan`(coo:5267 `run_repeating` 5분): 체인 전체에 `is_trading_day` **0건**(`auto_trader:1884`·`intraday_scanner.py:58` 시간창만) → 휴장일에 KIS 박제 랭킹으로 `buy_market(code,1)` 최대 10종. 현재 `verification_mode._DEFAULT_END=2026-05-30`으로 휴면. ②`job_limit_up_split_check`(coo:5293 `interval=120`): 휴장일 가드는 있으나 **장중 시간 가드 없음** → 거래일 24시간 720회 발화, 06:00·21:00에도 `pnl>=25`면 `sell_market` 시도. 형제 `position_safety:415`는 `in_market` 시각까지 검사 = **표준 불일치**.

**C. 검증 계층 (자율 fix)**
- ~~**[F-100]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `_evening(ref)` 과거일 유예 없음 확인. 원 진단 보존: (8/5) **★`_evening()`이 벽시계라 08:30 아침점검에서 공매도·순위·시장11주체가 구조적 FAIL 불가 (HIGH)** — `data_verifier.py:309 return datetime.now().hour >= 19`가 **기준일(self.today)과 무관**. :368·:402·:417이 `if not _evening(): return PASS`. `daily_ops_check:166`은 매 평일 **08:30**에 소급(ref=전일) 검증을 돌리므로 세 채널이 3일 멈춰 있어도 전부 PASS → `notify_data_freshness`의 `core_ok`가 자동 True → **"✅ 핵심 채널 모두 정상"**. ★8/5 실증: 오전 09:49에 돌린 `--date 8/3·8/4`의 "✅ 공매도"는 실제 검증이 아니라 **유예**였다(전수 재계수 2,527/2,531로 결론은 독립 확인됨 — 초록불을 안 믿은 것이 방어선이 됐다). 처방=`self.today < date.today()`(과거일 판정)면 무조건 evening 취급.
- ~~**[F-101]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `TICKS_FILE_COV_BROKEN/TRUNC` 판정 배선 확인. 원 진단 보존: (8/5) **★ticks 파일 96% 결손도 verdict OK (HIGH)** — `playbook_shadow.py:85 TICKS_MIN_FILES=100`인데 유니버스 **2,531**(정상일 2,527) = 임계가 **3.95%**. 표본을 **존재하는 파일에서만** 뽑으므로(`:102-103`) 500종목만 수집돼도 40개 표본 전부 usable → `pct=100·OK`. 7/20에 막은 건 "행은 있고 값이 0"이었고 **파일 자체 부재 축은 무탐지**. 판정 원천(⑲/⑲-3/S-1 표본) 직결. 처방=`len(files)/유니버스` 커버리지 임계 추가(수집기 `_api_fail` 요약이 이미 계산돼 있는데 소비자 0).
- **[F-102]** (8/5) **수집기가 자기 실패율을 계산해 두는데 판정에 쓰는 계층이 0 (MED)** — ①`flow_collector.py:259-301` `_fetch_investor_api`가 rt_cd 거부 시 None인데 호출부에 else 없음 → `failed`는 **예외에만** 증가 = 전 종목 거부돼도 "실패0" ②`collect_all.py:241` 마커에 `total_codes` 미전달 → `flow/_last_update.json`이 매일 `coverage_pct: 0.0` 박제 = [F-49] 처방(정직한 지표를 검증기가 읽게)이 커버리지 축에선 무효 ③`run_nightly_pipeline:119` ⑬⑭를 `python -c`로 호출 후 반환값 폐기 → 2,531종목 전부 실패해도 exit 0 → nightly 30/30 유지.
- **[F-103]** (8/5) **실패 경로 페이싱 소실 — 7/20 tick_collector fix 미이식 (MED)** — `flow_collector.py:694-702`(KIS 공매도/신용)·`:573-576`(네이버)가 실패 시 `continue`로 **sleep을 건너뛴다**. `tick_collector.py:260`이 명시적으로 고친 결함("장애 중 요청 속도가 오히려 ~3배로 뛰어 레이트리밋 자기증폭")과 동일 패턴이 2,531종목×최장 스텝(timeout 5400s)에 잔존.
- **[F-104]** (8/5) **short/credit 스키마 드리프트 시 누적 히스토리 무경고 폐기 (MED)** — `flow_collector.py:703-715`가 컬럼 집합 불일치 또는 읽기 예외면 `df = df_new` = **과거 전부 버림**. daily fill 정본(`keep='first'`·과거 100% 보존)과 정반대. 로그 0줄이고 검증기는 마지막 행만 보므로 오히려 PASS.
- **[F-105]** (8/5) **A0 백필 검증이 존재하지 않는 키를 본다 → 영구 "정상" (MED)** — `verifiers/data_integrity.py:120-140`(봇 매시간 실행)이 `st.get("backfill_remaining")` 등 3키를 보는데, `save_morning_state`는 모든 키를 `state["data"][key]` **한 단계 아래**에 쓰고 `a0_result`는 애초에 state에 안 들어간다(`trading_coo:594`). 주석이 스스로 *"형식은 G1 코드에 따라 다름"*이라 추측을 인정 = **확인 안 된 키의 부재를 정상 증거로 채택**.

**D. 판정 파이프라인**
- ~~**[F-106]** (8/5) **★S-6 판정기가 '측정 불가'를 '미달'로 변환 — 비관 편향 (HIGH·D-2)**~~ — ✅ **8/5 당일 fix**. `ok = mono_fwd and (lift or 0) > 0 and ...`이 산출 불가(None·버킷<3)를 전부 False로 접어 **항상 폐기 쪽으로 기울었다**. S-1 경로엔 `MIN_JUDGE_N` 가드가 있는데(`observe_v2_paper:358`·`strategy_deadline_check:78`) S-6만 없었다. fix=①판정 전 blocker 확정(표본<MIN_JUDGE_N·버킷<3·리프트 None·초과수익 0건·장부 stale) ②blocker 있으면 **"⏸ 판정 불가 — 측정 불가는 미달이 아니다"** 출력 + **on_fail 상신 안 함** ③`MIN_JUDGE_N`은 정본 import ④`STATUS_ORDER`에 누락됐던 `COLD` 추가 ⑤장부 신선도 검사(로컬 미러 오판 차단). ★**VPS 원본 168건 실측 판정**: forward_d1/d3/d5 **3개 지평 전부 미달**(표본 144~155건·유효버킷 3개 = 판정 가능 상태). d5는 **역단조**(HOT −9.78 / RELAY −6.69 / WARMING −4.50 = 강할수록 나쁨)·선정 리프트 3개 전부 음수. **정직성 검정=아래 [F-107] 의혹 지표(③)를 통째로 빼도 ①②만으로 미달이라 결론 불변.**
- **[F-107]** (8/5) **S-6 ③번이 겹치지 않는 두 구간을 뺀다 (MED)** — `s6_..._judge.py:101` `x[H] - x["market_return"]`인데 `market_return`은 `sector_reversal_shadow.py:110` **D-1→D0 1일**이고 `x[H]`는 `:200` **D0→D+N**. 시점·길이 둘 다 불일치 = 벤치마크 차감이 아니라 무관한 상수 차감. 신호는 섹터 강한 날 나오므로 `market_return` 양수 비중이 커 **비관 편향**(raw −3.06 → excess −6.27). 현재 결론엔 영향 없으나(위 정직성 검정) **지표 자체는 폐기 또는 재정의 필요**.
- ~~**[F-108]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `strategy_deadline_check` alerts 승격 확인. 원 진단 보존: (8/5) **판정 증거 손상이 alerts로 승격되지 않는다 (HIGH)** — `strategy_deadline_check.py:124` 표본 부족 시 *"판정 유예(장부 손상/수집 장애 확인 필요)"* 문구를 **만들어 놓고 `lines`에만 넣고 `alerts`엔 안 넣는다**(대장 로드 실패 `:139`는 alerts.append 함 = 처리 비대칭). → `:184 if args.notify and alerts:`라 ⑲-4 텔레그램 0건 → `daily_ops_check:341 check_a9_deadlines`는 `score`를 **한 번도 안 보고** `alerts`만 봄 → 아침 점검 **초록불**. ★**8/7에 S-6가 정리되면 8/8~8/25 구간은 alerts=0** → 그 18일간 장부가 깨져도 텔레그램 무음+초록불.

**E. 문서·슬러지·인프라**
- ~~**[F-109]** (8/5) **★★CLAUDE.md가 5/27 폐기된 값을 "영구 룰"로 선언 (HIGH)**~~ — ✅ **8/5 당일 fix**. `CLAUDE.md:110` 룰 10이 `SYNC_AUTO_SOURCE = 'manual_president'`였는데 정본 `sajang_rules.py:108`은 `sync_auto_unknown`이고, 코드 주석이 *"5/27 08:15 사장님 분노 fix — 추정 라벨 = 단타봇 매수를 사장님 책임으로 떠넘김 잘못"*이라 명시. **CLAUDE.md는 매 세션 자동 로드**되므로 70일간 모든 세션이 폐기된 룰을 먼저 읽었다. ★교훈=**단일진실을 코드에 두는 것만으로 부족하다 — 룰 문서와 레지스트리의 값 일치를 기계가 검사해야 한다**(자가감사 확장 후보).
- ~~**[F-110]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — CLAUDE.md 실제 구현 규칙 표기 확인. 원 진단 보존: (8/5) **RULE-004는 구현 0건인데 백로그가 "해소"로 닫혀 있다 (MED)** — `tools/pre_commit_check.py` 실제 구현 = RULE-001/002/003/005/006/007/008. **004 grep 0건**. 그런데 §4 [P0-4]가 "RULE-002/004 신설"로 **취소선 완료** 처리됐고 본문은 002 작업만 서술. CLAUDE.md:64·166의 "RULE-001~004 자동 차단"은 **양방향 오류**(004 없음·실제 차단하는 005~007은 범위 밖). 7/31 교훈("문서가 자동차단한다고 말하는 걸 믿지 말고 구현을 확인")이 **같은 항목에서 절반만 적용**됐다.
- ~~**[F-111]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `asset_pool_loader` 죽은 BASE_DIR 폴백 제거 확인. 원 진단 보존: (8/5) **7/31 "undefined name 잔여 0건" 선언이 부정확 (MED)** — `utils/asset_pool_loader.py:467 undefined name 'BASE_DIR'` 잔존(도입 5/20 `6408e3b`·7/30 이후 변경 0 = **7/31 선언 시점에 확실히 존재**). 런타임은 `'BASE_DIR' in globals()` 단락으로 안전하나 ①기록이 틀렸고 ②**이 파일을 커밋하면 RULE-002가 HIGH로 차단**(오탐 지뢰) ③그 폴백 자체가 영구 사문(BASE_DIR 정의 0건 → 항상 False) → 테마 유니버스가 VPS 경로 1개에만 의존.
- **[F-112]** (8/5) **`AGENTS.md` 전체가 5/25 스냅샷 — Codex 세션 자동 로드 문서 (MED)** — 자체 모순(`:25` 4-Tier vs `:151` 3-Tier)·`:122` "5/26 D-Day 가동 흐름"에 CLAUDE.md가 붙인 **"현재 봇 OFF·실주문 0" 안전 단서가 없다** → Codex 세션은 봇 가동 중으로 읽는다. 7월 이후 체계(DAILY_ROUTINE·데드라인 대장·데이터 메인=VPS) 언급 0회. [F-109]·[F-110] 오류도 그대로 복제.
- ~~**[F-113]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `STRATEGY_DEADLINES.md` S-6 등재 확인. 원 진단 보존: (8/5) **`STRATEGY_DEADLINES.md`에 S-6가 없다 — 오늘 기준 D-2 (MED)** — JSON 활성은 S-1(8/29)·S-6(8/7) 2건인데 문서 §1은 S-1~S-5를 활성으로 표시하고 **S-6 한 글자도 없다**. `DAILY_ROUTINE:29` A9가 이 문서를 가리키므로 문서만 읽는 아침 루틴은 8/7 도래를 놓친다(기계 경로는 정상). §3 "판정 기록 누적"도 7/17 이후 정지(JSON엔 5건) = 문서 자신의 운영 룰 3 위반.
- ~~**[F-114]**~~ ✅ **소진 8/5** (70148cb) · ★8/7 장부 정합 — `trading_calendar.COVERED_YEARS` 확인. 원 진단 보존: (8/5) **2027 공휴일 0건 — 발생일이 확정된 재발 사고 (LOW→MED)** — `trading_calendar.py:69 ALL_HOLIDAYS = 2025 | 2026`, 연도 가드 없음 → **2027-01-01부터 조용히 "주말만 휴장"으로 퇴화**. 7/17 제헌절 유령신호와 **동일 뿌리**.
- **[F-115]** (8/5) **디스크·보존정책 3건 (LOW)** — ①`data_store/ticks/` **172,093 파일**(73일·일 2,527 증가)에 보존정책 0건, `sync_from_vps:109`가 매일 노트북으로 전송까지 함 — 용량(77MB)이 아니라 **파일 개수**가 rsync/스캔/백업을 먼저 터뜨린다 ②`bot_logger.py:84`가 `*_bot.log`만 glob → 실측 커버리지 **0.35%**(39/175M), `bot.log.*` 81개가 `backupCount=30`을 초과 잔존 ③로컬 `data_store/` **1.5GB 사문**(equal_levels·premium_levels·5min·1min·processed·raw 등 9개 전부 `DATA_DIRS` 미등재 = VPS 미러 아닌 로컬 잔재·3~6월 정지).
- **[F-116]** (8/5) **`_get_kis_session()` 3벌이 이미 갈라졌다 (MED)** — `flow_collector.py:60`(재시도 3·stale token 삭제 O) / `market_flow_collector.py:177`(3·O+sleep+chdir) / `extend_parquet_data.py:56`(**2·X**). 같은 이름·시그니처·로그 문구인데 토큰 복구 동작이 다름 = **복제 위험이 아니라 이미 발생한 divergence**. 인접: `mojito.KoreaInvestment(...)` 직접 생성 10곳이 정본 `KISTrader`의 토큰 갱신·에러판별을 우회.
- **[F-117]** (8/5) **고아 문서가 KRX 복구를 유도한다 (MED)** — `docs/ETF_LEADER_CRON_SETUP.md`가 `tools/refresh_etf_leaders.py` cron 등록을 지시하는데 **그 파일은 7/1 a1560fe에서 삭제**됐고 F-40이 VPS cron도 제거했다. 참조 문서 0건 = 완전 고아. **다음 운영자가 이 안내서대로 KRX 의존 잡을 되살릴 위험** → 삭제 또는 폐기 명시.
- **[F-118]** (8/5) **`DEAD_KIWOOM.md:45` 보류 7개 경로 전부 스테일 (LOW)** — 7개 전부 이미 `tools/manual/`로 이동 완료인데 문단이 7/1 격리 이전 상태로 동결. `codex_collab.py`가 "훅 참조"라는 서술도 오기(훅 실제 참조 3개에 없음).
- ~~**[F-119]** (8/5) **테스트 러너 부재 — 4-Tier Tier3를 일괄 증명할 수단이 없다 (MED)**~~ — ✅ **8/10 완료** (`tools/run_all_tests.py` 신설·[F-147] 동시 소진). 원 진단: pytest 미설치·`pytest.ini`/`conftest.py` 0건이라 순수 pytest 파일은 실행 수단 자체가 없었고, 자체 러너들은 손목록이라 누락이 초록불에 가려졌다(7/20 동형). ★**설계 계약 4개가 전부 과거 사고에서 나왔다**: ①분모=**발견 개수**(실행 개수 아님·[F-124-2]) ②**못 돌리는 파일은 SKIP이 아니라 FAIL**(조용한 skip이 [F-147]을 만들었다 — 미구현 fixture를 만나면 그 자리에서 예외) ③파일마다 subprocess 격리 ④하나라도 실패면 exit 1. pytest를 설치하지 않고 `tmp_path`/`monkeypatch`만 최소 구현(저장소가 실제 쓰는 전부)했고, **모르는 fixture는 시끄럽게 터지게** 뒀다. ★**첫 전체 실행이 즉시 일을 했다 — 40개 중 10개 실패**: 8건은 **내 러너 버그**(`runpy`가 같은 프로세스라 `sys.argv`에 `--exec`가 남아 `unittest.main()`/argparse가 자기 인자로 읽고 죽음 → argv 격리+원복으로 fix·회귀 고정), **2건은 진짜**(아래 [F-151]). 최종 **41/41 PASS**. 자기 검증 `tests/test_run_all_tests_8_10.py` **15/15**(음성 대조 6종: 실패 테스트·모듈레벨 실패·`__main__` exit 1·실행경로 없음·파싱 실패·미구현 fixture) — ★**기준을 HEAD에 묶지 않고 합성 픽스처를 임시 디렉터리에 써서 고정**(8/7·8/9에 이틀 연속 저지른 실수의 처방). ★자기 검증이 **내 MonkeyPatch 버그를 잡았다**(없던 키 undo 시 sentinel 객체를 그대로 써넣어 `os.environ`에서 TypeError) → `_MISSING` 모듈 상수로 fix + 계약을 테스트에 고정. 잔여=훅/nightly 자동 호출은 미배선([F-152]).
- **[F-151]** (8/10) **★테스트 3건이 사장님이 폐기한 룰을 아직 "정답"으로 주장하고 있었다 (MED — [F-119] 첫 수확)** — 러너를 만들자마자 드러난 진짜 실패 2파일. ①`bot/test_jarvis_decision.py` 3건 — 대형주 −10%→`SL_LARGE`·중형주 −5%→`SL_MID`·+10%→`TAKE_PROFIT`을 기대하는데, 이 룰들은 **5/25 사장님 룰("고점 −3% 일관"·트레일링 only)로 폐기**됐고 6/18에 SAJANG 단일진실로 통일됐다(`jarvis_decision.py:234-236·246-248` 주석이 폐기를 명시). ②`bot/test_pre_close_d_5_26.py` 1건 — 소스에 리터럴 `chg < 10.0`이 있는지 grep 하는데 **7/31 검수 HIGH-2가 그 리터럴을 `SAJANG.RULE_D_*` 파생으로 바꾸면서** 테스트만 낡았다. ★★**위험 방향은 "실패한다"가 아니라 그 반대다** — 누가 이 테스트를 *통과시키려* 코드를 고쳤다면 ①은 **폐기된 고정 TP·시총별 SL을 부활**시키고 ②는 **RULE-005~007이 막는 SAJANG 우회**를 되돌린다. 낡은 테스트가 폐기된 룰을 가리키는 **함정**이었고, 53일(①)·10일(②) 동안 아무도 몰랐던 이유는 **돌릴 수단이 없어서**였다. fix=기대값을 현행 사장님 룰로 정정 + ①에 `return ACTION_SL_LARGE/MID` 부재를 감시하는 테스트 신설(기대값을 바꾸면 '폐기 확인'이 사라지므로 별도 고정)·②는 리터럴 grep 대신 **SAJANG 경유 + 상수 값** 이중 검증(리팩터에 안 부서지고 우회도 잡음).
### ★ 8/13 데이터 점검 (사장님 지시 — 8/12·8/13 종가·csv·수급 적재 확인) 신규 등재 F-170

> ★이번 점검의 수확은 **판정이 아니라 대조**다. 8/12·8/13 적재는 정상인데, 8/11 업무일지
> 기록과 오늘 재측정을 나란히 놓자 **과거 수치가 바뀌어 있었다**. 매일 "오늘 적재됐나"만
> 보면 영원히 안 보이는 종류다 — **어제 맞다고 한 값을 오늘 다시 재는 것**이 검출기였다.

> ★★**정정 (같은 날 21시 — 배포 후 실측으로 내 추정이 깨졌다)**: 아래 [F-170] 은 fix 를
> 배포하고 19:40 재수집이 **신코드로 완주했는데 구멍 복구 0건**이었다. 원인을 원문 HTML 로
> 파고드니 — 네이버 표는 20행이 다 있으나 **최신 1행만 실값이고 나머지는 `외국인 보유주수`·
> `보유율` 이 전부 0** 이다(066910 실측: 8/12 순매매량 15,593인데 보유주수 0.00%). 파서가
> `rate<=0 and holding<=0` 을 거르므로 **과거 행은 되살릴 값 자체가 없다.**
> ⇒ ①**"구멍 226건 = 손실"은 과장**이었다. 대부분은 삭제된 행이 아니라 *그날 수집을 놓치면
> 네이버가 이후 0으로 덮어써 **영구 복구 불가**가 되는 자리*다(대형주 005930·000660 은 20행
> 실값 유지 — 즉 **소형주 한정 현상**). 확인된 진짜 삭제는 8/11 의 **−6** 규모다.
> ②따라서 fix 의 실효는 **네이버가 실값을 유지하는 종목(대형주)의 미래 손실 예방**뿐이고,
> **오늘 측정된 복구 이득은 0건**이다. ③fix 자체는 무해(있는 실값만 추가·삭제 0)라 되돌리지
> 않았다. ★남은 진짜 처방 = ghost 컷을 `row_date`(네이버가 준 값)가 아니라 **오늘 날짜**
> 기준으로 자르는 것 — 지금은 네이버가 스테일 응답을 주면 **멀쩡한 최근 행을 지운다**(−6의 경로).
> ✅ **[F-170④] 당일 소진** (eea014d·VPS 배포 20:49 — 사장님 지시 *"바꿔라 그러면 지금"*).
> ★**설계 전 실측을 먼저 했다**(오늘 교훈의 즉시 적용): ghost 의 원천
> `_fetch_foreign_rate_kis_snapshot` 이 **호출자 0건**([F-148] 고아 목록과 일치)이고 나머지
> foreign_exh 참조는 전부 읽기 — **막으려던 위협이 이미 사문**이고 살아있는 효과는 정상 행
> 삭제뿐이었다. 변경 = `df.index <= row_date` → `df.index <= _today_kst_ts()`(미래 일자만 컷).
> ★**거래일 캘린더로 자르지 않았다** — 캘린더가 틀린 적이 있다(7/17 제헌절). 캘린더 버그가
> **데이터 삭제로 번지는 경로**를 새로 만들지 않는다. ★삭제 행수를 **세어서 로그·warning 노출**
> (`ghost컷N`) — 이 사고가 오래 안 보인 유일한 이유가 *조용히 지웠다*는 것이었다.
> ★음성대조(행동): 구코드에 스테일 시나리오 투입 → **06-05·06-09 삭제 확인**, 신코드는 보존.
> 신규 2건 포함 8/8 · 전체 **47/47 PASS** · Codex 0건 · **VPS 실측**(005930 행수 80→80·`ghost컷0`).

- ~~**[F-170]** (8/13) **외국인소진율 과거 행이 사후에 사라진다 (MED)**~~ — ✅ **fix 배포** (d274aa6·VPS 19:37) / ★**실효는 위 정정 참조(복구 0건)**. 원 진단 보존: 8/11 일지 기록 **2,435종 → 8/13 재측정 2,429종(−6)**. ★**꼬리창 아티팩트가 아니다** — 같은 도구로 8/10은 **2,436 → 2,436 완전 재현**인데, 창이 깎이는 것이라면 **더 오래된 8/10이 먼저** 깎여야 한다. 실측 구멍(앞뒤 거래일 모두 존재하는 진짜 결손) **226건 / 9거래일 ≈ 1.0%**. ★**근본은 두 코드의 조합**: ①`collect_foreign_exhaustion` 의 ghost 컷 `df = df[df.index <= row_date]`(`flow_collector:591`)이 신규 행이 아니라 **파일 전체 이력**에 걸린다 ②`_fetch_foreign_rate_naver` 가 페이지 20일치를 **전부 파싱해 놓고 `rows[0]` 1행만 반환**한다(`:508`). → 네이버가 최신일을 한 번이라도 이전 날짜로 주면 그 뒤 행이 지워지고 **되채울 경로가 없다**. ★**두 번째 경로가 실은 더 흔하다** — 어제 재수집 로그 기준 **실패 86종(3.4%)이 상시**인데, 실패한 날은 행이 안 붙고 다음날엔 최신 1행만 붙으므로 **그날은 영구 공백**이 된다(삭제 없이도 구멍). fix는 두 경로를 동시에 막는다: `_fetch_foreign_rates_naver`(복수) 신설 → 파싱된 전 거래일 병합(같은 날짜는 네이버 우선) → **다음 수집에서 자가복구**. 이미 파싱한 데이터라 **HTTP 추가 0건**. 단수 함수는 `_fetch_foreign_rate_api` 하위호환용으로 존치(호출 site 2곳 전수 확인). ★**한계 정직 표기** = 네이버가 아예 표를 안 주는 종목(실측 439960·007460·270660·090370·017250 = 0행)은 **이 fix로도 복구 불가** — 구멍 226건 전부가 아니라 파싱되는 종목분만 메워진다. ★**부작용 1건 병기**: 병합이 `keep="last"` 라 같은 날짜는 네이버 현재값이 기존값을 덮는다(네이버 수치 정정 시 과거 값이 바뀔 수 있음). 단수 시절에도 최신 1행엔 이미 적용되던 성질이나, 적용 범위가 20일로 넓어졌다 — **판정 데이터가 바뀌는 성질이므로 사장님께 병기**. ★음성대조 = 구코드에서 되채움 테스트 FAIL + 함수부재 ERROR **2건 확인**(실패를 확인하지 않은 테스트는 보호가 아니다 — 8/6 교훈). Tier3 신규 4건 포함 **47/47 PASS**·Codex 지적 0건·배포 전 **전량 백업 3,510파일**.

### ★ 8/13 전체검수 (사장님 지시 — 코드·로직·버그·슬러지) 신규 등재 F-172~F-174

> ★이번 검수의 출발점은 오늘 낮 [F-171]이었다 — *"이건 단건인가 유형인가"*. 답은 **단건**이다
> (`bot/` 안의 날짜 미확인 최신값은 [F-171] 하나뿐). 그런데 **그 질문을 따라가다 더 큰 것**이
> 나왔다: 최신값이 아니라 **창(window) 자체가 없는** 코드([F-172]).
> ★4개 축 중 2개는 **깨끗했고 그게 결과다** — 억지로 건수를 만들지 않았다.

- **[F-172]** (8/13) **★★단타 픽 대형주 스캔이 "5일 누적"이라 적고 6개월 전 구간을 더한다 (HIGH)** — `tools/daytrading_picks.py:202` 주석 `# 5일 외인/기관 누적`, 상수 주석 `:165` `외인+기관 5일 누적 300억+` 인데 실제로는 `load_investor_flow(code)` 가 **파일 전체**를 반환하고(`flow_collector:1082-1087` — 행 제한 0) 그대로 `.values.sum()` 한다. **investor CSV 행수 중앙값 128거래일**(2026-02-04~). ★같은 착오가 필터 3개에 전파 = 누적금액(`dual_total`)·**조용함 필터**(`price_chg = closes[-1]/closes[0]` → 6개월 변동률을 ±15% 밴드로 판정)·매수일수(`foreign_buy_days`). *"조용함 ±15% (완화)"* 라는 docstring 의도는 **5일 창에서만 성립**한다 — 6개월 ±15%는 완화가 아니라 극단적으로 엄격하다. 반대로 300억 누적은 6개월이면 훨씬 헐거워진다. ★**효과 실측**(시총 2조+ 254종): 현재 구현 통과 **31종** vs 주석 의도(5일) 통과 **6종** · **교집합 2종** ⇒ **지금 뽑히는 31종 중 29종은 의도한 룰이면 안 뽑힌다.** 선정 집합이 사실상 다른 전략이다. ★**시간이 갈수록 드리프트**한다 — CSV가 매일 1행씩 쌓이므로 창이 계속 넓어진다(오늘 128일·석 달 뒤 190일). ★사정권 = `trading_coo._job_daytrading_picks`(A12 07:35 confirmed·16:45 preview)가 부르고 산출 `daytrading_picks.json` 은 `telegram_bot:4243` **PaperPortfolio 등록**에 쓰인다. `auto_trader:215` 는 같은 파일에서 `ewy_signal` 만 읽는다 — **실매수 직결 경로는 이 검수 범위에서 확인되지 않았다**(봇 OFF·실주문 0, 정직 표기). 다만 **페이퍼 장부가 판정 근거**이므로(사장님 *"페이퍼로 승률·수익금부터"*) 전략 판정을 이 창으로 오염시킨다. fix 후보 = `flow.tail(5)` 로 창 고정 / 또는 상수·주석을 실제 동작(전 구간)에 맞춰 정정 — **어느 쪽이 사장님 의도인지가 갈리므로 결정 사안**. 값 무변경 등재.
- **[F-173]** (8/13) **위험 게이트 3종이 ImportError 시 전부 fail-open (LOW — 현재 잠재)** — `bot/auto_trader.py:54-61` 의 폴백 stub: `check_market_risk_blocked(): return False, ""` · `is_foreign_exhaustion_blocked(_code): return False` · `should_trigger_inverse_alert(): return False`. import 가 깨지면 **로그 한 줄 없이 세 게이트가 동시에 열린다**([F-145] 킬스위치와 같은 계열이나 그건 호출부 1곳, 이건 stub 3종). ★**VPS 실측으로 심각도를 낮췄다** — `_SDK_AVAILABLE=True` · SUPABASE URL/KEY 설정됨 · `_get_client()` → `RiskGateClient` 생성 · `is_foreign_exhaustion_blocked('005930') → False` 정상 반환 = **게이트는 지금 살아 있다**. 즉 fail-open 은 잠재이지 활성이 아니다. (이 실측이 없었으면 "게이트가 죽어 있다"고 보고할 뻔했다.) ★역으로 **[F-171]의 무게는 올라간다** — 게이트가 실제로 동작 중이므로 66종이 낡은 값으로 판정받는 것도 실제다. 처방 = 폴백을 `position_safety.py:409-414` 형태(logger.error + 보수적 실패)로 통일.
- ~~**[F-174]** (8/13) **IMP-001 이 `from __future__ import` 를 구조적으로 100% 오탐 (LOW)**~~ — ✅ **당일 소진**. `tools/pre_commit_check.py` 의 IMP-001 은 *"이름이 파일에 1번만 등장하면 미사용"* 인데 `from __future__ import annotations` 는 **컴파일러 지시자**라 본문에 다시 등장할 수가 없다 → **항상 걸린다**(오늘 flowx 커밋에서 실제 발생). 오탐이 쌓이면 사람이 게이트를 꺼버린다([F-153] 그 자체) → `__future__` 라인 스킵. ★**양방향 검증**: `from __future__ import annotations` **통과** + 같은 파일의 진짜 미사용 `from pathlib import Path` **여전히 검출**(규칙이 무뎌지지 않았음을 음성대조로 고정).

**8/13 검수 — 깨끗하게 나온 축 (건수 0이 결과다)**

- **날짜 미확인 최신값 = 유형 아님**: `iloc[-1]`/`tail(1)` 전수 181건 중 **`bot/`(실매매 코드) 안은 [F-171] 단 1건**. 캐시 로더 4종(`load_investor_flow`·`load_foreign_exhaustion`·`load_short_balance`·`load_nationality`) 소비처를 전건 추적 — 나머지는 집계(`tail(n)`·`sum`)라 단일 최신값 의존 아님. ★부수 확인 = `load_short_balance` **소비처 0건**(고아 로더·[F-148] 계열, 무해라 미등재). 국적별은 **KRX 절대룰**이라 무접촉.
- **조용히 죽은 import 검출기 재실행 = 신규 0**: 361파일 스캔 **9건**으로 어제와 **동일**([F-165]·[F-166]·[F-167]·[F-146] 기등재분) — **회귀 없음**. 검출기가 어제 이후 새로 생긴 것을 찾지 못한 게 아니라 **새로 생기지 않았다**.
- **고아 문서 = 0건**: 문서 전수 스캔이 11개 문서를 물었으나 **전부 오탐/정상**이었다 — ①**내 스캔의 기준 디렉터리 오류 2건**(`tools/pre_commit_check.py`·`tools/codex_pre_commit.py` 는 저장소 루트에 실재하는데 `scalper-agent/` 기준으로 찾아 없다고 판정 — 그대로 올렸으면 없는 사고 2건을 만들었다) ②**장부 자기참조 1건**([F-117] 항목 본문이 `refresh_etf_leaders.py` 를 인용 — 8/6·8/7에 이어 **자기참조 함정 3회차**) ③일지의 일회용 scratchpad 스크립트 5건(정상) ④아카이브 문서 2건 ⑤**퀀트봇 교차참조 1건**(`codex_lite.py`·`quant_preflight.py`·`self_review_checklist.py`·`main.py` — VPS `quantum-master` 에 **전부 실재** 확인).

- **[F-171]** (8/13) **★소진율이 실매수 차단 게이트에 들어가는데 날짜를 안 본다 (MED — 사장님 결정 사안)** — [각주-2] 조사 중 발견. `bot/risk_gate_helper.py:182` 가 `rate = float(df["소진율"].iloc[-1])  # 가장 최신` 으로 **마지막 행을 날짜 확인 없이** 읽어 `client.is_blacklisted_for_exhaustion(rate)` 에 넣고, 그 판정이 `bot/auto_trader.py:650` **실매수 경로**에서 종목을 차단한다. ★그런데 이 채널은 **수집 실패가 상시**다(어제 재수집 로그 실패 86종·오늘 101종). 마지막 행 나이 **전수 실측(8/13 기준)** = 당일 2,408 · 1~3일 57 · 4~7일 5 · 8~30일 15 · **31일+ 46종**(최악 **78일**·마지막 행 2026-05-27, 예: 232530·260870·140610·217950·229500) ⇒ **66종이 1주 넘게 스테일한 값으로 게이트를 통과/차단당한다.** 주석이 `# 가장 최신` 이라 **코드가 스스로를 틀리게 설명**하는 자리이기도 하다(오늘 [F-170] 과 같은 계열 — 라벨과 값의 출처가 다르다). ★인접 = `auto_trader.py:60` 의 import 실패 폴백 `def is_foreign_exhaustion_blocked(_code): return False` 는 **fail-open**([F-145] 동형)이고, `_get_client()` 도 SUPABASE 환경변수 없으면 None → False. 즉 이 게이트는 **여러 층에서 조용히 열린다**. ★**봇 OFF·실주문 0이라 지금 사고는 아니다.** fix 후보 = ①마지막 행의 날짜가 N거래일 이내일 때만 rate 사용(아니면 게이트 판단 보류) ②스테일 시 로그·집계 노출. **매수 게이트 동작 변경이라 사장님 결정 사안** — 값 무변경 등재만.
- **[각주-2] 결론 (8/13 — 조사 완료·조치 불요)** — 소형주 `보유수량 == 외국인_수량` **33종/2,416**(8/12)은 **우리 버그가 아니다**. 네이버 원문 표에서 그 종목들은 최신 행의 `외국인 보유주수` 가 그날 `외국인 순매매량` 과 같은 값으로 게시되고(066910 8/13 둘 다 2,546), 우리는 그걸 그대로 복사한다. `소진율` 도 네이버 `보유율` 과 일치. ★소비처 조사 = **`보유수량` 을 읽는 곳은 `data/spacex_watchlist.py:145-146`(`result["holding"]`) 한 곳뿐**이고 매매 게이트 0건(단 spacex_watchlist 자체의 라이브 여부는 미추적 — 정직 표기). 실제로 매매에 닿는 값은 `보유수량` 이 아니라 **`소진율`** 이었고, 그 경로에서 나온 진짜 문제가 위 [F-171] 이다.

### ★ 8/11 전체검수 (사장님 지시 — 코드·로직·버그·슬러지) 신규 등재 F-165~F-169

> ★이번 검수의 수확은 **개별 3건이 아니라 검출기**다. [F-156]([F-40] 유형)을 어제 **손으로**
> 찾았는데, 같은 모양이 저장소에 더 있는지 사람이 읽어서는 알 수 없다. 그래서 그 패턴을
> AST로 기계화했고 — **[F-156]을 스스로 다시 찾아냈고(검출기 검증), 새로 3계열을 더 찾았다.**
> ★1차 실행은 **48건**이었고 그 대부분이 내 오탐이었다(`from data import trade_journal` 같은
> **서브모듈 import**를 `__init__.py` 이름 집합에 없다고 계수). 고쳐서 **10건**으로 줄인 뒤
> 전건 원문 확인했다. 48건을 그대로 올렸으면 오늘 하루 지킨 원칙을 내가 깼다.

- **[F-165]** (8/11) **섹터 로테이션 조회가 영구 실패 — 매일 도는 잡인데 (MED·[F-156] 동형)** — `data/limit_up_engine.py:326` 이 `from data.rotation_detector import detect_rotation` 하는데 **그 이름은 저장소에 없다**. 정답은 **`analyze_rotation()`**(`rotation_detector.py:347` — `RotationReport` 반환). `try/except` 안이라 조용히 실패하고 `_get_sector_rotation()` 이 **항상 초기값 `{"phase":"미확인","favorable":True}`** 를 돌려준다. ★**죽은 코드가 아니다** — `trading_coo._job_limit_up_engine`(C7L)이 매일 부른다. ★**사정권 실측 = 매매 무접촉**: `rotation_favorable` 소비처는 `limit_up_engine:585·821·949·1075` 와 `upload_limit_up.py:93`(대시보드 업로드)뿐이고 **매수 게이트로 쓰이는 곳 0건**. 다만 **사장님이 보시는 화면이 항상 "미확인 / 🟢 유리"** 로 나온다(`:905` 아이콘) — [F-161]과 같은 "화면이 사실과 다른" 유형. fix 방향은 ①`analyze_rotation` 으로 배선 ②[F-40]처럼 표시 자체를 걷어냄 — **사장님 결정 사안**이라 값은 건드리지 않았다.
- **[F-166]** (8/11) **FLOWX 로 올라가는 KR `volume_ratio` 가 영구 1.0 (MED·[F-156] 동형)** — `data/flowx_universe_updater.py:133` 이 `from data.csv_loader import load_stock_data` 하는데 **csv_loader 는 클래스만 제공**한다(`class CSVLoader` — 정답은 `CSVLoader().load_ohlcv(code)` 또는 `.load(code)`). `except Exception: pass` 뒤 `return 1.0` 이라 **KR 종목 volume_ratio 는 언제나 정확히 1.0** 이고 그대로 FLOWX 에 업로드된다(`:88-89`). ★US 경로(`_calc_volume_ratio_us`)는 정상이라 **KR 만 중립값**이다 — 두 시장을 비교하면 KR 이 구조적으로 무신호로 보인다. ★진입 경로는 `telegram_bot.py:3452`(사장님 명령)라 cron 상시는 아니지만, **부를 때마다 틀린 값을 외부 시스템에 쓴다**.
- **[F-167]** (8/11) **`bot.telegram_bot.send_message` — 6개 도구가 존재한 적 없는 이름을 부른다 (LOW)** — `event_detector:679`·`market_health:1198`·`volume_scanner:332`·`daytrading_picks:687`·`foreign_accumulation_scanner:307`·`swing_scan:700` 이 모두 `from bot.telegram_bot import send_message`. **`telegram_bot.py` 에 그 최상위 함수는 없다**(있는 건 클래스 안의 `context.bot.send_message(...)` = 텔레그램 라이브러리 메서드). ★`git log -S "def send_message"` **0건 = 한 번도 존재한 적이 없다**. ★**완전 무성은 아니다** — handler 가 `print("텔레그램 전송 실패: {e}")` 를 찍는다. 그리고 **cron 노출 0**(crontab 에 `--telegram` 사용 잡 0건)이라 수동 CLI 에서만 드러난다. 그래서 LOW 지만, **"텔레그램으로 받아보자" 하는 순간 6개 도구가 전부 실패**한다.
- **[F-168]** (8/11) **★이 유형을 기계가 세게 할 것 — 검출기는 만들었고 배선이 남았다 (MED·처방)** — 위 3계열 + [F-156] = **"없는 이름 import + 예외 삼킴 → 기능 영구 사망"** 이 확인된 것만 **4계열 9사이트**다. 공통점은 **전부 손으로 찾혔다**는 것이고, 그건 다음 것도 손으로 찾아야 한다는 뜻이다. 검출기(AST: `try` 안의 `ImportFrom` → 대상 모듈 최상위 이름 집합과 대조 → 조용한 handler 판정)는 이번 검수에서 작성해 **[F-156] 재검출로 검증**했다. 배선 후보는 [F-152]와 같은 3안(pre-commit 변경파일 한정 / nightly 1스텝 / 08:30 점검 편입). ★**보수적 설계 원칙**을 유지할 것: 외부 모듈 제외·서브모듈 import 통과·파싱 실패 시 판정 포기 = **못 찾는 쪽으로 틀리게**. 오탐이 늘면 [F-153]처럼 사람이 게이트를 끄게 된다.
- **[F-169]** (8/11) **단일진실 import 가 실패하면 라벨만 하드코딩으로 갈린다 (LOW)** — `tools/strategy_deadline_check.py:111-113` 이 `from observe_v2_paper import NOTIONAL_KRW, CAP_VIEW_N` 실패 시 `300_000, 5` 로 폴백한다(`:121-123` `MIN_JUDGE_N` 도 동일). ★**지금은 휴면**이다 — VPS 운영 인터프리터로 실측하니 세 import 전부 OK(`CAP=5`·`NOTIONAL=300000`·`MINN=10`). ★그런데 **계산과 라벨의 출처가 다르다**: 실제 수치는 `_v2_paper_cum_net()` 이 `_cum()` 에 위임해 **observe_v2_paper 의 진짜 CAP** 로 계산하고, 스코어보드 문자열의 `상한{CAP_VIEW_N}건` 은 **폴백될 수 있는 쪽**을 쓴다. 누가 `CAP_VIEW_N` 을 리네임하면 **계산은 새 값·라벨은 5** 로 조용히 갈린다 — 오늘 [F-164]·가드 라벨 사고와 같은 계열(*값과 라벨의 출처가 다르면 언젠가 갈린다*). fix 후보: 폴백 제거하고 **실패 시 시끄럽게**(스코어보드는 이미 "장부 없음" 경로가 있다).

### ★ 8/11 데이터 점검 (사장님 지시 — 8/10·8/11 종가·csv·수급 적재 확인) 신규 등재 F-162~F-164

> 점검 자체는 통과다(8/10 전 채널 정상·8/11 은 외국인소진율만 19:40 재수집 대기 = 설계된 동작).
> 아래 2건은 그 점검 도중 **딸려 나온 것**이고, 하나는 내가 **방향을 반대로 읽을 뻔한 것**이다.

- **[F-162]** (8/11) **★수급 채널에 "거래 없는 날의 행"이 매일 쌓이고 판정 장부까지 들어갔다 (MED·[F-123] 수급판)** — 실측: `investor` 행은 있는데 매매 **수량·금액이 전부 0**인 (날짜,종목) = **7,138쌍**, 그중 종가(daily) 채널에도 행이 없어 **양 채널이 독립적으로 '거래 없음'에 동의**하는 strict = **1,387쌍**(5/27~8/11). 6거래일 내내 그런 종목 **28종** — 한화(000880) **83,800원으로 7/29부터 13일째**, 동양생명(082640) 8,250원(8/6 마지막 거래). ★**근본은 우리 수집기가 아니다**: `_fetch_investor_api`는 KIS `output`에 있는 행만 append 하고 병합은 `pd.concat`+dedup(keep=last)+sort 로 **forward-fill 이 없다**(`flow_collector.py:289-291`) → **KIS가 정지 종목에 기준가+플로우0 행을 준다**. 원본 충실이므로 수집기를 고치는 것은 오답이고, 문제는 **소비자가 "행이 있다"를 "그날 거래했다"로 읽는 것**이다. ★**사정권 — (날짜,종목) 쌍 대조**(8/6 [F-123] 확립 방법·종목 단위 교집합은 *"언젠가 신호"*라 오판): `observe_v2_paper_ledger`(**라이브 후보 PB-A v2**) 신호 1,306쌍 중 strict 교집합 **0건** — **체결강도 200+ 게이트가 막았다**(8/6과 같은 이유를 독립 표본에서 재확인). `playbook_shadow` PB-B **1건 유령거래**(226340 @ 8/10 진입 09:19:30→청산 15:49:20 인데 **그날 종가 행 자체가 없다**·8/07 다음이 8/11 = 7/17 휴장일 유령신호와 같은 구조, 이번엔 휴장일이 아니라 정지 종목). `institution_accum_scan` **1건**([F-163]). early_variant·reentry·theme_relay·catalyst·ranking·daytrading_picks = **0건**. ★**수치 영향은 정직하게 미미하다**: 그 유령 1건은 `strength_gate=False`라 **게이트 통과 집계(n=6)에서 이미 제외**돼 있고, 미게이트 '전체' 평균은 −1.458 → 유령 제외 시 **−1.495로 오히려 나빠진다**(ret=0.0 이라 음수 평균을 희석하고 있었다·왜곡 0.037%p). **즉 크기가 문제가 아니라 구조가 문제다** — 체결강도 게이트가 없는 전략이 하나라도 추가되면 그대로 속는다. ★처방 후보(**판정식 불변**·[F-123]과 같은 자세): ①`(날짜,종목)` 유효거래일 판정을 정본 헬퍼로 만들어 소비자가 부르게 ②장부 생성 시 `no_trade: true` **병기만** ③사장님 결정 전까지 값 무변경. ★교훈=**"수급 행이 있다" ≠ "그날 거래했다"**.
- **[F-164]** (8/11) **20:10 요약이 `⚠️`를 띄우면서 하단은 `✅ 핵심 채널 모두 정상`이라 말한다 (LOW·표기)** — `notify_data_freshness.build_summary:146` 이 `fx["status"] in ("PASS","PARTIAL")` 로 **PARTIAL 을 정상으로 계수**하고, `bad` 목록에는 `STALE` 일 때만 추가한다(`:161`). 그래서 외국인소진율이 부분수집이면 **행은 `⚠️`, 결론은 `✅ 모두 정상`** 인 메시지가 사장님께 간다. ★코드 독해가 아니라 **실측으로 재현**했다 — 8/11 19:46 실행분이 `⚠️ 외국인소진율 2026-08-11 (7/10)` + `✅ 핵심 채널 모두 정상` 을 동시에 출력(19:51 재수집 완료 후에는 10/10 로 해소). ★**의도된 설계일 수 있다** — 네이버 소진율은 부분 실패가 상시라 PARTIAL 을 실패로 치면 매일 빨간불이 되고, 그건 [F-153]의 "사람이 경보를 끄게 만드는" 마모다. 그래서 **값을 바꾸지 않고 등재만** 한다. 사장님 판단 사안: ①현행 유지(부분수집은 정상 운영) ②결론 문구를 `✅ 핵심 채널 정상 (외국인소진율 부분수집)` 처럼 **단서 병기** ③PARTIAL 임계(현재 `ok >= checked-1`)를 조정. 처방 ②가 값 불변이라 가장 가볍다.
  - ✅ **8/14 소진 — 사장님 승인 처방 ②(커밋 0ed7e50)**. 판정(`core_ok`)은 불변, **문구만** 실상에 맞춤: "모두"는 전부 깨끗할 때만 쓰고 아니면 무엇이 부분인지 병기. ★★**VPS 실측이 픽스처가 못 잡은 회귀를 잡았다** — 1차 구현이 신용 **82종(정상 대역)**을 매일 "결손"으로 띄웠다. 신용은 실측 96.8~97.0%가 정상이라 그 문구가 **매일** 나갔을 것이고, 그건 [F-153]이 말한 마모를 **새로 만드는** 셈이다(게다가 82종의 성격을 규명하지 않은 채 사장님께 "결손"이라 보고하게 된다). → 신용은 **정상대역 이탈(PARTIAL/FAIL)일 때만** 노출로 수정 후 재실측 통과. [[feedback-fixture-is-not-the-external-world-8-13]]의 정확한 재현 = 픽스처 23/23은 이걸 못 봤다.
- **[F-163]** (8/11) **기관매집 스캔이 거래정지 종목을 대기 후보로 올린다 (LOW·[F-162] 파생)** — `institution_accum_scan.standby_stocks[24]` 에 **동양생명(082640)** 이 `close: 8250`·`pension_buy_days: 7`·`pension_score: 124` 로 올라 있는데, 그 8,250원은 **8/6 이후 갱신이 없는 박제가**다(daily 마지막 행 8/6·investor 8/7·8/10·8/11 전부 매매 0). 스캐너에 **'최근 거래 여부' 가드가 없다**. record-only·실주문 0 이라 손실 경로는 아니나, **사장님이 보는 후보 목록에 살 수 없는 종목이 섞인다**. [F-162] ①처방이 들어가면 같이 소진된다.

### ★ 8/10 전체검수 (사장님 지시 — 코드·로직·버그·슬러지) 신규 등재 F-156~F-161

> 착수 동기가 하나 더 있었다 — **오늘 hard_kill 을 실전으로 켰다([F-91])**. 켠 쪽이 그 경로를
> 감사해야 한다. 실제로 가장 큰 것이 거기서 나왔다([F-157]).

- ~~**[F-157]** (8/10) **★★★hard_kill 이 부분체결 1회로 스스로를 영구 무장해제한다 (HIGH)**~~ — ✅ **8/10 당일 fix**. 연쇄: ①`sell_market` 은 기본 **3분할**이고 `success = total_filled > 0` = **1청크만 접수돼도 True** ②hard_kill 이 그 success 만 보고 **메모리에서 포지션 삭제** ③KIS 엔 잔량 존재 ④5분 뒤 `sync_positions` 케이스 A 가 `source="sync_auto_unknown"` 재등록 ⑤`_is_protected` 가 `sync_auto*` 를 **보호**로 판정 → **그 종목은 영구히 −5% 최후 방어선 밖**. ★**이 경로는 오늘 dry_run 해제로 처음 열렸다**(그전엔 매도 자체가 없어 루프가 시작되지 않았다). ★**장외 큐 경로도 같은 결함**이었고 더 나빴다 — `del pending[code]` 까지 해서 "다음 시초에 반드시 판다"는 **예약 안전망까지 함께 소멸**. ★fix=체결 대조 **정본 재사용**(`_reconcile_fill`/`_actual_fill_qty` 는 이미 있었고 `smart_buy`·`chase_buy`·`smart_sell` 3곳이 쓰는데 **시장가 경로만 빠져 있었다** — 하필 hard_kill 이 그 경로다): `sell_market` 반환에 `filled_qty`/`requested_qty`/`partial` **추가**(`success` 의미 불변 → 호출처 10곳 영향 0) + hard_kill·큐는 **전량 확인 시에만 삭제**, 부분이면 수량만 줄이고 **원래 source 유지** + 저널은 `filled > 0` 이면 **실체결량으로** 적재(부분체결 손익이 장부에서 사라지던 것 — 5/22 "guardian 가짜 매도 회계 결함"과 같은 자리). 회귀 `tests/test_hard_kill_partial_fill_8_10.py` **10/10**(음성 대조 2종 포함 — 삭제→sync재등록→보호→제외 고리를 **실재 부품**으로 이었고, 구 `sell_market` 반환에 체결량이 없었음을 역치환으로 확인). ★**작업 중 내가 만든 결함 2건도 잡았다**: `else:` 가 `if fully_closed:` 에 붙어 부분체결이 "실패"로 이중 기록되던 것(→`elif not success:`), 그리고 테스트가 **내 주석과 보호취소 분기를 계수**해 코드가 옳은데 FAIL 을 내던 것([F-89]/[F-100] 자기참조 계수 — 오늘 세 번째).
- ~~**[F-158]** (8/10) **★★레짐별 손절이 룰(−3%)보다 조여진다 — '레짐'이라는 이름 뒤의 [F-90] (HIGH)**~~ — ✅ **8/10 당일 fix**. `macro_strategy.get_adjusted_sl()` 이 `min(base, response.sl_pct)` 라 **스태그플레이션 −2.00% · 비용상승 −2.50%**. 소비처 `auto_trader.py:3710`(`loss_percent = get_adjusted_sl(loss_percent)`)은 **라이브 매수 경로**. ★**지금까지 사고가 안 난 이유는 설계가 아니라 우연**이다 — `get_current_regime()` 이 nightwatch 48h 초과면 '안정'(0.035)으로 폴백하는데 금요일 산출분은 월요일에 72h 라 **레짐이 사실상 늘 '안정'**이었다. **그 폴백을 고치는 순간 위반이 즉시 유효**해지므로 순서 의존을 없애려 임계 자체에 하한을 걸었다(`max(rule, min(base, regime))`). 정의값(0.020/0.025)은 **보존**(리포트·향후 사장님 결정용) — 실효값만 눌린다. ★**내가 처음 오판했다**: 레짐 키가 **한글**인데 영어 키로 재서 전 레짐 −3.00%로 보였고 "이상 없음"으로 넘어갈 뻔했다. 정의된 키 전수로 다시 재서 잡았다 → 그 오판 통로를 테스트에 고정. 회귀 `tests/test_macro_regime_sl_8_10.py` **6/6**(음성 대조=구식 `min()` 이 실제로 2.0/2.5% 를 낸다). ★잔여(등재만): `base` 를 룰보다 느슨하게 주면(config 변경 시) 0.035 가 나올 수 있다 — 현재 config `stop_loss_pct: 0.03` 이라 실경로는 정확히 −3.0%.
- **[F-156]** (8/10) **ETF 추천의 섹터 모멘텀 기여가 영구 0 (MED·[F-40] 유형)** — `data/etf_recommender.py:232` 가 `from data.sector_momentum import load_sector_report` 하는데 **그 이름은 저장소에 없다**(모듈 실제 export = `analyze_sectors`·`_save_report` 등). `try/except Exception: pass` 안이라 **조용히 실패**하고 `sector_hot` 이 영구 `{}` → 섹터 모멘텀 가산이 항상 0. 소비 경로는 살아 있다(`morning_recommendation.py:3091` → `generate_etf_recommendations`, telegram 표시). ★fix 방향은 두 갈래이고 **사장님 결정 사안**: ①`analyze_sectors` 로 배선해 기능을 되살릴지 ②[F-40] 처럼 가산항을 걷어낼지. 지금은 **값을 만들지 않고 사실만 등재**한다(선정 동작 불변).
- ~~**[F-159]** (8/10) **`_base_sl` 죽은 계산 + 주석이 코드와 다르다 (LOW)**~~ — ✅ **실제 소진은 8/10** (`0b3b08b` — 죽은 계산 제거 + 주석 정정 완료·`auto_trader.py:888-895` 에 `★8/10 검수 [F-159]` 근거 주석 존재·`_base_sl` 실사용 0건 재확인). ★**8/11 장부 정정** — 코드는 8/10에 고쳐졌는데 **취소선이 안 그어져 하루 동안 '열림'으로 보였다**. [F-88]/[F-100] 과 같은 **'조용한 소진'**이고, **LEDGER-1 이 자기 docstring 에 "못 잡는다"고 명시한 바로 그 유형**이 실제로 발생한 첫 사례다(8/7에 세운 재발차단은 '열림+완료 동시 존재'는 잡지만 '완료인데 표기 없음'은 못 잡는다). ★교훈 재적용=**착수 전에 구현을 연다** — 오늘 소진 후보로 골라 파일을 여니 이미 고쳐져 있었다(8/7 [F-100] 과 같은 자리에서 같은 이득). 원 진단 보존: `bot/auto_trader.py:892` `_base_sl = get_adjusted_sl(...)` 인데 저장소 전체 사용 0건이었고, 주석은 *"macro_strategy 실패 시 고정 SL(3.5%) 적용"* 이라 코드(3.0%)와도 SAJANG 과도 달랐다.
- ~~**[F-160]** (8/10) **레짐 폴백 임계가 문서와 다르고, 매주 월요일 레짐이 무효 (LOW)**~~ — ✅ **8/11 소진**. 같은 임계가 **세 곳**에서 서로 다른 값을 말하고 있었다: docstring *"24h+ stale"* · 인라인 주석 *"24시간 이상"* · 코드 `if age_hours > 48`. ★**문구를 고치는 대신 중복될 숫자 자체를 없앴다** — `NIGHTWATCH_STALE_HOURS = 48` 모듈 상수 1개를 두고 비교식이 그것을 참조하며, **docstring 에는 숫자를 적지 않고 상수 이름을 가리킨다**(적는 순간 재발하는 자리이므로). [F-158] 주석의 `48h` 언급도 상수 참조로 정리. ★**행동은 바꾸지 않았다** — 주말 구조적 폴백(금요일 산출분이 월요일에 72h → **매주 월요일 사실상 '안정'**)은 **사실로 등재**하되, 임계 변경은 매매 동작을 바꾸므로 **사장님 결정 사안**으로 남긴다(레짐 SL 이 룰보다 조여지던 위험은 [F-158] 하한으로 이미 차단). 회귀 `tests/test_macro_stale_threshold_8_11.py` **9/9**(AST 기반). ★**음성 대조 3종**: ①구판(docstring 24h + 코드 48) 두 검사 모두 검출 ②**문구만 48로 맞춘 반쪽 fix 도 검출**(이게 안 걸리면 다음 사람이 숫자를 다시 두 곳에 적어도 초록불) ③**모듈 주석의 옛 문구 인용은 위반이 아님**을 고정. ★③이 핵심이다 — 파일 전체 grep 검사기였다면 **사고 경위를 적은 내 주석을 위반으로 세어 옳은 코드에 FAIL** 을 냈을 것이고, 그게 **오늘 네 번째 자기참조 계수**였을 것이다([F-89]·[F-100]·8/10 [F-157]). 그래서 검사 사정권을 `ast.get_docstring` + 비교식 노드로 좁혔다.
- **[F-160]** (8/10) **레짐 폴백 임계가 문서와 다르고, 매주 월요일 레짐이 무효 (LOW)** — `macro_strategy.get_current_regime()` docstring 은 *"24h+ stale"* 인데 코드는 **48h**(`:199`). 그리고 금요일 산출분은 월요일에 **72h** 라 **매주 월요일 '안정' 폴백**이 구조적으로 발생한다(VPS 실측: 8/7 16:35 산출 → 8/10 "72h stale"). 레짐 게이트는 7/10에 PB-A 에 역효과로 확인돼 매매 영향은 없으나, **"레짐이 동작한다"는 전제로 다른 판단을 하면 틀린다**. 문서-구현 일치 + 주말 보정 여부는 별건.
- **[F-161]** (8/10) **매크로 리포트가 사장님께 폐기된 트레일링 값을 보여준다 (LOW·[F-90] 부수와 동형)** — `macro_strategy.py:341` 이 `f"  트레일링: -{response.trailing_pct*100:.1f}%"` 로 **−1.5%/−1.8%/−2.0%** 를 출력한다. 매매 소비는 0건(`trailing_pct` 를 읽는 매매 코드 없음)이지만 **사장님이 보는 화면이 룰 3(−3%)과 다른 값을 말한다** — 오늘 `trading_cto` 에서 고친 것과 같은 유형([F-109] 계열). 같은 처방(SAJANG 파생 또는 표시 제거) 적용 필요.

- **[F-155]** (8/10) **★★★장중 명분 탐지는 "없어서 못 한 게 아니라 45일간 안 돈 것"이었고, 켰다면 잃었다 (HIGH·전략 방향)** — [F-87] 처방("명분을 장중에 알아야 한다")을 실행하려 착수했더니 **`catalyst_scanner._intraday_ignition`이 6/26에 이미 구현돼 있었다**(`scan_catalyst`가 `_is_today_loaded`로 장중/마감후 자동 분기). 그런데 **catalyst_archive 29일 870건이 전부 `intraday: False`** = **단 한 번도 장중 경로가 실행된 적 없다**. 원인=스케줄이 nightly 18:28 하나뿐이고 그 시각엔 항상 오늘봉이 적재돼 있어 일봉 분기만 탄다. ★**오늘 [F-147]과 같은 모양** — *"장중 탐지가 있다" ≠ "장중 탐지가 돈다"*. ★★**그래서 cron을 배선하기 전에 과거 ticks로 재현해 봤고, 그게 옳았다**: 도구 `tools/manual/f155_intraday_ignition_backtest.py`(판정은 원본 `_intraday_ignition` import·**입력만 D-1로 슬라이스**해 look-ahead 차단·명분(뉴스) 축은 18:28 산출이라 **제외**). **67 유효일(3/30~8/10) 실측 — 점화 180건(2.69/일) 승률 30.6% 건당 net −0.944%p 누적 −169.91%p**(상한5건/일: 151건 승 27.8% **−1.169%p**). 같은 기간 v2 전 신호는 승 54.6% +0.261%p. **즉 그대로 배선했으면 손실이었다.** ★가설(미검증)=`vol_ratio`가 *09:30 누적 거래량 ÷ 20일 **일간** 평균*이라, 30분 만에 하루 평균의 2배를 채우는 종목은 **파라볼릭 블로우오프**다 — 임계가 마감후 비교용으로 설계됐는데 장중에 그대로 쓰면 훨씬 극단적인 종목만 걸린다(시각별 민감도는 도구의 `--at` 그리드로 측정 가능·미실행). ★★**가장 중요한 함의 — 사장님 말씀이 데이터로 확인됐다**: 6/24 사장님 확언은 *"**명분 있는** 끼 종목"*이었고, 오늘 측정한 것은 **명분을 뺀 '끼(점화)' 단독**인데 그게 −0.944%p다. **명분 없는 끼는 안 된다는 것을 데이터가 보여준다.** → 다음 단계는 점화 임계 재보정이 아니라 **장중에 명분(뉴스)을 얻는 것**(뉴스는 실시간 게시라 장중 조회 가능·현재 유일한 미구현 조각). ★교집합 lift **2.33**(6건/기대 2.6)로 점화는 v2와 **같은 종목을 보는 경향**이 있다 — [F-87]의 명분↔v2 lift 0.25(서로 밀어냄)와 방향이 반대라, **'끼'와 '명분'은 서로 다른 종목군**임을 시사(표본 6건이라 방향만·법칙화 금지). ★⚠**주의**: 위 v2 +0.261%p는 **전 신호 비실행 수치**다([F-32] 4층 미통과) — 실행가능 cap5 는 스코어보드대로 마이너스다. 점화와의 정당한 비교는 cap5끼리이고 그래도 점화가 크게 열위.
- **[F-154]** (8/10) **★[사장님] VPS 실상태가 "제한 라이브" 자세가 아니다 — 재가동 전 확인 (HIGH·[F-119] 첫 VPS 수확)** — 러너를 VPS에서 돌려 **로컬 PASS·VPS FAIL** 1건이 나왔다: `bot/test_restart_readiness_5_27.py`의 `AUTO_BUY_ENABLED is False`. 실측 대조 — **커밋된** `data/trade_runtime_config.json`은 스위치 7종 전부 `false`·`max_order_amount_krw` **300,000**인데, **VPS 실파일**은 7종 전부 `true`·**5,000,000**/일 25,000,000이다. ★**사고가 아니다** — 파일 안에 근거가 박혀 있다(`_re_apply_by: sajangnim_command_paper_그냥해라` · `_re_apply_note: 네이처셀 포함 5종 PAPER (사장님 명령)` · 5/28 09:44). 오늘 실주문 위험도 없다(`PAPER_ONLY: true`가 최종 방벽·`is_paper_only()=True` 실측). 배포도 안전하다(`deploy_pull.sh`가 stash→pull --ff-only→stash pop 으로 런타임 파일 보존·mtime 8/9 06:00 = 매일 복원 확인). ★**그럼에도 남는 것 3가지**: ①**PAPER_ONLY 하나만 뒤집히면** 7종 전부 ON·건당 500만·일 2,500만이 즉시 유효해진다 — 회귀 테스트가 encode 한 "제한 라이브(300k)" 자세가 **아니다**. 재가동 시 어느 쪽이 정본인지 **사장님 결정** 필요. ②그 테스트는 커밋본을 기준으로 삼으므로 **VPS에서 영구 빨간불**이다 → [F-153]의 "사람이 경보를 끄게 만드는" 마모를 이 파일이 매일 만든다. ③`trade_runtime_config.json`·`kill_switch.json`은 **추적 파일인데 VPS 로컬 수정 상태**라, 누가 이 파일을 커밋하면 `stash pop` 충돌로 **사장님 5/28 설정이 조용히 어긋날 수 있다**(스크립트 자신이 *"WARN: stash pop 충돌 — 수동 점검"*이라 인정). 처방 후보=런타임 상태 파일을 추적에서 빼고 `.example` 템플릿만 커밋 / 또는 테스트가 **커밋본이 아니라 실행 환경의 의도된 자세**를 읽도록 분리.
- **[F-153]** (8/10) **★Codex 게이트가 "결함"과 "주제 라벨"을 구분 못 한다 — 늑대소년 위험 (MED)** — 오늘 F-119 커밋에서 Codex가 **CRITICAL로 차단**했는데, 본문은 정반대였다: *"사장님 룰 위반을 방지하기 위해 테스트가 올바르게 수정되었습니다"* · 총평 *"전체적으로 변경 사항은 사장님 영구 룰을 준수"*. 즉 **Codex는 통과라고 말하면서 `[CRITICAL]` 토큰을 붙였고**(고정 TP·시총별 SL이라는 *주제*의 중대도), 게이트(`tools/codex_pre_commit.py`)는 그 문자열만 grep 해 막았다. 사장님 확인 후 CODEX_SKIP 진행(근거=SAJANG 실측 3종 일치·코드 무변경·41/41 PASS). ★**차단 자체는 fail-closed라 안전 방향이다.** 진짜 위험은 다른 데 있다 — **오탐이 반복되면 `--no-verify`가 습관이 되고, 그때 진짜 CRITICAL이 같이 통과한다.** 이건 [F-40]/[F-121]처럼 *"경보가 죽는" 방향이 아니라 "경보를 사람이 끄게 만드는"* 방향의 마모다. 처방 후보: ①판정을 토큰 grep이 아니라 **구조화 출력**(Codex에 severity 필드를 JSON으로 요구)으로 ②본문에 "위반 없음/준수" 류 문구가 있으면 토큰만으로 차단하지 않고 **사장님 확인 요구로 강등** ③skip 사용 이력을 로그에 남겨 빈도를 관측(현재 `ops/codex_inbox/`에 원본은 남는다). ★교훈=**게이트의 오탐률은 게이트의 수명이다.** ★★**8/10 하루에 2회 발생**(오전 F-119 커밋·오후 전체검수 커밋). 2회차도 본문은 항목마다 *"사장님 룰 위반 **없음**"* 인데 `[CRITICAL]` 라벨이 붙었다. 판정부 실물 확인 = `tools/codex_pre_commit.py:108` `critical_found = "[CRITICAL]" in review_text or "CRITICAL]" in review_text` — **순수 토큰 grep**(진단대로). ★**사장님 결정(8/10) = 게이트는 그대로 두고 SKIP만.** 단타봇이 안전 게이트를 자의로 완화하지 않는다는 뜻이고 옳다. **대신 빈도를 세는 것이 단타봇 몫이다** — 이 줄에 발생 횟수를 누적 기록하고, 늘어나면 사장님께 다시 올린다(1일 2회는 이미 낮지 않다).
- **[F-152]** (8/10) **테스트 러너가 아직 자동으로 안 돈다 ([F-119] 잔여·MED)** — `tools/run_all_tests.py`는 손으로 불러야 한다. pre-commit 훅에 넣으면 커밋마다 30초, nightly에 넣으면 상주 파이프라인 변경이라 둘 다 즉단 사안이 아니다. 후보: ①pre-commit에 **변경 파일과 관련된 테스트만** ②nightly 끝에 1스텝(실패 시 텔레그램) ③아침 08:30 점검에 편입. ★지금 상태 = "돌릴 수단은 생겼으나 아무도 안 부르면 [F-147]과 같은 자리로 돌아간다." ★**배선 시 필수 조건 — 인터프리터를 고정할 것**: 8/10 VPS 실측에서 시스템 `python3.11`은 **36/41**, 실제 운영 인터프리터 `/home/ubuntu/bodyhunter/venv/bin/python3.11`은 **40/41**이었다(차이 4건은 전부 `ModuleNotFoundError`). systemd `ExecStart`와 nightly cron이 쓰는 것은 venv 쪽이다. 즉 **"VPS에서 돌렸다"는 것만으로는 증거가 못 된다 — 어느 인터프리터인지가 결과를 바꾼다**(7/14 "로컬≠VPS"의 한 층 아래).
- **[F-120]** (8/5) **SAJANG 고아 26건 — 헬퍼가 검증만 되고 쓰이지 않는다 (LOW·F-70 확장)** — 상수 외에 **판정 메서드**도 포함: `can_buy`(:331 — `sajang_rules.py:154`가 *"매수 전 호출 의무"*라 명시하고 CLAUDE.md 룰 8도 같은 취지인데 **참조 0건**)·`is_rule_b/c_triggered`·`is_limit_up_split_triggered`·`get_pre_close_d_position`·`get_sync_auto_position` — 전부 유일 참조가 테스트. `get_limit_up_hard_stop`은 참조 0. 매매 코드가 상수를 직접 비교해 룰을 만족시키므로 위반은 아니나, [F-69]가 겨눈 "소비자 존재 검사"의 정확한 사각.

**★ 8/5 오후 소진 (사장님 "나머지는 자동으로 너가 진행") — 9건 완료**

> 진행 기준: 사장님 룰을 **복원**하는 것·명백한 버그·검증 맹점·문서 정합만 자율 진행.
> 룰 자체를 **바꾸거나 완화**하는 건([F-92] 자금비율·[F-93] 손절폭·[F-94] 추매·[F-95] 룰D)은
> 손대지 않고 남겼다. 전건 회귀 검증 후 커밋.

| ID | 조치 | 회귀 검증 |
|---|---|---|
| **F-90 (부분)** | `intraday_eye.py` ALIVE 분기 `0.985` → `SAJANG.get_trailing_sl()` (룰 3 복원) · ★8/7 정정 — 이 행은 원래 `~~F-90~~`(전부 소진)로 그어져 있었으나 **잔여가 있다**(지지/저항 2분기). 항목은 §4에 **열린 채로** 둔다 | 고점 10,000 → 9,850 → **9,700**. 지지선/저항선 기반 2분기(:651·:689)는 **축이 달라 미변경**(사장님 판단 사안으로 잔존) |
| ~~F-96~~ | `fetch_open_orders`·`fetch_balance`에 `rt_cd != "0"` 검사 추가 (fail-open → fail-closed) | 게이트 로직 대조 4케이스 PASS(응답없음·레이트리밋=False / 정상 0건·1건=True). 파일 내 11곳 규약과 일치 |
| ~~F-100~~ | `_evening(ref)` — 과거일 판정이면 벽시계 유예 없음 | 단위: `_evening(어제)=True`·`_evening(오늘)=False`(12시 기준). 실제 검증기 회귀 = 여전히 전 채널 PASS(**데이터가 진짜 신선해서**이지 유예가 아님) |
| ~~F-101~~ | `ticks_health`에 유니버스 대비 **파일 커버리지** 임계(BROKEN<50%·TRUNCATED<90%) + `files`/`file_cov_pct` 출력 | ★**스코어보드 불변 확인**: 73 관측일 전부 99.8%↑ → 유효일 판정 0건 변화. 재실행 **66건 40.9% −39.47%p 동일** |
| ~~F-106~~ | S-6 판정기 "판정 불가" 게이트(오전 완료) | 로컬 낡은 미러에서 5개 blocker와 함께 "⏸ 판정 불가" 출력·on_fail 미상신 |
| ~~F-108~~ | 판정 증거 손상을 `alerts`로 승격 | 음성 대조: `_load_ledger` 몽키패치로 장부 소실 시뮬 → **"판정 근거 소실" alert 발생**. 정상 시 미발생 |
| ~~F-110~~ | CLAUDE.md의 "RULE-001~004" → 실제 구현(**001·002·003·005·006·007·008** + TG/UNI/CSV/IMP-001)으로 정정 | `pre_commit_check.py` 규칙 id 전수 grep으로 확정. **RULE-004는 존재한 적 없음** |
| ~~F-111~~ | `asset_pool_loader.py:467` 죽은 `BASE_DIR` 폴백 제거 | ★**저장소 전수 undefined name = 0건**(선언 아닌 실측). RULE-002 오탐 지뢰 제거 |
| ~~F-113~~ | `STRATEGY_DEADLINES.md`에 현재 활성(S-1·S-6) 표 추가 + S-2~S-5 폐기 명시 | JSON 정본과 대조. 문서 경로로도 8/7 S-6 도래가 보인다 |
| ~~F-114~~ | `trading_calendar`에 `COVERED_YEARS` 파생 + 미등재 연도 ERROR 로그(연 1회) | 회귀 4케이스 PASS(7/17 제헌절·평일·신정·삼일절). 2027 조회 시 ERROR 발화·2회째 억제. ★**날짜는 추측해 넣지 않았다** — 음력/대체공휴일 오기가 같은 종류의 사고이므로 |

- **[F-121]** (8/5) **★`theme:*` 고신뢰 소스가 기여 0건으로 죽어 있다 (MED·[F-40] 유형)** — `asset_pool_loader.load_theme_universe_stocks()`의 주 경로 `/home/ubuntu/jgis/data_store/theme_universe.json`이 **VPS·로컬 양쪽에 실재하지 않는다**(8/5 실측). 폴백은 [F-111]로 애초에 사문이었다. 소비처 `:279`가 `source_map`에 `theme:{subtheme}`를 넣는데 항상 0건이고, `get_high_confidence_candidates` docstring은 *"theme:* 소스 단독으로도 고신뢰 처리"*라 선언한다 = **선정 축 하나가 조용히 사라진 상태**. 8/5 조치=**값을 만들지 않고 없다는 사실을 WARNING 로그로 남김**(선정 동작 불변). 복구는 막내(정보봇) 산출물 배선 사안이라 **사장님 결정**.

**★ 오탐 기각·정정 (7/31 8건 전건 기각의 연속 — 에이전트 보고는 무조건 직접 재검증)**
- ❌ **기각: "S-6 증거가 6/10에서 56일 정지·생산 중단"** — VPS 원본은 **8/4 18:08·168 레코드(6/09~8/04)로 생산 정상**. 에이전트가 **로컬 미러(6/10·5건)를 VPS로 오인**해 판정했다(7/31 오탐 유형 1의 재발). 단 그 밑의 사실은 유효: `sector_reversal_shadow.json`이 sync·freshness 미등록. ★그러나 **sync 미등록은 결함이 아니라 결정**이다 — `sync_from_vps.py:111`이 *"shadow/paper는 의도적 sync 제외(6/10 결정) — 판정은 VPS에서 직접"*이라 명시. 단타봇이 사장님 결정을 뒤집지 않는다. 대신 **판정기에 신선도 가드**를 넣어 로컬 오판을 차단([F-106] fix에 포함).
- ❌ **기각(단타봇 자신의 오경보): "VPS venv에 pyflakes 없음 = RULE-002 사망"** — pre-commit은 VPS에서 돌지 않는다. 훅이 쓰는 로컬 python 3.13에 pyflakes 3.4.0 설치돼 있고 **양성 대조로 실제 차단(HIGH·exit 1) 확인**. 단타봇의 8/5 두 번째 오경보(1차는 ticks 컬럼 인덱스) — **보고 전에 걸렀다.** 잔여 위험은 `check_undefined_names`가 pyflakes 부재 시 **조용히 skip**한다는 것(다른 인터프리터로 커밋하면 무음 통과) → skip 사실을 1줄 출력하도록 보강 필요 (LOW).
- ✏️ **정정: `_get_kis_session` 3벌 "byte-identical"** → 실제로는 **이미 divergent**([F-116]).

### 8/6 신규 등재 — F-122·F-123·F-124 (전건 당일 소진) + F-88 부분 소진

- ~~**[F-124]** (8/6) **RULE-002가 조용히 꺼지는 통로 — pyflakes 부재 시 무음 skip (LOW)**~~ — ✅ **8/6 당일 완료**. `check_undefined_names`가 pyflakes 미설치/실행실패에 **말없이 `[]`를 반환**해서, 다른 인터프리터로 커밋하면 규칙이 통째로 꺼진 채 `[pre-commit] 검수 통과 ✓`만 찍혔다. ★이 규칙은 **66일간 미구현이었다는 사실 자체가 7/31 P0**였는데, 구현 후에도 *"돌았는지"*를 아무도 못 보면 같은 자리로 되돌아간다(8/5 오탐 기각 문단에서 잔여 위험으로 지목해 둔 것). fix=**차단이 아니라 고지**(환경 하나로 작업이 멈추면 안 됨)·커밋당 1회만·설치 명령 병기. 회귀=미설치 3파일 연속 → 고지 1회·반환 전부 `[]`(차단 없음) / 실행실패(OSError) 경로도 고지 / **★양성 대조: 정상 경로는 undefined name 1건 HIGH·line 12 그대로 검출·고지 미발화**.
- ~~**[F-117]** (8/5) **고아 문서가 KRX 복구를 유도한다 (MED)**~~ — ✅ **8/6 완료**. `docs/ETF_LEADER_CRON_SETUP.md`에 **폐기 배너**(최상단 표 — 스크립트 7/1 삭제·cron 7/30 제거·Step B가 KRX 수집·소비자는 신선도 가드로 항상 가산 0) + **cron 블록 직전 등록 금지 경고**(가장 위험한 복붙 지점). ★삭제하지 않고 명시한 이유=7/7 교훈(*삭제 대신 격리·명시*) — 지우면 다음 사람이 처음부터 다시 만든다. `etf_leader_sc` 가산항 제거는 **사장님 결정** 표기.
- ~~**[F-124-2]** (8/6) **★손으로 적은 테스트 목록이 사장님 룰 검증 1건을 삼키고 있었다 ([F-119] 부수·실사고)**~~ — ✅ **8/6 완료**. `data/test_sajang_rules.py`의 러너 목록이 정의된 **21개 중 20개만** 담고 있었고, 빠진 하나가 하필 **`test_morning_auto_buy_disabled`** — 5/27 사장님 *"9시 장시작하자 마자 매수를 했잖아 진짜 코드를 제대로 짜라고"* 사고의 영구 차단(`MORNING_AUTO_BUY_DISABLED`) 검증이다. 분모가 **'실행한 개수'**라 `20/20 PASS`로 나와 **룰 하나가 무검증인 사실이 100% 초록불에 가려졌다**(7/20 *"성공 카운트≠성공 증거"* 동형). 같은 파일 `bot/test_rule_d_dday_5_27.py`는 5개 중 4개만 돌리고 `4/4 PASS`(빠진 것은 pytest `tmp_path` 픽스처 테스트라 **어떤 경로로도 실행 불가**였음 — [F-119] pytest 부재). fix=**손목록 폐기·모듈에서 자동 발견**(`globals()` 스캔 + `tmp_path` 주입) + **분모를 '발견 개수'로**. ★**전수 스윕으로 범위 확정**: AST로 러너 목록 보유 12파일 전건 대조 → 누락은 이 2건뿐(나머지 10개는 정의=목록 일치). 회귀=**21/21·5/5 PASS**(둘 다 이전엔 실행조차 안 되던 테스트 포함) + **음성 대조**(실패 테스트 주입 → 발견 6건·exit 1).
- **[F-88] 부분 소진** (8/5 등재 → 8/6) — 처방 ①`_load_active_codes` docstring 정정 **완료**(*"3채널 동반 정지"*는 사실 아님 → 정지 판별 성립 채널은 **daily 단독**·함수 동작은 원래 맞았고 틀린 건 이유를 설명하던 문장). 처방 ②수급 커버리지 활성 분모는 `verify_channels`가 이미 양쪽 병기 중 → **잔여 없음**.

- ~~**[F-122]** (8/6) **★20:10 하트비트가 8/5 마커 리네임을 못 따라가 죽어 있었다 (HIGH)**~~ — ✅ **8/6 당일 완료**(08aab8d). `notify_data_freshness._morning_ops_heartbeat:189`가 옛 문구 `"발송 완료"`를 찾는데 8/5 리네임 후 성공 마커는 `[ops][SEND_OK]`·실패는 `[ops][SEND_FAIL]`이라 **어느 쪽도 옛 문구를 포함하지 않는다** → ①성공한 날에도 매일 `⚠️ 발송 미확인`이 나가고 ②성공·실패가 **구분 불가**(진짜 미발송에도 평소와 같은 ⚠️) = *"감시자를 감시하는"* 계층(7/31 Tier1 H-2b)이 조용히 사망. ★**근본은 어제 내가 적은 원칙을 어제 내가 안 지킨 것** — 마커를 상수로 단일진실화하며 *"쓰는 문자열과 읽는 문자열이 갈리면 카운트가 조용히 0이 된다"*고 써 놓고 **파일 밖 소비자를 세지 않았다**. fix=`MARK_SENT`/`_LEGACY_SENT` import 위임(단일진실을 파일 경계 밖까지·import 실패해도 20:10 발송은 안 죽게 폴백). 회귀 4케이스(신마커·옛마커·실패·미실행) + VPS `--dry-run` **`✅ 아침점검(08:30) 실행·발송`** 실측.
- ~~**[F-89 잔여]** (8/5→8/6) **실발송 경로 미검증**~~ — ✅ **8/6 08:30 cron 첫 실전으로 검증 완료**. 발송 성공(`[ops][SEND_OK]`·1회차)이나 **본문 마지막에 `⚠️ 지난 1회 미수신(2026-08-06)` 거짓 병기가 붙어 나갔다 = 발송에 성공한 메시지가 자기 자신을 미수신으로 보고**(사장님께 없는 사고를 보고). 원인=`main()`이 자기 헤더를 `print`한 **뒤에** `pending_unsent()`로 자기 로그를 읽는데, cron이 `-u`(무버퍼) `>>`라 **헤더가 먼저 파일에 닿아** 자기 블록이 *"마커 없이 끝난 죽은 블록"*으로 잡혔다. 어제 Tier1로 보강한 '죽은 블록 계수'(OOM/SIGKILL 대응·옳은 보강)가 이 오작동의 원료였고 **틀린 것은 읽는 시점**이었다. ★**어제 22/22 PASS가 왜 못 잡았나** = 픽스처가 `pending_unsent()`를 합성 로그로 직접 호출해서 `main()`의 stdout이 그 파일로 흘러드는 **배선 층이 테스트에 없었다**(5/25 *"단위 테스트 PASS ≠ 실제 적용"*과 같은 형태·대상만 매매→알림). fix=**읽기를 쓰기 앞으로**(버퍼링 방식과 무관하게 자기참조가 구조적으로 불가) + 회귀에 `buffering=1` append 핸들로 cron `-u >>` 재현 층 신설(**구코드 음성 대조 = 정확히 2건 실패**) + 스캐너 접두 규칙을 신규 마커·dry 토큰까지 확대. 32→**37/37 PASS**.
- ~~**[F-123]** (8/6) **`ticks_health`가 행 생존성을 안 본다**~~ — ✅ **8/6 당일 완료**(05c2a87→**3e66b89 재설계**). fix=`_count_live_files()` 전수·단축평가(살아있는 종목은 첫 행에서 확정·박제만 끝까지 읽어 **2,527파일 ≈1.9초**) + `ticks_health(day, deep=False)` 옵트인 + `verify_channels`에 **⑧-2 틱 생존성** 행 신설. VPS 실측 8/5 **살아있음 2,486/2,527(98.38%)·박제 41건**. ★**verdict 판정식 무접촉** — 74 관측일 전수 재대조에서 verdict·usable_pct·files·file_cov_pct·last_med **차이 0건**, VPS 92일 루프 **OK 65일 동일** = 유효일 불변 → 스코어보드 불변. ★★**자기 적발(중요)**: 1차 구현(05c2a87)은 기존 **40개 표본**에 얹어서 **1/40 = 2.5%p 눈금**이었는데 실제 박제는 **1.6%** — VPS에서 최근 전 일자가 `live 100.0`으로 나와 *"박제 0"*으로 읽혔다 = **없는 청정을 보고하는 지표**. **해상도보다 작은 효과는 그 해상도로 못 잰다**([[feedback-resolution-measurement-trap-7-24]]의 재발이고 이번엔 **내가 밟았다** — 그 교훈을 내가 쓴 메모리에 적어두고도). ★`deep` 기본값 False인 이유=`f21/f83/f86/f87/r1/r4/r5`·`observe_v2_paper`가 이 함수를 **90여 일 루프**로 호출(전수가 기본이면 도구마다 ~3분) → **켜지 않으면 값을 내지 않는다(None)**: 못 잰 것을 잰 것처럼 보이는 숫자를 두지 않기 위해. 원문 보존:
- **[F-123-원안]** (8/6) **`ticks_health`가 행 생존성을 안 본다 — 정지 종목 박제를 정상으로 계수 (LOW·매매 영향 0 실증)** — 정지 종목의 8/5 ticks는 **전 시간대 `price=전일종가 / change=0 / strength=0 / volume=0`의 박제**다(033170 실측·정상 종목 005930은 strength 59~88·volume 100만+). `price>0`이므로 유효율에 **정상으로 집계**된다: 8/5 2,527파일 중 **박제 40건(1.6%)**·8/4 37건(1.5%), 박제 39/40이 daily 결손과 정확 대응(나머지 1건 278990은 daily 거래량 20주의 초저유동성=수집실패 아님). 즉 *"수집 성공"*으로는 맞으나 **"판정 원천이 살아있는 종목 수"는 2,527이 아니라 2,487**이다. [F-101](8/5)에서 **파일 커버리지** 축을 넣으며 **행 생존성** 축을 못 본 사각. ★**매매 영향=0 (가정 아닌 실증)** — 구조가 7/17 제헌절 유령신호와 같아 (날짜,종목) 쌍으로 장부 17일 **전 신호 1,057건 전수 대조 → 박제된 날에 잡힌 신호 0건**. PB-A v2의 **체결강도 200+ 게이트**가 `strength=0`을 구조적으로 차단(7/17엔 게이트가 없었다). 종목 단위 교집합은 5종이 나오지만 그건 *"언젠가 신호였다"*이지 *"박제된 날에"*가 아니므로 **쌍 대조가 유일한 정답**. 처방=`ticks_health`에 `live_files`(강도·거래량 중 하나라도 >0) 병기 — 유효일 판정식은 **건드리지 않는다**(스코어보드 불변 유지·[F-101] 규약).

- **[F-13]** (7/17) **universe 재편입 파이프** — prune 도구는 제거만 함. 거래재개 종목(1~4주 정지군 포함)·신규상장 추가는 KIS 마스터/순위 API 결합 별도 설계 필요 (LOW·월 1회 prune 재실행 시 함께 검토).

### 8/7 신규 등재 — F-125·F-126 + 장부 정합(LEDGER-1 신설)

- **[F-125]** (8/7) **⑥ "수급 4종" 로그가 폐기 stub 2종을 성공처럼 남긴다 (LOW·[F-53] 계열)** — nightly ⑥이 `{'investor': 2531, 'foreign_exh': 2422, 'short_bal': 0, 'short_vol': 0}`로 **완료**를 찍는다. 실측 확인: `flow_collector.py:860` 주석대로 short 2종은 **KRX 중단 후 의도된 no-op**이고 실제 공매도는 ⑬(KIS)가 100.0%로 담으므로 **데이터 사고 아님**. 그러나 로그만으로는 *폐기*인지 *고장*인지 구분 불가 — [F-53]에서 ⑫가 6종 전멸에도 `status:ok`였던 것과 같은 통로. 처방=⑥ 반환에 종류별 상태(ok/retired/failed) 부여 또는 명칭을 "수급 2종(+폐기 2)"으로 정직화. 매매 무관.
- **[F-126]** (8/7) **실측 도구가 수집 창과 겹치면 없는 차이를 만든다 (LOW·방법론)** — [F-82] 신·구 판독기 전수 대조에서 `005300 foreign_exh 8/5`가 **구=False 신=True**로 1건 튀었다. 원인 추적 결과 **코드 차이가 아니라 19:40 재수집이 그 파일을 쓰는 중**이었고, 구·신을 순차로 읽는 사이 행이 늘었다(파일 자체는 재조회 시 양쪽 동일). ★교훈=**대조 실측은 수집 창(18:00 nightly·19:40 재수집·20:10) 밖에서 돌린다.** 안 그러면 레이스를 결함으로 보고하게 된다.

**★ 8/7 장부 정합 — 모순 9건 해소 + LEDGER-1 신설**

> 8/5 소진 커밋(70148cb)이 완료 **표만 덧붙이고** 원 불릿의 취소선을 긋지 않아 같은 ID가 §4 안에서 '열림'과 '완료'로 **동시 존재**(9건). 8/7에 단타봇이 [F-100]을 미소진으로 오인해 **이미 고친 것을 다시 고칠 뻔했다.** 열린 항목 99 → **90**.

| 처리 | 대상 | 근거 |
|---|---|---|
| 소진 표기 | F-96·F-100·F-101·F-108·F-110·F-111·F-113·F-114 (8/5·70148cb) | 8/7에 **코드에서 각각 재확인** 후 표기 |
| 소진 표기 | F-88 (8/6·05c2a87) | 완료 표기가 **아예 없던** 건 — 모순도 아니라 영구 미소진으로 남을 뻔했다 |
| **열어 둠** | F-90 | ★8/5 표가 `전부 소진`으로 그었으나 **부분**이다(ALIVE 분기만 복원·지지/저항 2분기 잔존·커밋 메시지도 그렇게 적었다). 표를 사실로 정정하고 항목은 **열어 둠 — 잔여는 사장님 결정** |
| 재발 차단 | **LEDGER-1** (`tools/pre_commit_check.py`) | 열림+완료 공존 시 HIGH 차단. ★음성 대조로 **구 장부에서 9건 전건 검출** 확인(`tests/test_backlog_ledger_8_7.py` 14/14). 자기참조 가드=인라인 코드 인용은 미계수(8/7 실제 발생) |

> ★사정권 정직 표기 — LEDGER-1은 **'조용한 소진'은 못 잡는다**(F-88형). 코드와 장부를 대조해야 하는데 일반화가 안 된다. 막는 것은 *"닫은 걸 안 닫힌 것처럼 남겨두는"* 통로 하나뿐이다.

### 8/9 전체검수 신규 등재 — F-127~F-143 (에이전트 4축 + 단타봇 VPS 실측)

> 검수 범위: 8/9 새벽 유입 4커밋(FLOWX·미검증) + 실매매 안전 + 사장님 룰 + 슬러지/문서 + 중복구현.
> ★에이전트 보고는 전건 단타봇이 재검증했고, **기등재 재발견은 신규로 세지 않았다**([F-70]·[F-77]·[F-90]·[F-91] 등).

**★ 봇 재가동 전 필수 (HIGH)**

- ~~**[F-127]**~~ ✅ **소진 8/9** — **COO 알림 채널 전체 사망 (HIGH)** — `TradingCOO.__init__`은 `bot/auto_trader/cfo/cto`만 설정하는데 알림 job 4곳이 **미정의** `self._send_alert`(8참조)·`self.trader`(2참조)를 썼다. 전부 `if not X: return` **뒤**라 **보고할 게 있을 때만** AttributeError로 죽었다(실패 양상 반전). ★VPS 실측 14일 = 봇시야 **140**·상한가연속 **52**·섹터동조 **26**건. ★7/31 P0-3가 봇시야의 KST NameError를 고치면서 **5줄 아래 이 버그는 안 건드려** "고쳤다" 선언 후에도 계속 죽었다. fix=같은 파일 21곳이 쓰는 `getattr` 정본으로 복원 + `tests/test_coo_alert_wiring_8_9.py`(AST·7/7·음성대조 8+2 확인).
- **[F-128]** (8/9) **★FLOWX emit이 매도 close 앞에 비가드로 놓임 (HIGH·봇 재가동 차단)** — `trade_tracker.py:497·546`(매수측 `:429·767`). 같은 파일 기존 계측은 **전부** `try/except: pass` + "비차단(매도 무손상)" 주석인데 **신규 emit 4곳만 벌거벗은 채 상태 커밋 앞**에 있다. `PaperEventRejected`는 `ValueError` 서브클래스라 전파 → `check_paper_prices`에서 raise 시 같은 try의 **PaperPortfolio 블록 전체(-10% 서킷 포함)가 스킵**된다. 처방=emit을 `try/except`로 감싸고 `close()`/`register()` **뒤**로 이동(F-129·F-130 폭발반경 동시 차단). ★다른 세션의 **의도된 fail-closed** 설계를 뒤집는 것이라 단타봇 단독 변경 안 함 — 사장님 확인.
- **[F-135]** (8/9) **74일 박제 ACTIVE 행 (HIGH·재가동 전 정리)** — `data_store/active_trades.json` `updated`가 **2026-05-27 09:08** 정지, `204320` `hold_start 5/27`·`time_stop_days 5`로 74일째 ACTIVE. `paper_close_eod`가 돌았으면 진작 닫혔어야 한다 = **이 코드는 프로덕션에서 한 번도 실행된 적 없다**("오류 로그 없음"이 검증 증거가 못 됨). 재가동 첫 15:25에 이 행이 곧바로 F-128 경로로 들어간다.

**신규 유입 코드 (FLOWX) — 프로세스·배선**

- **[F-136]** (8/9) **★기록↔실제가 정반대 + 정규 배포경로 우회 (HIGH·프로세스)** — 커밋 메시지는 *"VPS 실측 미실행: 상위 지시상 push/deploy 금지"*인데 실제로는 **push·VPS배포·systemd enable·1회 실행(01:56)** 전부 완료. 게다가 `deploy_pull.log`에 8/9 기록 **0건**이고 VPS reflog엔 수동 `pull --ff-only` **4회** = `deploy_pull.sh`가 하는 **kill_switch·trade_runtime_config 보존이 미작동**한 채 pull. 이번엔 kill_switch 무사했으나 **가드가 작동한 게 아니라 운이 좋았던 것**. 봇도 01:51 재시작돼 **미검증 `trade_tracker.py`가 가동 중 프로세스에 적재**됨(실주문은 kill switch로 차단).
- **[F-129]** (8/9) **dedupe 키에 가격이 없어 재시도가 영구 잠금 (MED)** — `flowx_paper_adapter.py:131-136` event_id에 가격·수량 미포함인데 `:258-266`은 전 필드 일치를 요구 → **같은 거래를 다른 가격으로 재기록하면 무시가 아니라 예외**. 테스트(`:230`)는 재시도 가격을 동일하게 넣어 happy path만 본다(8/6 "픽스처가 실패 층을 우회"의 변형).
- **[F-130]** (8/9) **장부 1줄 손상 = 이후 모든 페이퍼 청산 정지 (MED)** — `:319-330` `iter_events()`가 파싱 실패 시 raise, `emit()`이 매번 경유. fail-closed 자체는 옳으나 **대상이 장부 쓰기가 아니라 매매 상태 전이까지 번졌다**.
- **[F-131]** (8/9) **`cost_complete`가 구조적으로 영원히 0 (MED)** — `entry_fee/exit_*/data_asof/source_record_id` 등을 **신규 4파일과 그 테스트 밖에서 생산하는 곳이 0건**. 18:00 summary는 `cost_complete_rate: 0.0`을 영원히 출력한다(초록으로 변할 수 없는 지표). 테스트가 green인 건 `setattr`로 직접 넣기 때문.
- **[F-132]** (8/9) **신규 산출물 sync·⑳ 미등록 — ★7번째 재발 (MED)** — `data_store/flowx_paper_events/`·`flowx_paper_summary/latest.json`이 `sync_from_vps.CORE_FILES`·`DATA_DIRS`·⑳ freshness·§2 운영표 **전부 0건**. CLAUDE.md가 "작성 시점에 같이"라 명시한 그 항목.
- **[F-133]** (8/9) **18:00 타이머 — 휴장일 가드·실패알림 0 (MED)** — `OnCalendar=*-*-* 18:00:00` + `Persistent=true`로 **주말·공휴일 포함 매일**, `summarize_flowx_paper.py`에 `is_trading_day()` 0건. `OnFailure=` 0건이라 **죽어도 아무도 모른다**. ★`as_of`가 데이터 신선도가 아니라 **자기 실행 시각**이라, 나중에 ⑳에 물리면 데이터 0건인 날도 GREEN(7/7 완료위장 재현). ★단타봇 정정=nightly와 18:00 동시각이나 **nightly가 flowx 무참조라 데이터 레이스는 아니다**(초기 판단 과했음).
- **[F-134]** (8/9) **`trade_id` 포맷 변경 + 스키마 확장 (LOW)** — `PC_{YYYYMMDD}_{code}` → `paper:KR:{code}:{uuid4hex}`. 결정적 키가 **난수**가 되어 재실행 멱등성·사후 감사 조인이 사라짐(파싱 소비자 0건이라 즉시 회귀는 없음·의도된 변경).

**실매매 안전 (재가동 전 사장님 결정)**

- **[F-138]** (8/9) **★sync_auto 포지션이 3중으로 매도 차단인데 문서는 반대 (HIGH·사장님 결정)** — `auto_trader.py:357-360`·`position_safety.py:118-120`·`manual_position_protection.py:39`가 전부 `sync_auto_unknown` prefix를 차단 → 트레일링·룰B·룰C·EOD·hard_kill **전부 무효인 순수 buy&hold**. 그런데 `position_safety.py:199-202` 주석과 CLAUDE.md 룰 10은 *"트레일링/룰C 자동 작동 OK"*라 명시. 코드 3층이 일관되므로 버그가 아니라 **문서와 구현 중 무엇이 정본인지 사장님 결정**.
- **[F-139]** (8/9) **`dynamic_target.py`가 SAJANG 참조 0건 — 라이브 SL이 −5~−8% (HIGH·사장님 결정)** — `:130-137`이 `market_health.get_regime_rules()`의 `sl_max_pct`(레짐별 0.05~0.08)로 SL을 만들고 `auto_trader.py:702·3532`에서 `pos["stop_loss"]`로 직행. 사장님이 `NORMAL_SL_PCT`를 바꿔도 **라이브 SL은 안 따라간다**.
- **[F-140]** (8/9) **부분체결을 완전청산으로 오인 (HIGH)** — `_wait_for_fill`(kis_trader.py:1585)이 부분체결에도 True. `smart_sell`이 `filled_qty`를 계산하는데 **repo 전체에서 읽는 소비자 0건** → `auto_trader.py:3831-3839`가 `success`만 보고 `_positions.pop()`. ★최악 연쇄=hard_kill(−5%·하한가·미체결 확률 최고) → 접수만 되고 장부 삭제 → sync가 `sync_auto_unknown`으로 재등록 → **[F-138]에 걸려 자동방어 영구 무효**.

**슬러지·문서·중복 (LOW~MED)**

- **[F-137]** (8/9) **`deploy/bodyhunter.service`가 부재 경로 3개를 가리킴 (MED)** — `/home/ubuntu/Prophet-Agent-System/{,.env,scalper-agent/venv/bin/python}` 전부 **VPS에 없음**, 엔트리포인트도 실가동본과 다름(`bot/telegram_bot.py` vs `run_bot.py --once`). 배포하면 봇이 안 뜬다. 실가동본은 `/etc/systemd/system/bodyhunter-bot.service`.
- **[F-141]** (8/9) **문서 스테일 12건 (MED)** — ★`REVIEW_3TIER_RULE.md:106-115`가 **8/5에 CLAUDE.md에서 고친 그 결함을 그대로 보존**(RULE-004 구현 0건인데 표에 있고, 실제 차단하는 9개는 표 밖). CLAUDE.md:30이 이걸 "메인 문서(필독)"이라 지정. + CLAUDE.md:80-82가 아직 *"RULE-002가 is_trading_day 자동 차단"*이라 주장(실제는 pyflakes undefined-name) + LEDGER-1이 어느 문서 목록에도 없음 + REVIEW_3TIER_RULE에 Codex/Tier4 0회 + "자동화" 주장 2건 구현 0(변경라인수 임계·post-commit 회귀) + nightly 29단계(실제 30) + STRATEGY_DEADLINES §3 판정기록 0건(JSON엔 4건) + ⑯ 자동metric이 삭제된 스텝을 가리킴 + CLAUDE.md 경로 기준 혼재(`utils/` 4건이 루트에서 미해결) + "다른 11개 job"(실측 138중 71).
- **[F-142]** (8/9) **중복 구현 클러스터 (MED)** — 텔레그램 raw 발송 **12벌**(`daily_learner.py:50`·`position_guardian.py:51`은 **바이트 동일**)·chunking 4벌·KST 정의 **22곳**+표현 18회+`_today*` 래퍼 14개(**같은 이름 다른 반환형** 2쌍)·`_get_kis_session` 3벌(**각자 다른 가드를 잃음** — `market_flow_collector`는 라이브 토큰 probe 누락, `extend_parquet_data`는 CWD락·stale purge 누락)·mojito broker 생성 10벌·`_safe_int/_safe_float` **26정의 3변종**·atomic write 50+ 인라인·`load_dotenv` **5가지 루트 경로**. ★최우선=`_get_kis_session` 1본화(`flow_collector.py:60`이 superset).
- **[F-143]** (8/9) **★phantom import — 09:15 픽 알림이 항상 실패 (MED)** — `bot.telegram_bot.send_telegram_message`·`send_message` **둘 다 저장소 정의 0건**. `tools/daytrading_picks.py:681·687`이 1·2차 폴백 모두 phantom이라 `send_telegram()`이 **항상 False**를 반환하고 실패 2줄만 print. `tools/foreign_accumulation_scanner.py:299`는 폴백조차 없음. `verifiers/_common.py:26`은 HTTP 폴백이 있어 무해.

**★[F-127] 잔여 — 사장님 결정**: AttributeError는 고쳤으나 **알림 채널 자체가 자동매매 스위치에 묶여 있다.** `telegram_bot.py:2823` `if auto_trade and not auto_trade_locked:` 안에서만 `auto_trader.start(_send_alert)`가 호출되는데 kill switch가 5/28부터 ON이라 **`_send_alert`가 한 번도 주입된 적이 없다**(VPS 30일 `자동매매 시작` 로그 **0건**·INFO는 26,415건 잡히므로 유의미). 결과: `getattr` 정본 21곳도 `alert_fn=None`으로 **조용히 skip**. 즉 매매와 무관한 **관측 알림(섹터동조·상한가연속·봇시야·바닥신호)까지 매매 스위치에 인질**로 잡혀 있다. 처방 후보=관측 알림을 20:10·08:30이 쓰는 `output/telegram_alert.TelegramAlert` 경로로 분리. **매매 동작 변경이 아니나 알림 정책 변경이라 사장님 승인 후 착수.**

**8/9 추가 등재 — F-144~F-146 (phantom import 축)**

- **[F-144]** (8/9) **★섹터 로테이션 게이트가 영구 무효 (HIGH·[F-53] 계열)** — `data/limit_up_engine.py:326`이 `from data.rotation_detector import detect_rotation`을 부르는데 **그 이름은 저장소에 없다**(정본은 `analyze_rotation`·`rotation_detector.py:347` — 리네임이 호출부를 두고 갔다). `except Exception → logger.debug`(디버그라 평시 안 보임)로 삼켜져 `_get_sector_rotation()`이 **항상 기본값** `{"phase":"미확인","favorable":True}`를 반환. 소비자 3곳 전부 무증상 열화: `:746` 매수 reasons에 로테이션이 **한 번도 안 붙음** · `:866` `monitor_days_adj`가 **항상 5**(LATE/REVERSAL→3·EARLY/STAGING→7 분기 도달 불가) · `:1073` 게이트가 **아무것도 막은 적 없음**. 7/31 RULE-002·[F-53]과 같은 모양 = **쓰여 있으나 돌지 않는 가드**.
- **[F-145]** (8/9) **★킬스위치 가드가 fail-open — 호출부 (HIGH·잠재)** — `bot/telegram_bot.py:1389-1400` `try: from bot.trade_kill_switch import is_auto_trade_disabled … except Exception: pass` 직후 `:1402 self.auto_trader.start(...)`. 심볼은 **현재 존재하므로 라이브 버그는 아니나**, import가 실패하면 가드가 **로그 한 줄 없이 사라지고 자동매매가 시작**된다. 정본 `trade_kill_switch.py:30-33`은 파일 부재·예외에 **True(차단)**로 fail-closed인데 **호출부가 그 보호를 무효화**한다. 형제 3곳 비교 = `position_safety.py:409-414`가 최선(logger.error + 보수적 실패), `trading_coo.py:143-157`·`vwap_split_buy.py:48-57`은 무로깅 False. 처방=`position_safety` 형태로 통일.
- **[F-146]** (8/9) **phantom import 11건 — [F-143] 확장 (MED)** — `bot/telegram_bot`에 `send_message`·`send_telegram_message` **둘 다 모듈 레벨 정의 0건**인데 **9곳**이 import한다: `data/event_detector.py:679`·`data/market_health.py:1198`·`data/volume_scanner.py:332`·`tools/swing_scan.py:700`(전부 `--telegram` 플래그 **사망**) · `tools/daytrading_picks.py:681·687`(1·2차 폴백 **둘 다 phantom** = 09:15 픽 알림 항상 False) · `tools/foreign_accumulation_scanner.py:299·307`. ★`verifiers/_common.py:26`만 HTTP 폴백이 있어 **살아있으나 100% 백업 엔진으로** 돌고 있고, 실패 로그가 ImportError를 *"token/chat_id 없음"*으로 보고해 **진단을 오도**한다(이게 nightly·무인점검 알림 척추다). + `data/etf_recommender.py:232 load_sector_report` phantom(`except: pass`) → `sector_hot`이 **항상 `{}`** → ETF 추천의 섹터 모멘텀 축이 조용히 0 기여([F-74] 재확인). ★F821 미정의 이름은 **341파일 전수 0건** — 7/31 RULE-002 게이트는 유지되고 있다.

**8/9 최종 등재 — F-147·F-148 (데드코드 4중확인 축)**

- ~~**[F-147]** (8/9) **★테스트처럼 생겼으나 실행 경로가 없다 (MED)**~~ — ✅ **8/10 완료** ([F-119] 러너로 동시 소진). 그 파일은 이제 **실제로 돌고 2/2 PASS**다(`tools/run_all_tests.py --exec tests/test_flow_collector_naver_frgn.py`). 어제 "회귀 보호 0"이라 보고한 자리가 오늘 보호를 갖췄다. ★더 중요한 건 **같은 모양이 다시 생기지 못하게 막은 것** — 러너의 `classify()`가 "test_ 함수는 있는데 `__main__`도 최상위 def도 없다"를 `UNRUNNABLE`로 판정해 **FAIL(exit 1)** 을 낸다. 조용히 초록불을 주지 않는다(음성 대조로 고정). 원 진단 보존: `tests/test_flow_collector_naver_frgn.py`(115줄)는 `__main__` **0건**이고 pytest 픽스처(`tmp_path`/`monkeypatch`)를 쓰는데 **pytest 미설치·`pytest.ini`/`conftest.py`/`pyproject.toml` 전부 0건**([F-119]). 즉 **어떤 경로로도 돌 수 없어 회귀 보호가 0**인데 파일명·구조는 보호가 있는 것처럼 보인다. ★같은 검사에서 무근거 삭제를 막은 대조도 함께 확인 = `bot/test_scenario_fields_5_31.py`·`test_score_kki_5_31.py`·`tools/test_zerorow_guard_6_22.py`는 **모듈 레벨에서 실행되므로 살아있다**(죽지 않았다). ★교훈=**"테스트가 있다"≠"테스트가 돈다"** — 5/25 "단위 PASS ≠ 실제 적용"의 한 층 앞.
- **[F-148]** (8/9) **살아있는 모듈 안의 고아 함수 14개 (LOW·[F-70] 확장)** — 각각 **전 프로젝트에서 자기 `def` 줄 1건만** 존재(문자열·문서·cron 포함 토큰 인덱스로 확인·동적 디스패치 0). ★패턴 2가지가 의미 있다: ①**"목표가 API" 3종 세트가 통째로 미배선** — `premium_levels.get_target_levels`·`gap_support.get_gap_target_levels`·`equal_level_detector.get_eq_target_levels`(세 모듈 자체는 라이브·다른 함수는 쓰임) ②**반쪽 CRUD** — `event_detector`에서 `get/update/remove_macro_theme`는 `telegram_bot`이 쓰는데 **`add_macro_theme`만 호출자 0** = 매크로 테마를 **추가할 방법이 없다**. 나머지=`collect_5min_yfinance`·`fetch_post_bars_ohlc`·`scan_candidates_trix`·`get_regime_context`·`score_quant_candidate`·`detect_gap_realtime`·`_fetch_foreign_rate_kis_snapshot`·`get_policy_bonus`·`get_hot_theme_codes`·`get_etf_market_direction`.
- **[F-146 보강]** `data/csv_loader.py`는 `class CSVLoader`만 정의하는데 `data/flowx_universe_updater.py:133`이 `from data.csv_loader import load_stock_data`를 부른다 — **저장소 전체에 그 이름이 없다**(phantom 12번째·[F-74] 기존 지적 재확인).

**8/10 등재 — S-6 폐기 작업 중 발견**

- **[F-149]** (8/10) **★활성 전략이 0이 되면 A9가 영구 초록불 (MED — 8/29 도래)** — S-6 폐기로 `strategy_deadlines.json`의 `status=='active'`가 **S-1 하나만** 남았다. 지금은 S-1이 매일 ❌미달로 alerts를 띄워 무음이 아니지만, **8/29에 S-1이 판정되면 active=0 → ⑲-4 alerts=0 → `daily_ops_check:341 check_a9_deadlines`가 alerts만 보므로 아침 A9 영구 초록불**이 된다. 그 뒤로는 대장이 깨져도(스키마 변형 [F-36]·장부 손상 [F-108]) 어느 계층도 짖지 않는다. ★이건 8/5 [F-108] 원 진단이 *"8/7에 S-6가 정리되면 8/8~8/25 alerts=0"*로 예고했던 상태가 **S-1 하나 덕분에 미뤄졌을 뿐**임을 뜻한다 — 예고가 빗나간 게 아니라 **유예된 것**이다. fix 방향 = "active 전략 0건"을 정상이 아니라 **WARN**으로(운영기에 심판할 전략이 없다 = 진화가 멈춘 상태) + `score` 경로도 A9 판정에 반영([F-108] fix가 alerts만 승격한 절반).
- **[F-150]** (8/10) **DAILY_ROUTINE의 nightly 단계 수가 실제와 1 어긋나 있었다 (LOW)** — §2 표가 *"현재 29단계"*라 적고 있었으나 8/10 이전 실제는 **30단계**였다(nightly.log `요약: 30/30`·AST 실측 30). 오늘 ③ 제거로 우연히 29가 되어 **틀린 숫자가 저절로 맞아버린** 자리다. A2 판정은 로그 한 줄 안의 `ok == total` 동적 비교라 **판정에는 영향 없었다**(하드코딩 없음을 확인함). 그래서 LOW지만, 성격은 [F-100]/[F-110] 계열 — **문서가 구현을 안 세고 적은 수치**. 근본 처방은 문서에 숫자를 적지 않거나(범위 표기) pre-commit이 세는 것. ★기록 이유 = *우연히 맞은 것을 맞았다고 두면 다음에 또 어긋난다.*

## §5. 자기성찰 템플릿 (매일 업무일지 마지막 섹션)

```markdown
## 자기성찰 (7/16 사장님 지시 — 매일 개선하는 단타봇)
1. **오늘 잘한 것**: (1~3개, 데이터/근거 포함)
2. **미흡·실수**: (숨기지 않기 — 정직이 자산. 없으면 "재점검했으나 발견 못함"이라 쓰고 내일 다시)
3. **새 교훈**: (영구 메모리 저장할 것인지 O/X 판정 포함)
4. **내일 적용 1가지**: (§4 각주에서 골라도 됨 — 구체적 행동 1개)
5. **데이터 성찰**: 전일 ⑲-3 페이퍼 장부에서 미스/특이 1건 → 근본원인 1줄 (단타봇 강점 = 매일 숫자가 나온다 — 웹봇 프레임 7/16 채택)
```

**운영 원칙**: 성찰은 반성문이 아니라 **개선 엔진**이다. ②에서 찾은 것이 다음날 ④가 되고, ④가 각주를 소진하고, 교훈이 메모리가 된다. 매일 1%씩.

**3단 승격** (웹봇 프레임 7/16 채택): 각주(1회 관찰) → 체크리스트 항목(반복 확인 시 §1 표 승격) → 영구 규칙(메모리/코드 규칙화). 미반영 각주는 다음 날 자동 승계.

### ★ 8/14 전체검수 (사장님 지시 — 코드·로직·버그·슬러지) 신규 등재 F-175~F-177

> ★이번 검수의 출발점은 사장님 데이터 점검 지시였다. 판정은 **전 채널 정상**이었는데,
> 수확은 **게이트 자체를 의심한 것**이다 — 표본 10종이 결손 2%를 81.7% 놓친다는 계산에서
> [F-33]·[F-34]가 이미 15일 열려 있었음을 발견했고, [F-33]은 당일 소진했다.
> ★오늘 단타봇이 **두 번** 같은 실수를 했다: 이미 등재된 것([F-33]·[F-34])을 "새 발견"으로
> 착각. 둘 다 **착수/등재 직전 장부 확인**으로 막았다 — 그 습관이 [[feedback-ledger-breaks-both-ways-8-7]]의 실효다.

- **[F-175]** (8/14) **★수집 파이프라인 cron 4개가 `-u` 없이 돌아 로그가 버퍼링된다 (MED)** — `crontab` bodyhunter 항목 7개 중 **`-u` 있는 것은 사람이 보는 2개뿐**(`notify_data_freshness`·`daily_ops_check` = 7/31·8/5에 만든 것). 실제 수집·관측 파이프라인 **4개는 전부 버퍼링**: `run_nightly_pipeline`·`run_foreign_exh_late_recollect`·`observe_v2_runner`·`intraday_learning_v2`. ★**증상 실측(오늘 당한 것)** — 19:40 재수집 cron이 정상 기동(PID 182756·19:40:01)했는데 **7분간 로그가 0바이트**라 단타봇이 "미실행"으로 오판할 뻔했다(프로세스 직접 확인으로 정정). ★**더 큰 위험은 진단 유실**: 파이썬 기본 stdout 버퍼는 8,192B인데 **오늘 nightly 블록 전체가 8,269B**다. 즉 파이프라인이 죽으면 **마지막 미flush분(최대 8KB)이 사라지고**, 그 크기가 곧 전체와 맞먹으므로 초반 크래시면 사실상 진단 불가다. (과장 방지 표기: 정상 종료 시엔 flush되므로 평소 로그는 온전하다 — 문제는 **비정상 종료 때**다.) ★fix는 한 줄(`python3.11 -u`)이지만 **crontab = VPS 상주 인프라 변경**이라 7/10 룰대로 **사장님 승인 사안**. 값 무변경 등재.
- **[F-176]** (8/14) **소진율 검증은 아직 표본 10종 — [F-33] 전수화의 사각 (LOW)** — `notify_data_freshness._foreign_exh_status:75` 가 `_load_active_codes(10, today)` 를 쓴다(DataVerifier CHECKLIST 미포함 보조 체크라 [F-33] 범위 밖이었다). ★**표본의 불안정성이 오늘 눈에 보였다** — 동시점 구/신 대조에서 같은 데이터인데 한 번은 `9/10`, 한 번은 `10/10` 이 나왔다(랜덤 표본이라 시행마다 흔들림). 실제 전수는 **2,417/2,477 = 97.6%**. ★이 채널은 부분 실패가 상시(오늘 실패 88종)라 전수화하려면 PARTIAL 임계 설계가 함께 필요하다 — [F-33]처럼 **정상 대역 실측 후** 임계를 정할 것(추측 금지). 다음 소진 후보.
- **[F-177]** (8/14) **`try/except` 뒤 `pass` 가 bot/·verifiers/ 에 98건 (LOW·미조사)** — 7/4 [fix5] 교훈(*try/except 완료위장으로 nightly ④가 이틀 사망*)의 잠재 재발면. ★**핵심 경로는 깨끗하다**(`data_verifier`·`notify_data_freshness`·`daily_ops_check` = **0건**). 98건 전수 판별은 이번 검수 범위를 넘어 **계수만 하고 등재**한다(정직 표기: *"위험하다"가 아니라 "안 봤다"*). 우선 조사 대상 = 매매 경로(`bot/auto_trader`·`position_safety`)와 nightly 스텝 러너.
- **[F-178]** (8/21) **일지 표의 계수에 분모 정의 병기 (LOW)** — 8/14 일지 소진율 칸(2,408·2,417)은 *전체 파일 기준*인데 표 머리말은 "활성 분모 전수"라 정의가 섞여 있었다. 8/21 소급 점검에서 활성 교집합(2,359·2,379)과 나란히 놓자 **−49·−38 = "과거행 삭제 재발"로 보였고**(8/13 사고와 동형), 파일 전체 스캔으로 재측정해서야 **삭제 0건·기록 바이트 재현**을 확인했다. 정의 없는 숫자는 다음 날의 오보 재료 → 소진율은 **전체/활성 두 값 병기**, 다른 채널도 분모를 칸에 적는다.
- **[F-182]** (8/29) **★"사후에 계산 가능"은 "관측 중"이 아니다 (HIGH·당일 소진)** — 8/21 [S-8] 등재 때 *"일봉 종가 신호라 사후 재구성 100% 가능 → 관측 인프라 추가 0"* 이라 판단했다. 판단 자체는 맞지만 **그 결과 8일간 아무 기록도 없었고 아무도 보고 있지 않았다** — 10/2 판정까지 6주가 통째로 blind가 될 뻔했다. 7/17 사장님 대장의 취지는 *'판정 없이 무기한 도는 것'의 차단*이고, **판정 대상이 매일 보이지 않는 것**은 같은 실패다. → fix `tools/s8_observe.py` + 체커 호출(**새 cron·새 스텝 0** — 체커는 이미 nightly ⑲-4 + 08:30 등록·비용 **실측 3.5초**·graceful 실패 시 alerts 승격). 첫 관측치 = 신호 330건·완성 3일·**리프트 +0.12%p**(0과 구별 불가)·표본 15/150.
- **[F-183]** (8/29) **★폐기한 전략이 스코어보드 첫 줄을 대표하고 있었다 (HIGH·당일 소진)** — 8/21 [S-1] 폐기 시 신호를 선정 피처로 보존하려고 러너·⑲-3 장부를 **유지**했는데, 그 부작용으로 폐기 전략의 페이퍼가 계속 누적됐다: **8/21 121건 −67.93%p → 8/29 148건 −85.91%p (8일간 −17.98%p)**. 그 숫자가 매일 20:10·08:30 **첫 줄**로 사장님께 갔다. **판정 대상이 아닌 것이 첫 줄을 대표하면 오보다.** → fix ①라벨 `📉 [S-1 폐기 8/21·판정 대상 아님]`(값·집계 무변경·문구만) ②첫 줄을 [S-8] 관측치로 교체(7/17 약속의 취지는 *숫자로 시작하라*이고 그 숫자는 **판정 대상**의 것이어야 한다). ★잔여 = **러너 cron 중단 여부는 상주 인프라라 사장님 결정**(현재 유지·실주문 0).
- **[F-184]** (8/29) **`supabase_key_verify.py` cwd 의존 (LOW·당일 소진)** — 8/21에 내가 만든 도구가 `sys.path.insert(0, os.getcwd())`라 `cd scalper-agent`에서 돌리면 상위 `shared/`를 못 찾아 죽었다. `tools/manual/`의 다른 도구는 처음부터 `__file__` 기준인데 **이것만 빠져 있었다**. fix 후 두 위치 모두 4/4. ★고치면서도 **경로를 세다 한 단계 틀렸고**(3단계→실제 4단계) `parents[0..3]`을 출력해 정정 — **세지 말고 찍어라.**
- **[F-181]** (8/21) **★VPS에 tracked 파일을 수동 복사하면 17:45 자동배포가 조용히 실패한다 (HIGH·당일 차단)** — `deploy_pull.sh`는 `git pull --ff-only`이고, 런타임 보존(stash) 대상은 **`kill_switch.json`·`trade_runtime_config.json`·`universe.json` 3개뿐**이다. 그 밖의 tracked 파일이 VPS에서 modified면 pull이 거부되고 로그에 *'pull FAILED (non-ff/conflict) — 수동 점검 필요'* 만 남는다(**알림 없음**). ★오늘 실제로 그 상태였다 — 검수 중 `strategy_deadline_check.py`·`strategy_deadlines.json`을 VPS에 직접 복사했고, 그대로 뒀으면 **오늘 밤 nightly가 S-8 없는 구버전 대장을 읽어 '활성 전략 0건' 오보를 사장님께 발송**할 뻔했다([F-89] '없는 사고를 보고' 계열의 재발 경로). 게다가 커밋한 코드 9건 전부가 VPS에 안 갔을 것. ★당일 처리 = `git checkout HEAD -- <파일>` 후 정상 pull(로컬 변경 62줄이 커밋본의 부분집합이라 손실 0 확인 후 실행)·untracked 사본 제거·**운영 상태 파일은 무접촉**(kill_switch/trade_runtime modified 유지 확인). ★**규약** — VPS 실측을 위해 파일을 직접 복사했으면 **세션 종료 전 `git status`로 원복**한다. 복사는 `/tmp`에서 실행하는 방식을 우선(오늘 `lift_screen.py`만 repo 안에 복사해 문제가 됐고, `cap5`·`s8_gate`는 /tmp 실행이라 무해했다). ★잔여 = deploy_pull 실패 시 **알림이 없다**는 것 자체 (미소진).
- ~~**[F-179]** (8/21) 전략 판정 기준에 시장 벤치마크가 없다~~ — ✅ **8/21 당일 소진**(4dba752). 근거: [S-1] 기준이 `>0`인데 같은 기간 **시장 동일가중 T+3 +0.37%p·T+5 +0.98%p** — 아무거나 사도 플러스인 구간에서 `>0`은 통과선이 아니었고, 명분 축은 시장 대비 **−6.05%p(t −6.33)**로 가치를 파괴하는데 절대수익만 보면 '그냥 마이너스'로 보인다. fix = ①`strategy_deadlines.json` note에 **신규 등재 `benchmark` 필드 의무** 명문화 ②체커가 benchmark 없는 활성 전략을 매일 alerts로 표면화 (★**자동 폐기·자동 실패 처리는 안 함** — 이 도구의 불변식 유지). ★같은 커밋에 **활성 전략 0건 침묵 차단** 동봉 — [S-1] 폐기로 active=0이 되자 체커가 스코어보드+헤더만 출력하고 alerts=0이 됐다(**이 파일 주석이 경고하던 위험구간이 실현**). 경보 마모 방지로 **경과일 병기**, 상태 파일 없이 대장 `reported` 최댓값 역산(8/5 규약). 검증 = 신규 테스트 **18/18**(음성대조 2건: 구코드가 두 케이스 모두 놓침) · 전체 회귀 **49/49** · Codex 0건 · **VPS 운영경로 실측**(alerts 1건·A9 WARN 승격).
- **[F-180]** (8/21) **`paper_learning.insights`에 날 효과로 교란된 수치가 실려 있다 (MED)** — *"ma20 지지 vs 이탈 d3: −1.43 vs −3.75 (지지 +2.32%p)"* 가 매일 nightly ⑤-2에서 출력되는데, **동일일 대조(38일 짝)에서 리프트는 −0.22%p**(플러스날 15/38 · t −0.33)다. 지지 종목이 좋은 날에 많이 나온 것을 종목 우위로 읽은 것. 6/13 '이평선 게이트 승격 후보'의 근거이기도 해 **승격되면 없는 엣지로 매매할 뻔했다**. → 처방: `_insights()`의 단순 그룹 비교를 **동일일 대조 리프트**로 교체(도구는 오늘 만든 `a_control.py`). read-only 산출물이라 매매 영향 0이나, **근거로 인용되는 순간 위험**이라 MED.
- ~~**[S-7 후보]** (8/21) 휴면 명분주~~ — ❌ **당일 기각(8/21 오후)**. 등재 전 *"이 조건 중 어느 것이 일하는가"* 를 분해했더니 **명분 키워드 기여가 음수**였다: 시장 전체 pos20≤0.05(명분 무관·일 137종) T+3 리프트 **+0.38%p** vs 명분∩저점권(일 7.2종) **−1.76%p** → 기여 **−2.14%p**(T+5 −1.92 · T+10 −3.71). 즉 어제 본 pos20 플러스는 **명분 덕이 아니라 pos20 팩터 자체**였고 명분 필터는 그것을 깎는다. ★[F-25](등재 전 신호잔존 선검증) 규약이 **없는 전략을 30거래일 관측하는 것**을 막았다. ★남은 것 = **pos20 팩터**(저점권 +0.36~+1.52·지평 길수록↑·단 t 0.6~1.8 약함)와 **[G-1] 고점권 회피**(pos20 0.90+ T+3 **−1.31%p · t −2.37** = 8/21 측정 중 **t>2를 넘긴 유일한 방향성**). 근거 = `docs/전략_방향_판정_8_21.md` §7-③.
- **[T-1]** (8/21) **리프트 스크리닝 — 탐색 절차 (사장님 승인 대기)** — 우리는 수급·공매도·신용을 **100% 깨끗하게 유지하는데 어떤 전략도 쓰지 않고**, 그 피처가 시장 대비 리프트를 내는지 **한 번도 잰 적이 없다**. 절차 = 후보 피처(외국인/기관 순매수 연속일·공매도 잔고 급감·신용 감소·소진율 변화 등) 전수 → **동일일 대조 리프트 + t** → **t>2 인 것만** 후보 등재. **통과 0건이면 '0건'이라 보고**(억지 후보 금지). 도구는 8/21에 만든 `a_control.py` 재사용. ★이건 전략이 아니라 **탐색 절차**라 데드라인 부착 가능(제안: 승인일+10거래일).
  - ★**8/21 1라운드 실행 완료** — 피처 21종 · 유니버스 2,531종 · 55거래일(5/22~8/11) 동일일 대조. **매수 후보 t>2 통과 0건**(규약대로 0건 보고). 최고가 `외인 3일 연속 순매수` +0.31%p·**t +1.87**로 문턱 아래. ★대신 **회피 축 2건이 통과**: `pos20 고점권(≥0.90)` T+3 **−1.60%p·t −2.84**, `기관 3일 연속 순매수` T+3 **−0.34%p·t −2.43**. ★★**외인(+1.87)과 기관(−2.43)이 정반대**이고 둘을 AND로 묶으면(`외인+기관 동시`) −0.12로 나빠진다 = **기관이 신호를 오염시킨다** → 2라운드 축 도출.
  - ★**2라운드(외인 매수 ∩ 기관 매도 계열 + T+10 지평 추가)** — t>2 통과 4건. ★단 **2라운드 피처는 1라운드 결과를 보고 만든 것이라 in-sample 편향**(같은 데이터에서 가설 생성·검정)이라 **walk-forward 전에는 후보로 인정하지 않는다**(7/30 R-5가 이 검사로 기각된 전례). ★T+10에서 회피 축이 더 강해짐: `기관 3일 매수` **t −5.57** · `신용비중 높음(rate>2)` **t −3.80** · `pos20 고점권` **t −2.99** — 회피 축이 매수 축보다 일관되게 강하다.
  - ★**다중검정 정직표기** — 27피처를 재면 |t|>2가 우연히 ~1.4건 나온다(양측 5%). 실제 9건이라 전체적으로는 우연 이상이지만 **개별 항목의 진위는 walk-forward로만** 가린다.
  - 도구 = `tools/manual/lift_screen.py`(read-only·`--wf` walk-forward 모드 내장).
  - ★**walk-forward(100일 A/B 분할)** — 통과 7건 중 **상위 3건은 두 구간 리프트 크기까지 일치**(외인3일매수∩기관3일매도 **A +0.41 / B +0.42** · ∩기관순매도(당일) **+0.26 / +0.27** · ∩pos20≤0.5 **+0.17 / +0.20**). t는 표본수에 좌우되지만 **크기 재현은 진짜 효과의 특징**. 셋 다 같은 구조 = **외국인이 사고 기관이 파는 종목**이고 **단독(외인만)보다 조합이 더 크고 더 일관**. ★walk-forward가 내 결론 3건을 뒤집음: 공매도잔고 감소(A +6.40/**B −0.08** = A구간 이상치)·기관3일매수 회피축(A **+1.20**/B −3.49 = 불가)·pos20 고점권(A t −0.91/B t −2.49 = **구간 의존적**, '유일하게 확실'은 과했다).
  - ★**cap5 관문 통과**(`tools/manual/cap5_constraint_test.py`) — 100일 8,263건(일 82.6건). S-1은 전신호 +0.173 → cap5 **−0.609**로 부호가 뒤집혔는데(장중 순차 = 선착순 역선택), 이 축은 **어떤 선택 규칙에서도 리프트 부호 유지**(코드순 +0.27·강도상위 +0.32·무작위 +0.47~+0.79). **일봉 종가 동시 발생이라 그 경로가 원천적으로 없다** = 구조적 차이 실측 확인. 단 **거래대금 상위만 −0.22로 파괴**(대형주 배제 근거).
  - ★★**미해결 — 절대수익 전환 실패**: 전신호 리프트 +0.41%p인데 **건당 절대 −0.441%p**. 역산하면 이 100일 구간 **시장 T+3 ≈ −0.65%p**다. 즉 [F-179] 근거로 쓴 '시장 +0.37'은 **6/29~8/20 구간 값**이고 3/18~8/11에선 **부호가 반대**. → **정정**: *"기준 >0이 너무 낮았다"는 구간 의존적 표현*이었다(벤치마크 필요성은 오히려 강해짐 — 시장이 구간마다 ±0.5%p 넘게 흔들리므로 절대 기준은 어느 방향으로도 무의미). → **이 축은 단독 불가 · BROAD_DOWN 회피 게이트 결합 필수**.
  - ⚠**슬리피지 경고**: 유일한 절대 플러스인 `거래대금 하위`(+0.118%p/건)는 **유동성 낮은 종목**이라 체결 슬리피지가 그 크기를 통째로 먹을 수 있다. **이 줄을 근거로 라이브를 논하지 않는다.**
  - ✅**[S-8] 등재 완료 (8/21 · D-42 · 2026-10-02)** — ★등재 직전 2건 추가 실측이 **내 제안을 둘 다 기각**: ①**BROAD_DOWN 게이트 결합 역효과**(건당 +0.118 → **−0.049**·표본 100일→44일) — *"게이트 결합 필수"* 기각 ②**거래대금 1억 필터는 in-sample 튜닝**(전체 t +2.58로 최고였으나 walk-forward **A구간 −1.305**로 무필터 −0.780보다 나쁨). ★★**모든 조합에서 A구간 마이너스** = cap5 플러스는 전부 B구간(6~8월) 효과. ★**대조군(외인 단독)이 cap5에서 더 좋다**(+0.499 vs −0.211) → 기관 조건이 **리프트 단계엔 이롭고 실행 단계엔 해로울** 가능성. ⇒ **층 구분**: 전 신호 리프트 ✅통과(A +0.41/B +0.42) / **cap5 실행 규칙 ❌없음**. → **"돈 버는 전략"이 아니라 "실행 규칙을 찾는 관측"으로 등재**. 판정=두 관문 모두(①리프트>0 통과 ②5건 규칙이 A/B 양 구간 net>0 **미충족=과제**)· benchmark `market_equal_weight_same_day_T3`·대조군 외인 단독·최소 150건·**3번째 연장 금지**·**관측 인프라 추가 0**(일봉 신호라 사후 재구성 100%). 도구 `tools/manual/s8_gate_slippage_test.py`. ★슬리피지 실측 정정: 하위 5건 중앙 **0.63억**·30만원 주문은 중앙값의 **0.48%** — 명목 30만원에선 치명적이지 않다(최소 0.041억대만 위험).

---

**Last updated**: 2026-08-14 ([F-33] 표본→전수 + [F-164] 모순표기 **당일 소진**(0ed7e50) · [F-34] 정량화+연쇄 보강 · F-175~177 신규. 매매 룰 무변경·실주문 0)
**직전**: 2026-08-07 ([F-82] 날짜 판독 정본화 소진 + **장부 정합**(모순 9건 해소·열림 99→91) + LEDGER-1 신설. 매매 룰 무변경)
**불변식**: 실매매 OFF·실주문 0·매도 무손상·KRX 무접촉·SAJANG 단일진실·관측 없이 flip 금지
