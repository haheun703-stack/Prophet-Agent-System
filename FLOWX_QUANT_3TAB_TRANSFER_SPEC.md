# FLOWX 퀀트봇 이관 지시서 — 피보나치/섹터 로테이션 3탭

## 프로젝트 소개
이 프로젝트는 **Body Hunter v4** — 한국 주식 단타봇(자동매매 시스템)입니다.
KIS API(한국투자증권) 실계좌 매매, 텔레그램 봇 UI, 7-Group COO 파이프라인,
BRAIN 시장분석, NXT 야간매매, FLOWX VIP 콘텐츠를 포함합니다.
CFO(재무)/CTO(기술)/COO(운영) 3-Officer 구조로 운영됩니다.

---

## 1. 이관 범위

기존 퀀트 대시보드(6탭: BRAIN판단/섹터수급/섹터모멘텀/섹터로테이션/ETF수급/ETF추천)를 **제거**하고,
아래 3개 패널로 교체합니다. 데이터는 모두 `dashboard_swing` 테이블의 JSONB 컬럼에서 가져옵니다.

| 탭 | 컬럼명 | 데이터 원본 | 종목 수 | 갱신 주기 |
|----|--------|-------------|---------|-----------|
| **전체 피보나치 눌림목** | `fib_stocks` | bottom_scan.json (시총 1조+) | 최대 50 | 매일 16:30 |
| **대형주 피보나치** | `fib_leaders` | fib_leaders.json (시총 TOP 30) | 30 | 매일 16:30 |
| **섹터 로테이션 맵** | `sector_rotation` | fib + 수급CSV + 등락률 | 10~17섹터 | 매일 16:30 |

---

## 2. 공통 사항

### 2-1. 데이터 소스
```sql
SELECT fib_stocks, fib_leaders, sector_rotation
FROM dashboard_swing
WHERE date = '2026-04-10'
ORDER BY date DESC LIMIT 1;
```

### 2-2. 빈 데이터 처리
3개 컬럼 모두 `null`, `[]`, `{}` 가능. 빈 경우:
```
데이터가 없습니다. 다음 갱신 시 업데이트됩니다.
```

### 2-3. 탭 배치
```
┌─────────────────────────────────────────────┐
│ [전체 피보나치] [대형주 피보나치] [섹터 로테이션] │  ← 탭 선택
├─────────────────────────────────────────────┤
│                                             │
│           선택된 탭의 콘텐츠                   │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 3. 탭 1: 전체 피보나치 눌림목 (`fib_stocks`)

### 3-1. 데이터 구조

```typescript
// fib_stocks: FibStock[]  (최대 50종목)
interface FibStock {
  code: string;           // "000250" — 종목코드
  name: string;           // "삼천당제약"
  sector: string;         // "제약"
  cap: number;            // 260613 (시총, 억원)
  price: number;          // 609000 (현재가, 원)
  w52h: number;           // 1233000 (52주 고점)
  w52l: number;           // 127600 (52주 저점)
  drop: number;           // -50.6 (52주 고점 대비 하락률 %)
  fib_zone: string;       // "DEEP" | "MID" | "MILD" | "SHALLOW"
  fib_zone_label: string; // "50%+ 하락 (바닥 매수 구간)"
  fib_382: number;        // 549862 (38.2% 되돌림 가격)
  fib_500: number;        // 680300 (50% 되돌림 가격)
  fib_618: number;        // 810737 (61.8% 되돌림 가격)
  fib_status: string;     // "38.2% 아래 (깊은 하락)"
  target: number;         // 1048050 (목표가)
  upside: number;         // 72.1 (상승여력 %)
  per: number;            // 3700.9
  pbr: number;            // 69.85
  frgn: number;           // 5.58 (외국인 보유율 %)
}
```

### 3-2. 실제 데이터 예시
```json
[
  {"code":"128940","name":"한미약품","sector":"제약","cap":68459,"price":252000,"w52h":433500,"drop":-41.9,"fib_zone":"DEEP","fib_zone_label":"50%+ 하락 (바닥 매수 구간)","fib_382":279685,"fib_500":337850,"fib_618":396014,"fib_status":"38.2% 아래 (깊은 하락)","target":368475,"upside":46.2,"per":36.0,"pbr":3.2,"frgn":20.9},
  {"code":"263750","name":"펄어비스","sector":"게임","cap":43810,"price":78600,"w52h":128200,"drop":-38.7,"fib_zone":"DEEP","fib_382":77102,"fib_500":91850,"fib_618":106597,"fib_status":"50%~61.8% 사이","target":108970,"upside":38.6,"per":13.1,"pbr":1.17,"frgn":44.6},
  {"code":"035720","name":"카카오","sector":"인터넷","cap":235282,"price":43300,"w52h":68400,"drop":-36.6,"fib_zone":"DEEP","fib_382":47854,"fib_500":55475,"fib_618":63095,"fib_status":"38.2% 아래 (깊은 하락)","target":58140,"upside":34.3,"per":20.5,"pbr":0.75,"frgn":33.7}
]
```

### 3-3. UI 디자인

#### 헤더
```
📐 피보나치 눌림목 — 바닥 매수 후보
52주 고점 대비 크게 하락한 우량주 (시총 1조+)
```

#### fib_zone별 섹션 그룹핑
종목을 `fib_zone`별로 묶어서 표시. 서버에서 이미 DEEP→MID→MILD→SHALLOW 순 정렬됨.

```
── 🔴 50%+ 하락 (바닥 매수 구간) — 13종목 ──

| 종목       | 섹터   | 하락률  | 피보나치 위치  | 상승여력 | PER  | 외국인 |
|-----------|-------|--------|-------------|--------|------|-------|
| 카카오뱅크  | 금융   | -38.8% | 38.2% 아래   | +39.0% | 15.2 | 33.1% |
| 알테오젠    | 바이오 | -38.1% | 38.2% 아래   | +37.4% | 55.3 | 12.8% |
| ...

── 🟠 40~50% 하락 (중간 눌림) — 8종목 ──

| 삼천당제약  | 제약   | -50.6% | 38.2%~50%   | +72.1% | 3700 | 5.6%  |
| ...

── 🟡 30~40% 하락 (1차 눌림) — 9종목 ──
...

── 🟢 15~30% 하락 (얕은 조정) — 12종목 ──
...
```

#### fib_zone 색상 규칙

| fib_zone | 아이콘 | 배경색 | 텍스트 | 의미 |
|----------|--------|--------|--------|------|
| DEEP     | 🔴 | #FEF2F2 | #DC2626 | 50%+ 하락, 바닥 매수 |
| MID      | 🟠 | #FFF7ED | #EA580C | 40~50% 하락, 중간 눌림 |
| MILD     | 🟡 | #FFFBEB | #CA8A04 | 30~40% 하락, 1차 눌림 |
| SHALLOW  | 🟢 | #F7FEE7 | #65A30D | 15~30% 하락, 얕은 조정 |

#### 컬럼 포맷

| 필드 | 표시명 | 포맷 | 비고 |
|------|--------|------|------|
| name | 종목 | 텍스트 | 굵게 |
| sector | 섹터 | 텍스트 | |
| drop | 하락률 | `-38.8%` | 빨간색 |
| fib_status | 피보나치 위치 | `38.2% 아래` | |
| upside | 상승여력 | `+39.0%` | 초록색 |
| per | PER | `15.2` | |
| frgn | 외국인 | `33.1%` | |
| cap | 시총 | `6.8조` 또는 `2,606억` | cap >= 10000 → 조, else → 억 |

#### 정렬
- 기본: fib_zone별 그룹핑 후 하락률 큰 순 (서버에서 정렬됨)
- 선택: 상승여력순, PER순, 외국인순 토글

#### 피보나치 게이지 (선택, 종목 클릭 시)
```
52주 저점 ━━━━ 38.2% ━━━━ 50% ━━━━ 61.8% ━━━━ 52주 고점
                            ▲
                        현재 위치
```
- `fib_382`, `fib_500`, `fib_618`, `price`, `w52h`, `w52l`로 위치 계산

---

## 4. 탭 2: 대형주 피보나치 (`fib_leaders`)

### 4-1. 데이터 구조

```typescript
// fib_leaders: FibLeader[]  (30종목, 시총 TOP 30)
interface FibLeader {
  code: string;        // "005930"
  name: string;        // "삼성전자"
  sector: string;      // "전기전자"
  cap: number;         // 10560634 (억원)
  price: number;       // 206000 (현재가)
  w52h: number;        // 223000 (52주 고점)
  w52l: number;        // 52900 (52주 저점)
  w52h_date: string;   // "20260227" (고점 날짜)
  drop: number;        // -7.62 (하락률 %)
  fib_382: number;     // 117878
  fib_500: number;     // 137950
  fib_618: number;     // 158021
  fib_status: string;  // "고점 근접" | "38.2% 아래 (깊은 하락)" | ...
  fib_zone?: string;   // 자동 계산 (drop 기반)
  per: number;         // 40.4
  pbr: number;         // 3.45
}
```

### 4-2. 실제 데이터 예시
```json
[
  {"code":"005930","name":"삼성전자","sector":"전기전자","cap":10560634,"price":206000,"w52h":223000,"w52l":52900,"w52h_date":"20260227","drop":-7.62,"fib_382":117878,"fib_500":137950,"fib_618":158021,"fib_status":"고점 근접","per":40.4,"pbr":3.45},
  {"code":"000660","name":"SK하이닉스","sector":"전기전자","cap":5915430,"price":1027000,"w52h":1099000,"w52l":162700,"drop":-6.55,"fib_382":520366,"fib_500":630850,"fib_618":741333,"fib_status":"고점 근접","per":35.0,"pbr":9.37},
  {"code":"005380","name":"현대차","sector":"운송장비","cap":953147,"price":489500,"w52h":687000,"w52l":175800,"drop":-28.75,"fib_382":371078,"fib_500":431400,"fib_618":491721,"fib_status":"50%~61.8% 사이","per":11.0,"pbr":1.2}
]
```

### 4-3. 핵심 차이점 (vs 탭 1)
- **탭 1 (전체 피보나치)**: 시총 1조+ 중 **크게 하락한 종목** 위주 (바닥 매수 후보)
- **탭 2 (대형주 피보나치)**: 시총 TOP 30 **무조건 표시** (하락률 무관, 삼성전자도 포함)

### 4-4. UI 디자인

#### 헤더
```
📊 대형주 피보나치 — 시총 TOP 30
하락률 무관, 국내 대표 대형주의 피보나치 레벨
```

#### 테이블 (그룹핑 없이 한 테이블)

```
| # | 종목       | 섹터     | 시총      | 현재가      | 하락률   | 피보나치 위치        | PER  |
|---|-----------|---------|----------|-----------|--------|------------------|------|
| 1 | 삼성전자    | 전기전자 | 105.6조  | 206,000   | -7.6%  | 고점 근접          | 40.4 |
| 2 | SK하이닉스  | 전기전자 | 59.2조   | 1,027,000 | -6.6%  | 고점 근접          | 35.0 |
| 3 | 현대차      | 운송장비 | 9.5조    | 489,500   | -28.8% | 50%~61.8% 사이    | 11.0 |
| ...
```

#### fib_status 색상 매핑

| fib_status | 색상 | 의미 |
|------------|------|------|
| 고점 근접 | 🟢 초록 | 52주 고점 90% 이상 |
| 61.8% 위 (회복 중) | 🟢 연초록 | 61.8% 위로 회복 |
| 50%~61.8% 사이 | 🟡 노랑 | 중간 구간 |
| 38.2%~50% 사이 | 🟠 주황 | 눌림 구간 |
| 38.2% 아래 (깊은 하락) | 🔴 빨강 | 깊은 하락 |

#### 컬럼 포맷

| 필드 | 표시명 | 포맷 |
|------|--------|------|
| name | 종목 | 굵게 |
| sector | 섹터 | 텍스트 |
| cap | 시총 | `105.6조` (cap/10000) 또는 `9,531억` |
| price | 현재가 | `206,000` (콤마) |
| drop | 하락률 | `-7.6%` (빨강) / `+0.5%` (초록, 양수일 때) |
| fib_status | 피보나치 위치 | 위 색상표 적용 |
| per | PER | `40.4` |

#### fib_zone 자동 분류 (프론트에서 계산)
`fib_zone` 필드가 없는 경우 `drop` 값 기반 자동 부여:
```typescript
function getFibZone(drop: number): string {
  const abs_drop = Math.abs(drop);
  if (abs_drop >= 50) return "DEEP";
  if (abs_drop >= 40) return "MID";
  if (abs_drop >= 30) return "MILD";
  if (abs_drop >= 15) return "SHALLOW";
  return "NEAR_HIGH";  // 대형주 전용 — 고점 근접
}
```

#### 정렬
- 기본: 시총순 (서버에서 정렬됨)
- 선택: 하락률순, PER순

#### 피보나치 미니 게이지 (각 행에 인라인)
```
[저점 ━━━━━ 38.2 ━━ 50 ━━ 61.8 ━━━━━ 고점]
                                    ▲ 현재가
```
- 좁은 바 형태로 각 행 옆에 표시 (선택사항)

---

## 5. 탭 3: 섹터 로테이션 맵 (`sector_rotation`)

### 5-1. 데이터 구조

```typescript
// sector_rotation: SectorRotation
interface SectorRotation {
  timestamp: string;       // "2026-04-10 16:40"
  total_sectors: number;   // 17
  total_stocks: number;    // 279
  sectors: Sector[];       // 섹터 배열 (점수순 정렬)
}

interface Sector {
  sector: string;          // "2차전지"
  count: number;           // 23 (해당 섹터 종목 수)
  total_score: number;     // 85.2 (종합 점수)
  momentum: number;        // 32.5 (모멘텀 점수, 등락률 × 10)
  flow_score: number;      // 22.7 (수급 점수, ±30 상한)
  dual_bonus: number;      // 30 (쌍매수 보너스, 3종목×10)
  avg_chg: number;         // 3.25 (평균 등락률 %)
  avg_drop: number;        // -35.2 (평균 하락률 %)
  avg_upside: number;      // 48.3 (평균 상승여력 %)
  net_flow_억: number;     // 1250 (순수급, 억원)
  dual_buy_3d: number;     // 3 (3일 쌍매수 종목 수)
  up_count: number;        // 18 (상승 종목 수)
  down_count: number;      // 5 (하락 종목 수)
  deep: number;            // 5 (DEEP zone 종목 수)
  mid: number;             // 3
  mild: number;            // 8
  shallow: number;         // 7
  cap_조: number;          // 12.5 (섹터 시총 합, 조원)
  cap_억: number;          // 0 (1조 미만일 때만 사용)
  warning: string;         // "" 또는 "개인 주도 상승 (수급 미확인)"
  stage: string;           // "선도" | "추격" | "대기" | "후발"
  stage_num: number;       // 1~4
  stage_color: string;     // "GREEN" | "YELLOW" | "RED"
}
```

### 5-2. 실제 데이터 예시
```json
{
  "timestamp": "2026-04-10 16:40",
  "total_sectors": 12,
  "total_stocks": 180,
  "sectors": [
    {
      "sector": "2차전지", "count": 15, "total_score": 72.3,
      "momentum": 25.0, "flow_score": 17.3, "dual_bonus": 30,
      "avg_chg": 2.50, "avg_drop": -38.2, "avg_upside": 55.1,
      "net_flow_억": 850, "dual_buy_3d": 3,
      "up_count": 12, "down_count": 3,
      "deep": 4, "mid": 3, "mild": 5, "shallow": 3,
      "cap_조": 18.5, "cap_억": 0,
      "warning": "", "stage": "선도", "stage_num": 1, "stage_color": "GREEN"
    },
    {
      "sector": "반도체", "count": 12, "total_score": 45.1,
      "momentum": 15.0, "flow_score": 20.1, "dual_bonus": 10,
      "avg_chg": 1.50, "avg_drop": -25.3, "avg_upside": 32.0,
      "net_flow_억": 1200, "dual_buy_3d": 1,
      "up_count": 8, "down_count": 4,
      "deep": 1, "mid": 2, "mild": 4, "shallow": 5,
      "cap_조": 85.2, "cap_억": 0,
      "warning": "", "stage": "추격", "stage_num": 2, "stage_color": "GREEN"
    }
  ]
}
```

### 5-3. UI 디자인

#### 헤더
```
🔄 섹터 로테이션 맵
피보나치 + 수급 + 모멘텀 기반 자금 흐름 예측 (12섹터 / 180종목)
2026-04-10 16:40 기준
```
- `total_sectors`, `total_stocks`, `timestamp` 사용

#### 로테이션 단계별 그룹핑

```
━━━ 🟢 선도 섹터 (total_score 50+) ━━━

┌─────────┬──────┬──────┬────────┬────────┬───────┬──────┬──────────────────┐
│ 섹터     │ 종합  │ 종목수│ 모멘텀  │ 수급(억)│ 쌍매수 │ 상승/하│ 경고               │
├─────────┼──────┼──────┼────────┼────────┼───────┼──────┼──────────────────┤
│ 2차전지  │ 72.3 │ 15   │ +2.50% │ +850   │ 3종목  │ 12/3 │                    │
│ 정유     │ 55.0 │ 8    │ +1.80% │ +420   │ 2종목  │ 6/2  │                    │
└─────────┴──────┴──────┴────────┴────────┴───────┴──────┴──────────────────┘

━━━ 🟢 추격 섹터 (total_score 20~49) ━━━

│ 반도체   │ 45.1 │ 12   │ +1.50% │ +1200  │ 1종목  │ 8/4  │                    │
│ 전력     │ 32.0 │ 9    │ +0.80% │ +300   │ 1종목  │ 5/4  │                    │

━━━ 🟡 대기 섹터 (total_score 0~19) ━━━

│ 건설     │ 12.5 │ 7    │ +0.30% │ -50    │ 0종목  │ 3/4  │                    │

━━━ 🔴 후발 섹터 (total_score 0 미만) ━━━

│ 방산     │ -15.0│ 6    │ -1.20% │ -800   │ 0종목  │ 1/5  │ 개인 주도 상승      │
```

#### stage 색상 규칙

| stage | stage_num | 색상 | 배경 | 의미 |
|-------|-----------|------|------|------|
| 선도 | 1 | #16A34A | #F0FDF4 | 자금 유입 중, 매수 고려 |
| 추격 | 2 | #22C55E | #F0FDF4 | 자금 유입 시작, 관심 |
| 대기 | 3 | #EAB308 | #FEFCE8 | 횡보/관망 |
| 후발 | 4 | #DC2626 | #FEF2F2 | 자금 이탈, 매수 주의 |

#### 컬럼 포맷

| 필드 | 표시명 | 포맷 | 비고 |
|------|--------|------|------|
| sector | 섹터 | 텍스트 | 굵게 |
| total_score | 종합 | `72.3` | 양수=초록, 음수=빨강 |
| count | 종목수 | `15` | |
| avg_chg | 모멘텀 | `+2.50%` | 양수=초록, 음수=빨강 |
| net_flow_억 | 수급 | `+850억` | 양수=초록(유입), 음수=빨강(이탈) |
| dual_buy_3d | 쌍매수 | `3종목` | 0이면 회색 |
| up_count/down_count | 상승/하락 | `12/3` | |
| warning | 경고 | 텍스트 | 빈 문자열이면 숨김, 있으면 주황 뱃지 |

#### 섹터 클릭 시 상세 (선택)
```
2차전지 (15종목 · 18.5조)
  평균 하락: -38.2%  |  평균 상승여력: +55.1%

  피보나치 분포:
  🔴 DEEP 4 │ 🟠 MID 3 │ 🟡 MILD 5 │ 🟢 SHALLOW 3

  점수 구성:
  모멘텀 +25.0 | 수급 +17.3 | 쌍매수 +30.0 = 72.3점
```
- `deep`, `mid`, `mild`, `shallow` 필드로 피보나치 분포 표시
- `momentum`, `flow_score`, `dual_bonus` 필드로 점수 구성 분해

#### 정렬
- 기본: 종합 점수순 (서버에서 정렬됨)
- 선택: 수급순 (`net_flow_억`), 모멘텀순 (`avg_chg`), 쌍매수순 (`dual_buy_3d`)

---

## 6. 데이터 흐름 요약

```
[매일 16:00~16:30]
  bottom_scan.json ──┐
  fib_leaders.json ──┤──→ upload_swing.py
  flow/*_investor.csv┘        │
                              ├──→ _build_fib_stocks()      → dashboard_swing.fib_stocks
                              ├──→ _build_fib_leaders()      → dashboard_swing.fib_leaders
                              └──→ _build_sector_rotation()  → dashboard_swing.sector_rotation
                                                                    │
[프론트엔드]                                                          │
  Supabase SELECT ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
    ↓
  3개 탭 렌더링
```

---

## 7. Supabase 컬럼 확인

`dashboard_swing` 테이블에 아래 3개 JSONB 컬럼이 이미 존재합니다:
```sql
-- 이미 생성 완료
fib_stocks     JSONB DEFAULT '[]'::jsonb
fib_leaders    JSONB DEFAULT '[]'::jsonb
sector_rotation JSONB DEFAULT '{}'::jsonb
```

RLS: `service_role`만 쓰기 가능, `anon`은 읽기만 가능 (기존 정책 적용).

---

## 8. 피보나치 레벨 설명 (툴팁용)

| 레벨 | 의미 |
|------|------|
| 38.2% | 가장 약한 되돌림. 여기서 반등 = 강한 상승 추세 |
| 50% | 중간 되돌림. 가장 많이 사용되는 지지선 |
| 61.8% | 황금비율 되돌림. 여기를 깨면 추세 전환 가능 |
| 현재가 < 38.2% | 38.2% 아래까지 하락 → 깊은 눌림, 바닥 매수 후보 |
| 현재가 > 61.8% | 61.8% 위로 회복 → 상승 추세 복귀 중 |
| 고점 근접 | 52주 고점의 90% 이상 → 신고가 도전 중 |

---

## 9. 로테이션 점수 계산 방법 (참고)

```
종합 점수 = 모멘텀 + 수급 점수 + 쌍매수 보너스

모멘텀 = 평균 등락률 × 10  (전일 종가 기준)
수급 점수 = min(30, max(-30, 순수급 / 10000))  (±30 상한)
쌍매수 보너스 = 쌍매수 종목 수 × 10  (외인+기관 동시 순매수 3일)

선도: 50점 이상
추격: 20~49점
대기: 0~19점
후발: 0점 미만
```

경고 플래그: 모멘텀 > 20 + 순수급 < 5,000만원 → `"개인 주도 상승 (수급 미확인)"`

---

## 10. 체크리스트

- [ ] 기존 6탭(BRAIN/섹터수급/모멘텀/로테이션/ETF수급/ETF추천) 제거
- [ ] 3탭 UI 구현 (전체 피보나치 / 대형주 피보나치 / 섹터 로테이션)
- [ ] `dashboard_swing` API에서 `fib_stocks`, `fib_leaders`, `sector_rotation` 필드 포함
- [ ] fib_zone 색상 + 그룹핑 적용 (탭 1)
- [ ] fib_status 색상 매핑 (탭 2)
- [ ] stage별 그룹핑 + 색상 (탭 3)
- [ ] 빈 데이터 안내 메시지
- [ ] 정렬 토글 (각 탭)
- [ ] (선택) 피보나치 게이지 바
- [ ] (선택) 섹터 상세 팝업
