# FLOWX 매집 레이더 패널 지시서

## 개요
외국인이 조용히 3일 이상 매집 중이나 **아직 주가가 안 오른 "미발화" 종목**을 감지하여 표시.
가온전선 패턴(4일 매집 → 5일 만에 +42%)의 초기 진입 시점을 포착하는 것이 목적.

## 배치 위치
```
┌─────────────────────────────────┐
│  NXT 야간매수 TOP 5  (기존)      │
├─────────────────────────────────┤
│  ★ 매집 레이더 (NEW)            │  ← 여기
├─────────────────────────────────┤
│  오늘의 매매 타임라인 (기존)      │
└─────────────────────────────────┘
```

## 데이터 소스

### 테이블: `intelligence_accumulation_radar`
| 컬럼 | 타입 | 설명 |
|------|------|------|
| date | DATE PK | 감지일 |
| stocks | JSONB | 종목 배열 (아래 참조) |

### stocks JSONB 구조
```json
[
  {
    "code": "003030",
    "name": "세아제강지주",
    "frgn_days": 4,
    "accel_b": 16.3,
    "chg5": -8.0,
    "tag": "쌍매수",
    "last_dual": true,
    "supply_score": 125,
    "combined_supply": 42.5
  }
]
```

### 필드 설명
| 필드 | 의미 | 표시 방법 |
|------|------|-----------|
| code | 종목코드 | 내부 식별용 |
| name | 종목명 | 메인 텍스트 |
| frgn_days | 외인 연속 순매수 일수 | "외인 4일 매집" |
| accel_b | 최근 2일 평균 외인 순매수(억) | "+16억/일 가속" |
| chg5 | 5일 등락률(%) | "-8.0%" 또는 "보합" |
| tag | 분류 태그 | 뱃지로 표시 |
| last_dual | 마지막날 쌍매수 여부 | 쌍매수 아이콘 강조 |
| supply_score | 매집 강도 점수 | 정렬 기준 (높을수록 강함) |
| combined_supply | 외인+기관 합산 순매수(억) | 참고용 |

## UI 디자인 가이드

### 섹션 헤더
```
🔍 매집 레이더 — 외인 조용한 매집 감지
```
- NXT TOP5보다 **가벼운 톤** (카드형 X → 리스트형)
- 배경: 약간 어두운 톤 (다크 블루 계열)

### 종목 리스트 (각 행)
```
┌──────────────────────────────────────────────┐
│ [쌍매수]  세아제강지주                          │
│ 외인 4일 매집 · +16억/일 가속 · 5일 -8.0%      │
└──────────────────────────────────────────────┘
```

### 태그 뱃지 색상
| tag 값 | 의미 | 색상 |
|--------|------|------|
| 쌍매수 | 외인+기관 동시 매수 (최강) | 빨간색/핫핑크 |
| 가속전환 | 매수 가속 중 (accel > 20억) | 주황색 |
| 바닥매집 | 조용히 매집 (기본) | 파란색 |
| 외인매집 | 외인 단독 매집 | 하늘색 |

### 핵심 포인트 표시
- **chg5가 마이너스**일수록 "미발화" 강조 → 텍스트 색상 초록(긍정 시그널)
- **chg5가 5% 이상**이면 "이미 출발" → 회색 처리 또는 비표시
- **last_dual = true**면 종목명 옆에 🔥 아이콘

### 빈 상태
감지 종목이 0개일 때:
```
매집 레이더 — 오늘은 감지된 종목이 없습니다
```

## 데이터 쿼리

```javascript
// 오늘 날짜 기준 최신 1건
const { data } = await supabase
  .from('intelligence_accumulation_radar')
  .select('date, stocks')
  .order('date', { ascending: false })
  .limit(1)
  .single();

// stocks 배열을 supply_score 내림차순 정렬
const sorted = data.stocks.sort((a, b) => b.supply_score - a.supply_score);
```

## 갱신 주기
- **매일 16:35~16:40** (G7 파이프라인 Stage 4, C36)
- NXT TOP5와 동시 갱신
- 주말/공휴일엔 마지막 거래일 데이터 유지

## 데이터 없는 날 처리
- 테이블에 해당 날짜 row가 없으면 → "오늘은 감지된 종목이 없습니다"
- stocks 배열이 비어있으면 → 동일 처리

## 참고: 감지 로직 요약 (백엔드)
1. 유니버스 전체(~2,889종목) flow CSV 스캔
2. 외인 3일+ 순매수 AND 5일 등락률 < 5%
3. 최근 2일 가속도 > 이전 3일 (매집 세기 증가)
4. 시총 2,000억~5조 필터 (소형주/대형주 제외)
5. supply_score 상위 최대 8종목 선정
