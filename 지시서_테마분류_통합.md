# ============================================
# 테마 분류체계 통합 지시서
# ============================================
# 대상: 퀀트봇, 정보봇, 블로그봇, 웹봇
# 작성: 2026-05-13 | 커밋: d54934b
# ============================================

## 1. 왜 이걸 만들었는가

기존 시스템은 KRX 23개 업종(섹터)으로 분류했다.
문제: "전기전자" 하나에 반도체, 2차전지, 디스플레이, MLCC가 전부 섞여있었다.
실제 시장은 **테마 세트 단위**로 움직인다:
- MLCC 테마 → 삼성전기, LG이노텍, 대덕전자, 기가비스 **이것들만** 같이 오른다
- 2차전지 → 에코프로, 포스코퓨처엠, LG에너지솔루션 **이것들만** 같이 움직인다
- "전기전자" 전체가 오르는 게 아니다

그래서 KIS 테마코드 마스터(302개 테마, 2333종목)를 기반으로
**모든 봇이 공유하는 중앙 테마 분류체계**를 만들었다.

---

## 2. 아키텍처

```
theme_collector.py ─── KIS 마스터 다운로드 ──→ theme_map.json (676KB)
                                                    │
                                    ┌───────────────┤
                                    ▼               ▼
                          shared/theme_service.py  (중앙 API)
                                    │
                    ┌───────┬───────┼───────┬───────┐
                    ▼       ▼       ▼       ▼       ▼
                단타봇   퀀트봇   정보봇  블로그봇   웹봇
```

**핵심**: 모든 봇은 `shared/theme_service.py`를 import해서 테마를 조회한다.
절대로 각자 테마맵을 파싱하지 않는다.

---

## 3. shared/theme_service.py — 사용법

### import 방법

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent.parent))
# ↑ 프로젝트 루트까지 올라가야 함 (각 봇 폴더 구조에 맞게 조정)

from shared.theme_service import get_themes, group_by_theme, get_theme_entries
```

### 핵심 함수

| 함수 | 용도 | 반환 |
| ---- | ---- | ---- |
| `get_themes(code)` | 종목의 테마 이름 리스트 | `["MLCC", "5G", "스마트폰"]` |
| `get_theme_entries(code)` | 종목의 테마 전체 정보 | `[{"theme_code": "045", "theme_name": "MLCC"}, ...]` |
| `get_codes(theme_name)` | 테마의 소속 종목 리스트 | `["009150", "011070", ...]` |
| `get_theme_size(theme_name)` | 테마의 전체 종목수 | `12` |
| `is_noise_theme(theme_name)` | 노이즈 테마 여부 | `True/False` |
| `group_by_theme(codes, min_count=2)` | 종목 리스트를 테마별 그룹핑 | `[{"theme", "codes", "count", "density"}, ...]` |
| `get_all_themes()` | 전체 302개 테마 데이터 | `{theme_code: {"name", "codes"}}` |
| `get_raw_map()` | 원본 theme_map 전체 | `{"themes", "code_to_themes", ...}` |
| `reload()` | 캐시 무효화 후 재로드 | `None` |

### 노이즈 테마 (자동 필터)

세트매매와 무관한 테마는 자동 제외된다:
"신규 상장", "상장주", "FTA", "뉴딜", "배당"

---

## 4. 퀀트봇 지시사항

### 4-1. 이미 완성된 것 (단타봇이 만듦)

단타봇이 매일 16:45에 다음 데이터를 생성하고 Supabase에 업로드한다:

| JSON 파일 | Supabase 테이블 | 내용 |
| ---- | ---- | ---- |
| `theme_flow.json` | `quant_theme_flow` | 282개 테마별 기관/외인 수급 |
| `theme_momentum.json` | `quant_theme_momentum` | 282개 테마별 HOT/COLD 모멘텀 |

퀀트봇은 이 데이터를 **읽기만** 하면 된다.

### 4-2. 퀀트봇이 해야 할 일

**A. Supabase에서 테마 데이터 읽기**

```python
# quant_theme_flow 읽기
resp = supabase.table("quant_theme_flow").select("*").order("date", desc=True).limit(1).execute()
theme_flow = resp.data[0] if resp.data else None

# quant_theme_momentum 읽기
resp = supabase.table("quant_theme_momentum").select("*").order("date", desc=True).limit(1).execute()
theme_momentum = resp.data[0] if resp.data else None
```

**B. 기존 섹터 분석에 테마 레이어 추가**

기존에 "반도체/전자 섹터에 기관 자금 유입" 이라고 썼다면,
이제는 더 정확하게:

```
기존: "반도체/전자 섹터 기관 합의매수 (3일 연속)"
신규: "HBM 테마 기관 합의매수 (3일 연속) — SK하이닉스, 삼성전자, 한미반도체"
     "MLCC 테마 기관 매집 중 — 삼성전기, LG이노텍"
```

**C. 종목 분석 시 테마 정보 활용**

```python
from shared.theme_service import get_themes, group_by_theme

# 추천 종목의 테마 확인
themes = get_themes("005930")  # 삼성전자
# → ["3D낸드", "CXL", "HBM", "LED", "OLED", "반도체", ...]

# 오늘 추천 종목 5개가 같은 테마에 몰려있는지 확인
picks = ["005930", "000660", "006400", "051910", "373220"]
groups = group_by_theme(picks, min_count=2)
# → [{"theme": "아이폰", "count": 4, "density": 0.18}, ...]
# → "오늘 추천 중 아이폰 테마 4종목 집중 — 세트매매 가능성"
```

**D. BRAIN 분석에 테마 통합**

`quant_market_brain` 테이블의 hot_sectors/cooling_sectors에
기존 KRX 섹터 외에 **HOT 테마명**도 함께 포함하면 분석 정밀도가 올라간다.

```python
# theme_momentum에서 HOT 테마 가져오기
hot_themes = theme_momentum.get("hot_themes", [])
# → ["MLCC", "전기차충전소", "2차전지(생산)"]
# BRAIN 보고서에 반영
```

### 4-3. quant_theme_flow 데이터 구조

`themes` 배열의 각 항목:
```json
{
  "테마": "MLCC",
  "종목수": 8,
  "테마전체": 12,
  "기관_당일": 120.5,
  "기관_3일": 350.2,
  "기관_5일": 580.0,
  "기관_연속일": 3,
  "외인_당일": -50.0,
  "외인_3일": 80.5,
  "외인_5일": 200.0,
  "외인_연속일": 2,
  "판단": "합의매수",
  "설명": "기관과 외국인 모두 매수 중 (긍정적)",
  "보정점수": 10.0
}
```

판단 종류: `합의매수`, `합의매도`, `의견분열`, `기관매집`, `외인매집`, `기관이탈`, `외인이탈`, `중립`

### 4-4. quant_theme_momentum 데이터 구조

`themes` 배열의 각 항목:
```json
{
  "테마": "MLCC",
  "상태": "HOT",
  "순위": 3,
  "종목수": 8,
  "당일수익률": 2.35,
  "3일수익률": 5.10,
  "5일수익률": 8.20,
  "상승비율": 0.75,
  "가속도": 1.20,
  "부스트": 22.5,
  "주도주": [
    {"code": "009150", "name": "삼성전기", "chg_1d": 3.50},
    {"code": "011070", "name": "LG이노텍", "chg_1d": 2.80}
  ]
}
```

상태 종류: `HOT` (급등), `WARMING` (상승초기), `NEUTRAL`, `COOLING` (꺾이는중), `COLD` (급락)

---

## 5. 정보봇 지시사항

### 5-1. 텔레그램 리포트에 테마 정보 추가

기존에 "오늘의 섹터 수급" 리포트를 보낼 때,
**테마별 수급 요약**을 추가한다.

```
📊 오늘의 테마 수급 (282개 테마 분석)

🔥 자금 유입 테마 TOP 5
 1. 전기차충전소  기관3D +3,166억 외인3D +798억 [합의매수]
 2. 스마트폰     기관3D +3,567억 외인3D +67억
 3. MLCC        기관3D +3,920억 외인3D -682억 [기관매집]

🧊 자금 이탈 테마 TOP 3
 1. SSD         기관3D -1,200억 외인3D -800억 [합의매도]
 2. HBM         기관3D -500억 외인3D -1,500억

💡 세트 자금 유입: 전기차충전소, 스마트폰, MLCC
```

### 5-2. 종목 분석 시 테마 태그 달기

정보봇이 종목을 분석할 때 반드시 테마 태그를 포함한다:

```python
from shared.theme_service import get_themes

themes = get_themes("009150")  # 삼성전기
# → ["MLCC", "5G", "스마트폰", "전장(전자장비)"]

# 텔레그램 메시지에 포함:
# "삼성전기 (009150) #MLCC #5G #스마트폰"
```

### 5-3. 테마 세트 동시 급등 감지

```python
from shared.theme_service import group_by_theme

# 오늘 5%+ 급등 종목 리스트
surge_codes = ["009150", "011070", "006400", ...]

# 같은 테마에서 동시 급등한 세트 찾기
groups = group_by_theme(surge_codes, min_count=2)
for g in groups:
    if g["count"] >= 3:
        # "🔴 MLCC 테마 세트매매 감지! 삼성전기+LG이노텍+대덕전자 동시 급등"
        send_alert(f"🔴 {g['theme']} 테마 세트매매 감지! {g['count']}종목 동시 급등")
```

---

## 6. 블로그봇 지시사항

### 6-1. 블로그 포스트에 테마 분석 포함

블로그 콘텐츠 작성 시 종목을 테마 맥락으로 설명한다:

```
기존: "삼성전기가 오늘 3.5% 상승했습니다"
개선: "MLCC 테마 전체가 세트로 상승 중입니다.
      삼성전기(+3.5%), LG이노텍(+2.8%), 대덕전자(+4.2%)가 동반 상승.
      기관이 이 테마에 3일 연속 순매수 중이며, 합의매수 신호입니다."
```

### 6-2. 테마 데이터 사용

```python
from shared.theme_service import get_themes, get_codes, group_by_theme

# 종목의 테마 맥락 파악
themes = get_themes("009150")

# 같은 테마 종목들의 동향 파악
mlcc_codes = get_codes("MLCC")
# → 블로그에서 "MLCC 테마 12종목 중 8종목이 상승" 같은 맥락 제공
```

---

## 7. 웹봇 지시사항

### 7-1. Supabase 테이블 생성 (1회)

아래 SQL을 Supabase SQL Editor에서 실행:

```sql
CREATE TABLE IF NOT EXISTS quant_theme_flow (
  date        DATE PRIMARY KEY,
  themes      JSONB NOT NULL DEFAULT '[]',
  top_inflow  JSONB NOT NULL DEFAULT '[]',
  top_outflow JSONB NOT NULL DEFAULT '[]',
  signal      TEXT NOT NULL DEFAULT '',
  total_themes INT NOT NULL DEFAULT 0,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS quant_theme_momentum (
  date             DATE PRIMARY KEY,
  market_return_1d REAL NOT NULL DEFAULT 0,
  themes           JSONB NOT NULL DEFAULT '[]',
  hot_themes       JSONB NOT NULL DEFAULT '[]',
  cold_themes      JSONB NOT NULL DEFAULT '[]',
  rotation_signal  TEXT NOT NULL DEFAULT '',
  total_themes     INT NOT NULL DEFAULT 0,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 7-2. FLOWX 웹사이트 대시보드 UI

**방법 1 (권장)**: 기존 "섹터 수급" 패널 옆에 "테마 수급" 탭 추가
```
[섹터 수급] [테마 수급]     ← 탭 전환
```

**방법 2**: 테마를 메인으로, 섹터를 접기 형태로 배치
```
🔥 HOT 테마: MLCC(+2.3%), 전기차충전소(+1.8%)
📊 세부 섹터: 전기전자(+0.5%), 화학(+0.3%)  [접기 ▼]
```

### 7-3. API Route 예시 (Next.js)

```typescript
// app/api/quant/theme-flow/route.ts
import { supabaseAdmin } from '@/lib/supabase-admin'

export async function GET() {
  const { data } = await supabaseAdmin
    .from('quant_theme_flow')
    .select('*')
    .order('date', { ascending: false })
    .limit(1)
    .single()

  return Response.json(data)
}
```

---

## 8. 타임라인 (매일)

| 시각 | 주체 | 동작 |
| ---- | ---- | ---- |
| 06:00 | theme_collector.py | KIS 테마 마스터 갱신 → theme_map.json |
| 16:40 | sector_institution_flow.py | 섹터 수급 + **테마 수급** 분석 |
| 16:40 | sector_momentum.py | 섹터 모멘텀 + **테마 모멘텀** 분석 |
| 16:45 | upload_quant_dashboard.py | 7개 테이블 Supabase 업로드 |
| 17:00~ | 정보봇 | 텔레그램 리포트에 테마 정보 포함 |
| 17:00~ | 퀀트봇 | BRAIN 분석에 테마 레이어 반영 |
| 상시 | 웹봇 | FLOWX 대시보드에 테마 패널 표시 |

---

## 9. 주의사항

1. **theme_map.json은 단타봇이 관리한다.** 다른 봇은 수정하지 않는다.
2. **shared/theme_service.py**만 import한다. `theme_collector.py`의 함수를 직접 호출하지 않는다.
3. 한 종목이 여러 테마에 속한다 (삼성전자 → 3D낸드, CXL, HBM, LED, OLED, ...).
   이건 버그가 아니라 정상이다. 교차 집계가 되기 때문에 테마별 수급 합계는 섹터별 합계보다 클 수 있다.
4. `min_stocks=3` 필터가 걸려있어서 flow 데이터가 3종목 미만인 테마는 결과에서 제외된다.
5. 노이즈 테마("신규 상장", "상장주", "FTA", "뉴딜", "배당")는 자동 필터된다.
6. 기존 섹터(23개) 데이터는 **삭제하지 않는다**. 매크로 관점에서 여전히 유용하다.
