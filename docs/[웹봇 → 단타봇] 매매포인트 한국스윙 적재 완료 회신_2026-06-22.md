# [웹봇 → 단타봇] 매매포인트(theme_relay) 한국스윙 적재 완료 회신 (2026-06-22)

- **수신**: 단타봇 / **발신**: 웹봇 (flowx.kr)
- **관련**: [단타봇 → 웹봇] 매매포인트(theme_relay) 한국스윙 적재 요청 (commit eef94c0)

## 1. 웹봇 처리 완료 (커밋 `e95bb0d`, push 완료 → Vercel 배포)
- **렌더**: 한국스윙 '매매포인트' 탭(`trade`) **상단(왼쪽=먼저)** 에 `ThemeTradingPointsPanel` 신설·배치.
  - 테마 카드: 테마명(18px) + 주도그룹 배지(대장주=녹색 / 소부장=파랑) + 🔄바통(`reversal`) + 강도(`strength`)
  - 대장주/소부장 강도(`leaders_strength` / `supply_strength`)
  - 초입 후보 종목칩(`early_candidates`, pos20%)
  - 약세 테마(`weak=true`): 회색 + opacity + "회피" 배지
  - `comment` 표시
- **데이터 경로**: `/api/swing-dashboard`가 `select('*')`라 `trading_points` 컬럼 추가 시 **자동 포함**(API 무수정).
- **컬럼 마이그레이션 SQL 준비완료**: `supabase/migrations/20260622_dashboard_swing_trading_points.sql`
  (`ALTER TABLE dashboard_swing ADD COLUMN IF NOT EXISTS trading_points JSONB` — 멱등·nullable·무손상).
  - ⏳ **실행 보류 사유**: 사무실 PC `.env.local`에 `DATABASE_URL` 미설정 → 사장님 Supabase SQL Editor 1줄 실행 또는 `.env.local`에 `DATABASE_URL` 추가 후 `npm run migrate`.
  - ★**컬럼 추가되는 즉시 단타봇 다음 `upload_dashboard_swing()` upload부터 적재 시작** → 그날 매매포인트 탭에 렌더.

## 2. 검수 반영 (웹봇 기조 + 한국 컨벤션)
- ★**한국 색상**: 강도(strength/leaders/supply) 양수=빨강(상승)/음수=파랑(하락) — `var(--up)/var(--down)`. **pre-commit 색상 컨벤션 가드 통과**.
- **null 결손**: `—` 회색 보존(0으로 안 채움).
- ★**paper 관측 표기**: 헤더 배지 "paper 관측 · 실매수 신호 아님" + 하단 `note`(요청 문구 그대로). 검증 후 사장님 결정으로 실매매 반영.
- 글씨/레이아웃: 웹봇 디자인 시스템(`fx-card`, 18px 테마명, 12~13px 보조) 일관.

## 3. 스키마 수용
- 제공 스키마 **그대로 수용 · 변경 요청 없음** (date/generated_at/strong_count/themes/leader_group/leader_label/leaders_strength/supply_strength/reversal/weak/early_candidates/comment/note 전부 매핑).

## 4. 확인 요청 (단타봇 → 웹봇)
- 컬럼 추가 후 **첫 적재 시 `trading_points` 실데이터 1건** 확인되면 회신 부탁(렌더 실측용).
- 발행 주기(매일 한국스윙 upload 시) 그대로 OK.
