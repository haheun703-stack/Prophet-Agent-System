# [단타봇 → 퀀트봇] pykrx 투자자 데이터 6/9 멈춤 — 진단 완료 + 부활 경로

- **일자**: 2026-06-11(목) 19:58 KST
- **발신**: 단타봇 (read-only 조사, 정보봇 코드/DB 무수정)
- **수신**: 퀀트봇(정보봇) 세션 + 사장님
- **연관**: `[단타봇 → 웹봇] pension_scan 미적재 원인 회신_2026-06-11.md` / 웹봇 fde956d

---

## 결론 한 줄

**pykrx가 KRX 로그인 의무화로 죽어 정보봇 DB 투자자 데이터가 6/9에 멈춤 → 부활 경로 = KRX 계정을 VPS .env에 `KRX_ID`/`KRX_PW`로 설정 (pykrx 버전은 이미 로그인 지원 확인).**

## 진단 체인 (VPS 실측 6/11 19:5x)

1. 웹 health: `intelligence_pension_scan` latest=6/9 (웹봇 보고).
2. 단타봇 C41 잡 정상 — 스캔·업로드 3일 연속 200 OK. 단 6/10·11은 201(신규행) 없음 = **date=6/9 고정 upsert**.
3. 원천 `quant_investor_extra.json`: 매일 17:38 재생성되나 `period.to=20260609` 고정.
4. 생성기 `quantum-master/scripts/export_investor_for_scalper.py` = **무죄** — DB(최근 N거래일 쿼리)를 내보낼 뿐, `period` = DB 내 min/max. **DB가 6/9에서 멈춘 것.**
5. **pykrx 실호출 테스트** (quantum-master venv):
   ```
   stock.get_market_trading_value_by_date('20260608','20260611','005930', detail=True)
   → "KRX 로그인 실패: KRX_ID 또는 KRX_PW 환경 변수가 설정되지 않았습니다."
   → 반환 0행
   ```

## 부활 경로 (퀀트봇 액션)

- **A안 (권장·최소)**: KRX 정보데이터시스템(data.krx.co.kr) 계정 생성(무료) → VPS quantum-master `.env`(또는 환경)에 `KRX_ID`/`KRX_PW` 설정 → 투자자 수집 잡 다음 주기 자동 부활 + **6/10·6/11 백필** 확인.
  - VPS pykrx 버전이 로그인 모드 지원하는 것은 에러 메시지로 확인됨(구버전이면 "로그인" 언급 자체가 없음).
  - 계정 생성 = 사장님 결정/실행 사안.
- **B안 (A 불가 시)**: 종목별 연기금 대체 소스 검토 — KIS FHKST01010900은 외인/기관계/개인만(연기금 세부 없음), 키움 opt10059는 계좌 필요. KRX 직접 크롤은 동일 로그인 장벽.

## 영향 범위 (참고)

- 단타봇 pension_scan(C41)·웹 연금/스마트머니 화면 — 6/9 데이터로 graceful 동작 중, 긴급도 낮음.
- 정보봇 DB 투자자 기반의 다른 분석들도 6/9 이후 동결됐을 수 있음 — 퀀트봇 세션에서 영향 항목 일괄 점검 권장 (웹봇 P0 CPI·sector_fire와 별개).
- 부활 시 웹 health 자동 OK 전환(웹봇 확인).

— 단타봇 (봇 OFF · 실주문 0 · 본 조사 read-only)
