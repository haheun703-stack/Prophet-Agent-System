# FLOWX 스윙페이지 — 피보나치 눌림목 종목 패널 지시서

## 프로젝트 소개
이 프로젝트는 **Body Hunter v4** — 한국 주식 단타봇(자동매매 시스템)입니다.
KIS API(한국투자증권) 실계좌 매매, 텔레그램 봇 UI, 7-Group COO 파이프라인,
BRAIN 시장분석, NXT 야간매매, FLOWX VIP 콘텐츠를 포함합니다.

---

## 1. 변경 요약

### 제거할 것
기존 6개 탭을 **모두 삭제**:
- BRAIN 판단
- 섹터수급
- 섹터모멘텀
- 섹터로테이션
- ETF수급
- ETF추천

### 추가할 것
**피보나치 눌림목 종목** 패널 1개로 교체.
데이터 소스: `dashboard_swing.fib_stocks` (JSONB 배열)

---

## 2. 사전 작업 (완료됨)

```sql
-- 이미 실행 완료
ALTER TABLE dashboard_swing
ADD COLUMN IF NOT EXISTS fib_stocks JSONB DEFAULT '[]'::jsonb;
```

4/3, 4/4 데이터 이미 업로드 완료 (30종목).

---

## 3. fib_stocks 데이터 구조

```typescript
interface FibStock {
  code: string;           // "000250"
  name: string;           // "삼천당제약"
  sector: string;         // "제약"
  cap: number;            // 260613 (시총, 억원)
  price: number;          // 609000 (현재가)
  w52h: number;           // 1233000 (52주 고점)
  w52l: number;           // 127600 (52주 저점)
  drop: number;           // -50.6 (52주 고점 대비 하락률 %)
  fib_zone: string;       // "DEEP" | "MID" | "MILD" | "SHALLOW"
  fib_zone_label: string; // "50%+ 하락 (바닥 매수 구간)"
  fib_382: number;        // 549862 (38.2% 되돌림 가격)
  fib_500: number;        // 680300 (50% 되돌림 가격)
  fib_618: number;        // 810737 (61.8% 되돌림 가격)
  fib_status: string;     // "38.2% 아래 (깊은 하락)"
  target: number;         // 1048050 (목표가)
  upside: number;         // 72.1 (상승여력 %)
  per: number;            // 3700.9
  pbr: number;            // 69.85
  frgn: number;           // 5.58 (외국인 보유율 %)
}
```

### 실제 데이터 예시 (4/4 기준, 30종목 중 상위 5개)

```json
[
  {"code":"128940","name":"한미약품","sector":"제약","cap":68459,"price":252000,"w52h":433500,"drop":-41.9,"fib_zone":"DEEP","fib_zone_label":"50%+ 하락 (바닥 매수 구간)","fib_382":279685,"fib_500":337850,"fib_618":396014,"fib_status":"38.2% 아래 (깊은 하락)","target":368475,"upside":46.2,"per":36.0,"pbr":3.2,"frgn":20.9},
  {"code":"263750","name":"펄어비스","sector":"게임","cap":43810,"price":78600,"w52h":128200,"drop":-38.7,"fib_zone":"DEEP","fib_zone_label":"50%+ 하락 (바닥 매수 구간)","fib_382":77102,"fib_500":91850,"fib_618":106597,"fib_status":"50%~61.8% 사이","target":108970,"upside":38.6,"per":13.1,"pbr":1.17,"frgn":44.6},
  {"code":"035720","name":"카카오","sector":"인터넷","cap":235282,"price":43300,"w52h":68400,"drop":-36.6,"fib_zone":"DEEP","fib_zone_label":"50%+ 하락 (바닥 매수 구간)","fib_382":47854,"fib_500":55475,"fib_618":63095,"fib_status":"38.2% 아래 (깊은 하락)","target":58140,"upside":34.3,"per":20.5,"pbr":0.75,"frgn":33.7}
]
```

---

## 4. 배치 위치

기존 6개 탭이 있던 자리를 **피보나치 눌림목 종목** 패널로 교체.
"대시보드" 탭 아래, 종목 추천 섹션 위에 배치.

```
[대시보드 탭] ← 유지

━━━━━━━━━━━━━━━━━━━━━━━━━━━
📐 피보나치 눌림목 종목 (30종목)    ← 신규 (기존 6탭 교체)
━━━━━━━━━━━━━━━━━━━━━━━━━━━

[... 나머지 스윙 콘텐츠 ...]
```

---

## 5. UI 디자인 가이드

### 5-1. 헤더

```
┌─────────────────────────────────────────────────┐
│  📐 피보나치 눌림목 — 바닥 매수 후보              │
│  52주 고점 대비 크게 하락한 우량주 30종목           │
│  시총 3조원+ | 피보나치 되돌림 기준 분류             │
└─────────────────────────────────────────────────┘
```

### 5-2. fib_zone별 섹션 구분

종목을 `fib_zone`별로 그룹핑하여 표시:

```
── 🔴 50%+ 하락 (바닥 매수 구간) — 13종목 ──

┌──────────┬──────┬────────┬──────────────┬────────┬──────┬──────┐
│ 종목      │ 섹터  │ 하락률   │ 피보나치 위치  │ 상승여력 │ PER  │ 외국인│
├──────────┼──────┼────────┼──────────────┼────────┼──────┼──────┤
│ 카카오뱅크 │ 금융  │ -38.8% │ 38.2% 아래   │ +39.0% │ 15.2 │ 33.1%│
│ 알테오젠   │ 바이오│ -38.1% │ 38.2% 아래   │ +37.4% │ 55.3 │ 12.8%│
│ 크래프톤   │ 게임  │ -37.0% │ 38.2% 아래   │ +35.0% │ 14.7 │ 53.1%│
│ ...       │      │        │              │        │      │      │
└──────────┴──────┴────────┴──────────────┴────────┴──────┴──────┘

── 🟠 40~50% 하락 (중간 눌림) — 8종목 ──

┌──────────┬──────┬────────┬──────────────┬────────┬──────┬──────┐
│ 삼천당제약 │ 제약  │ -50.6% │ 38.2%~50%    │ +72.1% │3700.9│  5.6%│
│ 한국전력   │ 전기  │ -40.6% │ 38.2%~50%    │ +43.2% │  3.1 │ 56.0%│
│ ...       │      │        │              │        │      │      │
└──────────┴──────┴────────┴──────────────┴────────┴──────┴──────┘

── 🟡 30~40% 하락 (1차 눌림) — 9종목 ──
...
```

### 5-3. fib_zone 색상 규칙

| fib_zone | 색상 | 배경색 | 아이콘 | 의미 |
|----------|------|--------|--------|------|
| DEEP     | #DC2626 | #FEF2F2 | 🔴 | 50%+ 하락, 바닥 매수 |
| MID      | #EA580C | #FFF7ED | 🟠 | 40~50% 하락, 중간 눌림 |
| MILD     | #CA8A04 | #FFFBEB | 🟡 | 30~40% 하락, 1차 눌림 |
| SHALLOW  | #65A30D | #F7FEE7 | 🟢 | 15~30% 하락, 얕은 조정 |

### 5-4. 피보나치 레벨 시각화 (선택사항)

각 종목을 클릭하면 미니 차트 또는 게이지 표시:

```
52주 저점 ──── 38.2% ──── 50% ──── 61.8% ──── 52주 고점
                         ▲
                     현재 위치
```

- `fib_382`, `fib_500`, `fib_618` 값으로 위치 계산
- `price`가 어디에 있는지 시각적으로 표시

### 5-5. 컬럼 설명

| 필드 | 표시명 | 포맷 |
|------|--------|------|
| name | 종목 | 텍스트 |
| sector | 섹터 | 텍스트 |
| drop | 하락률 | `-38.8%` (빨간색) |
| fib_status | 피보나치 위치 | `38.2% 아래` |
| upside | 상승여력 | `+39.0%` (초록색) |
| per | PER | `15.2` |
| frgn | 외국인 | `33.1%` |
| cap | 시총 | `23.5조` 또는 `6,846억` |

### 5-6. 정렬

기본: **하락률 큰 순** (이미 서버에서 정렬되어 옴)
선택: 상승여력순, PER순, 외국인보유율순 정렬 토글 가능

### 5-7. 데이터 없을 때

`fib_stocks`가 `null`, `[]`, 또는 빈 배열이면:

```
┌─────────────────────────────────────────────┐
│  📐 피보나치 눌림목                           │
│                                              │
│  피보나치 스크리닝 데이터가 없습니다.            │
│  다음 갱신 시 업데이트됩니다.                   │
└─────────────────────────────────────────────┘
```

---

## 6. 데이터 흐름

```
[수동/주기적] bottom_scan.json 생성 (80종목, 시총 3조+)
       ↓
[자동] upload_swing.py → _build_fib_stocks() → 30종목 필터
       ↓
[자동] dashboard_swing.fib_stocks JSONB 업로드
       ↓
[프론트] API SELECT * FROM dashboard_swing WHERE date = ? → fib_stocks 렌더링
```

## 7. 피보나치 레벨 설명 (툴팁용)

| 레벨 | 의미 |
|------|------|
| 38.2% | 가장 약한 되돌림. 여기서 반등하면 강한 상승 추세 |
| 50% | 중간 되돌림. 가장 많이 사용되는 지지선 |
| 61.8% | 황금비율 되돌림. 여기를 깨면 추세 전환 가능 |
| 현재가 < 38.2% | 38.2% 아래까지 하락 — 깊은 눌림, 바닥 매수 후보 |
| 현재가 > 61.8% | 61.8% 위로 회복 — 상승 추세 복귀 중 |
