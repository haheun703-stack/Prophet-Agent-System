# FLOWX 스윙 페이지 — 프론트엔드 데이터 가이드

> **최종 수정**: 2026-03-29
> **Supabase 테이블**: `dashboard_swing` (메인), `swing_signals` (백업)
> **업데이트 주기**: 매일 장후 16:30~17:00 (COO G7 자동 실행)

---

## 1. 데이터 가져오기 (Supabase)

```typescript
// 오늘 날짜 데이터 가져오기
const { data } = await supabase
  .from('dashboard_swing')
  .select('*')
  .eq('date', '2026-03-28')
  .single();

// 최신 데이터 가져오기 (추천)
const { data } = await supabase
  .from('dashboard_swing')
  .select('*')
  .order('date', { ascending: false })
  .limit(1)
  .single();
```

---

## 2. 전체 컬럼 구조

### 2-1. 기본 필드 (최상위)

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `date` | text (PK) | 기준 날짜 | `"2026-03-28"` |
| `brain_verdict` | text | AI 판단 (공격/표준/방어/관망) | `"관망"` |
| `brain_pct` | int | 투자 비중 (0~100) | `0` |
| `brain_reason` | text | 판단 이유 요약 | `"방향 불명확..."` |
| `regime` | text | 시장 레짐 | `"PANIC"` |
| `regime_severity` | float | 레짐 심각도 (0~10) | `0` |
| `regime_desc` | text | 레짐 설명 | `""` |
| `market_comment` | text | 시장 한줄 요약 | `"야간 ☠️ 인버스 강력(-10점) \| 물가상승+경기둔화..."` |

### 2-2. 자산 배분

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `alloc_swing` | int | 스윙 비중 (%) | `0` |
| `alloc_gold_etf` | int | 금 ETF 비중 | `0` |
| `alloc_inverse` | int | 인버스 비중 | `0` |
| `alloc_group_etf` | int | 그룹 ETF 비중 | `0` |
| `alloc_small_cap` | int | 소형주 비중 | `0` |
| `alloc_cash` | int | 현금 비중 | `100` |

### 2-3. NXT 야간 신호

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `nxt_signal` | text | 신호 이모지 | `"☠️"` |
| `nxt_signal_text` | text | 신호 설명 | `"인버스 강력"` |
| `nxt_score` | float | 총점 (-15 ~ +15) | `-10.0` |
| `nxt_reason` | text | 판단 근거 | `"VIX 30+ 경고..."` |

### 2-4. 시장 지표 (숫자)

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `vix` | float | 공포지수 | `31.2` |
| `nasdaq_pct` | float | 나스닥 등락률 (%) | `-1.933` |
| `usdkrw` | float | 원달러 환율 | `1508.36` |
| `oil_pct` | float | 유가 등락률 (%) | `5.567` |
| `gold_pct` | float | 금 등락률 (%) | `-0.5` |
| `silver_pct` | float | 은 등락률 (%) | `0.0` |

### 2-5. 센서 (Brain Allocation)

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `smart_money_score` | float | 스마트머니 점수 | `0` |
| `smart_money_signal` | text | 스마트머니 신호 | `""` |
| `stress_index` | float | 스트레스 지수 | `0` |
| `stress_level` | text | 스트레스 레벨 | `""` |
| `rotation_signal` | text | 순환 신호 | `""` |
| `liquidity_score` | float | 유동성 점수 | `0` |

---

## 3. JSONB 필드 상세

### 3-1. `picks` — 추천 종목 (배열)

```json
[
  {
    "code": "069500",
    "name": "KODEX 200선물인버스2X",
    "grade": "",
    "score": 0,
    "sector": "인버스 (시장 하락 대비)",
    "rr_ratio": 0,
    "rr_verdict": "",
    "entry_price": 0,
    "target_price": 0,
    "stop_price": 0,
    "hold_days": 3,
    "conviction": "MEDIUM",
    "catalyst": "인버스 (시장 하락 대비)",
    "regime": "NXT",
    "tv_pattern": "",
    "news_sentiment": "NEUTRAL",
    "tech_score": 0,
    "supply_signal": "NXT",
    "nat_power_grade": ""
  }
]
```

**프론트엔드 표시 방법:**
- `name` → 종목명
- `sector` → 분류 태그 (색깔로 구분)
- `conviction` → HIGH=빨강, MEDIUM=노랑, LOW=회색
- `hold_days` → "3일 보유" 뱃지
- `catalyst` → 추천 이유

### 3-2. `etf_picks` — ETF 추천 (배열)

```json
[
  {
    "code": "132030",
    "name": "KODEX 골드선물",
    "category": "금/귀금속 (안전자산)",
    "signal": "BUY",
    "entry": 0,
    "sl": 0,
    "tp": 0,
    "reason": "금/귀금속 (안전자산)",
    "holding_days": 3
  }
]
```

### 3-3. `nxt_targets` — NXT 후보 종목 (배열)

```json
[
  {
    "code": "252670",
    "name": "KODEX 200선물인버스2X",
    "sector": "inverse",
    "tier": 1,
    "priority": 1,
    "supply_score": 5,
    "is_etf": true
  }
]
```

**프론트엔드 표시 방법:**
- `tier` → 1=빨강(최우선), 2=주황, 3=노랑
- `is_etf` → ETF 뱃지
- `supply_score` → 수급 점수 바 (0~10)

### 3-4. `watchlist` — 관심 종목 (배열)

```json
[
  {
    "code": "005930",
    "name": "삼성전자",
    "sector": "전기전자",
    "reason": "반등 감시 — 모멘텀 레짐",
    "trigger": "종가 72,000원 돌파 + 거래량 증가 시 진입 검토",
    "grade": "AA",
    "score": 85.3
  }
]
```

---

## 4. `analysis` JSONB — 핵심 분석 데이터

> **이게 3-Source 융합의 핵심입니다. 페이지 대부분이 여기서 나옵니다.**

### 4-1. `analysis.매크로_지표` — 세계 경제 6대 지표

```json
{
  "유가": {
    "값": 99.64,
    "20일평균_차이": 10.9,
    "60일평균_차이": 39.5,
    "방향": "급등",
    "위치": 100
  },
  "금": {
    "값": 4524.3,
    "20일평균_차이": -7.4,
    "60일평균_차이": -7.0,
    "방향": "급락",
    "위치": 17
  },
  "구리": { "값": 5.49, "방향": "하락", "위치": 8 },
  "공포지수": { "값": 31.05, "방향": "급등", "위치": 100 },
  "미국금리": { "값": 4.44, "방향": "상승", "위치": 98 },
  "환율": { "값": 1508.36, "방향": "상승", "위치": 100 }
}
```

**프론트엔드 제안:**
```
┌──────────────────────────────────────────┐
│  세계 경제 대시보드                        │
├──────────┬────────┬────────┬─────────────┤
│  지표     │  현재값  │  방향   │  60일 위치   │
├──────────┼────────┼────────┼─────────────┤
│  유가 🛢️  │  $99.6  │  ↑↑급등 │  ████████100│
│  금 🥇    │ $4,524  │  ↓↓급락 │  ██______17 │
│  구리 🔩  │  $5.49  │  ↓ 하락 │  █_______8  │
│  VIX 😱  │   31.1  │  ↑↑급등 │  ████████100│
│  금리 📊  │   4.44  │  ↑ 상승 │  ████████98 │
│  환율 💱  │ ₩1,508  │  ↑ 상승 │  ████████100│
└──────────┴────────┴────────┴─────────────┘
```

- `위치` (0~100): 60일 중 몇 번째로 높은지 → 프로그레스 바로 표시
- `방향`: 급등/상승/보합/하락/급락 → 아이콘+색깔
- `60일평균_차이`: 양수=위험(빨강), 음수=안전(초록)

### 4-2. `analysis.시장상태` — 시장 체질

```json
"스태그플레이션"
```

| 값 | 의미 | 색깔 | 위험도 |
|----|------|------|--------|
| `"안정"` | 정상 운영 | 🟢 초록 | 낮음 |
| `"비용상승"` | 원자재 비용 증가 | 🟡 노랑 | 중간 |
| `"수요둔화"` | 소비 위축 | 🟠 주황 | 높음 |
| `"스태그플레이션"` | 물가↑ + 경기↓ | 🔴 빨강 | 최고 |

### 4-3. `analysis.전략` — 대응 전략

```json
{
  "상태": "스태그플레이션",
  "설명": "물가상승 + 경기둔화 (최고 위험)",
  "투자비중": 30,
  "손절": 2.0,
  "추적손절": 1.5,
  "최대보유일": 3,
  "선호섹터": ["에너지", "방산", "금"],
  "회피섹터": ["소비재", "전기가스", "운송"],
  "방어ETF": ["KODEX 골드선물", "KODEX 인버스"],
  "추천ETF종류": ["commodity", "inverse"],
  "안내문": "원자재+인버스 중심, 3일 안에 청산"
}
```

**프론트엔드 제안:**
```
┌─────────────────────────────────────┐
│  🔴 스태그플레이션 — 최고 위험       │
│  "물가상승 + 경기둔화"               │
│                                      │
│  투자비중: ███░░░░░░░ 30%           │
│  손절선:  -2.0%  (타이트하게!)       │
│  최대 보유: 3일                      │
│                                      │
│  ✅ 선호: 에너지, 방산, 금           │
│  ❌ 회피: 소비재, 전기가스, 운송     │
│  🛡️ 방어: 골드선물, 인버스           │
│                                      │
│  💡 원자재+인버스 중심, 3일 안에 청산 │
└─────────────────────────────────────┘
```

### 4-4. `analysis.카테고리별_추천` — NXT 종목 그룹

```json
[
  {
    "분류": "인버스 (시장 하락 대비)",
    "순위": 1,
    "종목": [
      { "코드": "252670", "이름": "KODEX 200선물인버스2X", "등급": "Tier1", "ETF": true },
      { "코드": "251340", "이름": "KODEX 코스닥150선물인버스", "등급": "Tier1", "ETF": true }
    ]
  },
  {
    "분류": "금/귀금속 (안전자산)",
    "순위": 2,
    "종목": [
      { "코드": "132030", "이름": "KODEX 골드선물", "등급": "Tier1", "ETF": true }
    ]
  },
  {
    "분류": "원유/에너지 (유가 수혜)",
    "순위": 3,
    "종목": [
      { "코드": "267770", "이름": "KODEX 미국S&P에너지", "등급": "Tier1", "ETF": true }
    ]
  }
]
```

**프론트엔드 제안:**
- 카드 형태로 분류별 그룹
- `순위` 낮을수록 위에 표시
- `등급` Tier1=빨강 뱃지, Tier2=주황, Tier3=회색
- `ETF` true이면 "ETF" 뱃지

### 4-5. `analysis.섹터_온도계` — 22개 섹터 히트맵

```json
[
  {
    "섹터": "기타제조",
    "수익률": 4.72,
    "상태": "뜨거움",
    "상승비율": 73,
    "대표종목": [
      { "이름": "한화에어로스페이스", "등락": 12.5 },
      { "이름": "한국항공우주", "등락": 8.3 }
    ]
  },
  {
    "섹터": "제약",
    "수익률": 1.85,
    "상태": "따뜻",
    "상승비율": 58,
    "대표종목": [...]
  }
]
```

**프론트엔드 제안:**
```
┌─────────────────────────────────────────────┐
│  섹터 온도계 🌡️                              │
├──────────┬──────┬──────┬───────────────────┤
│  섹터     │ 수익률 │ 상태  │ 대표종목          │
├──────────┼──────┼──────┼───────────────────┤
│  기타제조  │ +4.7% │ 🔴뜨거움│ 한화에어로 +12.5% │
│  제약     │ +1.9% │ 🟠따뜻 │ 삼성바이오 +3.2%  │
│  통신     │ +1.5% │ 🟠따뜻 │ SK텔레콤 +2.1%   │
│  ...     │       │       │                   │
│  전기가스  │ -2.1% │ 🔵차가움│ 한전 -3.5%       │
└──────────┴──────┴──────┴───────────────────┘
```

- `상태` 색깔: 뜨거움=🔴, 따뜻=🟠, 보통=⚪, 차가움=🔵
- `상승비율` → 원형 프로그레스 (73% = 거의 다 오름)
- `수익률` → 양수=초록, 음수=빨강

### 4-6. `analysis.나이트워치` — 야간 AI 판단

```json
{
  "점수": -10.0,
  "신호": "☠️",
  "의미": "인버스 강력",
  "판단근거": "VIX 30+ 경고, 나스닥 급락, 유가 급등",
  "추천섹터": ["inverse", "precious_metals", "oil_resource"]
}
```

**프론트엔드 제안:**
```
┌─────────────────────────────────────┐
│  🌙 야간 AI 분석                     │
│                                      │
│  ☠️ 인버스 강력  -10.0점              │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│  -15        -10    0     +10   +15   │
│              ▲                        │
│                                      │
│  📋 VIX 30+ 경고, 나스닥 급락,       │
│     유가 급등                         │
│                                      │
│  추천 섹터: 인버스 | 금 | 에너지      │
└─────────────────────────────────────┘
```

- `점수` → 게이지 바 (-15 ~ +15)
- 양수=초록(상승), 음수=빨강(하락)
- -10 이하 = ☠️ 해골, +10 이상 = 🚀 로켓

### 4-7. `analysis.매매안내` — 시간대별 행동 가이드

```json
[
  { "시간": "09:00~09:15", "행동": "시장 방향 확인 → 인버스/금 ETF 먼저 진입" },
  { "시간": "09:15~09:30", "행동": "원유/에너지주 → 많이 빠졌으면 조금씩 분할 매수" },
  { "시간": "09:30~10:00", "행동": "방어주(통신/제약) 진입 검토" },
  { "시간": "10:00~11:00", "행동": "TV 스캔 결과 확인 후 추가 조정" },
  { "시간": "종일", "행동": "인버스 보유 유지 (보험 역할)" },
  { "시간": "주의사항", "행동": "물가상승+경기둔화 → 3일 안에 청산, 손절 -2%로 타이트하게" }
]
```

**프론트엔드 제안:**
```
┌─────────────────────────────────────────┐
│  📋 오늘의 매매 가이드                    │
├──────────────┬──────────────────────────┤
│  09:00~09:15 │ 시장 방향 확인             │
│              │ → 인버스/금 ETF 먼저 진입   │
├──────────────┼──────────────────────────┤
│  09:15~09:30 │ 원유/에너지주               │
│              │ → 많이 빠졌으면 분할 매수    │
├──────────────┼──────────────────────────┤
│  ⚠️ 주의사항  │ 3일 안에 청산              │
│              │ 손절 -2%로 타이트하게       │
└──────────────┴──────────────────────────┘
```

- 타임라인 UI (세로 시간 흐름)
- "주의사항" 행 = 빨강 배경

### 4-8. `analysis.경고` — 핵심 경고 (배열)

```json
[
  "전쟁 게이트 발동 — 유가+공포지수 위험 수준",
  "공포지수(VIX) 31.2 — 극도의 불안 구간",
  "유가 $99.6 — $100 돌파 임박, 추가 급등 가능",
  "나스닥 -1.9% 급락 → 월요일 갭하락 가능",
  "환율 1,508원 — 원화 약세, 외국인 이탈 주의",
  "시장 상태: 물가상승+경기둔화 — 최소 비중으로 방어"
]
```

**프론트엔드 제안:**
- 빨강 경고 배너 (맨 위에 고정)
- 경고 2개 이상이면 슬라이드로 순환 표시

### 4-9. `analysis.시장요약` — 한줄 요약

```json
"야간 ☠️ 인버스 강력(-10점) | 물가상승+경기둔화 (최고 위험) | 유가 60일 대비 +39% | VIX 31 | 투자비중 30%로 축소"
```

- 페이지 최상단 `market_comment` 또는 `analysis.시장요약` 표시
- `|`로 분리해서 태그처럼 배치 가능

### 4-10. `analysis.기준시각`

```json
"2026-03-29 17:28:43"
```

- 데이터 신선도 표시: "2시간 전 업데이트" 등

---

## 5. 추가 JSONB 필드

### 5-1. `portfolio` — 모델 포트폴리오

```json
{
  "win_rate": 65.2,
  "total_trades": 142,
  "current_picks": 8,
  "brain_cash_ratio": 100,
  "brain_pct": 0
}
```

### 5-2. `analysis` 기존 키 (Brain 기반)

| 키 | 설명 | 비고 |
|----|------|------|
| `brain_overview` | Brain 7.5Phase 요약 | Brain 있을 때만 |
| `liquidity_warning` | 유동성 경고 | Brain 있을 때만 |
| `macro_sentiment` | 매크로 감성 | Brain 있을 때만 |
| `key_levels` | 주요 가격대 | Brain 있을 때만 |

---

## 6. 페이지 레이아웃 제안

```
┌─────────────────────────────────────────────────┐
│  📢 경고 배너 (analysis.경고, 슬라이드)            │
├─────────────────────────────────────────────────┤
│  시장 한줄 요약 (market_comment)                   │
├──────────────────────┬──────────────────────────┤
│  🌙 야간 AI 신호      │  🔴 시장 상태             │
│  analysis.나이트워치    │  analysis.시장상태          │
│  점수 게이지 바        │  + analysis.전략            │
├──────────────────────┴──────────────────────────┤
│  🌍 세계 경제 대시보드 (analysis.매크로_지표)        │
│  유가 | 금 | 구리 | VIX | 금리 | 환율              │
├─────────────────────────────────────────────────┤
│  📊 추천 종목 (picks + etf_picks)                  │
│  카테고리별 카드 (analysis.카테고리별_추천)           │
├─────────────────────────────────────────────────┤
│  🌡️ 섹터 온도계 (analysis.섹터_온도계)              │
│  히트맵 또는 바 차트                                │
├─────────────────────────────────────────────────┤
│  📋 매매 가이드 (analysis.매매안내)                  │
│  시간대별 타임라인                                   │
├─────────────────────────────────────────────────┤
│  👁️ 관심 종목 (watchlist)                          │
│  반등 감시 리스트                                    │
├─────────────────────────────────────────────────┤
│  📈 모델 포트폴리오 (portfolio)                     │
│  적중률, 거래 수, 현금 비중                          │
├─────────────────────────────────────────────────┤
│  🕒 데이터 기준: analysis.기준시각                   │
└─────────────────────────────────────────────────┘
```

---

## 7. 조건부 표시 로직

```typescript
// 1) 시장 상태에 따른 배경색
const regimeColor = {
  "안정": "green",
  "비용상승": "yellow",
  "수요둔화": "orange",
  "스태그플레이션": "red",
}[data.analysis?.시장상태 ?? "안정"];

// 2) NXT 점수에 따른 신호등
const nxtColor = data.nxt_score >= 5 ? "green"
  : data.nxt_score <= -5 ? "red"
  : "yellow";

// 3) 경고가 있으면 배너 표시
const showWarning = (data.analysis?.경고?.length ?? 0) > 0;

// 4) Brain이 없으면 (brain_pct === 0) 카테고리별_추천 표시
const showCategories = data.brain_pct === 0
  && (data.analysis?.카테고리별_추천?.length ?? 0) > 0;

// 5) 섹터 온도계: 방향 아이콘
const phaseIcon = {
  "뜨거움": "🔴",
  "따뜻": "🟠",
  "보통": "⚪",
  "차가움": "🔵",
};

// 6) 매크로 지표 방향 아이콘
const dirIcon = {
  "급등": "↑↑",
  "상승": "↑",
  "보합": "→",
  "하락": "↓",
  "급락": "↓↓",
};
```

---

## 8. 데이터 없을 때 대비

| 상황 | 어떻게 대응 |
|------|------------|
| `analysis` 비어있음 | 기본 NXT+시장지표만 표시 |
| `picks` 비어있음 | "관망 모드입니다" 안내 + 워치리스트 표시 |
| `brain_pct === 0` | 카테고리별_추천 + ETF 중심 표시 |
| `경고` 비어있음 | 경고 배너 숨기기 |
| `매크로_지표` 비어있음 | NXT 숫자 지표(vix, nasdaq_pct 등) 사용 |
| `섹터_온도계` 비어있음 | 섹터 섹션 숨기기 |
| 데이터 2일 이상 오래됨 | "최신 데이터가 아닙니다" 경고 표시 |

---

## 9. 히스토리 활용 (차트용)

```typescript
// 최근 7일 데이터 → NXT 점수 추이 차트
const { data: history } = await supabase
  .from('dashboard_swing')
  .select('date, nxt_score, vix, nasdaq_pct, brain_pct, regime')
  .order('date', { ascending: false })
  .limit(7);
```

- `nxt_score` 7일 추이 → 라인 차트
- `vix` 7일 추이 → 공포지수 변화
- `regime` 7일 히스토리 → 시장 상태 변화

---

## 10. 실시간 데이터 (Supabase Realtime)

```typescript
// 실시간 업데이트 구독
const channel = supabase
  .channel('swing-updates')
  .on(
    'postgres_changes',
    { event: 'INSERT', schema: 'public', table: 'dashboard_swing' },
    (payload) => {
      console.log('새 데이터:', payload.new);
      // 화면 갱신
    }
  )
  .subscribe();
```

---

**끝. 이 문서로 프론트엔드 개발 시작 가능합니다.**
