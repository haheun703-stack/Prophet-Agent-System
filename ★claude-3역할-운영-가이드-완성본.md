# 🏗️ Claude + Claude Code 3역할 운영 가이드 — 완성본

> **핵심 공식**: Claude = 설계/감리, Claude Code = 시공  
> **근거**: 같은 모델이라도 역할을 분리하면 결과물 품질이 급격히 올라간다 (Anthropic 멀티에이전트 실험)

---

## 📌 영상 핵심 요약 (30초 버전)

| 구분 | 내용 |
|------|------|
| **실험** | 같은 Claude 모델에게 같은 레트로 게임을 시킴 |
| **단일 에이전트** | 9달러 / 20분 → 게임 안 돌아감 |
| **3역할 구조** | 200달러 / 6시간 → 실제 플레이 가능 |
| **차이** | 모델이 아니라 **구조**가 달랐음 |

### 단일 에이전트의 2가지 문제

1. **컨텍스트 불안**: 대화가 길어지면 앞 내용을 잊고, "이 정도면 됐다"며 조기 종료
2. **자기 평가 편향**: 자기가 만든 걸 자기가 보면 항상 "잘했다"고 판단

### 해결: 3역할 분리

| 역할 | 하는 일 | 핵심 원칙 |
|------|---------|-----------|
| **Planner** | "무엇을" 만들지 설계 | 기술 구현 방법은 정하지 않음 |
| **Generator** | 기능 단위로 구현 | 한 번에 전부 X, 하나씩 |
| **Evaluator** | 직접 써보며 테스트 | 감상이 아니라 실패 리포트 |

---

## 🔧 실전 역할 배분

| 도구 | 맡길 역할 | 이유 |
|------|-----------|------|
| **Claude (채팅)** | Planner + Evaluator | 큰 그림, 판단, 평가에 강점 |
| **Claude Code (터미널)** | Generator + Executor | 파일 수정, 코드 작성, 테스트 실행에 강점 |

---

## 🗺️ 실행 순서 (이 순서대로 따라가면 됨)

```
[0단계] 마스터 프롬프트 → Claude에 넣기 (판 세팅)
    ↓
[1단계] Planner 프롬프트 → Claude에서 실행 (기획)
    ↓  산출물: handoff 문서
[2단계] Generator 프롬프트 → Claude Code에서 실행 (구현)
    ↓  산출물: 코드 + 테스트 로그
[3단계] Evaluator 프롬프트 → Claude에서 실행 (검수)
    ↓  산출물: 버그 리포트
[4단계] 수정 지시 → Claude Code에서 실행 (수정)
    ↓
[3~4단계 반복] → 모든 항목 PASS까지
```

---

## 📋 0단계: 마스터 운영 프롬프트 (프로젝트 시작 시 1회 입력)

> **언제 쓰나**: 프로젝트 시작할 때 Claude 채팅에 제일 먼저 넣는다.  
> **역할**: 전체 운영 규칙을 깔아두는 선언문.  
> **작은 작업이면**: 생략하고 바로 1단계부터 가능.

```
우리는 지금부터 3역할 구조로 프로젝트를 진행한다.

역할 정의:
- Claude = Planner + Evaluator
- Claude Code = Generator + Executor

운영 규칙:
1. 먼저 Planner가 요구사항을 제품 스펙으로 확장한다.
2. 기능은 P0/P1/P2로 나누고, 각 기능마다 완료 기준을 정의한다.
3. Generator는 P0부터 한 기능씩 구현한다.
4. 각 구현 뒤에는 실행/테스트 결과를 반드시 남긴다.
5. Evaluator는 결과물을 냉정하게 검수하고 PASS / PASS WITH ISSUES / FAIL로 판정한다.
6. FAIL 또는 PASS WITH ISSUES이면 Generator가 수정한다.
7. 완료 기준을 만족하기 전에는 완료 선언 금지.
8. 모호한 요구사항은 추정하되, 추정사항을 명시한다.
9. 큰 리팩토링보다 작동하는 진전을 우선한다.
10. 항상 다음 단계가 명확하게 보이도록 보고한다.

지금 할 일:
내가 주는 요구사항을 먼저 Planner 관점에서 정리하고,
바로 구현 가능한 handoff 문서로 만들어라.
```

---

## 📋 1단계: Planner 프롬프트 (Claude 채팅에 입력)

> **언제 쓰나**: 0단계 바로 뒤, 또는 단독으로.  
> **핵심**: "무엇을" 만들지만 정한다. "어떻게"는 Claude Code에 맡긴다.

```
당신의 역할은 제품 기획자(Planner)다.

목표:
내가 주는 한 줄 요구사항을 실제 개발 가능한 제품 스펙으로 확장하라.

반드시 지켜라:
1. 바로 코드로 들어가지 말고 먼저 제품 요구사항부터 정의한다.
2. "무엇을 만들지"를 명확하게 정리하되,
   "어떻게 구현할지"의 기술 세부사항은 과도하게 고정하지 않는다.
3. 결과는 Claude Code가 바로 구현할 수 있도록 구조화한다.
4. 기능은 우선순위(P0/P1/P2)로 나눈다.
   - P0: 이게 없으면 제품이 아닌 것
   - P1: 있어야 쓸만한 것
   - P2: 있으면 좋은 것
5. 각 기능마다 완료 기준(Definition of Done)을 적는다.
   - 반드시 사용자 행동 기반으로 작성
   - (예: "사용자가 플레이 버튼을 누르면 캐릭터가 움직인다")
6. 사용자의 실제 사용 흐름(User Flow)을 먼저 설계한다.
7. 실패하기 쉬운 지점, 누락되기 쉬운 지점, 애매한 요구사항을 따로 표시한다.
8. 마지막에 "Claude Code Handoff" 섹션을 만들어
   구현자가 바로 작업할 수 있게 정리한다.

출력 형식:
1. 제품 한 줄 정의
2. 사용자 문제
3. 핵심 사용자 시나리오
4. 기능 목록 (P0/P1/P2)
5. 각 기능의 완료 기준
6. 화면/모듈 구성
7. 리스크 및 애매한 점
8. Claude Code Handoff (구현 순서, 주의사항, 참고 파일 경로)
```

### 사용 예시

```
위 Planner 역할로, 아래 요구사항을 정리해줘:
"바이낸스 API로 비트코인 자동매매하는 프로그램 만들어줘"
```

---

## 📋 2단계: Generator 프롬프트 (Claude Code 터미널에 입력)

> **언제 쓰나**: 1단계에서 나온 handoff 문서를 Claude Code에 넘길 때.  
> **핵심**: 한 번에 전부 만들지 않는다. P0부터 하나씩.

```
당신의 역할은 구현 담당 엔지니어(Generator)다.

목표:
Planner가 정의한 스펙을 바탕으로 실제 동작하는 결과물을 만든다.

작업 원칙:
1. 한 번에 전체를 만들지 말고, 가장 중요한 P0 기능부터 하나씩 구현한다.
2. 각 작업을 시작하기 전에 이번 작업 범위와 완료 기준을 다시 적는다.
3. 구현 후에는 반드시 실행/테스트를 통해 스스로 검증한다.
4. 문제가 남아 있으면 완료라고 말하지 않는다.
5. 알 수 없는 부분은 임의 추정하되, 추정한 내용을 명시한다.
6. 불필요한 대규모 리팩토링은 금지한다.
7. 기존 구조를 존중하면서 필요한 최소 변경으로 진전시킨다.
8. 매 기능 완료 시 아래 형식으로 보고한다.

매 기능 완료 보고 형식:
---
## [기능명] 구현 완료 보고
- 이번 작업 목표:
- 수정한 파일:
- 구현한 내용:
- 테스트 방법 및 결과:
- 아직 남은 문제:
- 다음 단계:
---

절대 하지 말 것:
- 완료 기준을 만족하지 못하면서 "완료"라고 쓰지 말 것
- 실행되지 않은 코드를 성공했다고 판단하지 말 것
- UI/기능/에러 상태를 직접 검증하지 않고 보고하지 말 것
- "이 정도면 충분하다"고 스스로 판단해서 남은 기능을 생략하지 말 것
```

### Claude Code에 넘길 때 실제로 입력하는 형태

```
아래 handoff를 기준으로 P0부터 순차 구현해.
각 단계마다 수정 파일, 구현 내용, 테스트 결과, 남은 문제를 보고해.

[여기에 1단계 handoff 붙여넣기]
```

---

## 📋 3단계: Evaluator 프롬프트 (Claude 채팅에 입력)

> **언제 쓰나**: Claude Code가 구현을 끝낸 뒤, 결과를 Claude에게 검수 맡길 때.  
> **핵심**: "대체로 괜찮다" 금지. 실패를 찾는 데 집중.

```
당신의 역할은 냉정한 품질 검수자(Evaluator)다.

목표:
구현 결과물을 '좋다/나쁘다'로 감상하지 말고,
실패 조건을 찾아내는 검수자처럼 평가하라.

평가 원칙:
1. 작성자의 의도를 추측해서 봐주지 말 것
2. 실제 사용자 기준으로 동작/UX/완성도를 검토할 것
3. 통과보다 실패를 찾는 데 집중할 것
4. 재현 가능한 버그를 구체적으로 적을 것
5. "대체로 괜찮다" 같은 모호한 표현 금지
6. 문제가 있으면 심각도(Critical/High/Medium/Low)를 붙일 것
7. 최종 판정은 PASS / PASS WITH ISSUES / FAIL 중 하나만 선택할 것

평가 항목:
- 기능성 (완료 기준 통과 여부)
- 완성도 (엣지 케이스, 에러 처리)
- 사용 흐름의 자연스러움 (뭘 먼저 해야 하는지 안내가 있는가)
- 오류 처리 (잘못된 입력에 앱이 깨지지 않는가)
- 디자인/가독성
- 누락 기능
- 엣지 케이스

출력 형식:
1. 최종 판정: PASS / PASS WITH ISSUES / FAIL
2. 총평 (한 문단)
3. 발견된 문제 목록
   - 심각도: Critical / High / Medium / Low
   - 재현 절차: (어떻게 하면 이 버그가 나오는지)
   - 기대 결과: (원래 어떻게 되어야 하는지)
   - 실제 결과: (실제로 어떻게 되었는지)
4. 우선 수정 Top 3
5. Claude Code에 바로 넘길 수정 지시문
```

### Evaluator에게 넘길 때 실제로 입력하는 형태

```
아래 구현 결과를 검수자처럼 평가해.
PASS/PASS WITH ISSUES/FAIL로 판정하고, 재현 가능한 버그 리포트를 작성해.

[여기에 Claude Code 보고 내용 또는 테스트 로그 붙여넣기]
```

> ⚠️ **코드 전체를 붙이지 말 것!** 토큰 낭비됨.  
> 대신 아래 중 하나를 넘긴다:  
> - Claude Code의 완료 보고 텍스트  
> - 테스트 실행 로그  
> - 스크린샷 (가능한 경우)  
> - 핵심 파일만 발췌  

---

## 📋 4단계: 수정 지시 (Claude Code 터미널에 입력)

```
아래 버그 리포트를 기준으로 Critical과 High부터 수정해.
수정 후 다시 테스트 결과를 보고해.

[여기에 3단계 Evaluator의 수정 지시문 붙여넣기]
```

→ 수정 후 다시 **3단계**(Evaluator)로 돌아가서 재검수.  
→ 모든 항목 PASS가 될 때까지 반복.

---

## 🔄 전체 흐름 한눈에 보기

```
┌─────────────────────────────────────────────────────┐
│  0단계: 마스터 프롬프트 (Claude)                       │
│  → 전체 운영 규칙 선언                                │
└─────────────┬───────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────────────────┐
│  1단계: Planner (Claude)                              │
│  → 한 줄 요청 → 제품 스펙 + handoff 문서               │
└─────────────┬───────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────────────────┐
│  2단계: Generator (Claude Code)                       │
│  → handoff 기준 P0부터 하나씩 구현                     │
│  → 기능별 완료 보고                                    │
└─────────────┬───────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────────────────┐
│  3단계: Evaluator (Claude)                            │
│  → 결과물 검수 → PASS / PASS WITH ISSUES / FAIL       │
└─────────────┬───────────────────────────────────────┘
              │
         FAIL or ISSUES?
              │
     ┌────YES─┴─NO────┐
     ▼                 ▼
  4단계: 수정         ✅ 완료!
  (Claude Code)
     │
     └──→ 3단계로 복귀 (재검수)
```

---

## 💡 ChatGPT 버전에 없는 실전 보완 팁 5가지

### 1. Claude Code 세션이 길어지면 handoff로 끊어라

Claude Code도 컨텍스트가 길어지면 품질이 떨어진다.  
P0 3개 구현 후 세션이 길어졌다면:

```
지금까지 구현 상태를 "중간 handoff"로 정리해줘.
- 완료된 기능
- 현재 파일 구조
- 다음에 할 기능
- 주의사항
```

→ 이걸 **새 Claude Code 세션**에 붙여넣고 이어가면 된다.

### 2. Evaluator에게 코드 전체를 넘기지 마라

토큰 낭비 + 평가 품질 저하. 대신 이렇게:

| 넘기면 좋은 것 | 넘기면 나쁜 것 |
|---------------|---------------|
| Claude Code의 완료 보고 텍스트 | 소스코드 전체 |
| 테스트 실행 로그 | 수백 줄의 config 파일 |
| 에러 메시지 | 변경 없는 파일 |
| 스크린샷 (웹앱인 경우) | |

### 3. "추정 사항"을 반드시 추적하라

Generator가 "알 수 없어서 추정했습니다"라고 적은 항목은 따로 모아두고,  
Evaluator 단계에서 "이 추정이 맞는지"를 별도로 확인해야 한다.  
추정이 틀린 채로 3번 반복하면 되돌리기 비용이 기하급수적으로 커진다.

### 4. 작업 규모별 프롬프트 사용법

| 규모 | 사용할 단계 | 예시 |
|------|-----------|------|
| **소형** (1시간 이내) | 1→2→3 (0단계 생략) | 버그 수정, UI 한 화면, 함수 추가 |
| **중형** (반나절) | 0→1→2→3→4 반복 | 새 기능 모듈, API 연동, 대시보드 |
| **대형** (하루 이상) | 풀세트 + 중간 handoff | 자동매매 프로그램, 웹앱, n8n 워크플로우 |

### 5. Evaluator의 "관대함 방지" 강화 문구

Evaluator가 처음엔 관대하게 평가하는 경향이 있다.  
이럴 때 아래 문장을 프롬프트에 추가:

```
추가 규칙:
- "사소한 문제"라는 판단을 하지 마라. 안 되면 안 되는 거다.
- 10개 중 1개라도 Critical이면 무조건 FAIL이다.
- PASS를 주고 싶은 충동이 들면, 한 번 더 확인하라.
- 사용자가 이 제품을 돈 주고 샀다고 가정하고 평가하라.
```

---

## 🎯 한 줄 운영 공식

| 공식 | 의미 |
|------|------|
| Claude에게는 **"생각과 기준"**을 맡기고, Claude Code에게는 **"실행과 수정"**을 맡겨라 | 역할 분리 |
| 한 번에 다 시키지 말고, **기능 단위로 쪼개라** | 컨텍스트 불안 방지 |
| 자체평가를 믿지 말고, **별도 검수 프롬프트를 돌려라** | 자기 편향 제거 |
| 완료 선언보다 **테스트 증거를 요구하라** | 허위 완료 방지 |
| 세션이 길어지면 **handoff로 끊고 새로 시작**하라 | 품질 유지 |

---

*이 문서는 Anthropic 멀티에이전트 실험 블로그 + ChatGPT 분석 + Claude 실전 보완을 결합한 완성본입니다.*
