# FLOWX /swing 페이지 — 웹봇 전달사항

## 1. Supabase 테이블 구조

스윙 페이지는 **2개 테이블**에서 데이터를 가져옵니다:

| 테이블 | 용도 | 키 |
|--------|------|-----|
| `swing_signals` | 메인 (종목 추천 + 분석) | date |
| `dashboard_swing` | 확장 (위 전부 + NXT 지표 + 자산배분 + 시장지표 + 센서) | date |

프론트에서는 `dashboard_swing`만 읽으면 전부 있습니다.

---

## 2. picks 배열 (종목 목록) — 변경됨

`picks` JSONB 배열 안에 **KRX 주간 매매 + NXT 야간 매매** 종목이 함께 들어옵니다.

### 각 종목 객체 필드:

```json
{
  "code": "015260",
  "name": "에이엔피",
  "category": "KRX",
  "category_label": "주간 매매",
  "star": true,
  "action": "매수",
  "grade": "A",
  "score": 50,
  "sector": "전자부품",
  "rr_ratio": 1.8,
  "entry_price": 752,
  "target_price": 812,
  "stop_price": 691,
  "hold_days": 3,
  "conviction": "HIGH",
  "conviction_label": "확신 높음",
  "catalyst": "TV_SCAN + 모멘텀",
  "reasons": ["모멘텀64", "RSI59", "MA20위", "거래량3.2x"],
  "close": 752,
  "chg_pct": 19.4,
  "rsi": 59.2,
  "vol_ratio": 3.19,
  "source": "TV_SCAN"
}
```

### 새로 추가된 필드 (프론트에서 사용해야 할 것들):

| 필드 | 타입 | 설명 | 값 예시 |
|------|------|------|---------|
| `category` | string | 매매 구분 | `"KRX"` 또는 `"NXT"` |
| `category_label` | string | 한국어 라벨 | `"주간 매매"` 또는 `"야간 매매"` |
| `star` | boolean | 진입 추천 여부 (★) | `true` / `false` |
| `action` | string | 행동 지시 | `"매수"` / `"관심매수"` / `"관찰"` |
| `conviction_label` | string | 확신도 한국어 | `"확신 높음"` / `"보통"` / `"낮음"` |
| `reasons` | string[] | 추천 근거 목록 | `["모멘텀64", "RSI59"]` |
| `close` | number | 현재가 | `752` |
| `chg_pct` | number | 등락률(%) | `19.4` |
| `rsi` | number | RSI | `59.2` |
| `vol_ratio` | number | 거래량 배수 | `3.19` |

### NXT 종목 추가 필드:

| 필드 | 타입 | 설명 |
|------|------|------|
| `is_etf` | boolean | ETF 여부 |
| `supply_score` | number | 수급 점수 |

---

## 3. picks 정렬 규칙

이미 정렬되어 내려옵니다:
1. `star: true` 먼저
2. 그 다음 `score` 높은 순

---

## 4. 프론트 렌더링 가이드

### 카테고리 구분
```
KRX (주간 매매) — 장중 09:00~15:30 진입
NXT (야간 매매) — 장후 15:40~08:00 진입
```

카테고리별로 섹션을 나누거나, 탭으로 구분하면 됩니다.

### star 표시
- `star: true` → 종목명 앞에 ★ 표시 + 강조 스타일
- `star: false` → 일반 표시

### action 표시
- `"매수"` → 빨간색 배지 (진입 OK)
- `"관심매수"` → 주황색 배지 (조건부)
- `"관찰"` → 회색 배지 (지켜보기)

### conviction_label 표시
- `"확신 높음"` → 진한 강조
- `"보통"` → 중간 톤
- `"낮음"` → 연한 톤

---

## 5. analysis 필드 — 모두 문자열

`analysis` JSONB 안에는 **문자열만** 있습니다 (React #31 수정 완료).

| 키 | 내용 예시 |
|----|----------|
| `macro_summary` | "글로벌 매크로 안정적..." |
| `commodity_summary` | "원자재 전반 상승..." |
| `sector_summary` | "반도체 섹터 과열..." |
| `flow_summary` | "외국인 순매수 지속..." |
| `risk_summary` | "리스크 레벨 LOW..." |
| `시장상태` | "안정" |
| `시장요약` | "VIX 15.2 안정 + 나스닥 +0.3%..." |
| `경고` | "유가 급등 \| 환율 상승" (여러 건은 \| 로 구분) |
| `전략요약` | "물가상승+경기둔화 \| 투자비중 30% \| 손절 -2%..." |
| `나이트워치` | "인버스 강력 (-10점) — 판단근거..." |
| `매매안내` | "[09:00~09:15] 행동 \| [15:40] 행동..." |

**주의**: 절대 중첩 객체 없음. 전부 string입니다.

---

## 6. 퀀트봇 대시보드 — 별도 테이블 5개

퀀트봇(/quant)은 **다른 테이블**에서 데이터를 가져옵니다:

| 테이블 | 내용 |
|--------|------|
| `sector_flow` | 섹터별 기관 수급 |
| `etf_fund_flow` | ETF 투자자별 수급 |
| `sector_momentum` | 섹터 온도계 (HOT/WARMING/COLD) |
| `etf_recommendation` | ETF 추천 종목 |
| `quant_market_brain` | BRAIN 6Phase 분석 + 종목 서술 |

이 테이블들은 스윙 페이지와 **완전히 별도**입니다.

---

## 7. 하위 탭 에러 원인

`/swing` 페이지 하위 탭 (BRAIN 판단, 섹터수급 등)에서 에러가 나는 이유:

1. **해당 테이블이 없거나** 아직 데이터가 없을 수 있음
2. **프론트 캐시** — 이전 버전 JS가 캐시되어 구 데이터 구조로 접근 시도
3. **잘못된 테이블 참조** — 하위 탭이 `sector_flow`, `sector_momentum` 등 퀀트봇 테이블을 읽으려 하는데, 그 테이블이 비어있거나 없으면 에러

**해결 방법**:
- 하위 탭에서 어떤 테이블을 읽는지 확인 → 해당 테이블에 데이터 있는지 Supabase에서 확인
- 데이터 없을 때 빈 상태(empty state) 처리 추가
- `dashboard_swing` 하나로 통합하거나, 필요한 테이블만 선택적으로 읽기

---

## 8. 데이터 갱신 타이밍

- **자동 갱신**: 매 거래일 16:30 (COO G7 파이프라인)
- **주말/공휴일**: 갱신 안 됨 (마지막 거래일 데이터 유지)
- **수동 갱신**: 필요시 백엔드에서 수동 실행 가능
