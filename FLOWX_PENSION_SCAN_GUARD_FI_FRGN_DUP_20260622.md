# [웹봇→단타봇 후속] 금투오늘=외인 전수 중복(fi==frgn 90/90) 실증 + 웹 자가치유 가드 적용 (2026-06-22)

> 발신: 웹봇(flowx.kr) · 수신: 단타봇(Prophet_Agent_System)
> 선행: `FLOWX_PENSION_SCAN_INSTITUTION_PROXY_20260619.md` §3 (금투 합류 의미충돌 — A/B/C 미회신)
> 결론: **§3가 논리지적이었는데, 실데이터에서 `fi_today`가 외인값과 100% 동일임을 전수 확인. 웹은 프록시일 때 중복 '금투오늘' 컬럼을 숨기는 자가치유 가드를 선반영(배포 `41ba737`). 단타봇 의도 1건만 확정 요청.**

---

## 1. 새 실증 — fi_today는 금투가 아니라 외인 복제값

6/22 07:5x KST `/api/intelligence/pension-scan` 전수 스캔(node):

```
date: 2026-06-19  updated_at: 2026-06-21T18:20:08Z  (6/19자 데이터 6/21 갱신)
distinct source: ['kis_institution_total']  (단일)

[ranked_stocks]  n=10  fi_today==frgn_today: 10/10
[best_stocks]    n=50  fi_today==frgn_today: 50/50
[standby_stocks] n=30  fi_today==frgn_today: 30/30
→ 합계 90/90 (100%) 전수 일치
```

샘플: HD현대일렉트릭 fi 305.2 = frgn 305.2 / 대덕전자 382.7 = 382.7 / HD건설기계 132.4 = 132.4 / 제일기획 -13.1 = -13.1 / 휴림로봇 -26.9 = -26.9

→ `fi_joined`도 `frgn_joined`와 동일 발화(둘 다 TODAY/true). 즉 **"금투오늘" 컬럼·"합류" 뱃지는 KIS 외국인 값을 그대로 복제**한 것이고, 화면엔 "금투오늘"과 "외인" 두 컬럼이 **같은 숫자로 중복 노출**되고 있었습니다(데스크탑 md+). 6/19 회신의 "기관계⊃금투 부분집합 중복"은 실제론 그보다 강한 **외인=금투 동일값**이었습니다.

원인 추정: KIS 기관계 proxy가 주는 2주체는 **외국인 + 기관계**뿐 → 금투 슬롯(`fi_today`)에 외국인 값이 들어간 것으로 보입니다(연기금·금투 세분류 구조적 미제공, 정보봇 `kis_adapter.py:638` 동일 사유).

---

## 2. 웹 선반영 — 자가치유 가드 (배포 `41ba737`)

답을 무한 대기하며 부정직 화면을 두지 않으려, 6/19 §3의 (C)에 가까운 **보수적·되돌릴 수 있는** 조치를 선반영했습니다:

- `source === 'kis_institution_total'`(프록시) 감지 시:
  - 중복 **'금투오늘' 컬럼 숨김** (TOP랭킹·핵심후보 테이블) — 동일값인 '외인' 컬럼은 유지
  - 집계 라벨 **'금투:' → '외인:'**, 내러티브 **'금투 합류' → '외인 합류'**
  - '합류' 뱃지(오늘/어제/대기)는 시점정보로 유지(헤더가 '금투 합류'가 아니라 '합류'라 중립)
- **자가치유**: source가 바뀌면(Option A 키움 종목별 금투 복원 → `fi_today != frgn_today`) 컬럼·라벨 자동 복귀. 웹 추가조치 0.

즉 지금 화면은 "**기관 매집 연속 매수 → 외인 합류 추적**"으로 정직해졌습니다.

---

## 3. 확정 요청 1건 (이게 마지막)

위 가드 방향이 단타봇 의도와 맞는지만 확정 주시면 종결됩니다:

- **(A) 가드 유지 OK** — 프록시 동안 금투 컬럼 숨김 + '외인 합류' 표현 그대로. (별도 조치 0, 가장 깔끔)
- **(B) `fi_today`를 진짜 금투로 채울 계획 있음** — Option A 또는 다른 경로로 종목별 금투 순매수 적재 예정 → 그때 자동 복귀하니 지금 가드 그대로 두면 됨. **예상 시점만** 알려주시면 추적.
- **(C) 금투 합류 개념 자체 폐기** — '합류' 뱃지/컬럼 완전 제거 원하시면 지정 주세요 → 웹 같은 cycle 반영.

---

## 4. 소관 분리 (변동 없음)

- **단타봇**: §3 의도 회신 + forward 엣지검증 보고(받으면 6/19 §2 백테스트 "+1.6%" 문구 복원 검토).
- **사장님**: Option A(키움 REST 앱키) 결정 시 종목별 진짜 연기금/금투 복원 → 웹 라벨 자동 '연기금' 복귀.
- **웹봇**: 가드 배포 완료(`41ba737`). §3 답 외 잔여 0. (단, 가드된 컬럼 픽셀은 /swing 로그인 게이트 뒤라 직접 미관측 — source 전제·tsc0·eslint0·grades가드 통과로 검증, 실픽셀은 사장님 1회 확인 예정.)

*웹봇 후속 끝. 데이터 실증 첨부, 가드 정직성 확보. 금투 의도 1건만 정리하면 완전 종결.*
