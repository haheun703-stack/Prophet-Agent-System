/**
 * [긴급 수정] 탭 네비게이션 2중 표시 제거
 *
 * ──────────────────────────────────────────────
 * 현재 문제:
 *
 *   ┌─ 상단 (기존 4탭 — 이걸 6탭으로 수정해야 함) ─┐
 *   │ [시장판단&전략] [매집합류시그널] [매매포인트] [수급추적] │
 *   └─────────────────────────────────────────────┘
 *
 *   ┌─ 하단 (새로 추가된 6탭 — 이건 삭제) ─────────┐
 *   │ [시장판단&전략] [매매포인트] [상한가엔진]        │
 *   │ [매집합류시그널] [수급임계점] [수급추적]          │
 *   └─────────────────────────────────────────────┘
 *
 * ──────────────────────────────────────────────
 * 해결:
 *
 *   1. 하단 6탭 바 JSX 완전 삭제
 *   2. 상단 기존 4탭 바의 탭 배열을 6탭으로 교체
 *   3. 결과: 탭 바 1개만 존재
 *
 * ──────────────────────────────────────────────
 */

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// STEP 1: 하단 중복 탭 바 삭제
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// SwingDashboardView.tsx (또는 해당 페이지 컴포넌트)에서
// 새로 추가된 두 번째 탭 네비게이션 JSX 블록을 찾아서 완전 삭제.
//
// 찾는 방법: "상한가 엔진", "수급 임계점" 텍스트가 포함된
// 탭 버튼 그룹(div/nav)을 찾으면 그게 새로 추가된 하단 탭 바.
// 그 전체 컨테이너 div를 삭제.

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// STEP 2: 상단 기존 탭 배열 수정
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// 기존 상단 탭 배열을 찾아서 교체:
//
// 변경 전:
//   ["시장판단 & 전략", "매집 합류 시그널", "매매포인트", "수급추적"]
//
// 변경 후:
//   ["시장판단 & 전략", "매매포인트", "상한가 엔진", "매집 합류 시그널", "수급 임계점", "수급추적"]

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// STEP 3: 상단 탭의 콘텐츠 렌더링 분기에 2개 추가
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// 기존 상단 탭 콘텐츠 분기 (if/switch)에 추가:
//
//   {activeTab === "상한가 엔진" && <LimitUpEnginePanel />}
//   {activeTab === "수급 임계점" && <TippingPointPanel />}
//
// 이미 import 되어있다면 그대로 사용.
// 안 되어있다면:
//   import LimitUpEnginePanel from "@/features/swing/ui/LimitUpEnginePanel";
//   import TippingPointPanel from "@/features/swing/ui/TippingPointPanel";

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 최종 결과 (탭 바 1개만 존재):
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// ┌──────────────────────────────────────────────────────────────────┐
// │ [시장판단&전략] [매매포인트] [상한가엔진] [매집합류시그널] [수급임계점] [수급추적] │
// └──────────────────────────────────────────────────────────────────┘
//
// (하단에 두 번째 탭 바 없음)
