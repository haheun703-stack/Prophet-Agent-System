# ============================================
# FLOWX 통합 지시서 — 단타봇 전용
# ============================================
# 이 문서를 단타봇 세션에 통째로 복붙하세요.
# 단타봇은 이 문서만 보면 됩니다.
# ============================================

## 1. 너의 역할

너(단타봇)는 FLOWX 서비스의 **QUANT 티어 (₩50,000/월)** 데이터를 공급하는 봇이야.
이건 FLOWX에서 가장 비싼 티어야. "내일 뭘 사야 하는지"를 직접 알려주는 프리미엄 시그널이니까.
매일 장 마감 후 3~5일 단기 매매 시그널을 Supabase DB에 올려야 해.
FLOWX 웹사이트(flowx.kr)가 이 DB에서 데이터를 읽어서 QUANT 구독자에게 보여줘.

**너의 담당 패널:**
- AI 추천 (유료 버전: 국가별 외인 수급 + 기관 동반 기반)
- 세력 포착 (비정상 거래량 + 수급 집중 종목)
- 스나이퍼 워치 (진입가/손절가/목표가 제시)

**너의 담당 테이블:** `short_signals` (1개만)

---

## 2. Supabase 접속 정보

```
SUPABASE_URL=https://[프로젝트ID].supabase.co
SUPABASE_KEY=[service_role 키]
```

> **주의:** 반드시 `service_role` 키를 사용해야 해. `anon` 키로는 쓰기 불가.
> 이 키는 .env 파일에 저장하고 절대 코드에 하드코딩하지 마.

---

## 3. 테이블 스키마 (너의 테이블 1개)

### short_signals
| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| date | DATE | 날짜 | '2026-03-16' |
| code | TEXT | 종목 코드 | '005930' |
| name | TEXT | 종목명 | '삼성전자' |
| grade | TEXT | 등급 | 'AAA' / 'AA' / 'A' / 'BBB' / 'BB' / 'B' / 'C' / 'D' / 'F' |
| total_score | NUMERIC | 종합 점수 (0~100) | 87 |
| foreign_detail | JSONB | 국가별 외인 순매수 | {"us": 25000, "eu": 12000, "asia": 5000, "other": 2000} |
| inst_support | BOOLEAN | 기관 동반 매수 여부 | true / false |
| entry_price | NUMERIC | 추천 진입가 | 83500 |
| stop_loss | NUMERIC | 손절가 | 80000 |
| target_price | NUMERIC | 목표가 | 90000 |
| holding_days | INT | 예상 보유일 | 4 (3~5일 범위) |
| signal_type | TEXT | 시그널 강도 | 'FORCE_BUY' / 'BUY' / 'WATCH' / 'AVOID' |
| volume_ratio | NUMERIC | 평균 거래량 대비 배수 | 1.8 (1.5 이상이면 세력 의심) |
| momentum_regime | TEXT | 현재 레짐 | 'MOMENTUM' / 'NORMAL' |

> **UNIQUE 제약:** date+code 조합이 유니크.
> **UPSERT 사용:** 같은 날 같은 종목 다시 올리면 업데이트.

### foreign_detail JSONB 형식 상세:
```json
{
  "us": 25000,      // 미국계 순매수 (주)
  "eu": 12000,      // 유럽계 순매수 (주)
  "asia": 5000,     // 아시아계 (중국/일본/싱가포르 등)
  "other": 2000,    // 기타
  "total": 44000    // 합계
}
```

### signal_type 기준:
- **FORCE_BUY**: 외인+기관 동반 + grade A 이상 + volume_ratio 1.5배 이상 → 적극 매수
- **BUY**: 외인 수급 양호 + grade BBB 이상 → 매수
- **WATCH**: 수급 방향은 맞는데 확신 부족 → 관찰
- **AVOID**: 수급 이탈 or 과열 → 회피

---

## 4. 설치할 패키지

```bash
pip install supabase
```

---

## 5. 업로드 코드

아래 코드를 `upload_short.py`로 저장해.

```python
"""
FLOWX 단타봇 → Supabase 업로드 모듈
담당: short_signals
"""
from supabase import create_client
import os
from datetime import date

# Supabase 클라이언트 초기화
supabase = create_client(
    os.environ['SUPABASE_URL'],
    os.environ['SUPABASE_KEY']
)


def upload_short_signals(rows: list):
    """
    단기 시그널 업로드 (여러 종목 한번에).
    
    Parameters:
        rows (list): [
            {
                'date': '2026-03-16',
                'code': '005930',
                'name': '삼성전자',
                'grade': 'AA',
                'total_score': 87,
                'foreign_detail': {
                    'us': 25000,
                    'eu': 12000,
                    'asia': 5000,
                    'other': 2000,
                    'total': 44000
                },
                'inst_support': True,
                'entry_price': 83500,
                'stop_loss': 80000,
                'target_price': 90000,
                'holding_days': 4,
                'signal_type': 'BUY',
                'volume_ratio': 1.8,
                'momentum_regime': 'MOMENTUM'
            },
            ...
        ]
    
    Returns:
        성공 시 upserted rows, 실패 시 에러 출력
    """
    try:
        result = supabase.table('short_signals').upsert(
            rows, on_conflict='date,code'
        ).execute()
        
        # 요약 출력
        force_buy = sum(1 for r in rows if r.get('signal_type') == 'FORCE_BUY')
        buy = sum(1 for r in rows if r.get('signal_type') == 'BUY')
        watch = sum(1 for r in rows if r.get('signal_type') == 'WATCH')
        print(f"[FLOWX] 단기시그널 업로드 완료: {len(rows)}개 종목")
        print(f"  ├─ FORCE_BUY: {force_buy}개")
        print(f"  ├─ BUY: {buy}개")
        print(f"  └─ WATCH: {watch}개")
        return result
    except Exception as e:
        print(f"[FLOWX] 단기시그널 업로드 실패: {e}")
        return None


def upload_single_signal(signal: dict):
    """
    단일 종목 시그널 업로드 (장중 긴급 시그널 발생 시).
    
    Parameters:
        signal (dict): 위와 동일한 형식의 단일 dict
    """
    try:
        result = supabase.table('short_signals').upsert(
            signal, on_conflict='date,code'
        ).execute()
        emoji = "🔥" if signal.get('signal_type') == 'FORCE_BUY' else "📡"
        print(f"[FLOWX] {emoji} {signal['signal_type']}: {signal['name']} (등급: {signal['grade']})")
        return result
    except Exception as e:
        print(f"[FLOWX] 단일 시그널 업로드 실패: {e}")
        return None
```

---

## 6. 기존 코드에 연결하는 법

너의 기존 수급 분석 + 스코어링이 끝나는 지점에 추가:

```python
from upload_short import upload_short_signals

# 너의 수급 분석 결과를 FLOWX 형식으로 변환
flowx_signals = []
for stock in today_analysis_results:
    flowx_signals.append({
        'date': str(today),
        'code': stock['code'],
        'name': stock['name'],
        'grade': stock['grade'],              # 너의 등급 판정
        'total_score': stock['score'],
        'foreign_detail': {
            'us': stock['foreign_us'],
            'eu': stock['foreign_eu'],
            'asia': stock['foreign_asia'],
            'other': stock['foreign_other'],
            'total': stock['foreign_total']
        },
        'inst_support': stock['inst_net'] > 0, # 기관 순매수 양수면 True
        'entry_price': stock['entry_price'],
        'stop_loss': stock['stop_loss'],
        'target_price': stock['target_price'],
        'holding_days': stock['expected_days'],
        'signal_type': stock['signal_type'],
        'volume_ratio': stock['vol_ratio'],
        'momentum_regime': stock['regime']
    })

# 업로드!
upload_short_signals(flowx_signals)
```

---

## 7. 실행 스케줄

| 시간 | 작업 | 함수 |
|------|------|------|
| 매일 15:40~16:00 | 장 마감 후 전체 시그널 업로드 | upload_short_signals() |
| 장중 (선택) | 긴급 시그널 발생 시 | upload_single_signal() |

---

## 8. 체크리스트

- [ ] `pip install supabase` 설치
- [ ] .env에 SUPABASE_URL, SUPABASE_KEY 설정
- [ ] upload_short.py 파일 생성
- [ ] 기존 수급 분석 코드 끝에 upload_short_signals() 추가
- [ ] 수동 테스트: 테스트 데이터로 upload 실행
- [ ] Supabase Table Editor에서 데이터 확인
- [ ] (선택) 장중 긴급 시그널 트리거 연결

---

## 9. 주의사항

- **너는 읽기 하지 마.** 쓰기만 해. 읽기는 웹봇(FLOWX)이 담당.
- **이 데이터는 ₩50,000/월 유료 콘텐츠야.** 정확도가 생명이야. 확신 없는 종목은 signal_type을 'WATCH'로 내려.
- **foreign_detail은 반드시 JSONB로.** Python dict 그대로 넘기면 됨.
- **inst_support는 Boolean.** 기관 순매수 > 0이면 True, 아니면 False.
- **volume_ratio:** 5일 평균 거래량 대비 오늘 거래량 배수. 세력 포착의 핵심 지표.
- **UPSERT니까** 같은 날 여러 번 실행해도 안전. 최신 데이터로 덮어쓰기 됨.
