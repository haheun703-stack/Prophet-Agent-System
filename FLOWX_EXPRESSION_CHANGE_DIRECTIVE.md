# FlowX 웹사이트 표현 변경 지시서 — 단타봇

> **날짜**: 2026-04-05
> **발신**: 웹봇 (FlowX 프론트엔드)
> **수신**: 단타봇 (Prophet Agent System)
> **목적**: 투자 추천으로 오해될 수 있는 표현을 중립적 분석 용어로 변경

---

## 왜 변경하나?

"적극매수", "매수", "매도" 등의 표현은 **직접적인 투자 추천**으로 오해될 수 있습니다.
FlowX는 투자 자문 서비스가 아니므로, 시장 분석/신호 탐지 도구에 적합한 중립 표현으로 전환합니다.

> **주의**: `순매수`, `순매도`, `과매수`, `과매도` 등 시장 사실을 기술하는 표준 금융 용어는 변경 대상이 **아닙니다**.

---

## 변경 대상 테이블 + 매핑표

### 1. `dashboard_swing` 테이블 — JSONB 내 `grade` / `action` 필드

| Before (현재) | After (변경) |
|--------------|-------------|
| `적극 매수` | `강력 포착` |
| `조건부 매수` | `조건부 포착` |
| `매수` | `포착` |
| `관심매수` | `관심` |
| `매도` | `경계` |

**검색 키워드**: 코드에서 `적극 매수`, `조건부 매수`, `grade`, `action`을 검색하세요.

### 2. `short_signals` 테이블 — `signal_type` 컬럼

| Before | After |
|--------|-------|
| `FORCE_BUY` | `STRONG_PICK` |
| `BUY` | `PICK` |

**검색 키워드**: 코드에서 `FORCE_BUY`, `signal_type`, `short_signals`를 검색하세요.

### 3. `dashboard_swing` — FX 모니터 `verdict` 필드

FX 모니터의 verdict는 `GREEN/YELLOW/RED` 컬러 코드이므로 **변경 불필요**.

### 4. 스윙 시그널 텍스트 (reason / description)

다음 패턴의 텍스트가 있다면 변경:

| Before | After |
|--------|-------|
| `분할 매수 검토` | `분할 진입 검토` |
| `분할 매도` | `분할 청산` |
| `추격 매수` | `추격 진입` |
| `매수 기회` | `포착 기회` |

### 5. 타임라인 / 일과표 텍스트 (dashboard_swing JSONB)

| Before | After |
|--------|-------|
| `급락 시 분할 매수 검토` | `급락 시 분할 진입 검토` |
| `목표가 도달 시 분할 매도` | `목표가 도달 시 분할 청산` |

---

## 변경하지 않는 것 (표준 금융 용어)

다음은 시장 사실을 기술하는 용어이므로 **변경하지 마세요**:
- `순매수` / `순매도` — 투자자별 매매 동향 사실
- `과매수` / `과매도` — RSI 기술적 지표 용어
- `외국인 매도 전환` — 시장 사실 설명
- `손절` — 리스크 관리 용어

---

## 실행 방법

1. 코드에서 위 테이블의 값을 생성하는 부분을 찾으세요
2. Before → After 매핑에 따라 문자열을 교체하세요
3. Supabase에 이미 저장된 과거 데이터는 변경 불필요 (프론트에서 하위 호환 처리)
4. 변경 후 다음 데이터 갱신 시점부터 새 표현이 적용됩니다

---

## 웹봇 대응 현황

웹봇(프론트엔드)은 이 지시서의 After 값에 맞춰 UI 매핑을 동기화합니다.
기존 Before 값도 당분간 하위 호환으로 지원합니다.
